# Context Notes

결정과 그 근거를 시간순으로 기록한다. 다음 세션이 같은 논의를 반복하지 않기 위한 문서다.
**결론만 적지 말고 기각된 대안과 기각 이유를 함께 적는다.**

---

## 2026-09-03 — Phase 0 계획 수립

### 배포 형태 — 로컬 우선 PWA + 클라우드 백업

기각한 대안.

- **로컬 단독 (계정 없음)** — 기기 간 학습이 불가능. 학습 앱은 이동 중 사용이 정상 패턴
- **다중 사용자 웹 서비스** — 서버 DB 호스팅 비용, 개인정보 보관 책임, 약관 부담.
  본인이 쓸지도 모르는 단계에서 지불할 비용이 아님
- 채택안은 mmtm에서 이미 검증된 패턴이라 재사용 코드가 많다

### 프레임워크 — React 19

Vue 3도 후보였다. 사용자가 Vue 3 경험이 있어 초기 논의에서는 Vue를 제안했으나,
**mmtm의 재사용 가능한 코드가 전부 React 기반**이라 뒤집었다. Vue로 가면 서비스 계층만
건지고 컴포넌트는 다시 쓰게 된다.

### 난이도 축 — JMdict 우선순위, KANJIDIC2 grade 기각

초기에는 한자 학년(`grade`)을 기본 축으로 잡았다. 목표가 N1/N2로 확정되면서 뒤집었다.

- `grade` 8이 중학 상용한자 전체를 한 덩어리로 묶는다. N1/N2 숙어를 구성하는 한자가
  대부분 여기 몰려서 **정작 변별이 필요한 구간에서 해상도가 0**이다
- 외부 빈도 코퍼스를 찾을 필요가 없었다. JMdict `ke_pri`/`re_pri`에 이미 들어 있다.
  `nfxx`는 500단어 단위 순위, `news1`은 상위 12,000, `news2`는 그다음 12,000
- EDRDG가 명시한 한계 — 신문 기반이라 편향돼 있고, 저빈도 구간 변별력이 약하며,
  우선순위 표시가 붙었지만 흔하지 않은 단어와 표시 없이도 구어에서 흔한 단어가 있다.
  상급 어휘를 다루므로 이 한계가 정확히 아픈 지점에 걸린다는 걸 전제로 쓴다

### 학습 목적 — 읽기 교정이 1차

사용자가 JLPT N1 보유자이고 한국 한자음 지식이 많아 뜻은 문맥으로 짐작한다.
병목이 읽기다. 따라서 뜻 학습을 주 기능에서 내리고 읽기 입력형을 주 출제로 삼았다.

이후 "새 숙어도 늘리고 싶다"는 요구가 추가되어 어휘 확장을 2차 목적으로 승격했다.
**한 카드에 두 목적을 섞지 않고 모드를 분리한 이유** — 섞으면 아는 뜻을 재확인하는
낭비와 모르는 뜻인데 읽기만 묻는 헛돎이 동시에 발생한다.

### 해금 단위 — 숙어 단일 카드 + 음독 파생 추적

기각한 대안.

- **안 A: 레벨 도달 시 음독 모드 개방** — 본편 뒤에 열리는 보너스는 안 쓰게 된다.
  상급자에게 초반 구간이 지루하다
- **안 B: 음독을 기본 학습 대상으로** — 음독 단독 암기는 표 외우기이고, 어느 쪽을 언제
  쓰는지는 숙어를 만나야 안다. 뜻 학습이 밀린다
- 채택안은 음독을 학습 대상이 아니라 **진단 도구**로 쓴다. 사용자가 보는 건 계속 숙어다.
  FSRS 스케줄러를 두 벌 돌릴 필요도 없어진다

### 카드 분리 — reading / meaning

`meaning`은 한 번 익히면 오래 가고 `reading`은 자주 흔들린다. 망각 곡선이 다른 걸
한 카드로 묶으면 FSRS가 둘의 평균을 학습해 양쪽 다 부정확해진다.

### 뜻 표시 언어 — 한국어 단일

영어 gloss 경유는 일본어 → 영어 → 한국어 2단계 변환이 된다. 반응 속도 손실, 한자어
대응의 정밀도 손실, 그리고 사용자의 한자어 지식이라는 자산이 작동하지 않는다.
영어 gloss는 DB에 보관만 하고 화면에는 띄우지 않는다.

**주의** — 동형이의어에 stdict 자동 대조 결과를 그대로 쓰면 틀린 뜻을 학습시킨다.
汽車를 조회하면 "기차"가 나오지만 일본어 汽車는 증기기관차 쪽이다. 정확히 이 프로젝트가
교정하려는 오류를 앱이 강화하게 된다. 그래서 `verified` 플래그가 필수다.

### 동기화 — 기기별 파일 분리

mmtm `syncManager.js`는 파일 단위 타임스탬프 비교로 충돌을 감지하고 사용자에게 선택을
요구한다(`ConflictResolutionModal`). 다이어리에는 맞다. 학습 앱은 두 기기 사용이 정상
패턴이라 충돌이 상시 발생하고, 한쪽을 버리면 양쪽 다 유효한 기록이 소실된다.

각 기기가 자기 파일만 쓰면 쓰기 충돌이 구조적으로 사라진다. 읽을 때 이벤트 ID 기준
합집합으로 병합한다. append-only 이벤트 로그 설계가 여기서 값을 한다.

### 폰트 — 통합 CJK OTC 기각, 지역별 파일 사용

Noto Sans CJK에 4개 언어를 담은 슈퍼 OTC / 언어별 OTC 형태가 있다. 기각 이유 셋.

1. `locl` GSUB 기능과 언어 태깅 지원에 의존한다. 태그가 누락되면 일본어 글리프로
   폴백하는데, 이 앱에선 우연히 맞지만 한국어 UI에 일본 자형이 샐 수 있다.
   **우연에 기대는 설계다**
2. 폰트당 6만 5천 자 이상. PWA 오프라인 캐싱에 과하다
3. 숙어와 UI를 시각적으로 구분하는 게 학습에 유리하다

### 스키마 불변 조건이 A그룹인 이유

mmtm `src/db/db.js`가 실물 근거다. v1에서 `entries: 'date'`로 시작했다가 v2에서
`[userId+date]`로 PK를 바꾸느라 임시 테이블을 만들고 v3에서 되돌리는 3단계 우회를 했다.
IndexedDB는 PK 변경이 불가능하다.

`cardType`, 오답 유형, `source`/`verified`도 같은 이유로 지금 넣는다. 나중에 추가하면
그전 기록에 필드가 없어서 진단 리포트가 반쪽이 된다.

### 이름 — yomenai (読めない)

"못 읽겠다"는 증상을 그대로 이름으로 삼았다. 기각한 대안.

- **yomikata (読み方)** — 무난하지만 특징이 없고, 흔한 단어라 선점 가능성이 높다
- **음독 / Eumdok** — 학습 단위를 정확히 가리키지만 한국 시장에 한정된다
- **한자다리 / Kanji Bridge** — 의미는 명확하나 평범하다

증상을 이름으로 삼으면 "일본어 앱"이라는 넓은 카테고리에서 즉시 벗어나고, 해당자는
바로 자기 얘기라고 알아본다. 초급자는 스스로 걸러지는데 이건 의도한 결과다.

**브랜딩 관련해 지금 하지 않기로 한 것** — 로고, 랜딩 페이지, 타깃 조사, 경쟁 분석.
본인이 3개월 써보기 전에는 제품이 뭔지 확정되지 않는다. 브랜딩의 실질은 진단 리포트
화면 하나이며, 그게 잘 나오면 스크린샷 한 장으로 설명이 끝난다.

**미확인** — 도메인과 상표 중복 여부를 확인하지 않았다. 공개를 결심하는 시점에 확인한다.

---

## 2026-09-03 — Phase 1 착수 (스캐폴딩 + 원본 데이터)

### 스캐폴딩

- `npm create vite@latest -- --template react-ts`로 생성. 현재 폴더에 문서가 이미 있어
  임시 폴더에 만든 뒤 소스·설정만 옮겼다.
- Vite 8 / React 19 / TS 6. 스캐폴드가 eslint 대신 `oxlint`를 기본으로 준다 — 그대로 둠.
- `vite-plugin-pwa`는 설치만 하고 `vite.config.ts`에 연동하지 않았다. PWA는 Phase 7
  범위이고, Phase 1~4는 Node 테스트 환경이라 서비스워커가 개발을 방해한다.
- `tsx`를 추가했다. `tools/*.ts`를 트랜스파일 없이 실행하기 위한 것. `tsconfig.tools.json`
  분리.
- 테스트는 vitest, `environment: 'node'` (Phase 4까지 DOM 불필요).
- `data/dict/`도 `.gitignore`에 추가했다. 빌드타임 사전 DB 산출물이라 재생성 가능.

### 원본 데이터 (data/raw/, 커밋 안 함)

| 파일 | 출처 | 버전 / 일자 | 라이선스 |
|---|---|---|---|
| `kanjidic2-all-3.6.2.json` | jmdict-simplified 릴리스 `3.6.2+20260831182826` | dictDate 2026-08-31, db 2026-243 | CC BY-SA 4.0 |
| `jmdict-eng-3.6.2.json` | jmdict-simplified 같은 릴리스 (117MB) | dictDate 2026-08-31 | CC BY-SA 4.0 |
| `JMdict_e.gz` | EDRDG ftp 원본 XML (10.5MB) | 다운로드 2026-09-03 | CC BY-SA 4.0 |

재현: `curl -sL <github release url>` + `tar -xzf`. KANJIDIC2 원본 XML은 JSON 변환본으로
충분해서 받지 않았다.

### KANJIDIC2 임포트 결과 (`tools/import-kanjidic.ts` → `data/dict/kanji.json`)

- 한자 13,108자. grade 1~8(상용) 2,136자. `korean_h` 보유 6,293자.
- 표외자 필터는 `jouyou = grade 1~8` 플래그로 파생. grade 9~10(인명용)·null은 표외 취급.
- 반복 기호 `々`는 KANJIDIC 문자가 아니다. 段々·人々 같은 숙어 처리 시 별도 대응 필요.

### ⚠️ jmdict-simplified가 우선순위 태그(`nfxx`)를 버렸다 — 밴드 설계에 영향

PLAN §4 난이도 밴드는 전적으로 `nf01`~`nf48` / `news1` / `news2` / `ichi1` / `spec1`
태그 범위에 기반한다. **jmdict-simplified 3.6.2는 이 태그를 전부 제거하고 form별
`common` 불리언 하나로 대체했다.** (raw 데이터에서 `"nf`, `"news1"` 등 검색 결과 0건 확인.)

반면 EDRDG 원본 `JMdict_e.gz`는 `<ke_pri>`/`<re_pri>`에 `nf22`, `news1`, `ichi1` 등을
그대로 보존한다 (검색으로 확인).

→ **A 채택 (사용자 확인 2026-09-03).** `tools/import-jmdict.ts`가 `JMdict_e.gz`를
`fast-xml-parser`로 직접 파싱한다. 내부 DTD subset을 정규식으로 제거해 `&n;` 같은
엔티티를 확장하지 않고 코드(`n`, `adj-no`, `vs` …)로 보존한다. jmdict-simplified JSON은
KANJIDIC2 변환본으로만 계속 쓴다. 기각: B(외부 빈도 코퍼스 방향, PLAN §4가 기각한 것),
C(파서 2개 유지 비용).

### JMdict 임포트 결과 (`tools/import-jmdict.ts` → `data/dict/idioms.json`)

- JMdict entry 218,691개 스캔 → 한자 표기(2자 이상) 숙어 후보 122,894개
- 제외: KANJIDIC 미등재 한자 포함 2개, 표외자(비상용 grade 9~10/null) 포함 15,360개
- **최종 숙어 107,532개.** 우선순위 태그 보유 18,472개(17.2%), 태그 없음 89,060개(82.8%)
- 대표 표제어 = 첫 번째 "한자만 2자 이상" `keb`. 읽기 = `re_restr` 없거나 그 표제어를
  포함하는 첫 `reb`. 品사·gloss·priority는 sense/k_ele/r_ele 전체 합집합.
- 검증: `tools/import-jmdict.verify.test.ts` — 원본 XML을 독립 재파싱해 시드 고정 표본
  20개의 표제어∈keb, 읽기∈reb, 품사코드⊆원본pos, 구성한자 일치 확인. **7 테스트 통과.**
  (57MB XML 재파싱이라 ~9.5초 소요.)

### Phase 1-4 실측 (`tools/measure.ts`)

| 항목 | 값 |
|---|---|
| 대상 숙어 총 개수 | 107,532 |
| 우선순위 태그 보유 (밴드 0~3) | 18,472 (17.2%) |
| 태그 없음 (밴드 4, 선택) | 89,060 (82.8%) |
| 글자수 2 / 3 / 4 / 5+ | 45,138 / 28,034 / 22,371 / 11,989 |
| 밴드 0 / 1 / 2 / 3 / 4 (잠정) | 4,033 / 3,945 / 10,441 / **53** / 89,060 |
| `idioms.json` (비압축) | 18.5 MB |
| `kanji.json` (비압축) | 2.47 MB |
| 밴드 0~3만: idioms 추정 | ~3.2 MB |

**⚠️ 밴드 3이 53개뿐 — 잠정 밴드 규칙의 결함.** `news2` 항목은 실데이터에서 거의 항상
`nf25`~`nf48`을 같이 달고 있어 현재 규칙(`nf` 있으면 무조건 밴드 0~2)이 전부 밴드 2로
빨아들인다. PLAN §4의 "밴드 2 = nf21~news1 끝 / 밴드 3 = news2 구간"을 실현하려면
경계를 nf 번호로 다시 그어야 한다 (대략 news1 ≈ nf01~24, news2 ≈ nf25~48).
**Phase 2 `build-bands.ts`에서 nf/news 상관을 실측해 확정한다. Phase 1은 이 규칙을
잠정으로만 쓴다 (PLAN §4 경계를 임의로 바꾸지 않음).**

### 미확정 결정 1 — 사전 DB 형식: **분할 JSON 권고 (사용자 최종 확인 필요)**

- 전체 21 MB, 밴드 0~3만 ~5.7 MB(gzip 시 대략 1.5 MB). PWA 정적 자산 캐싱에 무리 없음.
- 권고: **밴드 0~3을 기본 번들, 밴드 4는 opt-in 지연 로드 청크로 분리.** sql.js는 wasm
  런타임 + 복잡도를 더하는데 Phase 5까지 전문 검색 요구가 없다. 보류.
- 산출물이라 나중에 뒤집어도 마이그레이션 비용 0.

### 미확정 결정 2 — 일본 고유 숙어 뜻 확보 방식: **Phase 3로 이월**

정밀 규모는 한국어 대조(Phase 3) 없이는 못 낸다. 밴드 4의 82.8% 상당수는 투명한 복합명사
(国際運河, 党改革, 心理戦争)로 한국 한자음이 곧 뜻이라 "고유 숙어"가 아니다. 실질 판단
대상은 밴드 0~3의 18,472개. Phase 3에서 3분류 후 규모 확정 → 방식 결정.

---

## 미확정 — Phase 1 실측 후 결정

| # | 항목 | 상태 |
|---|---|---|
| 1 | 사전 DB 형식 (분할 JSON / sql.js) | **실측 완료. 분할 JSON 권고** (위 2026-09-03 절). 사용자 최종 확인만 남음 |
| 2 | 일본 고유 숙어 뜻 확보 방식 | **Phase 3로 이월.** 정밀 규모는 한국어 대조 후에야 나온다 |
| 3 | 한국어 대조 범위 (전체 / 상위 N) | Phase 3. 밴드 0~3 (18,472개)이 현실적 상한 |
| 4 | Tatoeba 예문 채택 여부 | Phase 6 |

## 미확정 — 서비스화 결심 시

- CC BY-SA 승계 범위. 가공 사전 DB와 앱 코드의 경계가 어디인지 단정할 수 없다.
  EDRDG 데이터를 쓰는 상용 서비스가 실재하므로 실무 관행과 조문 해석이 갈린다.
  확정하려면 EDRDG에 직접 문의해야 한다
- stdict 오픈 API의 서비스 제공·상업적 이용 조건. 데이터 덤프는 CC BY-SA 2.0 KR이지만
  API 이용 조건은 다를 수 있다
- Tatoeba 음성 파일은 화자별로 라이선스가 다르다. 일괄 사용 불가

---

## 검증하지 못한 것

- `E:\GIT_choan\Mmtm` 로컬 저장소는 직접 읽지 못했다. mmtm 관련 기술은 전부 공개
  저장소 `choan616/choan616.github.io` (main 브랜치)를 읽은 결과다. 로컬 쪽이 더
  최신이면 차이가 있을 수 있다
- 대상 숙어 규모는 추정하지 않았다. Phase 1 실측 항목이다

---

## 2026-09-03 — Phase 2 (레벨링 + 음독 매핑)

### 밴드 경계 확정 — nf 번호 단독, news1/news2 태그는 참조하지 않는다

Phase 1이 남긴 "밴드 3이 53개뿐" 결함의 원인을 실측으로 확정했다.

| 실측 항목 | 결과 |
|---|---|
| `news1` 이 붙는 nf 범위 | nf01~nf24 |
| `news2` 가 붙는 nf 범위 | nf25~nf48 |
| nf 없이 news 태그만 있는 항목 | 0건 |
| nf 있고 news 태그 없는 항목 | 0건 |

news1/news2는 nf 번호에 완전히 종속된 파생 정보였다. 따라서 밴드는 nf 하나로 결정한다.
PLAN §4의 "밴드 2 = nf21~news1 끝 / 밴드 3 = news2 구간"이 그대로 nf21~24 / nf25~48이 된다.
**PLAN의 경계 정의를 바꾼 게 아니라 실데이터로 해석을 확정한 것이다.**

경계 교차 17건은 한 표제어에 표기형이 여럿이라 nf가 둘 붙은 유니온 아티팩트가 대부분이고
(協同組合 = nf09 + nf47), 순수 불일치는 6건(逓信·容態·萎縮·淡白·証取法·大路)이다.
가장 낮은(가장 흔한) nf를 쓴다 — 대표 표기형의 빈도가 밴드를 정해야 한다.

### 밴드 분포 (확정)

| 밴드 | 범위 | 개수 | 비중 |
|---|---|---|---|
| 0 | nf01~10 | 4,033 | 3.8% |
| 1 | nf11~20 | 3,945 | 3.7% |
| 2 | nf21~24 | 1,509 | 1.4% |
| 3 | nf25~48 | 7,889 | 7.3% |
| 4 | nf 없음 | 90,156 | 83.8% |

밴드 3이 53 → 7,889로 정상화됐다. 기본 학습 대상(밴드 0~3) 17,376개.
밴드 2가 1,509로 얇은 것은 news1이 nf 24구간뿐인데 밴드 0·1이 그중 20구간을 가져가기
때문이며, PLAN §4 정의를 따른 결과다.

### 밴드 4의 성격 — "태그 없음"이 아니라 "빈도 순위 없음"

`ichi1`/`spec1`/`gai1`만 있고 nf가 없는 1,096개를 어디 둘지가 결정 지점이었다.

기각한 대안.

- **밴드 2·3에 편입 (Phase 1 잠정 규칙)** — 빈도 근거 없이 주력 구간에 넣는 것이다.
  실물을 보면 亜米利加(アメリカ)·硝子(ガラス)·混凝土(コンクリート) 같은 외래어 当て字와
  御客様·御菓子 같은 비표준 한자 표기가 큰 비중이라 학습 대상이 아니다
- **별도 밴드 5 신설** — 밴드는 난이도 축인데 "순위 미상"은 난이도가 아니다. 축이 오염된다

채택 — 밴드는 오직 nf 빈도 순위로 산정하고, 이들은 밴드 4에 둔다. 정보 손실은 없다.
`IdiomRecord.common === true && band === 4` 로 정확히 이 부분집합이 식별되므로 Phase 4
출제 로직이 필요하면 가중치를 올릴 수 있다. **대가는 私自身·賞味期限·一年間처럼 실제로
흔한 항목이 기본 학습 대상에서 빠지는 것이다.** 다만 이들은 한국 한자음이 곧 뜻인 투명한
복합어라 교정 가치가 낮다.

### 밴드 육안 검수 결과 (`tools/review-bands.ts`, 시드 20260903)

밴드별 무작위 30개 + 경계 nf20/21/24/25 각 20개를 뽑아 확인했다. **명백한 오분류 없음.**

- 밴드 0 — 緊張·分析·通知·過去·都市. N3~N2 수준으로 일치
- 밴드 1 — 温床·遺言·熟知·劣勢·血友病. N2 대로 일치
- 밴드 2 — 配役·掃討·総帥·痛恨·絶句. N1 경계로 일치
- 밴드 3 — 定款·恩給·腐葉土·河畔·輝度·生殺与奪. N1 대, 주력 구간으로 적절
- 밴드 4 — 恒等関係·項目応答理論·五輪休戦. 전문용어·투명한 복합명사. "선택" 취급 타당

경계 검수에서 nf20(色気·変換·在籍)과 nf21(大通·技巧·占拠·本心)의 체감 난이도 차이는
거의 없었다. 빈도 순위 축이므로 경계에 불연속이 없는 것이 정상이고, 오분류 근거가 아니다.
nf24/nf25도 같다.

**검수에서 드러난 별개 문제 둘 (Phase 3~4 입력).**

1. 고유명사가 학습 대상에 섞인다 — 千島列島, 日本学術会議, 荒木, 滝川, 稲田.
   뒤 셋은 성씨이면서 일반명사이기도 해서 단순 제외가 어렵다
2. 순수 훈독 숙어가 섞인다 — 浜辺, 生糸, 山奥, 冬物, 頭数, 北風.
   1차 목적이 음독 교정인데 성격이 다르다. 아래 음독 매핑이 `kind` 로 자동 분류해준다

### 음독 매핑 — 최소 비용 분해

숙어를 (한자, 읽기) 쌍으로 쪼갤 때 문자열 단순 비교로는 안 된다. 음운 변형 후보를
생성해 읽기와 맞춰보고, 여러 해석이 가능하면 **변형 비용이 최소인 해석**을 고른다.

구현한 변형 규칙과 비용.

| 규칙 | 예 | 비용 |
|---|---|---|
| 連濁 (어두 유성화) | 三日月 つき→づき | 1 |
| 促音便 (말미 く/つ/ち/き/り → っ) | 学校 がく→がっ | 1 |
| 半濁音 (っ·ん 뒤 は행→ぱ행) | 発表 ひょう→ぴょう | 0 (의무적) |
| 連声 (ん 뒤 あ행→な·ま행) | 反応 おう→のう | 2 |
| 促音添加 (접두 훈독 + っ) | 真白 ま→まっ | 1 |
| 연용형 흡수 | 買取 か.う→かい | 0.5 |
| 음훈 전환 경계 | 重箱 じゅう+ばこ | 0.5 |
| KANJIDIC 읽기 목록 순위 | 出 の で.る 대 い.でる | 0.01/위 |

기각한 대안.

- **문자열 앞뒤 그리디 매칭** — 학교(がく|がっ)처럼 표면형이 여럿인 순간 무너진다
- **음독만 매칭하고 훈독은 버리기** — 重箱·湯桶 읽기를 인식하지 못하게 된다.
  `MIXED_READING` 오답 유형이 PLAN §6에 있으므로 훈독도 1급으로 다뤄야 한다
- **형태소 분석기(kuromoji) 사용** — 런타임 의존이 늘고, 분석기는 단어 경계를 주지
  한자 단위 읽기 배분을 주지 않는다. Phase 6 예문 처리 때 다시 검토한다

### 음독 매핑에서 실제로 어려웠던 지점 넷

1. **오쿠리가나 흡수.** 買取(かいとり)에서 買가 か가 아니라 かい를 덮는다. KANJIDIC의
   `か.う`에서 5단 연용형 かい를 유도해야 한다. 이걸 넣기 전 밴드 0~3 실패율이
   2.1~4.3%였고, 넣은 뒤 1.0~1.9%로 떨어졌다. 실패 목록의 절반이 이 부류였다
   (取引·売上·手続·問合·割引·値上·貸出·消印…)
2. **KANJIDIC이 탁음형을 별도 읽기로 싣는다.** 金의 `-がね`, 取의 `-ど.り`, 彫의 `-ぼり`.
   그대로 두면 金:かね와 金:がね가 다른 쌍이 되어 **음독 숙달 집계가 쪼개진다.**
   같은 한자에 청음형이 함께 있으면 청음을 대표형으로 삼고 `rendaku` 변형을 붙인다.
   → **단, 훈독에만 적용한다.** 음독의 シ/ジ, ハン/バン은 呉音·漢音으로 갈린 별개 읽기다.
   次의 ジ는 シ의 탁음화가 아니므로(目次 もくじ) 병합하면 진단이 틀어진다
3. **現代仮名遣い에서 ち·つ의 連濁가 둘로 갈린다.** 鼻血 はなぢ 대 年中 ねんじゅう.
   ぢ/づ만 생성하면 中 チュウ→じゅう를 놓친다. ぢ·じ, づ·ず를 모두 후보로 낸다
4. **동점 해석.** 思出(おもいで)를 思=おも + 出=いで(い.でる)로도, 思=おもい + 出=で로도
   쪼갤 수 있고 변형 비용이 같다. KANJIDIC 읽기 목록이 대략 통용도 순이라는 점을 이용해
   순위에 0.01/위의 미세 비용을 매겨 통용 읽기 쪽으로 붙인다. 어떤 실제 변형(최소 0.5)
   보다도 작아 순위가 변형 판단을 뒤집지 못한다

### 음독 매핑 결과

| 항목 | 값 |
|---|---|
| 성공 | 103,172 / 107,532 (95.95%) |
| 실패 | 4,360 (4.05%) — 熟字訓·当て字 3,838 + 카타카나 읽기 522 |
| 고유 (한자, 읽기) 쌍 | 4,001 |
| 음훈 혼독(重箱·湯桶) | 9,802 (성공분의 9.50%) |
| 밴드 0/1/2/3 실패율 | 1.02% / 1.67% / 1.06% / 1.69% |
| 밴드 4 실패율 | 4.55% |
| `onyomi-map.json` | 12.0 MB (비압축) |

**실패는 버그가 아니라 설계된 거부다.** 남은 밴드 0~3 실패 269건은 為替·果物·時計·
素人·相撲·梅雨·白髪·玄人·八百屋·景色·眼鏡 같은 熟字訓이다. 한자 단위 읽기가 존재하지
않으므로 억지로 쪼개면 **존재하지 않는 음독을 학습시키게 된다.** 실패 목록은
`data/dict/onyomi-failures.tsv`에 밴드·사유와 함께 남긴다.

해결하지 않고 남긴 계통적 실패 둘.

- **日本 = にほん** (日本語·日本人·日本海·日本画 등 밴드 0~3에 11건). 日=に는 ニチ+ホン →
  ニッポン → ニホン 의 음편형이고 KANJIDIC에 없다. 일반 규칙을 만들면 "음독 말미 모라
  탈락"이라는 과잉 생성 규칙이 되어 다른 곳을 망친다. 예외 표를 두는 것도 미끄러운 비탈이라
  보류한다
- **真赤 = まっか** (真白·真最中은 성공). ま + っ + あか에서 모음 탈락까지 일어난다.
  真〜 접두형 중 이 형태만 남는다

### 음독 그래프 순환 검사 — 두 종류를 다 본다

- **숙어 → (한자, 읽기) 이분 그래프** — 위상 정렬(역방향 Kahn)로 전체 노드 소진 확인.
  구성상 이분이라 순환이 나올 수 없지만, Phase 4 출제 순서 최적화가 이 구조를 순회하므로
  단정하지 않고 검사한다
- **변형 파생 그래프** — 노드가 가나 문자열, 간선이 (원형 → 변형형)이다. 連濁·促音便·
  半濁·連声 규칙이 서로를 되먹이면 후보 생성이 발산한다. **실제 순환 위험은 이쪽에 있다.**
  모든 원형에서 규칙 폐포를 전개한 뒤 색칠 DFS로 검사한다

둘 다 순환 없음. 빌드 스크립트가 순환을 만나면 산출물을 쓰지 않고 예외를 던진다.

### 산출물

| 파일 | 크기 | 내용 |
|---|---|---|
| `data/dict/bands.json` | 1.3 MB | 숙어 id → 밴드, 분포 요약 |
| `data/dict/onyomi-map.json` | 12.0 MB | (한자, 읽기) 쌍 목록 + 숙어별 분해 |
| `data/dict/onyomi-failures.tsv` | 215 KB | 매핑 실패 목록 (밴드·사유별) |

전부 `.gitignore`된 재생성 가능 산출물이다. `npm run build:bands` / `build:onyomi`.

---

## 2026-09-03 — Phase 3 (한국어 대조 배치)

### 동형동의 ↔ 동형이의 판별 방법 — 수동 검수 큐

배치가 자동으로 가르는 것은 **JP_UNIQUE(일본 고유) vs NEEDS_REVIEW(검수 필요)** 둘뿐이다.
동형동의(1)와 동형이의(2)의 최종 판정은 사람이 `korean-review.tsv`의 `verdict` 칸에
1/2/3을 채우고 `apply:korean-review`를 돌리는 것으로 한다.

기각한 대안.

- **LLM 뜻 비교** — (표제어, JMdict 영어 gloss, stdict 한국어 정의)를 Claude에 넣어
  동일 여부 판정. PLAN이 `source:'llm'`을 허용하고 정확도도 가장 높다. 그러나 우리가 가진
  일본어측 정보는 영어 gloss뿐이라(JMdict는 일본어 정의가 없다) 2단계 번역 손실이 있고,
  `汽車`처럼 뉘앙스차를 동형이의로 볼지가 프롬프트 설계에 민감하다. 별도 키·비용도 든다.
  규모를 먼저 본 뒤 재검토한다
- **원어 한자 일치만으로 자동 분류** — stdict `原語`가 표제어와 같은 글자면 동형동의로
  간주. `工夫`(공부)·`放心`(방심)처럼 글자는 같아도 뜻이 갈리는 게 정확히 이 앱의 교정
  대상이라 자동 동형동의 처리는 위험하다. `hasOriginMatch` 플래그로 남겨 검수 속도만 돕는다

### 검수 큐에 音만 겹치는 것(`手紙`→`收支`)도 넣는다

`手紙`(편지)는 한국어에 `手紙`라는 한자어가 없다. 후보 `수지`로 stdict를 치면
`收支`·`樹脂`가 나온다. `原語` 대조로는 불일치라 JP_UNIQUE로 떨굴 수도 있지만,
**한국 한자음에 이끌린 오독(`KO_INTERFERENCE`)이 정확히 이 지점에서 난다.** 그래서
후보로 stdict 항목이 하나라도 잡히면 전부 NEEDS_REVIEW로 보내 사람이 본다.
JP_UNIQUE는 "어떤 후보로도 stdict 결과 0건"인 경우만이다.

결과적으로 Phase 3 완료 기준("동형이의어 10개가 2번으로 분류")은 두 단계로 충족된다 —
배치가 10개를 NEEDS_REVIEW(잠정 2번) 큐에 넣고, 사람이 2번을 확정한다.
`tools/match-korean.test.ts`가 fixture로 이 경로를 고정한다(23 테스트).

### 신자체 ↔ 정자 대조 — KANJIDIC2 variant에서 자동 추출

stdict `原語`는 정자(`醫學`)로 쓰이고 JMdict 표제어는 신자체(`医学`)라 글리프가 어긋난다.
손으로 이체자표를 적는 대신 `data/raw/kanjidic2-all-*.json`의 `misc.variants` 중
`jis208` 항목을 EUC-JP로 디코딩해 `신자체 → {정자,…}` 집합을 만든다
(`tools/lib/kanji-variants.ts`). `医学↔醫學`·`経済↔經濟` 통과, `手紙↔收支` 거부 확인.

- `jis212`/`jis213`은 제외했다. 디코딩이 불안정해 `鉄`의 variant가 `簗`로 나오는 등
  무관한 문자가 섞인다. `jis208`만으로 상용 신자체는 전부 커버된다
- 한 신자체가 복수 정자에 대응하면(`弁`→`辨/瓣/辯/辮`) 전부 집합에 담는다. 대조는
  위치별 소속 검사라 과포함이어도 다른 위치·독음 조건이 오탐을 막는다
- `収`는 KANJIDIC에 `korean_h`가 없다(밴드 0~3에서 유일하게 이 결함이 문제). variant
  `收`의 한자음 `수`로 자동 보강된다. 국자 `枠`는 대응 한자음이 없어 후보 생성 실패로
  기록된다(정상 — 한국어에 대응어가 없다)

### 재개 방식 — stdict 응답 디스크 캐시

`data/dict/.korean-cache.json`에 검색어별 응답을 저장한다. 이게 곧 재개 지점이다.
일 호출 한도 초과 등 API가 페이로드로 오류를 주면(`StdictApiError`) 캐시를 flush하고
exit 2. 다음 날 재실행하면 캐시된 검색어는 건너뛰고 이어서 진행한다. 스로틀은 호출당
50ms. `--limit=N`, `--only=id,id` 로 부분 실행.

### 산출물

| 파일 | 내용 |
|---|---|
| `data/dict/korean-match.json` | 숙어 id → {category, tentativeCategory, hasOriginMatch, matches[]} |
| `data/dict/korean-review.tsv` | NEEDS_REVIEW 검수 큐. `verdict` 칸에 1/2/3 채운다. 原語 일치 먼저 정렬 |
| `data/dict/korean-candidate-failures.tsv` | 한자음 후보를 못 만든 숙어(국자 등) |
| `data/dict/korean-class.json` | 검수 반영 최종 분류 + `koMeaning`(source `stdict`, verified `false`) |

`koMeaning.verified`는 항상 `false`. 표시 뜻의 정확성 검수는 별도 단계다(PLAN 리스크 표).
전부 `.gitignore`된 재생성 가능 산출물. `npm run match:korean` / `apply:korean-review`.

### stdict 응답 형식 — 첫 실행에서 두 가지 수정

- **결과 없는 검색어는 HTTP 200 + 빈 본문.** `res.json()`이 "Unexpected end of JSON input"으로
  터졌다. 빈 본문은 `[]`로 처리한다
- **`origin`·`pos`는 item 레벨, `definition`·`link`·`type`은 sense 레벨.** 처음엔 전부 sense
  안에 있다고 가정했다가 `originMatch`가 전부 false로 나와 발견

### 배치 실행 결과 (2026-09-03, 밴드 0~3, 17,376개, ~36분, 8건/s)

| 분류 | 수 | 비중 |
|---|---|---|
| JP_UNIQUE (일본 고유, 자동 확정) | 1,916 | 11.0% |
| NEEDS_REVIEW (검수 필요) | 15,456 | 89.0% |
| ├ 原語 한자 일치 | 13,155 | |
| └ 音만 일치 (KO 간섭 위험) | 2,301 | |
| 후보 생성 실패 | 4 | 枠(국자) 숙어뿐 — 정상 |

밴드 3의 JP_UNIQUE 비율이 높다 (1,042 / 7,889 = 13%). 고유명사·성청명(外務省)·
四字熟語·문화어(歌舞伎·横綱)가 이 구간에 몰린다.

**`originMatch` 플래그는 동형동의의 안전한 대리 지표가 아니다.** `工夫`(공부)·`汽車`(기차)·
`放心`(방심)·`勉強`(면강)·`大丈夫`(대장부) 등 대표 동형이의어가 전부 `原語` 일치다.
따라서 原語 일치 블록을 일괄 1번(동형동의)으로 밀면 이 앱이 교정하려는 바로 그 단어들을
오분류한다. 原語 일치 블록도 뜻 비교(사람 또는 LLM)가 필요하다.

**`originMatch` 오탐(false negative)이 있다.** `税`↔`稅`, `戸`↔`戶` 처럼 부수 단순화는
KANJIDIC variant에 없어 `減税↔減稅`·`江戸↔江戶`가 "音만 일치"로 분류된다. 오분류는
아니다(둘 다 NEEDS_REVIEW) — 큐 정렬 정확도만 떨어진다. `hasOriginMatch=13,155`는 실제
동형의 약간 과소 집계다.

### 검수 규모 — 15,456건. 여러 세션에 나눠 진행하기로

수동 검수 큐를 택했을 때 이 규모는 감안했지만(미확정 #3) 실측값이 커서, 한 번에 끝내지
않고 **세션 간 이어서 검수**하는 방식으로 간다. 방식 자체(LLM 병용 / 밴드 축소)는
아직 안 정했고, 검수를 진행하다 규모가 체감되면 그때 결정한다.

**세션 간 검수가 깨지지 않게 한 장치.**

- `match:korean` 재실행 시 `korean-review.tsv`의 기존 verdict(1/2/3)를 id 기준으로
  이어받는다. 사전 DB를 다시 빌드해도 채운 칸이 날아가지 않는다
- `--limit` / `--only` 부분 실행은 산출물을 `*.partial.*`로 써서 정식 검수 파일을 안 건드린다
- `apply:korean-review`는 채운 만큼만 반영하고 미검수 수를 보고한다. 여러 번 돌려도 됨

### 검수 초벌 — 로컬 Ollama (빌드타임)

브라우저 PWA는 로컬 Ollama에 못 닿으므로 LLM은 전부 빌드타임에 돈다. 배포물엔 Ollama
의존이 없다. 초벌 결과를 사전 DB에 굳혀 배포하고, 인앱에서 사람이 카드가 뜰 때
확정·수정한다(사용자 이벤트 로그). 같은 코퍼스면 초벌은 1회.

**모델 선정 — qwen3.5:latest.** 보유 모델 중 실측.

| 모델 | 결과 |
|---|---|
| qwen3.5:latest (6.6GB) | `think:false` + `format:json` 시 웜 1.3s/건, 컴팩트 JSON 정상. **채택** |
| gemma4:26b (17GB) | 웜 5~15s+/건, `format:json`인데도 `reason`이 반복 토큰 루프로 붕괴. 제외 |
| qwen3:8b, gemma2:9b | 대안 후보(교차 검증용). 지금은 안 씀 |

- **qwen3의 `/no_think` 프롬프트 지시는 무시된다.** 요청 본문 top-level `"think": false`를
  써야 사고 과정을 끈다. `format:json`을 켜면 사고 모델은 JSON을 `thinking` 필드로 보내고
  `response`가 비므로, `think:false`와 반드시 같이 쓴다
- **프롬프트를 `originMatch`로 분기한다.** Y(한자 확실 일치)면 3 보기를 빼고 1 vs 2만 묻는다.
  이렇게 안 하면 `工夫`(한자 동일)를 "뜻이 다르다"는 이유로 3(무관)으로 잘못 보낸다.
  n(원어 한자 불일치)이면 3(우연히 음만 겹침) vs 1·2(자형 차이일 뿐 같은 단어)를 묻는다
- 12건 스모크에서 `架空`(가공, 架空=공중 가설 ↔ JP=가상의)을 2로 정확히 잡음

**표본 150건 검증 (사람 라벨 대비).** 일치율 90.7%. 혼동표에서 class 3(무관, 원어 한자
불일치) 검출이 87/88로 거의 완벽 — `originMatch=n`(KO 간섭 핵심)에서 가장 정확하다.
1↔2 경계가 노이즈(1→2 5건, 2→1 4건). 불일치 14건을 뜯어보면 사람 라벨 오류가
6건가량(`角度`·`家族計画`·`島内`·`甘酢` 등), 진짜 애매 6건, 모델 실오류 3건 수준이라
**실질 정확도는 95%+**. 거친 사전 분류로 충분하다고 판단, 진행.

- 프롬프트 강화 시도(고유명사→3 규칙 추가)는 역효과 — 모델이 `無論`·`短編` 같은
  평범한 동형동의어를 "일본식 한자음 고유명사"라며 3으로 밀어 88.7%로 하락. 되돌림.
  "reason은 verdict와 일치해야 한다" 한 줄만 유지(`決別` 자기모순 해소)
- 초벌 배치 실행 — scope `band01+sound` 8,700건, qwen3.5, 건당 1.46s

### 초벌 배치 결과 (2026-09-04, 8,700건)

초벌 verdict 분포 — 1(동형동의) 6,107 / 2(동형이의) 732 / 3(무관) 1,861.
`apply:korean-review --trust-llm` 반영 후 `korean-class.json` 최종 분류:

| category | 수 | 구성 |
|---|---|---|
| 1 동형동의(교정) | 6,107 | 전부 초벌 |
| 2 동형이의(확장) | 7,488 | 초벌 732 + 잠정 6,756 (밴드 2~3 原語일치, 초벌 스코프 밖) |
| 3 일본 고유(확장) | 3,777 | JP_UNIQUE 1,916 + 초벌 1,861 |

`classSource` — llm 8,700 / default 8,672 / manual 0.

**스팟 검토 소견 — 품질 양호.**
- `n`(음만 일치) 블록 2,301건: 초벌 3이 1,861 (間近/幹根·肝心/奸心·建前/乾田 등 명백한
  무관). 초벌 1/2가 440 — `減税/減稅`·`江戸/江戶`·`決着/結着`·`骨格/骨骼`·`納税/納稅`처럼
  `originMatch` 오탐(KANJIDIC variant 표 결손: 税/稅, 戸/戶, 着/著 등)을 LLM이 "자형 차이일
  뿐 같은 단어"로 정확히 복구. n-분기 프롬프트가 의도대로 동작
- `Y` 블록 6,399건: 1이 5,679, 2가 720. 투명한 동형동의 우세, 타당

**Phase 3 완료 기준 — 알려진 동형이의어 10개 중 9개가 category 2.**
`主人`(id 1579780)만 초벌이 1로 줬다. JP 主人(남편·주인·고용주) ↔ KR 주인(소유자·주인장)은
부분 겹침이 있는 경계 사례로, "남편" 의미는 한국어에 없어 2가 맞다고 본다. 수동 검수
우선 대상(밴드 0 교정 시드)에 포함되므로 사람이 확정한다. fixture 테스트
(`match-korean.test.ts`)는 10개가 검수 큐로 들어가는 경로만 고정한다.

**남은 수동 검수 — 필수만 796건으로 축소 (사용자 요청).**

`tools/build-review-worklist.ts` 가 `korean-worklist.tsv` 를 tier 1+2 만으로 만든다
(`--tiers=` 인자, 기본 `1,2`). 나머지 14,660건은 초벌/잠정값을 신뢰한다.

- **tier 1 (440)** — `n` 블록 초벌 1/2. `originMatch` 오탐 복구(減税/減稅). ko_origin 이
  headword 이체자면 초벌 그대로. 빠른 훑기
- **tier 2 (356)** — 밴드0 `originMatch=Y` 초벌 2. 교정↔확장 갈림, 가장 흔한 밴드.
  전수 신중 검토. `工夫`·`汽車`·`主人` 등이 여기. stdict 가 엉뚱한 뜻풀이를 물어와
  초벌이 2로 준 경우(`結果`→"열매맺음")는 1로 되돌린다
- 지연 (tier 3~9) — `n` 초벌 3 (스팟상 정확), 밴드0~1 Y 초벌 1 (투명한 동형동의),
  밴드 2~3 Y 6,756 (초벌 없음). Phase 4 에서 카드 풀 진입 시 처리
- verdict 칸은 `?` 플레이스홀더. `build:review-worklist` 재실행 시 채운 값 이어받음
- `main` 브랜치는 없고 작업은 `phase-3-korean-match` 브랜치. Phase 4 가 그 사이 완료돼
  이 브랜치 히스토리에 Phase 3 → Phase 4 → Phase 3 검수 순으로 섞여 있다

### Phase 1 임포트 버그 수정 — sK/iK/oK 표기가 표제어로 뽑히던 문제

검수 중 `魚信`(あたり)를 발견. `魚信`은 `当たり`(あたり)의 `<ke_inf>&sK;</ke_inf>`
(검색 전용 표기)다. `import-jmdict.ts`가 "첫 순한자 2자+ keb"를 표제어로 잡는데,
`当たり`·`当り`가 오쿠리가나를 포함해 걸러지고 맨 뒤 검색 전용 `魚信`이 표제어가 됐다.

수정 — `sK`/`iK`/`oK` 표기는 표제어 후보에서 제외. 순한자 표제어가 그 표기뿐인 엔트리
(`当たり`↔`魚信` 류)는 코퍼스에서 드롭. `rK`(드문 한자)는 유지 — 인쇄물에 나온다.

영향 — 코퍼스 107,532 → **106,803** (−729). 밴드 0~3 17,376 → **17,217**.
`魚信`은 이제 별도 엔트리 `ぎょしん`(id 1810840)만 남는다. `御茶`(おちゃ)·`御苦労様`·
`顔付`·`筋書` 등 드롭. 전 파이프라인 재실행 — bands / onyomi / match-korean(캐시 재사용) /
draft(캐시 재사용) / worklist(verdict 355건 보존) / apply. 스냅샷 테스트 2개 갱신.

최종 `korean-class.json` — 동형동의 6,219 / 동형이의 7,351 / 일본고유 3,643
(사람 355 / 초벌 8,260 / 잠정 8,598).

### 도구

| 스크립트 | 역할 |
|---|---|
| `tools/lib/ollama.ts` | `judge(model, input)` — originMatch 분기 프롬프트, think off, JSON, 재시도 |
| `tools/draft-korean-review.ts` | `--sample=N` 층화 표본 추출 / `--validate` 사람 대비 일치율 / 기본은 초벌 생성 |
| `tools/apply-korean-review.ts` | 사람 verdict > (`--trust-llm`) 초벌 > 잠정. `classSource` 태깅 |

- 초벌 캐시 `data/dict/.korean-llm-cache.json` (id+model 키, 재개 지점)
- 초벌 산출 `data/dict/korean-llm-draft.tsv` (id, model, verdict, reason — 모델별 누적)
- `--scope` 기본 `band01+sound` — 밴드 0~1 NEEDS_REVIEW + 전 밴드 음만 일치 (~9k건)

### 다음 세션 착수점

1. `data/dict/korean-review-sample.tsv` (층화 표본 200건, 이미 생성됨)의 `verdict` 칸에
   1/2/3 을 직접 채운다
2. `npm run draft:korean-review -- --validate --in=data/dict/korean-review-sample.tsv`
   → qwen3.5 일치율·혼동표·불일치 목록·건당 시간 확인. 일치율이 낮으면 프롬프트 손보고,
   대안 모델(qwen3:8b 등)도 같은 표본으로 잰다
3. `npm run draft:korean-review` (scope=band01+sound, ~9k건, ~3시간) → `korean-llm-draft.tsv`
4. `npm run apply:korean-review -- --trust-llm` → `korean-class.json`
5. 그 뒤 사람이 `korean-review.tsv` `verdict`를 우선순위 높은 것부터(음만 일치, 밴드 0
   교정 시드) 채우면 다음 `apply` 실행에서 초벌을 덮어쓴다. 전부 채우면 Phase 3 완료

### 산출물 (재실행 가능, .gitignore)

| 파일 | 크기 | 내용 |
|---|---|---|
| `data/dict/korean-match.json` | 12.6 MB | 숙어 id → 분류 + matches[] |
| `data/dict/korean-review.tsv` | 2.5 MB | NEEDS_REVIEW 15,456행. verdict 칸 비어 있음 |
| `data/dict/korean-candidate-failures.tsv` | 175 B | 枠 숙어 4건 |
| `data/dict/.korean-cache.json` | 11.5 MB | stdict 응답 캐시 (재개 지점) |

`korean-class.json`(최종 분류)은 사람이 verdict를 채운 뒤 `apply:korean-review`로 생성한다.

### 일본 고유(category 3) 뜻 확보 방식 — 여전히 미결

Phase 1-4에서 이월된 항목. category 3 = JP_UNIQUE 1,916 + 검수에서 3번 판정될 분량.
규모가 이제 대략 나왔으니(하한 1,916) 방식 결정 가능 — 영어 병기 / 직접 작성 / LLM 초벌+검수.

---

## 2026-09-04 — Phase 4 (학습 코어)

Phase 3의 Ollama 초벌 배치가 아직 도는 중이지만 Phase 4는 `korean-class.json`의 *값*이
아니라 *형태*(`byId[id].category`)에만 의존해서 병행했다.

### 순수 도메인 모듈을 `src/lib/`로 옮겼다

`tools/lib/{readings,onyomi,bands}.ts` → `src/lib/`. 임포트 6줄 수정.

오답 판정이 런타임에서 `decompose`를 그대로 써야 한다. 사용자가 입력한 오답을
같은 최소 비용 분해로 파싱해야 "어느 자리에서 어떤 원형을 골랐나"를 알 수 있고,
그게 없으면 連濁·促音·음독 선택을 구분할 수 없다.

- 기각 — **런타임에서 `tools/lib/`를 직접 임포트.** 동작은 하지만 의존 방향이
  거꾸로다(앱 → 빌드 스크립트). tsconfig 프로젝트도 갈라져 있다
- 기각 — **음운 규칙표를 런타임에 다시 쓴다.** 連濁·半濁·促音 테이블이 두 벌이 되면
  한쪽만 고치는 사고가 난다
- 채택 — 순수 도메인은 `src/lib/`, `tools/`가 그걸 가져다 쓴다. 역방향 의존이 없다.
  세 파일 다 node 임포트가 없어서 그대로 옮겨졌다

`bands.ts`의 `bandOf(idiom: IdiomRecord)`만 `{ priority: string[] }` 구조 타입으로
바꿔 `tools/lib/dict.ts`(node fs 의존) 연결을 끊었다.

### IndexedDB 스키마 v1 — 테이블 하나

```
events: [userId+id], [userId+at], [userId+idiomId+cardType], [userId+deviceId+at], deletedAt
```

카드 상태를 저장하는 테이블을 만들지 않았다. FSRS 상태는 이벤트 재생으로 언제나
재계산되고, 저장하면 이벤트와 상태가 어긋날 수 있는 두 번째 원본이 생긴다.
성능이 문제가 되면 그때 파생 캐시를 추가한다 — **스토어 추가는 버전 올리기로 언제든
가능하다. 불가능한 건 PK 변경뿐이고(mmtm 실패 사례) 그건 v1에서 확정했다.**

`deletedAt`은 `number | null`로 두고 색인을 걸었다. IndexedDB는 null을 유효 키로 안
쳐서 이 색인엔 **삭제된 행만** 담긴다. 동기화에서 묘비만 훑을 때 그게 오히려 맞다.
활성 이벤트는 `[userId+at]` 범위 순회 후 걸러낸다.

`meaningKnown` 이벤트를 따로 뒀지만 `modeSet` 이벤트는 만들지 않았다. 모드는
(한국어 분류, 진단 응답, 뜻 카드 상태)의 순수 함수라 저장할 상태가 없다.

### 재생 결정론 — `enable_fuzz: false`

ts-fsrs 기본값이 이미 false지만 `generatorParameters`에 명시했다. 퍼지를 켜면 같은
(카드, 시각, 등급)이 매번 다른 간격을 내서 재생이 결정론적이지 않게 되고,
그러면 "상태를 저장하지 않는다"는 설계 전체가 무너진다.

`replay()`는 정렬을 호출부에 맡기지 않고 안에서 `compareEvents`(at → id)로 한다.
기기별 파일을 합집합으로 병합해 순서가 섞인 채로 넘겨도 같은 상태가 나와야 하기
때문이다. 테스트에서 뒤집기·회전 두 가지로 확인했다.

### 오답 판정 — 두 경로로 나눴다

**경로 1 — 답이 일본어로 파싱되는 경우.** 정답 분해와 자리별로 비교한다.
`kind`가 다르면 MIXED_READING, `base`가 다르면 ONYOMI_CHOICE, 같은 `base`에
표면형만 다르면 変異 종류를 본다.

**경로 2 — 파싱 안 되는 경우.** 문자열 관계(連濁·促音·長音)를 먼저 보고,
그다음에 KO 간섭을 본다.

우선순위는 `MIXED_READING > ONYOMI_CHOICE > RENDAKU > SOKUON > CHOON`.
구조적 오해가 표면 음운 변형보다 진단 가치가 크다고 봤다.

**KO_INTERFERENCE를 어떻게 정의했나.** 여기가 제일 오래 걸렸다.

- 기각 — **"한국음이 하나인데 일본 음독이 둘이면 KO 간섭".** 대부분의 ONYOMI_CHOICE가
  여기 걸린다. 무엇보다 PLAN §6 표가 `発端 → ハツタン`을 ONYOMI_CHOICE의 예로 명시했는데
  発은 한국음이 발 하나, 음독은 ハツ/ホツ 둘이라 이 규칙이 PLAN 자기 예시를 뒤집는다
- 기각 — **한국음 초성·중성·종성 → 일본 음독 대응표로 후보 생성.** 대응이 규칙적이긴
  하지만(종성 ㄱ→く/き, ㄴ→ん, ㄹ→つ/ち) 초성 ㅇ이 あ·や·が·な·ざ 5행으로 퍼지는 등
  너무 관대해서, *정답* 음독도 거의 항상 후보에 들어온다. 변별이 안 된다
- 채택 — **한국음이 같은 다른 한자의 음독을 끌어왔을 때.** 認識 → にんしょく 의 しょく는
  食·植(둘 다 한국음 식)의 음독이지 識의 읽기가 아니다. 정확히 "한자를 한국음으로 묶어
  기억하다 엉뚱한 일본음을 꺼낸" 상황이고 `kanji.json`만으로 계산된다

구현은 읽기 표를 넓혀 다시 분해해보는 방식이다. 각 한자에 한국음 형제들의 음독을
얹고 `decompose`를 다시 돌려서, 그제서야 파싱되고 그 조각이 자기 한자 읽기가 아니면
간섭으로 본다. 새 파서를 쓰지 않고 Phase 2 기계를 재사용한다.

**자기 한자의 다른 음독을 고른 경우는 여기 못 온다.** 그건 경로 1에서 ONYOMI_CHOICE로
먼저 잡히기 때문이다. PLAN 표와 어긋나지 않는다.

**한계.** 재현율은 낮게 잡았다(정밀도 우선). 실사용 로그가 쌓이면 Phase 5 리포트에서
"판정 불가(null)" 비율을 보고 규칙을 넓힐지 판단한다.

### 판정 규칙 두 개는 실측하고 고쳤다

- **장음 유무로만 갈리는 원형은 CHOON.** 数字 すうじ → すじ 는 数의 ス가 실재 음독이라
  분해가 성공해 ONYOMI_CHOICE로 나왔다. 한국어권 학습자에게 훨씬 흔한 오류는 장음 누락이고,
  `stripLongVowels(base)`가 같으면 CHOON으로 보낸다
- **促音便에 딸려온 半濁音은 따로 세지 않는다.** 発表 はっぴょう → はつひょう 는 促音 하나가
  원인인데 RENDAKU(半濁)와 SOKUON 둘이 잡혀 우선순위상 RENDAKU가 이겼다. ぱ행은 っ·ん
  뒤에서만 나오므로, 앞 자리가 이미 틀렸으면 半濁 차이는 결과이지 별개 오답이 아니다

### 출제 선택 — 무작위를 안 쓴다

기한 초과 우선(초과가 큰 순) → 신규는 밴드 오름차순, 같은 밴드면 미숙 음독 가중
내림차순. 동점은 idiomId로 깬다.

같은 입력이면 같은 세션이 나와야 진단이 재현 가능하다. 진입 진단의 무작위 표본은
Phase 5에서 시드를 명시해 따로 만든다.

미숙도는 구성 음독 오답률의 평균이고, 한 번도 안 나온 음독은 0.5를 준다.
확실히 미숙한 것(1.0)보다는 낮고 숙달된 것(0)보다는 높다.

모드 정원은 7:3인데, 한쪽이 정원을 못 채우면 남은 자리를 다른 쪽이 가져간다.
안 그러면 초반에 확장 후보가 적을 때 세션이 계속 짧아진다.

### 검증

`npm test` — 10 파일 117 테스트 통과. `tsc -b` / `oxlint` 클린.

세션 시뮬레이션은 두 풀로 돌린다. 고정본 24개(data/dict 없이도 도는 것)와
**실제 사전 DB 밴드 1~2에서 뽑은 400개**. 하루 한 세션씩 100회, 매 세션마다
이벤트 로그를 처음부터 재생해 상태를 만들고 그 상태로 다음 세션을 고른다 —
실제 앱과 같은 경로다.

빈 세션 불변 조건을 한 번 고쳤다. 처음엔 "빈 세션 0"으로 걸었는데 24개 풀에서
11회 나왔다. 작은 덱이 조용해지는 건 정상이고 실패가 아니다. 진짜 불변 조건은
**낼 카드가 남았는데 비어 나오면 안 된다**여서, 빈 세션마다 상태를 직접 훑어
미도입·기한 초과 카드가 없음을 확인하는 쪽으로 바꿨다. 400개 풀은 빈 세션 0.

### 미검증으로 남긴 것

Dexie의 실제 open/read/write 경로. `fake-indexeddb` devDependency가 필요한데
설치는 사용자 확인 사항이라 보류했다. 스키마 *정의*는 `STORES` 상수로 검증되고
Dexie에도 같은 상수가 들어가므로, 안 본 건 Dexie 자체의 동작뿐이다.

`tsconfig.app.json`에 `strict`가 없다 (`tsconfig.tools.json`에는 있다).
Vite 스캐폴딩에서 빠진 것으로 보이는데 Phase 4 범위 밖이라 안 건드렸다.
`src/lib/`는 tools 프로젝트에서도 검사되니 strict를 받지만 `src/core/`·`src/db/`는
아니다. Phase 5 착수 전에 켜는 게 좋다.

### 결정 확인 (사용자, 2026-09-04)

Phase 4에서 내린 판단 셋을 사용자에게 확인받았다. **셋 다 현행 유지로 확정.**

1. **IndexedDB v1 = `events` 테이블 하나.** 카드 상태 파생 테이블을 v1에 넣는 안은
   기각. 원본이 둘이 되면 이벤트와 상태가 어긋났을 때 어느 쪽이 맞는지 판단하는
   코드가 필요해진다. 성능 문제가 실제로 생기면 그때 스토어를 추가한다
2. **KO_INTERFERENCE는 좁은 정의(정밀도 우선).** 넓히는 안(ONYOMI_CHOICE 중 한국음이
   하나인 것까지 포함)과 판정 보류 안(Phase 5에서 사람이 표시) 둘 다 기각
3. **오답 우선순위와 半濁·連声 묶기 현행 유지.** `HANDAKU`를 7번째 유형으로 분리하는
   안은 기각 — 오답 유형은 스키마 불변 조건이라 값 추가가 가볍지 않다

### 미검증 항목 두 개를 닫았다

- **Dexie 실제 경로.** `fake-indexeddb` 6.2.5를 devDependency로 설치(사용자 승인).
  테스트마다 `new IDBFactory()`를 새로 만들어 `YomenaiDB` 생성자 옵션으로 주입한다.
  전역을 오염시키는 `fake-indexeddb/auto` 대신 이 방식을 썼다 — DB 이름이 같아도
  저장소가 격리되고, 앱 코드에 테스트용 분기가 안 들어간다.
  `db.table('events').schema`로 **선언한 복합 PK가 실제 스토어에 그대로 붙는지**를
  직접 확인한다. 스키마 문자열만 보는 `schema.test.ts`가 못 잡는 부분이다
- **`tsconfig.app.json`의 `strict`.** 켰고 기존 코드 수정 없이 통과했다.
  이제 `src/core`·`src/db`도 strict 검사를 받는다

`npm test` — 11 파일 127 테스트 통과.

## 2026-09-04 — Phase 4 마무리 (조립 지점 + 지연 검수 신호)

### 조립 순서를 `src/core/session.ts`로 꺼냈다

100회 시뮬레이션이 검증하던 조립 순서가 테스트 안에만 인라인으로 있었다.
`replay → assignMode → selectSession`과 `정오답 판정 → classifyMistake → gradeFor → 이벤트`
둘 다 그랬다. 이대로 두면 Phase 5 UI가 같은 순서를 다시 엮게 되고, 그 순간
"세션 100회 통과"가 보장하는 게 없어진다. 테스트가 실물이 아닌 평행 구현을 검증하는 꼴이다.

`buildSession` + `recordReadingAnswer`/`recordMeaningAnswer`/`recordMeaningKnown`으로 빼고
시뮬레이션이 그걸 쓰게 바꿨다. 이제 화면과 시뮬레이션이 같은 경로를 탄다.

`newEventId`를 `db/events.ts`에서 `core/types.ts`로 옮겼다. 이벤트 정체성은 저장소가 아니라
도메인 관심사이고, 안 옮기면 core → db 역방향 의존이 생긴다. `db/events.ts`는 재수출만 한다.

### 지연 검수 — 새 이벤트 타입 없이 붙였다

Phase 3이 검수 일부(tier 1 재확인, 밴드0~1 Y 초벌 1, 밴드 2~3 Y 6,700여건, `主人` 포함)를
**카드 풀 진입 시점으로 미뤄뒀다.** Phase 4가 그 신호를 실어 나른다.

`SessionCard.needsClassReview` = `classSource !== 'manual'` && 신규 카드 && 아직 안 물음.
`korean-class.json`의 `classSource`는 `manual`(355, 확정) / `llm`(8,260) / `default`(8,598)다.

- 기각 — **분류 확정용 새 이벤트 타입(verdict 1/2/3) 추가.** 오답 유형과 마찬가지로
  이벤트 스키마는 불변 조건이라 값을 늘리는 게 가볍지 않다. 그리고 런타임에 필요한 건
  1 대 2·3 갈림(교정 대 확장)뿐이고, 2 대 3 구분은 사전 쪽 관심사라 빌드타임에 남는다
- 채택 — **기존 `meaningKnown` 재사용.** "뜻은 알고 계셨나요"의 답이 곧 동형동의 여부이고,
  모드 배정 2단계가 이미 이 값을 미확정 분류보다 우선한다. 물어볼 이유와 쓸 곳이 정확히 겹친다

**묻는 단위는 숙어다.** 확장 숙어는 한 세션에 읽기·뜻 두 장이 같이 나오는데 두 번 묻지
않도록 `buildSession` 안에서 숙어 단위로 막았다.

**남은 것.** 묻는 화면(Phase 5)과, 응답을 `korean-class.json`으로 되돌리는 도구.
후자는 이벤트 로그 → 검수 TSV 방향이라 Phase 3 도구 쪽 일이다.

### 검증

`npm test` — 12 파일 144 테스트 통과. `tsc -b` / `oxlint` 클린.

시뮬레이션 불변 조건 둘을 손봤다.

- `reps` 대조에서 `meaningKnown`을 뺐다. FSRS 카드를 안 건드리는 이벤트라
  안 빼면 지연 검수가 걸린 뜻 카드에서 개수가 어긋난다
- 지연 검수는 미확정 분류 숙어에 대해서만, 숙어당 정확히 한 번 물었는지 확인한다

### Phase 3 지연 검수 — 재집계 (Phase 1 버그 수정 반영)

`sK/iK/oK` 표기 제외로 코퍼스가 106,803으로 줄면서 tier 건수가 조금씩 바뀌었다.
`korean-review.tsv` + `korean-llm-draft.tsv` + `korean-class.json`을 읽기 전용으로
다시 세어 확정한 값이다 (`build:review-worklist --tiers=all`은 사용자의 작업 파일을
덮어쓰므로 돌리지 않았다).

| tier | 건수 | 내용 | classSource |
|---|---|---|---|
| 1 | 439 | `n` 초벌 1/2 — originMatch 오탐 복구 | llm |
| 2 | 355 | 밴드0 Y 초벌 2 — **검수 완료** | manual |
| 3 | 1,785 | `n` 초벌 3 (무관) | llm |
| 4 | 2,977 | 밴드0 Y 초벌 1 | llm |
| 5 | 361 | 밴드1 Y 초벌 2 | llm |
| 6 | 2,698 | 밴드1 Y 초벌 1 | llm |
| 9 | 6,741 | 밴드2~3 Y (초벌 없음, 잠정값) | default |

- 검수 큐 15,356행 중 **확정은 tier 2의 355건뿐**, 잔여 15,001
- 여기에 큐에 안 들어간 JP_UNIQUE 1,857(`default`)을 더하면 미확정 **16,858**
  = `korean-class.json` 17,213 − manual 355
- 이전 기록 대비 tier 1은 440 → **439**, tier 9는 6,756 → **6,741**

**체크리스트 문구가 지연 대상을 좁게 적고 있었다.** "tier 1 / 밴드0~1 Y 초벌 1 /
밴드2~3 Y"만 적혀 tier 3(1,785)·tier 5(361)가 빠져 있었다. 실제로는 `manual`이 아닌
전량이 대상이고, 런타임 `needsClassReview`도 `classSource !== 'manual'` 하나로 판정하므로
JP_UNIQUE까지 자연히 포함한다. 체크리스트를 실측값으로 고쳤다.

**규모가 커 보이지만 문제가 아니다.** 카드가 실제로 풀에 들어올 때만 묻는 구조라,
사용자가 평생 학습할 숙어 수만큼만 질문이 발생한다. 미리 16,858건을 볼 일이 없다는 것이
검수를 여기까지 미룬 이유다.

**`korean-worklist-t1.tsv`(439행)는 생성만 됐고 verdict가 전량 `?`다.** tier 1을
"초벌 신뢰"로 넘긴 상태라 파일을 채울 필요는 없지만, 지연 검수에서 다시 만난다.

---

## 2026-09-04 — Phase 5 착수 (UI 선행 조건)

Phase 5 는 UI 전체라 여러 커밋으로 나눈다. 사용자와 이번 세션 범위를 **선행 조건만**
(런타임 사전 번들 + 폰트 파이프라인 + `lang` 속성)으로 합의했다. 화면 구현은 다음 세션.

### 런타임 사전 번들 — 분할 JSON 확정 (미확정 #1 종결)

context-notes 2026-09-03 이 "분할 JSON 권고, 사용자 최종 확인만 남음"으로 남겨둔 것을
확정했다. 기각한 대안 — 단일 JSON(초기 로드에 밴드 4 89k 포함), sql.js(전문 검색 요구가
아직 없어 문서가 기각 방향).

`tools/build-runtime-dict.ts` 가 `data/dict/` 빌드 산출물을 런타임이 바로 fetch 하는
형태로 조립한다.

| 파일 | 크기 (raw / gzip) | 내용 |
|---|---|---|
| `public/dict/base.json` | 5.3 MB / **999 KB** | 밴드 0~3, 16,970 숙어 (기본 번들) |
| `public/dict/band4.json` | 18.9 MB / 2.6 MB | 밴드 4, 85,579 숙어 (opt-in 지연 로드) |
| `public/dict/pairs.json` | 234 KB / 46 KB | (한자, 음독) 쌍 3,940 — 음독 맵 화면 |
| `public/dict/kanji.json` | 149 KB / 35 KB | 등장 한자 2,130 자의 한국 한자음·음훈독 — 오답 상세 |

레코드 형태 — `{id, headword, reading, pos, band, common, category, classSource, koMeaning,
pairIds}`. `pairIds` 는 `onyomi-map.json` 의 `byIdiom` 분해를 `src/lib/onyomi.ts` 의
`pairId(k, base, kind)` 로 만든다 (`session.sim.test.ts` 가 쓰던 것과 같은 경로).

**음독 분해가 없는 숙어(247 + 4,007)는 번들에서 뺐다.** 熟字訓·当て字라 학습 대상이
아니고 Phase 2 의 설계된 거부다. `pairIds` 가 빈 카드는 미숙 음독 가중이 무의미하다.

**밴드 4 는 한국어 대조를 안 돌렸다** (Phase 3 는 밴드 0~3 만). `category`/`classSource`
가 `null` 로 나오고, 로더 `normalizeIdiom` 이 `2`(확장)/`default` 로 채운다 — 모드 배정
1단계 기본값과 같다.

### 로더 — `src/dict/load.ts`

`loadBaseIdioms()` / `loadBand4Idioms()` / `loadPairs()` / `loadKanji()`. 모듈 레벨에서
Promise 를 캐시해 중복 fetch 를 막는다. URL 은 `${import.meta.env.BASE_URL}dict/…` 라
GitHub Pages 서브패스 배포에도 맞는다.

`RuntimeIdiom extends IdiomEntry` — `buildSession` 이 요구하는 5개 필드
(`idiomId, band, category, classSource, pairIds`)를 그대로 담고 화면용
(`headword, reading, pos, common, koMeaning`)을 더한다. 화면은 이 하나만 부르면 된다.

### 산출물을 `.gitignore` 에 넣었다

`public/dict/` · `public/fonts/` 는 재생성 가능한 빌드 산출물이라 `data/dict/` 와 같이
커밋하지 않는다. Phase 7 배포 스크립트가 `vite build` 앞에서 만든다.
`npm run build:runtime-dict`.

### 검증

`src/dict/load.test.ts` 5 테스트 — `_meta.count` ↔ 레코드 수 일치, 전 레코드
`pairIds`·`band`(0~3)·`category`(1/2/3)·`classSource` 유효, `pairId` 형식,
`buildSession(pool, [], {now, limit:20})` 이 풀을 그대로 받아 신규 카드 20장 구성.
밴드 4 는 `2`/`default` 로 정규화됨을 확인.

`npm test` — 13 파일 149 테스트 통과. `tsc -b` / `oxlint` 클린.

### 폰트 서브셋 — Noto Serif JP 지역판을 학습 문자로 좁힌다

CLAUDE.md 규칙 — 한중일 통합 코드포인트라 폴백이 한 번이라도 나면 사용자가 한국
자형을 학습한다. 그래서 서브셋이 학습 문자를 100% 덮는지 빌드가 직접 검증하고,
못 덮으면 exit 1 한다.

**원본** — `data/raw/fonts/` (커밋 안 함, `data/raw/` 가 이미 `.gitignore`).

| 파일 | 출처 | 용도 |
|---|---|---|
| `NotoSerifJP-Regular.otf` (5.9 MB) | notofonts/noto-cjk `Serif2.003`, `12_NotoSerifJP.zip` (지역판 SubsetOTF/JP) | 서브셋 원본 |
| `Pretendard-Regular.woff2` (748 KB) | orioncactus/pretendard v1.3.9, `web/static/woff2/` | 한국어 UI (완성형, 서브셋 불필요) |

통합 CJK OTC 를 안 쓰고 **지역판 파일**을 쓴다 — context-notes 2026-09-03 "폰트" 절의
결정 그대로다. 지역판은 글리프가 JP 자형으로 기본 선택돼 `locl` 태깅에 안 기댄다.

**도구** — `subset-font` 2.7.0 (wasm hb-subset) 로 서브셋, `fontkit` 2.x 로 검증.
`@types/subset-font`·`@types/fontkit` 도 devDependency. (사용자 승인 "서브셋 도구 설치".)

**`tools/build-fonts.ts`.**
- 대상 문자 = `public/dict/base.json` 의 숙어·읽기 전 문자 + 히라가나 U+3041–3096 +
  결합기호 U+3099–309F + 가타카나 U+30A1–30FF + `　、。・「」『』（）〜〰々〆〇ー`.
  가나 전 구간을 넣는 건 입력 필드·음독 표시가 폴백을 안 타게 하려는 것.
  **U+3097·U+3098 은 미배정이라 넣으면 안 된다** — 처음에 U+309F 까지 훑다가 커버리지
  99.91% 로 실패했고, 상한을 3096 으로 고쳤다.
- hb-subset 은 원본에 없는 문자를 조용히 버린다. 그래서 요청 text 를 "원본 cmap 에
  실제로 있는 문자"로 먼저 거른다.
- 검증: fontkit 으로 원본·산출 woff2 의 `characterSet` 을 읽어, 대상 문자 중
  ① 원본에 없는 것(진짜 폴백 위험) ② 원본엔 있는데 산출에서 빠진 것(hb-subset 버그)
  을 각각 센다. 하나라도 있으면 exit 1.
- 결과 — 대상 2,228자, 커버리지 **100.00%**, 산출 `NotoSerifJP-subset.woff2` **698 KB**.
- 기본은 밴드 0~3(base)만. `--all` 로 band4 포함(문자 집합·용량이 크게 는다).
  **밴드 4 를 설정에서 켜면 폰트도 `--all` 로 다시 만들어야 한다.**

**`src/styles/fonts.css`** — `@font-face` 2종(`font-display: swap`) + `--font-ja`
(`'Noto Serif JP', 'Hiragino Mincho ProN', 'Yu Mincho', … , serif`) / `--font-ko`
(`'Pretendard', system-ui, …`). `:lang(ja)`/`:lang(ko)` 셀렉터가 스택을 건다.
`main.tsx` 가 `index.css` 앞에서 로드. `url('/fonts/…')` 은 base `'/'` 전제이고
GitHub Pages 서브패스는 Phase 7 대응.

`src/styles/fonts.test.ts` 3 테스트 — 骨直次令 + 가나 전 구간 + base 전량이 서브셋에
담겼는지. `npm test` — 14 파일 152 테스트 통과. `tsc -b` / `oxlint` 클린.

### 이번 세션에서 안 한 것 (다음 세션)

`lang="ja"` 를 실제 요소에 붙이는 것과 骨/直/令 **육안 자형 확인**은 화면이 없어서
못 했다. CSS 기계는 준비됐으니 화면 만들면서 검증한다. `wanakana.bind()` 입력 필드,
7개 화면, 다크 모드, 카드 전환 150ms 실측도 다음 세션.

---

## 2026-09-04 — Phase 5 (학습 카드 화면까지)

사용자와 이번 범위를 "최소 홈 → 학습 카드"로 합의. 네비게이션은 라이브러리 없이
상태 기반(`src/App.tsx` 의 `screen` state). 오답 상세·진입 진단·리포트·음독 맵·설정은 다음.

### 앱 셸·스타일

- 데모 스캐폴딩 정리 — `src/App.tsx` 재작성, `src/App.css` 삭제(재작성으로 고아가 됨).
  `src/assets/{hero.png,react.svg,vite.svg}` 는 이제 참조 없음 — 삭제 안 하고 남겨둠
  (Vite 스캐폴드 잔재, 다음에 정리).
- `src/index.css` 재작성 — 무채색 · 다크 우선 토큰. 라이트는 `prefers-color-scheme: light`
  오버라이드. 색 토큰(`--ok`/`--ng`)은 정오답 피드백 전용 (PLAN §7). 다크 모드 *토글*은
  아직 없다 — OS 설정만 따른다. 별도 체크리스트 항목으로 남김.

### 사전·이벤트 로드 경로

`useStudySession` 이 `loadBaseIdioms()` + `loadKanji()` + `listEvents(db, LOCAL_USER_ID)` 를
병렬로 받아 `buildSession(pool, events, {now, limit:20})`. 오답 판정용 `MistakeContext` 는
슬림 `kanji.json`(`{kr,on,kun}`)을 `{onyomi,kunyomi}` 로 어댑트하고 `buildKoSiblingIndex`
에는 `{koreanH,onyomi}` 로 넘긴다.

`deviceId` — `src/db/device.ts` 가 `localStorage['yomenai:deviceId']` 에 `crypto.randomUUID()`
를 한 번 굳힌다. 프라이빗 모드 등 실패 시 세션 한정 임시 id.

### 카드 채점 흐름 — 이벤트를 언제 쓰나

읽기 카드는 **답 제출 시점에 이벤트를 쓰지 않는다.** 자신감 버튼(쉬웠다·헷갈렸다)이
FSRS 등급을 바꾸는데 이벤트는 append-only 라 나중에 못 고친다. 그래서
`submitReading` 은 판정만 해 피드백을 띄우고(`feedback` state), `next(confidence?)` 에서
`recordReadingAnswer({..., confidence})` 로 이벤트를 만들어 `appendEvent`.
정답이면 [다음]/[쉬웠다]/[헷갈렸다] 셋 다 이 경로, 오답이면 [다음](Again)만.

뜻 카드는 자기 채점 — "알았어요"면 멈추지 않고 바로 넘어가고(정답), "몰랐어요"면
뜻을 다시 보여준 뒤 [다음] (PLAN §7 "오답일 때만 멈춘다").

지연 검수(`needsClassReview`) — 카드 앞에 "뜻을 이미 알고 계셨나요" 프롬프트를 끼운다.
답은 `recordMeaningKnown` 이벤트. 모드 배정 2단계가 이 값을 미확정 분류보다 우선하므로
새 이벤트 타입이 필요 없다 (Phase 4 결정 그대로). 숙어당 한 번은 `buildSession` 이 이미 보장.

### 카드 전환 150ms 실측 — Playwright + 설치된 Chrome

- 사용자 선택. `@playwright/test` 만 설치하고 `channel: 'chrome'` 로 브라우저 다운로드 없이 돈다.
- 계측 — `useStudySession.advance()`/`answerClassReview()` 가 `performance.mark('yomenai:advance')`
  + `transitionSeq` 증가. `Study` 가 `transitionSeq` 변화에 **단일 rAF** 로
  `performance.mark('yomenai:shown')` + `measure('yomenai:transition', …)`.
  단일 rAF = DOM 커밋·레이아웃 뒤 페인트 직전. `transitionSeq` 로 트리거해서 피드백 표시
  같은 다른 렌더가 measure 를 중복 발생시키지 않게 했다 (처음엔 `[index,status]` 로 걸었다가
  전환 1회에 measure 가 2번 찍혔다).
- 결과 — 전환 36회, **중앙값 9ms / p95 10.4ms / 최대 10.7ms**. 150ms 대비 여유 큼.
  트랜지션 애니메이션을 카드에 안 걸었고 풀은 세션 시작 전에 로드된다.
- `tsconfig.e2e.json` 추가(루트 tsconfig references 에 연결) — `tests/` 도 `tsc -b` 검사.

### 검증

`npm test` 14파일 152 통과(불변). `npm run e2e` 통과. `tsc -b` / `oxlint` / `vite build` 클린.
라이트·다크 스크린샷으로 레이아웃·`lang="ja"` 자형(忠実 의 実)·wanakana 변환(chuujitsu→ちゅうじつ) 확인.

---

## 2026-09-04 — Phase 5 (나머지 화면: 오답 상세)

### 오답 상세 — 세션 내부 오버레이, 별도 screen 아님

읽기 오답 피드백에서 "자세히" 버튼으로 `ReadingCard` 안에서 `<MistakeDetail>` 로 교체한다.
`App.tsx` 의 `screen` state 를 늘리지 않았다 — 오답 상세는 항상 특정 카드의 맥락에서만
의미가 있고, 홈에서 독립적으로 열 이유가 없다. 체크리스트의 "또는 별도 접근"은
피드백 진입 하나로 충족된다고 봤다.

### 같은 음독 다른 숙어 — base 풀 역인덱스, 새 산출물 없음

`src/dict/pairIndex.ts` `buildPairIndex(pool)` 가 `loadBaseIdioms()` 결과를 pairId →
숙어[] 로 접는다. 빌드타임에 역인덱스 파일을 따로 만드는 안은 기각 — base.json 이 이미
런타임에 로드돼 있고(16,970건) 역인덱스는 1회 O(n) 이라 배포 자산을 늘릴 이유가 없다.
`loadPairIndex()` 가 모듈 레벨 Promise 로 캐시한다.

### 검증

`src/study/mistakeDetail.test.ts` 6 테스트 — `breakdown` 이 pairIds 를 (한자, 음독) 조각으로
펼치고 한국 한자음 병기, `buildPairIndex` 역인덱스가 자기 자신 제외 + limit. 스크린샷으로
忠実 분해(실 이 일본 자형 実) 레이아웃 확인. `tsc -b` / `oxlint` 클린.

### 앱 셸 확장 — screen enum + 홈 네비, 라우터 없음

`App.tsx` 를 `switch (screen)` 로 바꾸고 `Screen = 'home' | 'study' | 'onyomi' | …` 로
늘려간다. `Home` 은 `onNavigate(screen)` 하나만 받는다. 공통 스타일은 `src/app/screens.css`
(셸 화면은 카드 루프가 아니라 느린 스크롤 허용).

### 환경설정 — `src/app/settings.ts`, localStorage

세션 길이·모드 비율을 `yomenai:settings` 키에 둔다. `parseSettings` 가 클램프(길이 5~40)·
검증(ratio 합 > 0)을 하고 깨진 값은 기본값. `useStudySession` / `Home` 이 `loadSettings()` 로
읽어 `buildSession` 에 `limit`·`ratio` 로 넘긴다. `SESSION_LIMIT` 상수는 제거 —
`DEFAULT_SETTINGS.sessionLimit`(20)로 대체됐다. IndexedDB 스키마와 무관(사전/사용자 DB
경계 밖의 UI 설정)이라 localStorage 가 맞다.

## 2026-09-04 — Phase 5 (음독 맵)

### 숙달 분모 — pairs.json 전체가 아니라 base 풀이 참조하는 쌍

`pairs.json` 은 코퍼스 전체의 3,940 쌍이지만, 사용자가 만나는 건 밴드 0~3(base) 이
참조하는 3,000 쌍뿐이다. 진도 지표의 분모는 후자로 잡는다 —
`pool.flatMap(p => p.pairIds)` 를 `pairRows` 에 넘긴다. 기각 — pairs.json 크기(3,940)를
분모로 쓰는 안. 밴드 4 를 안 켠 사용자에게 영영 안 나오는 쌍이 분모에 껴서
"숙달률"이 구조적으로 낮게 나온다.

### 숙달 기준 — `seen≥3 && wrong/seen≤0.2`, 파생 상태 저장 없음

PLAN §7 "단순하게". 3상태(mastered/learning/unseen)를 `replay().onyomi` 집계에서 즉석
계산한다. 별도 테이블 없음(Phase 4 결정과 같은 이유). 정렬은 학습 중(오답률 내림차순)
→ 미학습 → 숙달. 화면은 기본적으로 "학습 중"만 보이고 토글로 전체를 편다 — 3,000행을
항상 그리지 않으려는 것.

### 검증

`src/core/onyomiMap.test.ts` 4 테스트 — 상태 분류, 중복 pairId 접기, 정렬 순서, 요약 집계.
스크린샷 `04-onyomi-map.png`(빈 상태) / `04-onyomi-map-all.png`(전체 목록, `lang="ja"` 자형).
`tsc -b` / `oxlint` 클린.

## 2026-09-04 — Phase 5 (진단 리포트)

### 리포트는 `replay()` 재파생 — 별도 집계 저장 없음

`Report.tsx` 가 `listEvents` → `replay(events, {pairsOf})` → `buildReport(state, pairs, nameOf)`.
`mistakeTotals` (기존)로 유형 분포, `state.onyomi` 로 취약 음독, `state.cards[*].mistakes.KO_INTERFERENCE`
로 간섭 숙어를 뽑는다. Phase 4 의 "상태를 저장하지 않는다" 를 화면에서도 지킨다.

### 취약 음독 기준 — `seen≥3 && wrong>0`, 오답률 내림차순 상위 8

음독 맵의 숙달 기준(`wrong/seen≤0.2`)과 다르다. 리포트는 "고쳐야 할 것"이라
오답이 하나라도 있으면(그리고 표본이 되면) 싣는다. 상위 8만 — 리포트는 훑는 화면이다.

### 차트 — 인라인 CSS 막대, 라이브러리 없음

`.bar-track`/`.bar-fill` 를 `display:block` 로 명시(그리드 아이템은 blockify 되지만 그
자식은 아니라, 안 하면 폭 0). 채움색만 `--ng` — PLAN §7 "색은 정오답 피드백과 리포트
차트에만". 한국음 간섭 콜아웃은 색 대신 좌측 보더 + 큰 숫자로 강조(무채색).

### 검증

`src/core/report.test.ts` 5 테스트 — 유형 분포 정렬/0 제외, 읽기 reps 만 합산, 취약 음독
필터·정렬, 간섭 숙어 수집, 빈 리포트. 시드 이벤트(실제 base.json id)로 스크린샷 2장.
`npm test` 17파일 167 통과. `tsc -b` / `oxlint` 클린.

## 2026-09-04 — Phase 5 (진입 진단)

### 무작위 표본 시드 — `DIAGNOSTIC_SEED = 20260904` 명시

context-notes 2026-09-04 "출제 선택" 절이 "진입 진단의 무작위 표본은 Phase 5에서 시드를
명시해 따로 만든다" 고 한 것을 이행. `pickDiagnostic` 은 mulberry32 로 밴드별 Fisher-Yates
셔플 후 30개씩. `selectSession` 은 여전히 무작위를 안 쓴다 — 진단만 시드 무작위다.

### 범위 — 밴드 1~3 각 30개(총 90). 밴드 0/4 제외

PLAN §6 "각 밴드에서 30~50개". 하한 30 채택 — 90문항도 일회성 진단으로는 길지만
`selectSession` 기본 학습 범위(minBand 1, maxBand 4 중 4는 선택)와 맞춘 게 밴드 1~3 이다.
밴드 0 은 "건너뛰기 기본", 밴드 4 는 "선택"이라 진단 대상이 아니다.

### 완료 판정 — localStorage 플래그, 결과는 이벤트 로그

`src/app/diagnostic-state.ts` 의 `yomenai:diagnosticDone`. 체크리스트가 "이벤트 로그에서
파생(밴드 커버) 하거나 localStorage 플래그" 를 허용했고, 후자가 단순하다. 진단 응답
자체(`review` + `meaningKnown` 이벤트)는 전부 로그에 있어 리포트·모드 배정이 바로 쓴다.
스키마 변경 없음. 완료하면 홈에서 "진입 진단 시작" 버튼이 사라진다(자동 리다이렉트는
안 건다 — 라우팅 복잡도·e2e 취약).

### "시작점 결정" — 표시만, 선택 로직은 안 건드림

결과 화면이 밴드별 정답률과 "밴드 N 부터 흔들립니다" 문구를 보여준다. `selectSession`
의 `minBand` 를 자동으로 바꾸는 것은 하지 않았다(요청 범위 밖, 추측성). 선택 로직은
이미 밴드 오름차순 도입이라 진단 없이도 낮은 밴드부터 나온다.

### `src/dict/mistakeContext.ts` — 오답 판정 컨텍스트 어댑터 추출

`useStudySession` 에 인라인이던 "slim kanji.json → MistakeContext" 변환을 꺼냈다.
`Diagnostic` 도 같은 변환이 필요해서 — 두 벌이 되면 한쪽만 고치는 사고가 난다.
`useStudySession` 은 한 줄(`mistakeContextFromKanji(kanji)`)로 줄었다.

### study.css — `.card-bottom` 가로 넘침 수정

`.card-bottom { align-self: end }` 는 교차축이 가로라 내용 폭으로 줄어들며 카드 왼쪽으로
흘러넘쳤다(진단 카드 입력 필드가 화면 밖으로). `align-self: stretch` 로 바꾸고
`.answer-row { justify-content: flex-end }` 로 버튼만 있는 행의 오른쪽 정렬을 유지했다.
세로 위치는 원래 `.card > .card-bottom { margin-top: auto }` 가 잡고 있어 영향 없다.
읽기/뜻/지연검수 카드 스크린샷으로 회귀 없음 확인.

### 검증

`src/core/diagnostic.test.ts` 6 테스트 — 시드 결정론, 밴드당 개수, 중복 없음, 밴드
오름차순, 요약 집계(meaningKnown·밴드 미상 제외). 스크린샷 3장(ask/known/result).
`npm test` 18파일 173 통과. `tsc -b` / `oxlint` 클린.

## 2026-09-04 — Phase 5 (홈 / 설정)

### 설정 값은 변경 즉시 localStorage 에 쓴다 (저장 버튼 없음)

`Settings.tsx` 가 스테퍼·프리셋을 누를 때마다 `saveSettings`. 세션 길이는 스테퍼
(5~40, step 5), 모드 비율은 프리셋 3개(7:3 / 5:5 / 읽기만=10:0). 자유 입력 비율 슬라이더는
안 만들었다 — `selectSession` 의 `ratio` 는 상대값이라 프리셋으로 충분하고, 입력 검증
표면을 줄인다. 백업은 Phase 7 이라 비활성 안내만.

### `parseSettings` 가 방어선

localStorage 는 사용자가 건드릴 수 있고 버전이 바뀌면 형태가 달라진다. `loadSettings`
가 항상 `parseSettings` 를 거쳐 클램프(길이)·검증(ratio 합>0)하고 깨진 값은
`DEFAULT_SETTINGS` 로 되돌린다. 반환값은 매번 새 객체(기본값과 참조 공유 안 함).

## 2026-09-04 — Phase 5 (다크 모드 토글)

### `data-theme` 속성 + 3-상태 (system / light / dark)

기존엔 `@media (prefers-color-scheme)` 만 따랐다. `src/app/theme.ts` 가 root 의
`data-theme` 를 세우고 `yomenai:theme` 에 저장한다. `system` 은 속성을 **지운다** —
그러면 CSS 가 자동으로 OS 미디어 쿼리로 되돌아간다. `main.tsx` 가 `createRoot` 전에
`applyTheme(loadTheme())` 해서 깜빡임이 없다.

### CSS — 속성 선택자 특이도로 우선순위 확보

`:root[data-theme='dark']` 는 특이도 (0,1,1) 이라 바깥 `:root` (0,0,1) 과
`@media … :root` (0,0,1) 을 소스 순서와 무관하게 이긴다. 그래서 OS 가 라이트여도
사용자가 다크를 고르면 다크가 나온다. 토큰 블록을 복제했다 — CSS 변수는 "변수의
변수" 재사용이 지저분해서, 값 12개를 두 블록에 그대로 적는 게 낫다. `color-scheme`
도 명시해 폼 컨트롤(progress·input)이 테마를 따르게 했다.

### 검증

`src/app/theme.test.ts` 2 테스트 — `parseTheme`(light/dark 만 통과), `applyTheme`(stub root
에 data-theme 세우기/지우기). 스크린샷 라이트·다크, reload 후 유지 확인. `npm test`
20파일 180 통과.

## 2026-09-04 — Phase 5 (전체 흐름 e2e)

### `tests/e2e/full-flow.spec.ts` — 진단 → 세션 → 리포트 완주

테스트 첫 스텝에서 `indexedDB.deleteDatabase('yomenai')` + `localStorage.clear()` 후
reload 로 결정론을 잡는다(체크리스트 권고). 진입 진단은 90문항을 전부 오답('zzz')으로
빠르게 넘기고, 결과 화면의 밴드 막대 3개 → 리포트의 리드·섹션 제목·간섭 콜아웃을
확인한다. 이어서 세션을 완주하고 리포트를 재진입한다.

### `workers: 1` 로 직렬화

`playwright.config.ts` 에 추가. `fullyParallel: false` 는 파일 내부만 직렬이라
`card-transition` 과 `full-flow` 가 2워커로 동시에 떠서 같은 오리진의 IndexedDB 를
서로 건드릴 수 있었다(full-flow 의 deleteDatabase). 단일 워커면 순서가 고정된다.

### 최종 상태

`npm test` 20파일 180 통과 · `tsc -b` 클린 · `oxlint` 클린 · `npm run e2e` 2 스펙 통과
(full-flow 12.4s, card-transition p95 14.3ms) · `vite build` 성공(js 363 kB / gzip 116 kB).

---

## 2026-09-04 — 오답 유형 enum 에 `OKURIGANA` 예약 + 훈독 숙어 취급 논의

### 배경 — 동사·형용사형 한자 조합을 넣을 수 있나

현재 코퍼스는 `import-jmdict.ts` 가 표제어를 `isKanjiOnly(keb) && 2자 이상` 으로 걸러
**한자 전용**이다. 그래서:

- する동사·な형용사 중 순수 한자 2자(認識·発達·明白·忠実)는 **이미 들어 있다**. `pos` 에
  `vs`/`adj-na` 가 그대로 담긴다. 추가할 데이터 없음
- 오쿠리가나가 붙는 동사·い형용사(認める·定める·忙しい·険しい)는 **설계상 제외**

(B) 오쿠리가나 부류를 넣으려면 건드릴 곳 — ① 코퍼스 필터 ② 음독 분해 엔진
(`src/lib/onyomi.ts` `decompose` 이 표제어를 한자 시퀀스로 가정. 認める=[認,め,る] 라
가나 리터럴 통과 + 認만 (한자, よみ) 쌍) ③ 파이프라인 전체 재실행 ④ 한국어 대조
(認める는 한국 한자어가 없어 대부분 JP_UNIQUE/NEEDS_REVIEW) ⑤ 포지셔닝(훈독 동사는
1차 목적 "음독 교정"과 성격이 다름).

**스키마는 안 바뀐다.** `IdiomRecord` 는 `headword`/`reading` 이 문자열이고 한자 전용을
타입으로 강제하지 않는다. 이벤트 로그도 `idiomId` 만 늘 뿐이다. IndexedDB 불변 조건
설계가 이 문을 열어뒀다 — 나중에 파이프라인+엔진 작업으로 추가 가능, 마이그레이션 없음.

### 결정 — `OKURIGANA` 오답 유형을 지금 enum 에 예약한다

**오답 유형은 스키마 불변 조건**(CLAUDE.md)이라 나중에 값을 늘리는 게 가볍지 않다.
`RENDAKU` 를 `HANDAKU` 로 쪼개는 안을 Phase 4 에서 "값 추가가 가볍지 않다"는 이유로
기각한 것과 같은 선상이다. (B) 확장을 언젠가 할 가능성이 있으므로, 하드코스트가 낮은
지금 **자리만 잡아둔다.**

기각한 대안 —
- **예약 안 하고 나중에 추가** — (B) 확장 시점에 enum 을 늘리면, 그 전에 쌓인 이벤트에
  그 유형이 없어 진단 리포트가 소급 반쪽이 된다. `cardType`/`source` 를 v1 에 넣은 것과
  같은 이유로 미리 넣는 게 맞다
- **기존 6종으로 커버** — 認る 류 오답은 `RENDAKU`/`CHOON`/`null` 로도 잡히긴 하나,
  "활용 어미를 틀렸다"는 진단 축이 뭉개진다. (B) 를 진지하게 할 거면 전용 축이 필요

**변경 범위 (분류기 로직은 안 건드린다 — 예약의 핵심).**
- `src/core/types.ts` `MistakeType` 에 `| 'OKURIGANA'` + 예약 주석
- `src/study/mistakeLabels.ts` `MISTAKE_LABEL` 은 `Record<MistakeType,…>` exhaustive 라
  `OKURIGANA: '오쿠리가나'` 추가 (화면엔 안 뜬다 — 분류기가 안 내므로)
- `src/core/report.ts` `MISTAKE_ORDER` 끝에 `'OKURIGANA'` (진단 가치 최하위)
- `src/core/mistakes.test.ts` `CASES` 를 `Partial<Record<…>>` 로 (대표 케이스 없음)
- `session.sim.test.ts` "6종뿐" 테스트는 그대로 6종 — 분류기가 안 내는 게 맞다. 제목만 정정
- `CLAUDE.md` / `PLAN.md §6` 표에 반영

`classifyMistake` 는 손대지 않았다. `DECOMPOSED_PRIORITY` 도 5종 그대로. (B) 를 실제로
구현할 때 오쿠리가나 자리 비교를 넣고 이 배열에 우선순위를 끼운다.

### 훈독 숙어를 별도 메뉴로? — 지금은 아니다, 세션 필터로 남겨둔다

**현황** — 순수 훈독 2자 숙어(浜辺·生糸·山奥·北風)는 이미 코퍼스에 있고, 음독 매핑이
각 쌍을 `kind: 'on'|'kun'` 으로 태깅한다. 음훈 혼독(重箱·湯桶)은 9,585건으로 `MIXED_READING`
이라는 전용 오답 유형까지 있다. `selectSession`/`mode` 는 아직 음독 숙어와 훈독 숙어를
구분하지 않는다.

**별도 메뉴 찬성 논거** — 음독 교정은 "한국 한자음은 아는데 일본 음독이 다르다"라
사용자의 한자어 지식이 자산이자 간섭원이다. 훈독은 한국어 다리가 없는 순수 어휘·읽기
암기라 학습 문제가 다르다. 진단 리포트의 축(`ONYOMI_CHOICE`·`KO_INTERFERENCE`)이
음독 중심이라 훈독 오답이 섞이면 "진단 도구" 신호가 흐려진다 (PLAN §0).

**그럼에도 지금 별도 메뉴를 안 만드는 이유** —
- 과설계. 본인이 3개월 써보기 전엔 제품 형태를 확정하지 않는다는 PLAN 원칙과 같다
  (브랜딩·인증을 미룬 것과 동일)
- 훈독의 큰 덩어리는 실은 重箱/湯桶 혼독이라 음독 성분이 있고 음독 교정 프레임의
  이득을 본다. 깨끗한 이분이 아니다
- 밴드 0~3 의 순수 훈독 2자 숙어는 수가 적어 독립 정보구조를 세울 만큼은 아닐 수 있다
- 더 가벼운 수단이 있다 — `selectSession` 에 `kind` 기반 필터(훈독 포함/제외)나
  밴드 4 처럼 opt-in 토글. 별도 IA 보다 싸다

**대신 지금 인지해 둘 것** — 진단 리포트가 훈독-only 오답을 별도 버킷으로 빼거나
제외해야 음독 교정 신호가 깨끗하다. (B) 확장 여부와 무관하게 유효한 우려다.

**결론** — 훈독은 별도 메뉴가 아니라 세션 모드/필터 후보로 남긴다. (B) 오쿠리가나
동사를 넣게 되면 그때 같은 필터로 묶는다. 실사용에서 훈독이 "못 읽겠다"의 잦은
원인으로 드러나면 승격한다 (데이터 기반, PLAN "3개월 후 판단").

---

## 2026-09-04 — 일본어 폰트를 명조 → 산스로 변경 + 입력 포커스 확대 방지

### 일본어 폰트: Noto Serif JP → Noto Sans JP (결정 번복)

**당초 결정**(context-notes 2026-09-03 폰트 절, PLAN §7) — 숙어·가나는 `Noto Serif JP`
(명조). 근거는 ① CJK 자형 정확성 ② 숙어와 UI(산스)를 시각적으로 구분 ③ 명조가 한자
학습의 전통 자형.

**변경**(사용자 요청) — 산스로 통일. `Noto Sans JP` 지역판 OTF(notofonts/noto-cjk
`Sans2.004`, `16_NotoSansJP.zip`)를 원본으로 같은 서브셋 파이프라인에 태운다.

- ①(자형 정확성)은 서체 계열과 무관하다. 지역판 산스도 글리프가 JP 자형으로 기본
  선택되고, `lang="ja"` + 서브셋 100% 커버라 한국 자형 폴백이 안 난다 (스크린샷에서
  忠実의 実가 신자체로 렌더됨 확인)
- ②(구분)은 약해지지만, UI 는 Pretendard 로 그대로 산스라 크기·자간·배치로 구분된다
- 산출 용량은 오히려 준다 — 서브셋 woff2 698 KB(명조) → **496 KB**(산스)

**변경 범위** — `tools/build-fonts.ts`(원본 파일명·산출 파일명), `src/styles/fonts.css`
(`@font-face` family `Noto Sans JP`, `--font-ja` 스택을 일본 고딕 폴백 `Hiragino Kaku
Gothic ProN` / `Yu Gothic` / `Meiryo` / `sans-serif` 로, 최후는 `serif` 대신 `sans-serif`),
`src/styles/fonts.test.ts`(경로), PLAN §7, checklist. `data/raw/fonts/` 원본 교체
(Serif 제거). `--font-ja` CSS 변수명은 그대로 둬서 `study.css`·`screens.css` 는 안 건드림.

**기각 안** — 파일명을 `NotoJP-subset.woff2` 처럼 서체 중립으로. 지금 한 번 더 바꿀
계획이 없어 `NotoSansJP-subset.woff2` 로 명시. 다시 바꾸면 그때 rename.

### 입력 포커스 시 화면 확대 방지

iOS Safari 는 `font-size < 16px` 인 입력에 포커스가 가면 화면을 확대한다.

- `index.html` viewport 메타에 `maximum-scale=1, user-scalable=no` 추가.
  접근성상 핀치 줌을 막지만 개인용 PWA 이고, 숙어는 이미 `clamp(48px,16vw,88px)` 로 크게 띄운다
- `src/index.css` 의 `input` 규칙에 `font-size: max(16px, 1rem)` 하한. 유일한 입력
  `.kana-input` 은 이미 22px 라 안전하지만 미래 입력까지 커버

### 검증

`npm test` 20파일 180 통과 · `tsc -b` / `oxlint` 클린 · `build:fonts` 커버리지 100% ·
`vite build` 성공. 다크 스크린샷 `scratchpad/sans-reading.png`·`sans-feedback.png`.

---

## 2026-09-04 — Phase 6 착수: TTS (확인 단계 전용)

### 범위 — Tatoeba 는 이번에 안 하고 TTS 만

사용자 요청이 TTS 로 한정됐다. Tatoeba 예문 커버리지 실측은 Phase 6 의 남은 항목으로
그대로 둔다.

### TTS 위치 — 문제 풀이가 아니라 확인 단계에만 (사용자 요청)

기각한 대안 — **문제 풀이 화면(숙어 제시 → 입력)에도 소리 버튼을 둔다.** 읽기 카드는
"숙어를 보고 읽기를 산출"하는 카드다(PLAN §6 "객관식이 아니어야 한다. 보기에서 역추론이
되면 산출 능력을 측정하지 못한다"). 문제 풀이 중 정답 발음을 들려주면 정확히 그
역추론 경로가 열린다. 그래서 소리 버튼은 **읽기 카드는 피드백(정오답 확인) 이후에만,
뜻 카드는 뜻을 확인한 뒤(`revealed || graded`)에만** 렌더한다.

### 무엇을 읽어주나 — 헤드워드가 아니라 읽기(히라가나)

기각한 대안 — **한자 헤드워드를 그대로 읽힌다.** `汽車`·`工夫` 같은 동형이의어는 엔진이
어느 뜻으로 읽을지 모호하고, 잘못 읽으면 이 앱이 교정하려는 바로 그 오류(엉뚱한 음)를
강화한다. **채택 — 정답 읽기(`expected`/`idiom.reading`, 이미 확정된 히라가나)를
`lang='ja-JP'` 로 읽힌다.** 세그먼트 발음은 정확하고, 피치 악센트가 부정확할 수 있는
정도의 손실만 감수한다. 읽기 교정이 1차 목적이라 이 트레이드오프가 맞다.

### 자동재생 안 함 — 버튼 탭

기각한 대안 — **오답 시 자동재생.** 교정 효과는 크겠지만 카드 루프가 0~1 탭을 목표로
하는데(PLAN §7) 매번 오디오가 끼어들면 그 리듬이 깨지고, 빠르게 넘기는 사용자에게는
소음이 된다. 버튼으로 남기고, 실사용에서 필요하면 설정에 "오답 시 자동재생" 토글을
추가한다 (지금은 안 만듦 — 요청받지 않은 설정성).

### 구현 — `src/study/tts.ts`

`Tts` 인터페이스(`available`/`speak`/`cancel`) 뒤에 `createWebSpeechTts()`. PLAN §7 이
말한 "TTS 인터페이스 분리"가 이거다 — 나중에 다른 엔진(서버 TTS 등)으로 바꿔도 화면
쪽(`ReadingCard`/`MeaningCard`)은 안 건드린다.

- `synth`/`Utterance` 를 **주입받는다**(기본값은 브라우저 전역). node 테스트 환경엔
  `window` 가 없어 자동으로 `available:false` 가 되고, 테스트는 가짜 엔진을 넣어 검증한다
- `ja-JP` 음성은 `voiceschanged` 이벤트로 비동기 채워진다 — 없어도 `lang='ja-JP'` 만으로
  진행(엔진이 알아서 고름). 발화 직전 `synth.cancel()` — 카드가 빠르게 넘어가도 겹쳐 읽지
  않게
- `rate: 0.9` — 학습용으로 살짝 느리게

### 검증

`src/study/tts.test.ts` 4 테스트 — 엔진 없음(no-op) / 빈 입력 무시 / ja-JP·이전 발화
취소 / ja 음성 없을 때 lang 만으로 진행.

`tests/e2e/tts.spec.ts` — `speechSynthesis.speak` 를 가로채 호출 인자를 기록.
실브라우저에서 문제 풀이 화면엔 "소리 듣기" 버튼이 0개, 피드백 진입 후엔 나타나고
클릭하면 `ja-JP` 로 호출됨을 확인. 스크린샷 `scratchpad/tts-question.png`
(버튼 없음) / `tts-feedback.png` (버튼 있음, 忠実→ちゅうじつ 아래 배치).

`npm test` 21파일 184 통과 · `tsc -b`/`oxlint` 클린 · `npm run e2e` 3 스펙 통과
(전환 p95 15.1ms) · `vite build` 성공.

### 카카오톡 중복 전송 — 재시도 정책 변경

`KakaotalkChat-MemoChat` 이 "session expired" 로 실패한 뒤 곧바로 재시도해 성공시켰더니
사용자에게 같은 메시지가 두 번 도착했다. 첫 시도가 에러를 반환해도 서버 쪽에서는
전송이 됐을 가능성이 있다는 뜻이다. **앞으로 이 도구가 에러를 내면 자동으로 즉시
재시도하지 않는다** — 결과를 있는 그대로 보고한다. 코드 변경 사항 아님, 이 세션의
행동 정책.

---

## 2026-09-04 — Phase 6: Tatoeba 예문 커버리지 실측

### 데이터 (data/raw/tatoeba/, 커밋 안 함)

| 파일 | 내용 | 다운로드 |
|---|---|---|
| `jpn_sentences_detailed.tsv` | 일본어 문장 248,888개 | downloads.tatoeba.org/exports/per_language/jpn/, 2026-09-04 |
| `jpn-kor_links.tsv` | 일-한 문장 번역쌍 2,261건 | 〃 |
| `jpn-eng_links.tsv` | 일-영 문장 번역쌍 280,653건 (참고용) | 〃 |

`tools/measure-tatoeba.ts` — 표제어 첫 글자로 후보를 좁힌 뒤 `startsWith` 로 표기 매칭
(읽기 검증 없음, 동형이의어는 구분 못 함 — 순수 조사 도구라 감수). 산출물 안 남기는
일회성 스크립트. `npm run measure:tatoeba`.

### 결과

**표제어가 담긴 문장이 있는 숙어 비율 (밴드별, 표기 매칭)**

| 밴드 | 커버리지 |
|---|---|
| 0 | 90.5% (3,631/4,014) |
| 1 | 75.0% (2,935/3,915) |
| 2 | 65.8% (988/1,501) |
| 3 (PLAN §4 "주력") | **50.4%** (3,921/7,787) |
| 4 (선택) | 12.5% |
| 밴드 0~3 합계 | 66.6% (11,475/17,217) |

**한국어 번역 연결 — 0.9%.** 전체 일본어 문장 248,888개 중 한국어 번역쌍은 2,261건.
영어 번역은 280,653건으로 넉넉하지만, PLAN §6 이 이미 영어 gloss 경유를 기각했다
("일본어 → 영어 → 한국어 2단계 변환… 영어 gloss는 DB에 보관만 하고 화면에는 안 띄운다").
그 원칙을 예문에도 그대로 적용하면 **번역이 붙은 예문은 사실상 안 된다.**

### 판단 — 무번역 예문만 채택 (사용자 확인, 2026-09-04)

숙어 표기만 나오는 예문(무번역)은 밴드 0~1 에선 쓸 만하지만(75~90%), PLAN 이 "주력"
이라 부르는 밴드 3 은 절반뿐이라 카드마다 있다 없다가 들쭉날쭉해진다. 번역 붙은
예문은 원천적으로 거의 불가능하다(0.9%).

기각한 대안 둘 — **Phase 자체를 뺀다**(checklist 의 escape 조항. 무번역이라도 참고
용도로 값이 있다고 판단해 안 뺐다), **다른 예문 출처를 찾는다**(국립국어원 등. 추가
조사가 필요해 이번 작업 범위 밖으로 미룸). **채택 — 무번역 예문을 확인 단계에만
곁들인다.** 교정 모드(뜻은 이미 앎) 카드에 특히 맞고, 확장 모드 읽기 카드에 나와도
해는 없다(뜻 카드가 아니라 읽기 카드에만 붙이므로 뜻을 미리 보여주는 문제가 없다).

### 구현 — `tools/build-examples.ts` + `src/dict/load.ts` + `ReadingCard`

- **선별 규칙** — 표제어 문자열 매칭(읽기 미검증, measure-tatoeba.ts 와 같은 방식) 후보
  중 **60자 이하만, 그중 짧은(=쉬운) 순으로 최대 3개.** 순수 함수 `pickExamples()` 로
  빼서 `tools/build-examples.test.ts` 5 테스트로 고정. 결과 — 밴드 0~3 대상 17,217개 중
  11,377개(66.1%, 길이 필터로 실측치 66.6%보다 살짝 줄었다)에 예문 확보, 문장 27,517개.
  `data/dict/examples.json` 1.5 MB(재생성 산출물, `data/dict/` 관례대로 미커밋)
- **런타임 필터링** — `build-runtime-dict.ts` 가 `data/dict/examples.json` 을 읽어 실제
  `base`/`band4` 에 실린 id 로만 좁혀 `public/dict/examples.json` 을 낸다(534 KB gzip).
  원본이 없으면(`build:examples` 를 안 돌렸으면) 조용히 건너뛴다 — Tatoeba 원본은 수동
  다운로드라 다들 받아두는 게 아니다. `tools/lib/tatoeba.ts` 로 TSV 파서를
  `measure-tatoeba.ts` 와 공유(중복 제거)
- **지연 로드** — `loadExamples()`(`src/dict/load.ts`) 는 세션 시작 시 안 부른다.
  534 KB gzip 을 매 세션 초입에 얹으면 base.json(999 KB gzip)과 합쳐 초기 로드가
  거의 두 배가 된다. 대신 **읽기 카드 피드백이 처음 렌더될 때** `ReadingCard` 의
  `ExampleSentence` 서브컴포넌트가 fetch 한다 — 캐시된 Promise 라 카드마다 재요청 안 함.
  실패(산출물 없음)해도 `.catch(() => new Map())` 로 조용히 빈 기능이 된다
- **위치 — TTS 와 같은 확인 단계 전용.** 문제 풀이 화면(숙어 → 입력)엔 안 보이고,
  읽기 카드 피드백에서만. 뜻 카드엔 안 붙였다 — 확장 모드는 무번역 예문이 "별 도움
  안 됨"(사용자 확인 시 옵션 설명) 이라는 판단 그대로다
- `ExampleSentence` 는 `key={idiomId}` 로 카드마다 새로 마운트해 상태를 리셋한다.
  처음엔 effect 안에서 `setSentence(null)` 을 직접 불렀다가 oxlint
  `react(set-state-in-effect)` 경고를 만났고, `KanaInput` 이 이미 쓰던 key 리마운트
  패턴으로 바꿨다

### 품질 메모 — 크라우드소싱 원문이라 톤이 들쭉날쭉하다

60자 제한을 통과해도 문장 톤이 고르지 않다(예: 御前 항목에 "殺す。" 가 포함된 대사체
문장이 뽑힘). 개인 사용 단계라 지금은 그대로 두지만, 공개를 고려하게 되면 문장
난이도·톤 큐레이션(간단한 규칙 필터든 사람 검수든)을 다시 볼 만하다.

### 검증

`tools/build-examples.test.ts` 5 테스트(순수 선별 함수) · `src/dict/examples.test.ts`
3 테스트(런타임 산출물 무결성) · `tests/e2e/examples.spec.ts`(실브라우저, 문제 풀이
화면 0개 / 확인 단계에서 `lang="ja"` 로 노출, 20장 세션 중 최소 1회 확인, 犬は忠実です。
스크린샷 `scratchpad/example-feedback.png`). `npm test` 23파일 192 통과.
`npm run e2e` 4 스펙 통과(카드 전환 p95 14.7ms — 예문 fetch 는 전환 시간에 안 얹힘).
`tsc -b`/`oxlint`/`vite build` 클린.

---

## 2026-09-05 — Phase 7 (클라우드 백업, 코드 부분)

### 스키마는 이미 준비돼 있었다

`EventBase.deviceId` 와 `[userId+deviceId+at]` 색인이 Phase 1부터 PLAN §5 원칙 3
("동기화는 기기별 파일 분리")을 위해 미리 들어가 있었다. `getDeviceId()`
(`src/db/device.ts`)도 이미 모든 이벤트 생성 경로에 배선돼 있었다. 그래서 이번
작업은 스키마 변경 없이 순수 추가로 끝났다 — CLAUDE.md "스키마 변경 시 먼저 알린다"
조항이 발동할 일이 없었다.

### mmtm `GoogleDriveService` 를 그대로 이식하지 않았다

PLAN §8 표는 "가져올 것"에 `cloudStorage/` 전체를 적어 뒀지만, 실제로 옮긴 건
개념(로그인 → 폴더 → 파일 업로드/다운로드)뿐이고 구현은 새로 짰다.

- **gapi 클라이언트 라이브러리를 안 쓴다.** mmtm 은 GIS(로그인 토큰) + gapi(REST 래퍼)
  두 스크립트를 로드한다. yomenai 는 GIS 로 토큰만 받고 Drive REST 는 `fetch` 로 직접
  부른다 — 로드할 스크립트가 1개로 줄고, gapi 의 거대한 미타입 전역을 앱 코드에 안
  들인다. Drive API 는 REST 문서가 fetch 로 바로 옮겨 적을 만큼 단순해 이 대체가
  안전하다고 판단했다.
- **암호화(`crypto.js`, AES-GCM+PBKDF2)는 이번엔 스킵한다.** mmtm 은 일기장이라
  민감도가 다르다. yomenai 이벤트 로그(숙어 ID·정오답·소요시간)는 공개돼도 실질적
  피해가 적고, `drive.file` 스코프라 이 앱이 만든 파일만 접근 가능하다. 필요해지면
  `uploadOrReplace`/`downloadFile` 호출 지점에 감싸 넣을 수 있어 나중에 추가해도
  구조를 안 흔든다. **기각한 대안**: PLAN §8 표대로 지금 포팅 — 요청받지 않은 기능을
  먼저 넣는 셈이라 보류.
- **Dropbox·`CloudStorageFactory`는 안 만든다.** PLAN §2 가 "Google 로그인"만
  명시한다. 공급자가 하나뿐이면 팩토리 추상화는 조기 일반화다.
- **버전 관리(`MAX_BACKUPS_TO_KEEP`, 타임스탬프 파일명)도 없다.** mmtm 은 백업마다
  새 zip 을 쌓지만, yomenai 는 기기당 파일 하나(`reviews-{deviceId}.json`)를
  계속 덮어쓴다 — 원본(IndexedDB)이 이미 append-only 로그라 별도 버전 이력이 필요
  없다.

### 병합 로직은 새로 설계했다 — mmtm 의 `syncManager` 충돌 해결 UI를 다시 기각

PLAN §8 이 이미 "syncManager.js 의 충돌 처리는 가져오지 않는다"고 못 박아 뒀다
(재확인, 새로 기각한 게 아니라 기존 결정 재확인). `src/sync/sync.ts` 의 `syncNow`는
1) 이 기기 이벤트 전량을 자기 파일에 덮어쓰고 2) 남의 파일들을 읽어 `bulkPut` 으로
합친다. 이벤트는 append-only 라 같은 id 는 항상 같은 내용이므로 덮어쓰기가 항상
안전하고, 파일당 쓰는 주체가 하나뿐이라 쓰기 충돌이 구조적으로 없다.

### `getDeviceId()` 는 sync.ts 안에서 부르지 않는다

처음엔 `syncNow` 내부에서 `getDeviceId()` 를 직접 호출했다가 테스트가 깨졌다 —
Node(vitest) 환경엔 `localStorage` 가 없어 `getDeviceId()` 가 매번 새 UUID를
반환해, 업로드 파일명이 호출마다 달라지고 "내 파일" 판정이 어긋났다. 기존 코드
(`useStudySession.ts`, `Diagnostic.tsx`)가 이미 따르던 관례 — `getDeviceId()` 는
UI 경계에서 한 번만 부르고 그 값을 아래로 넘긴다 — 를 그대로 따라 `syncNow(db,
deviceId, drive?)` 로 시그니처를 바꿨다. `Settings.tsx` 가 `getDeviceId()` 를 불러
넘긴다.

### 테스트 — 가짜 Drive 를 메모리 Map 으로

`DriveClient` 인터페이스(`googleDrive.ts`)를 `sync.ts` 가 기본값으로만 쓰고
매개변수로 받게 해, 테스트는 실제 Google API 를 안 건드리고 `Map<string,string>`
하나를 공유하는 `FakeDrive` 두 인스턴스로 "브라우저 프로파일 2개"를 흉내 낸다.
`sync.test.ts` 의 "두 브라우저 프로파일에서 각각 학습 후 병합해도 이벤트 손실이
없다" 테스트가 체크리스트의 검증 문구를 문자 그대로 구현한다 — A→B→A 순서로
동기화한 뒤 양쪽 DB 가 6건 전부를 갖고, 재동기화해도 안 늘어나는지 확인.
실제 OAuth 플로우(GIS 팝업)는 이 프로젝트의 e2e(Playwright)로도 검증 못 한다 —
테스트 계정을 자동화에 못 넣는다. 실기기 로그인·동기화 육안 확인은 사용자 몫으로
checklist 에 남겼다.

### 동기화는 수동 트리거만 — 설정 화면 안에서만 실행

카드 루프 150ms 예산(CLAUDE.md 성능 제약)과 무관하게 하려고 자동/백그라운드 동기화는
넣지 않았다. "지금 동기화" 버튼(설정 화면)을 눌러야만 `fetch` 가 나간다. 앱 시작 시
`restoreSession()`(조용한 재로그인)만 자동으로 시도한다 — 이건 네트워크 요청 1회고
학습 화면과 겹치지 않는 설정 화면 마운트 시점에만 일어난다.

### 검증

`src/db/events.test.ts` +6(`listDeviceEvents`/`importEvents`), `src/sync/sync.test.ts`
4개(로그인 거부, 업로드, 병합+자기 파일 스킵, 2기기 병합+재동기화 멱등).
`npm test` 24파일 200 테스트 통과. `tsc -b`/`oxlint` 클린. `googleDrive.ts` 자체
(GIS 스크립트 로드, 실제 fetch 호출)는 단위 테스트를 안 붙였다 — 브라우저 전역
(`window.google`, 팝업)을 흉내 내봐야 실제 OAuth 플로우를 검증하지 못해 실익이
적다고 판단했다. 대신 인터페이스 뒤로 격리해 `sync.ts` 쪽 로직은 전부 테스트로
덮었다.

### 보류 — GitHub Pages 배포 (사용자 결정 대기)

체크리스트의 "GitHub Pages 배포"를 시작하기 전에 막힌 게 하나 있다. CI(GitHub
Actions)가 `public/dict/*.json` 을 만들려면 `data/dict/*.json` 이 있어야 하는데,
이 디렉터리는 `.gitignore` 대상이라 저장소에 없다. 원본(JMDict/KANJIDIC2/Tatoeba)은
다시 받을 수 있지만, **Phase 3 한국어 대조 검수 결과(`korean-class.json`)는 여러
세션에 걸친 수작업 산출물이라 원본에서 재생성이 안 된다.** `.gitignore` 주석
("재생성 가능")이 애초에 이 항목엔 안 맞았던 셈이다.

해결책 후보(사용자에게 물어봄, 아직 결정 안 됨):
- (A) `public/dict/*.json` · `public/fonts/*.woff2` (최종 런타임 산출물)만 `.gitignore`
  에서 빼고 커밋 — CI 는 `vite build` 만 하면 됨. `data/dict/`(중간 산출물)는 계속 제외
- (B) `data/dict/*.json` 전체를 커밋 — CI 가 `build:runtime-dict`/`build:fonts` 까지
  돌림. 원본 폰트 파일(`data/raw/fonts/*.otf`)도 어떻게든 CI 에 올려야 함
- (C) 로컬에서 빌드해 만든 `public/dict`·`public/fonts`를 배포 직전에 수동으로 커밋
  (일회성, 자동화 없음)

부수적으로 짚어 둘 점 — GitHub Pages 로 배포하는 순간 이 데이터는 공개된다. 이건
커밋 여부와 무관하게 Pages 배포 자체의 성격이다. checklist Phase 8 이 이미
"EDRDG에 CC BY-SA 승계 범위 문의"·"stdict API 이용약관 확인"을 "서비스화 판단 전
필수"로 분리해 뒀는데, Phase 7 배포가 그 확인보다 먼저 일어나게 된다. 개인 프로젝트
성격상(계정 시스템 없음, 광고 없음) 문제 삼을 정도는 아니라고 보지만 결정은
사용자 몫이라 판단을 미리 내리지 않았다.

### GitHub Pages — 사용자가 "public/dict·fonts 커밋" 안 선택

세 가지 안 중 A(런타임 산출물만 커밋)를 골랐다. `.gitignore` 를 고쳐 `public/dict/`·
`public/fonts/` 는 커밋 대상으로 돌리고, `data/dict/`(중간 산출물, Phase 3 검수
결과 포함)는 계속 제외했다. `data/raw/`(원본, 재다운로드 가능)도 그대로 제외.

- `vite.config.ts` — `base` 를 `command === 'build' || isPreview` 일 때만 `/yomenai/`
  로 바꿨다. `choan616.github.io` 는 이미 다른 프로젝트(mmtm)가 쓰고 있어 yomenai 는
  `choan616/yomenai` project page 로 `/yomenai/` 서브패스에 뜬다. `npm run dev` 는
  base 그대로 `/` — 로컬 개발 흐름을 안 건드린다. `src/dict/load.ts` 가 이미
  `import.meta.env.BASE_URL` 로 사전 URL 을 짓고 있어 추가 수정이 필요 없었다.
- `.github/workflows/deploy.yml` — `npm ci && npm run build` → `actions/deploy-pages`.
  사전 파이프라인 단계(`build:runtime-dict`/`build:fonts`)는 CI 에 안 넣는다 — 커밋된
  `public/dict`·`public/fonts` 를 그대로 쓴다.
- 커밋 용량 — `public/dict/*.json` 약 26MB(gzip 전, base+band4+examples+pairs+kanji)
  + `public/fonts/*.woff2` 약 1.2MB. 저장소가 그만큼 커진다. 사용자가 트레이드오프를
  알고 선택.
- 남은 건 사용자 몫 — 저장소 Settings → Pages → Source 를 "GitHub Actions" 로 1회
  설정해야 워크플로가 실제로 배포한다.

### 버그 — 배포된 사이트에서 Google 로그인이 "설정 안 됨" 에러

실기기로 `https://choan616.github.io/yomenai/`에서 로그인이 안 된다는 보고를 받고
확인했다. `.env`는 로컬에만 있고 `.gitignore` 대상이라 CI 에는 없는데, `VITE_` 접두어
환경변수는 **빌드 시점** 값이 번들에 그대로 박힌다(Vite `loadEnv` 가 `process.env`
에서 `VITE_` 접두어 키를 읽어 온다 — `node_modules/vite/dist/node/chunks/node.js`
확인). `.github/workflows/deploy.yml` 의 빌드 스텝에 이 값을 넘기는 코드가 애초에
빠져 있었다 — 로컬 `.env` 설정만으로는 CI 빌드에 반영되지 않는다는 걸 처음 워크플로
작성할 때 놓쳤다.

**고침**: `npm run build` 스텝에 `env: VITE_GOOGLE_CLIENT_ID:
${{ secrets.VITE_GOOGLE_CLIENT_ID }}` 추가. 사용자가 저장소 Settings → Secrets and
variables → Actions 에 같은 이름으로 값을 등록해야 한다(클라이언트 ID 자체는 공개
값이라 secret 이 아니어도 되지만, GitHub UI 상 가장 익숙한 경로라 secret 으로 안내함).

### 배포판 Google 로그인 "invalid_client" — client_id 에 스킴이 붙어 있었다

로컬은 되는데 `https://choan616.github.io/yomenai/` 에서만 "401: invalid_client / The
OAuth client was not found" 가 떴다. 하룻밤 지나도 그대로라 전파 지연은 아니었다.
Playwright 로 배포판을 직접 열어 OAuth 팝업 URL 을 까 보니 client_id 가
`https://291869999860-….apps.googleusercontent.com/` — 앞에 `https://`, 뒤에 `/` 가
붙어 있었다. GitHub Actions secret `VITE_GOOGLE_CLIENT_ID` 에 값을 URL 형태로 넣은
게 원인. 로컬 `.env` 는 맨몸 값이라 안 걸렸다.

처음 번들 확인 때 `291869999860-…googleusercontent\.com` 부분 문자열만 grep 해서
앞뒤에 붙은 스킴·슬래시를 못 봤다 — 확인이 부실했다. 앵커 없는 정규식의 함정.

**고침 2가지**:
1. 사용자가 GitHub secret 값을 맨몸 client_id 로 교체 후 재배포
2. `googleDrive.ts` 에 `normalizeClientId()` 추가 — `trim()` + `^https?://` 제거 +
   끝 `/` 제거. 같은 입력 실수가 또 나도 안 깨지게. `googleDrive.test.ts` 5 테스트

### 배포판 로그인 삽질 마무리 (2026-09-06)

두 겹의 문제였다.
1. **invalid_client** — GitHub secret `VITE_GOOGLE_CLIENT_ID` 에 값을 `https://…com/`
   형태로 넣어서 배포 번들에 스킴·끝슬래시가 박혔다. `normalizeClientId()` 로 방어
   (커밋 75ad8b7). 로컬 `.env` 는 맨몸이라 안 걸렸었다.
2. **수정 배포 후에도 "액세스 차단됨" 잔상** — 폰 브라우저가 옛 번들을 캐시. mmtm 이
   `choan616.github.io/` 루트에 workbox 서비스 워커를 깔아둔 것도 확인(scope `/`,
   `NavigationRoute` 로 모든 네비게이션을 mmtm `index.html` 로 바인딩). yomenai 자산을
   런타임 캐시하진 않지만 같은 origin 이라 캐시/SW 간섭 소지가 있다. 시크릿 창에서
   즉시 정상 → 일반 창은 "설정 → Safari → 방문 기록 및 웹사이트 데이터 지우기" 로 해결.

**남은 관찰 대상** — mmtm 서비스 워커가 `/yomenai/` 네비게이션을 가로챌 가능성.
지금은 육안상 yomenai 가 정상 로딩되지만, 다른 기기·다른 사용자에서 mmtm `index.html`
이 뜨는 일이 생기면 (a) mmtm workbox 설정에 `navigateFallbackDenylist: [/^\/yomenai\//]`
추가 재배포, 또는 (b) yomenai 에 `/yomenai/` scope 서비스 워커를 붙여(vite-plugin-pwa
이미 설치됨) 하위 scope 로 mmtm SW 를 덮는 방법. 아직 조치 안 함 — 증상 나오면.

배포판 실기기 검증 성공: Google 로그인 → 지금 동기화 → Drive `YomenaiSync/
reviews-0bc19888-….json` 생성 (오전 8:12). Phase 7 완료.

---

## 2026-09-06 — 재미·디자인 (Phase 9 착수)

Phase 7까지 끝내고 실사용해 본 소감이 **"재미가 없다"**였다. 코드를 열어보니
취향 문제가 아니라 상당 부분 **미구현**이었다.

### 진단 — 실측한 것 넷

1. **모션이 앱 전체에 0개.** `transition`·`animation`·`@keyframes` 가 CSS 세 파일
   어디에도 없었다. PLAN §7 은 모션을 둘로 나눴는데(카드 루프 150ms 이하 / 셸·리포트는
   느리고 신중하게) **앞의 절반만 구현됐다.** "빠르게"는 지켰고 "음미하게"는 빠졌다.
   앱이 죽어 보이는 가장 직접적인 원인
2. **세션 종료가 영수증.** `20 / 20` 숫자 하나. 매 세션의 클라이맥스가 정보량 0
3. **앱의 자산이 맞혔을 때 안 보인다.** 음독 그물망과 한국음 대조가 `MistakeDetail`
   에만 있다. 잘하고 있을 때 이 앱이 특별한 이유가 사라진다
4. **팔레트에 정체성이 없다.** `--ok #3ddc84`(안드로이드 그린), `--ng #ff6b6b`(흔한 코랄),
   회색-청 배경. "무채색"이 절제가 아니라 미완성으로 읽힌다

### 방향 — 보상이 아니라 발견

포인트·하트·리그·**스트릭은 기각**. 진단 도구 포지셔닝을 깨고, 그 게임은 다른 앱이
더 잘한다. 이 앱의 쾌감은 "내 한자 지식이 작동하는 순간"이라 **앱이 나를 관찰해서
말해주는 것**을 재미의 축으로 삼는다. append-only 이벤트 로그라 과거 재계산이 공짜라서
가능한 방향이다.

### 1. 세션 종료 = 리포트의 축소판 + 오늘의 발견

`src/core/sessionSummary.ts`. 세션 경계는 이벤트 로그에 없어서 **시간 간격
(`SESSION_GAP_MS` 30분)으로 자른다** — 별도 세션 이벤트를 넣지 않은 건 스키마 불변
조건을 건드리지 않으려는 것이다.

- 기각 — **세션 시작/종료 이벤트 타입 추가.** 이벤트 스키마는 불변 조건이라 값 추가가
  가볍지 않고, 시간 간격으로 충분히 갈린다
- "새 음독"은 이전 로그와 전체 로그를 각각 재생해 **정답 0회 → 1회 이상**으로 바뀐 쌍을 센다
- **"오늘의 발견"은 한 줄만.** 여러 개를 늘어놓으면 대시보드가 되고 안 읽힌다.
  우선순위는 `CLEAN_RUN` > 좋아지는 추이 > 한국음 간섭 > 새로 뚫은 음독 > 나빠지는 추이 >
  취약 음독. "드물수록, 그리고 이 앱만 할 수 있는 말일수록 앞"

`useStudySession` 이 이벤트를 DB 에 덧붙이면서 세션용으로도 모아 둔다. 종료 시 DB 를
다시 읽지 않아 방금 쓴 이벤트와의 경합이 없다.

**린트가 실제 버그 둘을 잡아줬다.** `priorEvents` 를 `useRef` 로 뒀다가 렌더에서
`.current` 를 읽었고(oxlint react/refs 경고), `events` 객체를 렌더마다 새로 만들어
`SessionSummary` 의 effect 가 무한히 다시 도는 상태였다. state + `useMemo` 로 고쳤다.

### 2. 모션 — 리포트·요약에만

키프레임은 `index.css` 에 한 번 정의하고 `.report`·`.summary-screen` 으로 **좁혀서**
건다. 카드 루프에 실수로 걸리면 150ms 예산이 깨진다.

`prefers-reduced-motion: reduce` 에서 `animation-duration: 0.01ms !important` 를 쓴다.
`animation: none` 이 아닌 이유는 `fill-mode` 로 유지되는 최종 상태(막대 길이 등)를
잃지 않기 위해서다.

### 3. 색 — 정답에서 색을 뺀다

| | 이전 | 지금 |
|---|---|---|
| 정답 | `#3ddc84` 초록 | `#a8a29b` 무채 |
| 오답 | `#ff6b6b` 코랄 | `#e8563c` 朱 |
| 배경(다크) | `#131417` 회색-청 | `#141210` 먹색 |

**정답에 색을 주면 정답도 이벤트가 된다.** PLAN §7 이 "오답일 때만 멈춘다"고 했으니
색도 같은 규칙을 따라야 한다. 정답은 조용히 지나가고 오답만 붉게 서면 리듬이 생기고
오답이 진짜 신호가 된다. 朱는 일본 교정 부호·인장(朱肉)의 색이라 "교정지" 은유와도 붙는다.

정오답을 색만으로 구분하지 않는다는 원칙은 그대로다 — 아이콘·위치가 여전히 병행된다.

**명조체는 재검토하지 않았다.** 산스 통일은 2026-09-04 사용자 결정이라 유지한다.

### 검증

Playwright 로 실제 브라우저에서 20장 세션을 완주시켜 확인했다 (설치된 chromium-1228
을 `executablePath` 로 지정 — playwright 1.62 가 요구하는 1234 는 안 받았다).

- 종료 요약 렌더 확인, "오늘의 발견"이 `WEAK_ONYOMI` 로 정상 선택됨
- 계산된 스타일 — 기본 `fade-up 0.46s`(스태거 0.21s), `reduce` 에서 `1e-05s`
- 실측 토큰 — `--bg #141210` / `--ng #e8563c` / `--ok #a8a29b`
- `npm test` 26 파일 223 테스트 통과, `tsc -b` / `oxlint` 클린, `npm run build` 성공

### 발견한 기존 결함 (미수정)

**리포트의 "정답률"이 실제와 다르다.** `buildReport` 의 `totalMistakes` 는 *분류된*
오답 수라 `classifyMistake` 가 `null` 을 낸 오답이 빠진다. 브라우저 검증에서 20장을
전부 틀렸는데 리포트는 "읽기 17회 중 오답 0회 · 정답률 100%"로 나왔다 — 입력이
아무 유형에도 안 맞아 전부 미분류였기 때문이다.

실사용에선 대부분 6종 안에 들어와 차이가 작겠지만, **세션 요약(실제 `correct` 기준)과
리포트(분류 기준)가 서로 다른 정답률을 말하게 된다.** `state.onyomi` 의 `seen`/`wrong`
에 실제 오답 수가 있으니 고칠 수 있다. Phase 9 범위 밖이라 손대지 않았다.

### 4~6번 (음독 메아리 · 재대결 · 校正紙 루비)

**음독 메아리 (`src/core/echo.ts`).** 정답 직후 카드 아래에 `圧 あつ 7번째 · 迫 はく 3번째`.

- **전체 로그를 다시 재생하지 않는다.** 카드 전환 예산이 150ms 라 재생을 끼울 수 없다.
  세션 시작 시점 집계(`session.state.onyomi`)에 이번 세션 누적을 더해 센다
- `pairId` 문자열(`한자:on|kun:원형`)을 파싱해 쓴다(`parsePairId`). 쌍 사전을 따로 불러오면
  세션 시작이 느려진다
- 노출 횟수이지 정답 횟수가 아니다 — 오답도 "만난 것"으로 센다
- 브라우저에서 확인된 부수 효과가 좋다. 루비는 그 자리의 **표면형**(`あっ`)을, 메아리는
  추적 중인 **원형**(`あつ`)을 보여준다. 促音便이 일어난 걸 나란히 볼 수 있다

**재대결 (`buildRematch`).** 예전에 틀린 읽기 카드만, 오답 많은 순 → 최근 틀린 순.

- **기한을 무시한다.** 복습 기한을 기다리지 않고 다시 붙는 게 이 세션의 전부라
  `selectSession` 을 안 탄다
- 대가 — 답안이 정규 이벤트로 기록되어 FSRS 일정에 영향을 준다. 이르게 맞히면 안정도가
  덜 오르고, 틀리면 정상적으로 lapse 가 잡힌다. 후자가 맞는 정보라 그대로 뒀다
- 기각 — **"연습용 채점" 플래그.** 이벤트 스키마에 값을 늘려야 하는데 불변 조건이다
- 확인 질문(`needsClassReview`)을 안 끼운다. 이미 만난 숙어들이라 물어볼 게 없다

**`CardState.wrong` 추가.** 재대결 정렬을 `lapses + 분류된 오답 수`로 하려다 이중 계산인
걸 발견했다. `mistakes` 는 *분류에 성공한* 오답만 세므로 미분류 오답이 빠지고, `lapses` 는
Review 상태에서만 오른다. 실제 오답 수를 `replay` 가 직접 세게 했다.

**이 필드로 2026-09-06 절에 적어 둔 리포트 정답률 결함을 한 줄로 고칠 수 있다.**
`buildReport` 의 `totalMistakes` 를 `card.wrong` 합으로 바꾸면 된다. 4~6번 범위가 아니라
손대지 않았다.

**校正紙 루비 (`src/core/ruby.ts`).** 정답 읽기를 아래 줄이 아니라 한자 **위에** 얹는다.

- Phase 2 `decompose` 를 그대로 써서 자리별 표면형을 뽑는다. 분해 실패(熟字訓 등)면
  숙어 전체에 루비 하나 — `<ruby>特徴<rt>とくちょう</rt></ruby>` 도 유효하다
- `々` 는 `decompose` 가 전개하지만 조각 수가 원문 글자 수와 1:1 이라 원문 글자에 그대로 얹는다
- **오답이면 루비가 朱, 정답이면 무채.** 빨간 펜으로 고친 자국이 남는 감각이다.
  정답에 색을 안 주는 원칙(2026-09-06 절)과 같은 규칙

### 검증 (4~6번)

`npm test` 28 파일 **247 테스트** 통과, `tsc -b` / `oxlint` 클린, `npm run build` 성공.

Playwright 실측.

- 오답 카드 루비 — `忠ちゅう実じつ` (자리별로 붙음)
- 홈 재대결 버튼 — `재대결 17 · 예전에 틀린 것만`
- 재대결 세션 17장 진입, 표제어→읽기 수집 후 정답 제출
- 정답 카드 메아리 — `圧 あつ 7번째` / `迫 はく 3번째`

### 리포트 정답률 결함 수정 — 안 B (분리 + 노출)

2026-09-06 절에 적어 둔 결함이다. `buildReport` 의 `totalMistakes` 가 *분류된* 오답만
세는데 화면이 그걸로 정답률을 계산해서, `classifyMistake` 가 `null` 을 낸 오답이
정답으로 둔갑했다.

- 기각 — **안 A(한 줄).** `totalMistakes` 를 실제 오답 수로 바꾸면 수치는 맞아지지만
  분류기가 얼마나 놓치는지가 화면에서 사라진다. 진단 도구가 자기 분류 실패를 감추는 꼴이다
- 채택 — **안 B.** `totalWrong`(실제)과 `totalMistakes`(분류됨)를 나누고 차이를
  `unclassified` 로 노출한다. 정답률·오답 수·"전체 오답의 N%" 분모는 전부 `totalWrong`

**분류기 재현율이 이제 화면에 드러난다.** KO_INTERFERENCE 를 좁게 잡은 판단
(2026-09-04)이 실사용에서 얼마나 놓치는지 "유형을 못 붙인 오답 N회"로 보인다.
규칙을 넓힐지 말지를 감이 아니라 이 숫자를 보고 정할 수 있다.

**딸려 나온 문구 수정.** 분포 섹션의 빈 상태가 "오답이 없습니다."였는데, 오답은 17회인데
분류된 게 0인 상황에서 리드와 정면으로 모순됐다. `totalWrong === 0` 일 때만 그 문구를
쓰고, 아니면 "유형이 붙은 오답이 없습니다."로 갈라 놨다.

**검증.** 같은 시나리오(20장 전부 오답, 전량 미분류)를 브라우저에서 다시 돌렸다.

| | 이전 | 지금 |
|---|---|---|
| 리드 | 읽기 17회 중 오답 **0**회 · 정답률 **100%** | 읽기 17회 중 오답 **17**회 · 정답률 **0%** |
| 분포 | "오답이 없습니다." | "유형이 붙은 오답이 없습니다." |
| 미분류 | (표시 없음) | 유형을 못 붙인 오답 **17**회 |

취약 음독의 100% 오답률과도 이제 어긋나지 않는다. `npm test` 252 통과.

---

## 2026-09-06 — 학습 기록 초기화 + 카드 UI 재구성 범위

### 학습 기록 초기화

사용자 요청 3가지 전부 채택: 세션 중도 종료 / 로컬 기록 삭제 / Drive 백업 삭제.

- **세션 중도 종료는 이미 있었다** — `Study.tsx` study-bar 의 `✕`(`onExit`). 이미
  appendEvent 된 이벤트는 남지만 append-only 라 부분 세션도 정상이다. 추가 작업 없음.
- **로컬+Drive 를 한 동작으로 묶었다** — `resetLearning(db, drive?)` (`src/sync/sync.ts`).
  `db.events.clear()` + 로그인돼 있으면 `drive.deleteSyncFiles()`. 왜 묶었나 —
  로컬만 지우면 다음 `syncNow` 때 내 파일·다른 기기 파일에서 그대로 되살아난다.
  "진짜 초기화"는 Drive 파일까지 지워야 한다(사용자에게 먼저 설명하고 확정).
- `deleteSyncFiles()` 는 YomenaiSync 의 `reviews-*.json` **전부** 삭제(다른 기기 것 포함).
  개인 단일 사용자라 "완전 초기화" 의도에 맞다. 폴더는 남긴다(다음 sync 가 재사용).
- append-only 예외 — 평소엔 이벤트를 안 지우지만, 사용자가 명시적으로 누른 파괴적
  동작이라 통째 삭제가 맞다. `deletedAt` 묘비를 전 이벤트에 찍는 것보다 단순하고,
  Drive 파일도 같이 지우니 재생 일관성 문제도 없다.
- UI — 설정 화면 2단계 확인(초기화 → 정말 초기화/취소), 완료 시 `location.reload()`
  로 세션 훅·홈 통계 등 파생 상태를 확실히 비운다. 파괴적·희소 동작이라 리로드 허용.

### 학습 카드 UI — "레이아웃 재구성"으로 범위 확정, 개별 패치 안 함

사용자가 카드 UI 를 디자인 변경 대상(레이아웃 재구성)에 넣기로 했다. 확정 요구사항
하나: **입력 폼 고정** — `.kana-input` 이 일반 in-flow `<input>` 이라 모바일에서 포커스
시 소프트 키보드가 뜨면 브라우저가 입력을 키보드 위로 올리려 스크롤해 study-bar·숙어가
밀린다. 입력 영역을 뷰포트 하단에 고정(키보드 위)하는 방향.

**지금 CSS 패치를 안 하는 이유** — 레이아웃 재구성이 예정돼 있어 `position: sticky`
하나 붙였다가 재구성 때 다시 걷어내면 두 번 일한다. PLAN §9 의 "레벨링 확정 전 UI
만들면 두 번 만든다"와 같은 판단. 현행 스크린샷 → 문제점 정리 → 새 레이아웃 제안 →
승인 → 구현 순으로 간다. checklist "실사용 후속" 절에 기록.

### 학습 카드 3분할 고정 레이아웃 (재구성 실행)

검사표를 먼저 만들어 승인받고 착수했다 — 현행 8장 캡처 → 결함 6건 → 제안 → 결정 3건.
사용자가 "제안대로 진행"으로 권장안 전부 확정.

**결정 3건.**
- 입력창 자동 포커스 **유지** — 타이핑 속도가 학습 리듬이다. 레이아웃을 고정하면
  포커스 때문에 화면이 흔들리던 부작용만 사라지고 장점은 남는다
- 가운데 영역이 넘치면 **스크롤** — 글자를 줄여 한 화면에 맞추는 안은 기각.
  자형 학습이 목적인 앱에서 한자를 작게 만드는 건 본말전도다
- 예문은 **숙어 아래** — 하단 고정 블록 위에 두는 안보다 읽는 맥락이 붙는다

**골격.** `.study` 를 `100dvh` + `overflow:hidden` 으로 묶어 페이지 스크롤 자체를
없앴다. 브라우저가 포커스된 입력을 보이게 하려고 화면을 밀어 올릴 여지가 사라진다.
`index.html` viewport 에 `interactive-widget=resizes-content` 를 더해, 키보드가 뜨면
레이아웃 뷰포트가 줄고 `100dvh` 가 키보드 위로 재계산되게 했다. 이게 있어야 하단
블록이 키보드에 안 가린다.

`position: sticky` 는 **안 썼다.** 페이지가 스크롤하지 않으니 flex 컬럼의 마지막 칸이
곧 바닥이다. sticky 는 스크롤 컨테이너가 있을 때 필요한 장치고, 여기선 불필요한 복잡도다.

카드는 `.card-head`(고정) / `.card-body`(가변·세로중앙·`overflow-y:auto`) /
`.card-bottom`(고정) 3단. `.card-body` 래퍼를 카드 4종(읽기 문제·읽기 피드백·뜻·
지연검수·오답상세) JSX 에 추가했다 — CSS 만으로는 가운데 형제들을 한 덩어리로 묶을 수
없어서(grid row 배정은 취약) DOM 에 구조를 드러내는 쪽을 택했다.

**버튼 3슬롯.** `[보조][주][보조]` 그리드(`1fr 1.4fr 1fr`)로 통일하고 없는 보조는
`.slot`(visibility:hidden)으로 자리만 지킨다. '다음'의 x좌표가 상태가 바뀌어도 안
움직인다. 다만 이지선다(몰랐어요/알았어요, 몰랐다/알고있었다)는 `.choice` 2등분으로
따로 뒀다 — 두 선택지가 같은 무게라 3슬롯에 억지로 밀어 넣으면 위계가 거짓이 된다.

**피드백 색.** 카드 전면 배경 → 상단 띠(`.card.feedback .card-head`)로 좁혔다.
전면을 칠하면 빈 면적까지 물들어 신호가 옅어졌다. 밴드 태그를 판정 반대편(`margin-left:auto`)
에 세워 확인 단계에서도 난이도 맥락이 안 끊긴다.

**PLAN §7 과의 차이.** PLAN 은 "숙어는 화면 중앙 상단"이라 했고 기존 구현은
`margin-top: clamp(24px,12vh,120px)` 로 그걸 지켰다. 재구성 후에는 가운데 영역의
세로 중앙이다. 키보드가 올라온 상태(자동 포커스라 사실상 상시)에서는 줄어든 뷰포트의
중앙이 곧 "중앙 상단"에 해당해 의도가 유지된다고 봤다.

### e2e `tts.spec.ts` 는 이전부터 깨져 있었다

재구성 후 e2e 를 돌리니 tts 스펙이 실패했다. **내 변경 때문인지 확인하려고 `git stash`
로 되돌려 같은 스펙을 돌려 봤고, 변경 전에도 똑같이 실패했다.** 읽기 피드백에서
`.reading-shown` 을 기다리는데 그건 뜻 카드 전용 클래스다. 읽기 피드백이 루비(校正紙
컨셉)로 바뀌면서 테스트가 안 따라온 것이고, checklist 의 "e2e 4스펙 통과" 기록은
그 시점 이후로 낡아 있었다. `.headword.has-ruby rt` 를 보도록 고쳤다.

교훈 — e2e 를 커밋마다 안 돌리니 이런 게 조용히 썩는다. 카드 UI 를 건드리는 작업
전후로는 `npm run e2e` 를 돌린다.

### 버그 — 빈 답 Enter 가 오답으로 채점됨 (진단 "해설 먼저" 증상)

"진입진단에서 알고있었다 클릭 시 해설을 먼저 보여준다, 15회 근처"라는 제보.
Playwright 로 재현. 느린 클릭으로는 안 나오고, "입력→Enter→입력→Enter" 빠른 리듬을
흉내 내니 25/25 재현됐다.

**원인.** 다음 문항으로 넘어가면 `KanaInput` 이 `el.focus()` (재구성 결정대로 auto-focus
유지). 사용자가 새 문항을 읽기 전에 리듬으로 Enter 를 누르면 빈 값 `''` 이 제출되고,
`isCorrectReading(reading, '')` → false → 진단은 `setPhase('known')` 으로 해설(읽기
공개) 화면을 띄운다. 덤으로 오답 `review` 이벤트가 쌓여 `diagnosticSummary` 밴드
추정과 FSRS 재생이 오염된다. 세션에서도 같은 경로(빈 답 → 오답 피드백).

**고침.** `KanaInput.submit()` 에서 `value.trim() === ''` 이면 `onSubmit` 을 안 부른다.
Enter·확인 버튼 공통. 입력창은 여전히 uncontrolled(ref, wanakana bind 유지) — 값 추적
상태를 안 늘렸다. 빈 채로 확인을 눌러도 아무 일 없음(원래 그게 맞다).

**회귀 테스트.** `tests/e2e/diagnostic-empty-submit.spec.ts` — 알고있었다 직후
auto-focus 된 입력에 빈 Enter 를 6회 반복, 매번 `known` 으로 안 넘어가고 입력창이
그대로인지 확인.

---

## 2026-09-06 — 진단·세션 지루함 (Phase 9 2차 제안)

실사용 소감: "한 번 할 때부터 지루함. 재미 요소를 가미할지 고민. 목적의식 있으면
견디지만 의욕 없으면 금방 포기할 것." 흐름 캡처(첫 홈 → 진단 90문항 → 결과 → 세션 →
요약)로 마찰 5건을 짚었다.

**게임화는 안 한다.** 포인트·배지·스트릭·축하 애니메이션은 (1) PLAN §7 무채색·절제
정체성과 싸우고 (2) 3개월 실사용 데이터 없이 설계하면 헛다리다. 지금 유일한 사용자는
목적의식이 있어 견디는 케이스라, 의욕 없는 사용자의 이탈 지점을 아직 모른다. 3개월 뒤
"의욕 없는 날" 이탈이 실제 문제로 남으면 그 데이터로 다시 본다.

**레버 4개 (제안서 `scratchpad/tedium-proposal.html`).**
1. 적응형 진단 — 90 → 25~35. 밴드마다 결론 서면 조기 종료. "한 번의 약속 단위"를
   줄이는 게 의욕 없는 사용자에 가장 큰 레버
2. 편한 해요체 (토스 톤) — 사용자 요청. "따뜻함은 틀에, 정확함은 알맹이에". 근거 없는
   칭찬·이모지·느낌표 남발·반말 금지. 관찰은 그대로 관찰이되 말 걸듯이. 저비용
3. 루프 안 관찰 — 세션 요약의 Finding 을 확인 단계에도 한 줄. 이미 있는 자산을 앞당김
4. 세션의 형태 — 챕터 타이틀·중반 한 줄·마무리 표시

**말투 결정의 긴장.** 해요체가 "의욕 없으면 포기"에 직접 듣지만, PLAN §7 은 리포트를
"검사표" 컨셉의 앱 얼굴로 잡았다. 진단 리포트까지 해요체로 가면 무게가 빠진다. 권장 —
리포트는 절제 유지, 나머지(진단 프롬프트·세션·발견)만 해요체.

**"뜻 알았나요?" 제거 검토.** 진단 오답마다 끼는 인터스티셜은 진단 결과 계산
(`diagnosticSummary` 는 review 만 봄)에 안 쓰이고 탭만 두 배로 만든다. 빼면 진단이
순수 읽기 검사가 되고, 뜻 질문은 지연 검수(`needsClassReview`)가 실제 세션에서 물 때로
미뤄진다. Phase 3/4 지연 검수 설계가 이미 이 경우를 위해 만들어졌으니 정합적이다.
대신 모드 배정 2단계 신호가 그만큼 늦어진다 — 결정 사항으로 남김.

**phase 분할** — checklist "Phase 9 2차" 절. 9-A(홈+해요체) / 9-B(적응형 진단) /
9-C(루프 관찰) / 9-D(세션 형태) / 9-E(실기기 체감, 사용자). 서로 독립이라 9-A 부터
순차, 결정 5건 답을 받은 뒤 착수.

---

## 2026-09-06 — Phase 9 2차 실행 (9-A ~ 9-D)

사용자 결정 5건: (1) 진단 리포트도 해요체 (2) "뜻 알았나요?" 제거 (3) 노출 빈도는
권장안 (4) **설정에서 조절 가능하게** (5) 홈 갈림길 바로. 게임화는 안 함(앞 절).

### 9-A · 홈 갈림길 + 해요체 (커밋 7b4b489)

홈 — 진단 전 "진입 진단 시작"이 큰 주 버튼, "세션 시작 · 진단 건너뛰기"가 보조.
"세션 시작"을 **아예 숨기지 않은** 이유: e2e 여러 스펙이 그 문자열을 참조하고, 강제보다
권장이 낫다. 부분 문자열이라 `getByRole({name:'세션 시작'})` 이 계속 매칭된다.

해요체 — 임상 톤 → 편한 해요체. 진단 프롬프트·결과, 세션 요약 + Finding 5종, 리포트
전체, 빈/오답 안내, 설정 힌트. **화면 제목("진단 완료" 등)·버튼 라벨·분류 라벨·`lang=ja`
는 유지** — 제목은 검사표 정체성이고 버튼은 동사 단문이 맞다. e2e 참조 문자열 전부 보존.

### 9-B · 적응형 진단 (커밋 aeb067f)

`DIAGNOSTIC_PER_BAND` 30 → 12(상한만). `bandVerdict(seen, wrong)` — 오답 3 → 진단 종료 /
8개+80% → 다음 밴드 / 12개 상한. 90문항 → 전패 3 / 전부 안정 24 / 전형 ~13.

"뜻 알았나요?"(known 단계) 제거. `diagnosticSummary` 는 이 응답을 안 쓰고(review 만),
탭만 두 배로 만들었다. 뜻 질문은 지연 검수(`needsClassReview`)가 실제 세션에서 물 때로
미뤄진다 — Phase 3/4 지연 검수 설계가 이미 이 경우를 위한 것이라 정합적. 모드 배정
2단계 신호가 그만큼 늦어지는 건 감수.

진행률을 "0 / 90" → "밴드 N · M번째". 적응형이라 총 문항 수를 미리 못 준다.

`firstShaky`(< 0.8)와 `bandVerdict`(nextBand 는 ≥ 0.8) 임계값을 맞췄다 — nextBand 로
넘어간 밴드는 firstShaky 로 안 잡히게. 안 맞추면 "넘어갔는데 결과에선 그 밴드가 흔들림"
이 나올 수 있다.

### 9-C · 루프 안 관찰 + 설정 (커밋 4b6a9a0)

`observe.ts` `observeReading` — 정답 직후, 음독 이력이 나빴던(오답 ≥ 2, 오답 > 정답) 것
중 최악 하나. 지금은 한 종류(`WEAK_ONYOMI_RECOVERED`). `sessionSummary` 의 Finding 5종
중 카드 단위로 판정 가능한 건 사실상 이것뿐 — CLEAN_RUN·MISTAKE_TREND 는 세션/다세션
단위, KO_INTERFERENCE·ONYOMI_UNLOCKED 는 나중에 추가 여지. echo 와 같은 입력
(before 집계 + sessionEvents)으로 재생 없이 센다.

빈도 게이트는 `useStudySession` 이 세션 시작 시 `observeLevel` 을 한 번 읽어 ref 로 고정.
`cardsSinceObserve` / `observeShown` ref 로 [간격, 상한] 판정. off/normal/often =
∞·0 / 6·4 / 3·8. 설정 화면에 "관찰 문구" 3버튼.

### 9-D · 세션의 형태 (커밋 a1737cd)

`SessionShape.tsx` — `ChapterTitle`(첫 카드 앞 1.2초 오버레이) + `MidNote`(절반 지점
1.8초). 마지막 3장은 `.near-end`(count "곧 끝 ·", 진행률 무채). 전부 `progress.index`
파생, `useStudySession` 에 새 상태 안 만듦. `MidNote` 는 부모가 절반 카드일 때만 조건
렌더하고 자기가 1.8초 뒤 사라짐 — `setState` 를 effect 안에서 동기 호출하면 oxlint
`react(set-state-in-effect)` 라, 표시 판정은 부모가, 소멸만 자식이 `setTimeout` 으로.

모션은 `fade-up`(셸 급). 카드 루프 150ms 예산과 무관한 구간. `prefers-reduced-motion`
은 index.css 전역 규칙(`animation-duration: 0.01ms`)이 이미 접는다.

### 검증 총계

`npm test` 254 → 267 (bandVerdict 6 + observe 6 + settings 1). `npm run e2e` 5스펙
계속 통과, 카드 전환 p95 ~15ms(예산 150). full-flow e2e 는 적응형 진단으로 13.3s → 3.8s.
tsc/oxlint/build 클린. 9-E(실기기 체감)만 사용자 몫.

---

## 2026-09-06 — iOS 스탠드얼론 웹앱 이슈 3건

홈 화면에 추가해 웹앱(display: standalone)으로 띄우니 실기기에서 셋이 나왔다.

### 1. 키보드가 상단을 밀어 올린다 (여전히)

Phase 9-D 에서 `interactive-widget=resizes-content` + `100dvh` 로 잡았다고 봤는데,
**그 viewport 항목은 iOS 미지원**(Chromium 전용)이고 `100dvh` 는 키보드로 안 줄어든다.
iOS 는 키보드를 오버레이하고 문서를 스크롤해 포커스된 입력을 보이려 한다 → 상단이 밀린다.

`src/study/useViewportLock.ts` — `visualViewport.resize/scroll` 을 듣고 실제 보이는
높이를 `--vvh` 로 내려준다. `.study`/`.diag` 가 `height: var(--vvh, 100dvh)`. 스크롤이
0 이 아니면 `window.scrollTo(0,0)` 로 되돌리고 `body { overflow: hidden }`. Study/Diagnostic
둘 다 훅 호출.

### 2. 자동 포커스가 스탠드얼론에서 무시된다

iOS standalone 은 사용자 제스처 밖의 `element.focus()` 로 키보드를 안 올린다(브라우저
탭보다 엄격). 카드마다 `KanaInput` 을 `key` 로 리마운트하니 매번 새 포커스가 필요했고
그게 다 무시됐다.

고침 — `KanaInput` 을 리마운트하지 않는다. `key` 제거, `resetKey`(idiom id) prop 으로
값만 `el.value = ''` 비운다. 포커스가 유지된다. `ReadingCard` 재구성 — 피드백 중에도
KanaInput 마운트를 유지(`readOnly`, `disabled` 와 달리 iOS 는 readonly 에서 포커스·
키보드를 유지한다). 하단은 [readonly 입력 행] + [액션 행] 2단. `viewport lock` 덕에
둘 다 키보드 위에 남는다. `Feedback` 하위 컴포넌트를 없애고 `ReadingCard` 하나로 합침.
`detail`(오답 상세) 리셋은 effect 대신 `next()` 핸들러에서 `setDetail(false)`.

첫 카드는 여전히 한 번 탭해야 키보드가 뜬다(제스처 없이 마운트되니까). 그 뒤로는 유지.

### 3. 일본어 키보드 한자 변환 억제

입력창 `lang="ja"` 가 iOS 에서 일본어 IME(한자 변환 후보 바)를 부른다. wanakana 가
로마자→가나를 하므로 실제 입력은 라틴 문자다. `lang="en"` + `inputMode="text"` +
기존 autocorrect/autocomplete/autocapitalize off. `inputMode="latin"` 은 React 타입에
없어 못 씀. 표시되는 가나는 `.kana-input { font-family: var(--font-ja) }` 로 JP 폰트.
가나는 CJK 통합 대상이 아니라 자형 오학습 우려가 없어 `lang="ja"` 를 빼도 된다
(CLAUDE.md 규칙은 한자 때문).

### e2e 수정

세션 루프의 입력 체크를 `input.isVisible()` → `(isVisible && isEditable)` 로.
피드백 중 입력이 readonly 로 남아 `fill()` 이 걸리던 걸 회피. `isEditable()` 단독은
없는 요소를 auto-wait(테스트 타임아웃까지) 해서 hang — `isVisible()` 로 먼저 단락.

---

## 2026-09-06 — 뜻 카드가 읽기 카드보다 먼저 나오던 버그

실기기 제보: 뜻 카드(읽기 あっしゅく 가 보이는 화면)가 "힌트가 문제 풀이보다 먼저
나온다"는 느낌. 어휘 확장 모드 숙어는 `activeCardTypes('expansion') = ['reading',
'meaning']` 로 한 세션에 읽기·뜻 두 장이 다 나온다. 그런데 `select.ts` 의 타이브레이커가
`cmp(a.cardType, b.cardType)` 라 문자열 비교로 `'meaning' < 'reading'` → 같은 숙어의
뜻 카드가 읽기 카드보다 앞섰다. 뜻 카드가 읽기를 보여주니 뒤에 올 읽기 카드의 답이 샜다.

고침 — `CARD_ORDER = { reading: 0, meaning: 1 }` 로 같은 숙어면 읽기 → 뜻 순.
`byOverdue` / `byIntroOrder` 둘 다. 뜻 카드는 이제 읽기를 이미 물은 뒤에만 온다.

**"힌트(뜻 카드)는 언제 나오나" — 모드 배정(`mode.ts` assignMode, 뒤 단계가 이김):**
1. 한국어 대조 — 동형동의(cat 1) → 교정(읽기만). 동형이의(2)·일본 고유(3) → 확장(읽기+뜻)
2. "이 숙어, 뜻은 알고 있었어요?"(지연 검수 `ClassReviewPrompt`, 미검수 숙어가 풀에
   처음 들어올 때) — 몰랐다 → 확장. 알았다 → 교정
3. 세션 중 재배치 — 뜻 카드 FSRS 가 relearning(뜻 틀림) → 확장. 안정 → 교정
세션 비율(설정 "모드 비율", 기본 7:3)이 확장 카드 수를 정한다. "읽기만"이면 뜻 카드 0.

---

## 2026-09-07 — 세션 제시 순서를 섞기로 (무작위 0 원칙 완화)

실사용 소감: "너무 순서대로 나와서 모르는 한자도 대강 예측하게 된다." `selectSession`
은 "무작위를 쓰지 않는다 — 같은 입력이면 같은 세션이 재현 가능한 진단"이 원칙이었다
(PLAN §6). 그런데 밴드 오름차순 + 같은 음독 클러스터(圧縮→圧力→圧倒)라 순서가 읽혀,
읽기 시험이 무의미해졌다.

**절충** — *선택*은 우선순위대로(진단 가치 유지), *제시 순서*만 세션마다 섞는다.
`SelectOptions.seed?: number`. 주면 `shuffleByIdiom` — 숙어 단위로 그룹 지어 섞고
(같은 숙어의 [읽기, 뜻] 은 붙어 이동 + 내부 순서 유지), 안 주면 그대로.
`useStudySession` 이 정규 세션에 `seed: now` 를 준다 → 매 세션 다른 순서, 재현 불가.
테스트·`session.sim` 은 seed 를 안 줘서 결정론 유지.

재현 가능성이 필요했던 곳: (a) 진단 — 이미 자체 시드가 있고 분리됨, (b) 테스트 —
명시 시드로 대응, (c) "리로드하면 같은 세션" — 이벤트 로그가 원본이라 실익 없음.
그래서 정규 세션의 결정론은 버려도 손해가 없다고 판단.

`mulberry32` 를 select.ts 에 로컬 복제(diagnostic.ts 와 같은 계열). 5줄이라 공용 모듈
분리보다 복제가 낫다고 봄.

---

## 2026-09-07 — 수준과 처방 (Phase 10)

사용자 요청. "기능은 조금 안정된 것 같다. **진단 리포트에서 내 수준이 시각적으로
드러났으면** 좋겠다. **앞으로 어떤 점을 공부해야 하는지도** 알려주면 좋겠다. 그리고
**여전히 조금 재미가 없다.**"

### 진단 — 리포트가 답하지 않던 것 셋

1. **수준이 없었다.** 밴드별 정답률을 `diagnosticSummary` 가 이미 계산하는데 진단 결과
   화면에서 한 번 보여주고 버렸다. 리포트는 오답 유형·한국음 간섭·취약 음독만 있어서
   "내가 어디쯤인가"에 답하지 않았다
2. **처방이 없었다.** 취약 음독 목록은 진단이지 처방이 아니다. 게다가 `unlocksOf`
   (그 음독을 쓰는 숙어 수)가 `sessionSummary` 에 이미 있는데 리포트는 안 썼다 —
   "이 음독 하나 뚫으면 숙어 103개가 걸려 있다"는 파급력을 말할 재료가 놀고 있었다
3. **읽고 끝났다.** 리포트가 목록이라 발견이 행동으로 이어지지 않았다

### 재미 — 게임화는 이번에도 안 한다

포인트·배지·스트릭 기각은 2026-09-06 두 절에서 이미 결정됐고 그대로 간다. 축은
"발견"인데, **이번엔 발견을 행동까지 잇는다**. 사용자에게 갈래를 물어 (a) 처방 → 집중
세션 (b) 수준의 시간축(세션별 정답률 스파크라인·경계선 이동) 중 (a)를 선택받았다.

- 보류 — **(b) 시간축.** 세션이 몇 번 쌓이기 전엔 화면이 비고, (a)는 첫날부터 작동한다.
  기각이 아니라 다음 후보다

### 1. 수준 사다리 (`src/core/level.ts`)

`buildLevel(events, bandOf)` — 전체 로그의 밴드별 읽기 정답률.

- **`diagnosticSummary` 를 그대로 재사용한다.** 이름이 진단이지 하는 일은 "밴드별 읽기
  성적 접기"라 진단 직후에도 세션 100번 뒤에도 같은 함수가 낸다. **진단 결과를 따로
  저장하지 않는다** — append-only 로그라 언제든 다시 세면 된다 (PLAN §5 원칙 2).
  진단은 시작점일 뿐 수준의 출처가 아니다
- 기각 — **진단 결과를 스냅샷으로 저장.** 저장하는 순간 "진단 때의 수준"과 "지금 수준"이
  갈라지고, 어느 쪽을 리포트에 쓸지가 영원한 문제가 된다
- `LEVEL_SOLID_RATE` 0.8 은 `bandVerdict` 의 `OK_RATE` 와 **같은 값이어야 한다.** 다르면
  진단이 "안정"으로 판정해 넘긴 밴드가 리포트에서 "흔들림"으로 나와 두 화면이 서로 다른
  말을 한다. 9-B 에서 `firstShaky` 와 임계값을 맞춘 것과 같은 이유다
- `LEVEL_MIN_SEEN` 5 미만은 `thin`(표본 부족) — 판정을 안 한다. 2~3문항으로 "N1이 흔들린다"
  고 말하면 그건 진단이 아니라 점괘다
- `solidThrough` 는 **낮은 밴드부터 끊기지 않은 구간**만 센다. 밴드1이 흔들리는데 밴드2가
  안정이면 `solidThrough` 는 null 이다 — 표본 편중이지 실력 역전이 아니다

화면은 `Report.tsx` 의 `LevelSection`. **경계선(`.edge-line`)이 이 화면의 핵심 도형이다** —
안정 구간과 흔들리는 구간 사이에 실제로 선을 긋는다. 막대 색은 흔들리는 밴드만 朱,
나머지는 무채 (2026-09-06 "정답에 색을 주지 않는다"와 같은 규칙).

JLPT 근사는 `BAND_NOTE` 로 밴드명 옆에 흐리게 병기했다 — PLAN §4 "라벨은 자체 밴드명,
JLPT 는 설명 문구에만".

### 2. 처방 (`src/core/prescription.ts`)

우선순위는 "고치면 가장 많이 바뀌는 것" 순이다.

1. **표본 부족**(읽기 < 30회) → 처방 대신 "더 봐야 한다"만. 신호가 없는데 처방하면 거짓말
2. **지배적 오답 유형**(전체 오답의 30% 이상) → 규칙 하나를 익히면 여러 숙어가 한꺼번에 맞는다
3. **파급력 큰 취약 음독** 2개
4. **경계 밴드** → 시간을 어디 쓸지의 방향

최대 3개. `sessionSummary` 의 "발견 한 줄"과 같은 원칙 — 늘리면 대시보드가 되고 안 읽힌다.

**취약 음독을 파급력으로 전수 정렬하지 않는다.** 후보는 `report.weakOnyomi`(오답률 상위
8)를 쓰고 *그 안에서* `unlocksOf` 로 다시 세운다. 파급력만으로 전수 정렬하면 흔한 음독이
오답률과 무관하게 늘 1등이 된다 — 매번 같은 처방이 나오는 화면이 된다.

`MISTAKE_ADVICE` (`src/study/mistakeLabels.ts`) — 오답 유형별 규칙 한 줄. 라벨("촉음")만으로는
뭘 공부하라는 건지 알 수 없어서 규칙과 예시를 붙였다(ツ·チ·ク·キ 뒤 カ·サ·タ·ハ행 →
発達 はったつ / 라이먼의 법칙 → 三日月 みかづき / 重箱·湯桶).

### 3. 집중 세션 (`buildFocus`)

처방의 음독을 그 자리에서 실행한다. 재대결과 같은 계열이라 **기한을 무시하고
`selectSession` 을 안 탄다** — 이 세션의 전부가 "이 음독을 반복해서 만나는 것"이라
기한이나 모드 배분이 끼면 목적이 흐려진다.

**재대결과 다른 점 — 아직 안 본 숙어도 넣는다.** 처방이 "이 음독을 쓰는 숙어가 103개"라고
말했으면 그 103개를 실제로 열어야 말이 맞는다. 틀린 것만 다시 내면 그건 재대결이다.
정렬은 틀린 적 있음 → 안 봄 → 맞히기만 함.

기한 무시의 대가는 `buildRematch` 와 같다(FSRS 일정에 영향). "연습용 채점" 플래그는
이벤트 스키마 불변 조건이라 이번에도 안 만든다.

배선 — `SessionKind` 에 `'focus'` + `focusPairId` 인자, `App` 에 `focus` 화면과 `focusPair`
상태. 라우터가 없어 화면 상태가 문자열이라 쌍 id 를 별도 상태로 뒀다.

### `unlocksOf` 의 의미를 그대로 뒀다

실측에서 `人 じん` 이 158개, `公 こう` 가 103개로 나온다. 이건 **사전 풀 전체**에서 그
음독을 쓰는 숙어 수이지 "오늘 열리는 숙어 수"가 아니다. 과장으로 읽힐 여지가 있지만
(1) `sessionSummary` 의 같은 이름 콜백과 의미가 같아야 하고 (2) 문구를 "그만큼 걸려 있어요"
로 써서 사실 관계는 맞다. 실기기 체감(10-D)에서 과장으로 읽히면 그때 밴드 범위로
좁힌다.

### 검증

- `npm test` 267 → **290** (level 6 / prescription 7 / buildFocus 7)
- `npm run e2e` 5 → **6스펙 통과**. `full-flow` 의 리포트 단언을 새 구조로 갱신했고
  (`.report-lead` 가 이제 수준 섹션 안이라 읽기·정답률은 `.level .stat-line` 으로 옮겼다),
  `focus-session.spec.ts` 를 추가했다 — 세션 3회로 취약 음독을 쌓고 처방 버튼을 눌러
  **뜨는 카드의 표제어가 전부 그 한자를 품는지** 확인한다
- 카드 전환 p95 10.6ms (예산 150). `tsc -b`/`oxlint`/`npm run build` 클린
- 브라우저 실측 캡처 — 밴드1 86% 안정(무채) / **경계선** / 밴드2 57% 흔들림(朱) /
  밴드3 미학습, 처방 `公 こう`(103개) → `文 ぶん`(100개) → 밴드 2, 헤드라인
  "밴드 1까지 안정, 밴드 2가 경계예요"

### 남은 것

10-D 실기기 체감(사용자). 처방이 "다음에 이걸 하면 되겠다"로 읽히는지, 집중 세션이 한
음독만 반복해서 지루하지 않은지, 경계선이 과장돼 보이지 않는지.

---

## 2026-09-07 — 지속의 유인 (Phase 11-A~C)

사용자 질문. "재미를 위한 요소, 특히 **포기하지 않고 계속하도록 유인하는 방법**에는
어떤 것이 좋을까?"

### 스트릭을 다시 기각한 근거를 하나 더 적어둔다

2026-09-06 두 절에서 이미 기각했지만 근거가 "정체성과 안 맞는다"였다. 하나 더 —
스트릭의 실제 작동 원리는 "빠뜨리면 잃는다"는 **손실 회피**다. 의욕 없는 날엔 그게
죄책감으로 바뀌고, 한 번 끊기면 오히려 완전 이탈로 간다. 이 앱이 노리는 "의욕 없는
사용자"에 정면으로 안 맞는다.

### 이탈은 두 갈래다

**한 번의 약속 단위가 크다** / **나아지는 게 안 보인다**. 게임화는 둘 다 안 건드린다.
레버 넷을 제시하고 사용자가 순서를 정했다 — 2(예고) → 1(3장만) → 3(읽히는 문장),
4(시간축)는 뒤로.

- 보류 — **4. 시간축** (세션별 정답률 추이, 경계선 이동 이력). 지속에 가장 강하게 듣지만
  세션이 대여섯 번은 쌓여야 화면이 안 빈다. 기각이 아니라 다음 후보
- 안 함 — **오랜만에 돌아왔을 때 재적응 세션.** `sessionLimit` 20 이 이미 Anki 식
  "복습 400장" 벽을 가리고 있어 부채는 안 보인다. 다만 오래 쉬면 첫 세션이 전부 lapse 라
  우수수 틀리게 되는데, 그건 `selectSession` 에 "오랜만이면 쉬운 것부터"를 넣어야 해서
  비용이 크다. **실제로 며칠 쉬어보고 증상이 나오면 그때** 한다

### 1. 예고 (`src/core/nextUp.ts`)

세션 끝에 "意 い 음독 — 다음 세션에 6번 나와요" 한 줄. 자이가르닉 — 끝맺지 않고 남겨둔다.

- **다음 세션을 실제로 짜서 센다.** 짐작이면 빗나가고, 빗나가면 그 뒤로 안 믿는다.
  제시 *순서*만 시드로 섞이고 *어떤* 카드가 뽑히는지는 안 바뀌므로 예고가 맞는다
  (`SelectOptions.seed` 주석)
- **보상이 아니라 약속이다.** 그래서 "발견" 축을 안 벗어난다. 잘했다고 주는 게 아니라
  다음에 뭐가 온다고 알려주는 것
- 2회 미만은 예고 안 함 — 한 번 나오는 걸 예고하면 그냥 카드 목록이다

### 2. "3장만" (`QUICK_SESSION_LIMIT`)

9-B 가 진단에 쓴 논리(90 → 13, 한 번의 약속 단위를 줄인다)를 세션에 적용했다.
홈의 선택지가 "20장이냐 안 하냐"면 의욕 없는 날엔 "안 함"이 이긴다.

- **`LIMIT_MIN`(5) 아래 값이라 설정으로는 못 만든다. 그게 의도다** — 기본 리듬은 그대로
  두고 진입로만 따로 낸다. 설정을 안 건드리므로 다음 세션은 원래 길이로 돌아온다
- 기각 — **설정의 `sessionLimit` 을 임시로 덮어쓰기.** localStorage 를 건드리면 "오늘만"이
  아니게 되고, 되돌리는 코드가 또 필요하다. 화면 상태로 넘기는 게 맞다
- `useStudySession` 을 옵션 객체로 바꿨다. 위치 인자 3개면 `('normal', undefined, 3)` 이 된다

### 3. 읽히는 문장 (`src/core/readable.ts`)

오늘 맞힌 숙어가 든 Tatoeba 예문 하나를 요약에 얹는다 — `犬は忠実です。`(忠実 강조).

- **보상을 점수가 아니라 실제로 읽히는 경험으로 준다.** "뜻은 아는데 못 읽는" 사람이
  대상이니(PLAN §0) 문장 하나가 술술 읽히는 순간이 이 앱이 줄 수 있는 진짜 증거다
- **가장 짧은 문장**을 고른다. 짧을수록 "읽혔다"가 분명하고 끝까지 읽기 전에 안 포기한다
- 밴드 3 예문 커버리지가 50%라 카드 단위론 들쭉날쭉하지만(2026-09-04 절), 20장 중
  하나만 있으면 되니 **세션 단위로는 거의 매번 뜬다**
- 예문은 무번역이다. 여기서 필요한 건 뜻이 아니라 읽힌다는 사실이라 문제가 안 된다

### 요약이 3블록이 됐다 — 감시 대상

발견(관찰) + 문장(증거) + 예고(약속). 레지스터가 셋 다 달라서 넣었지만
"발견은 한 줄만"(2026-09-06) 원칙과 긴장이 있다. 대시보드로 읽히면 줄인다 (11-F).

### 버그 — 짧은 화면에서 통계 값이 잘렸다

요약이 길어지자 flex 항목이 줄면서 `.summary-stats` 의 `overflow:hidden` 이 `dd` 를 잘라
**라벨만 남았다**(390×620 에서 실측). `.summary-screen` 에 `overflow-y:auto` +
`justify-content: safe center` + 자식 `flex-shrink: 0`. `safe center` 는 넘칠 때만 위
정렬로 떨어져 제목이 안 잘린다. 전에도 잠재해 있던 결함이고 이번에 길어져 드러났다.

---

## 2026-09-07 — 대조 (Phase 11-D~E)

사용자 질문. "반복적으로 틀리는 것을 교정하는 방법은 역시 **같은 유형의 문제를 계속
제시해서 암기하도록 하는 것뿐**일까? **음운 규칙 같은 해설은 필요없을까?**"

### "반복이냐 해설이냐"는 이분법이 틀렸다

- **반복만으로는 부족하다.** 촉음·연탁·장음은 생성 규칙이지 외울 항목이 아니다.
  発達을 백 번 외워도 出発이 안 맞아지지만 규칙을 잡으면 둘 다 맞는다. 반복으로 고치면
  비용이 숙어 수에 비례하고 규칙으로 고치면 한 번이다
- **해설만으로도 부족하다.** 음운 규칙은 선언적 지식인데 읽기 산출은 절차적이다.
  이해하는 것과 0.5초 안에 나오는 것은 다른 능력이라 해설이 반복을 대체하지 못한다.
  **해설은 반복의 횟수를 줄여줄 뿐이다**
- **답은 대조다.** 규칙이 걸린 것과 안 걸린 것을 나란히 봐야 규칙의 *경계*를 배운다.
  같은 유형만 계속 주면 "이 자리엔 항상 촉음"이라는 **과잉일반화**가 생겨서, 고치려던
  오답 대신 새 오답이 만들어진다. 이게 반복-only 의 진짜 위험이다

### 진짜 빈 구멍 — 6종으로 분류만 하고 대응은 하나였다

오답을 6종으로 나눠놓고 어느 유형이든 결국 "다시 냄"이었다. 유형마다 처방이 달라야 한다.

| 유형 | 성격 | 처방 |
|---|---|---|
| 촉음·연탁·장음 | 규칙형 | 대조 쌍 + 규칙 한 줄 |
| 음훈 혼독 | 반규칙형 | 重箱·湯桶 두 패턴 |
| 음독 선택 | 어휘형 | 같은 한자의 다른 음독 나란히 |
| 한국음 간섭 | 어휘형 | 한국음 대조 병기 |

**아래 둘엔 해설을 안 붙인다.** 発을 はつ로 읽을지 ほつ로 읽을지는 어휘별로 정해진
것이라 규칙이 없고, 규칙이 없는 자리에까지 글을 늘리면 있는 해설까지 안 읽힌다.
`RULE_MISTAKES` 가 이 경계다.

### 표면형이 대조의 기준이다 (`src/core/surface.ts`)

`surfaceOfPair` — 그 쌍이 이 숙어에서 실제로 낸 소리. Phase 2 의 `decompose` 를 그대로
쓴다(`rubyOf` 와 같은 경로). **원형으로는 대조가 안 선다** — 発 はつ 하나로는 発達 はっ 와
発言 はつ 가 안 갈린다.

### 오답 상세의 "같은 음독"을 대조군으로 (`contrastGroups`)

평평한 목록은 "같은 음독을 쓰는 것들"까지만 말한다. 표면형별로 갈라 최대 2군,
**지금 틀린 숙어가 속한 군이 항상 먼저**다. 실측(圧迫) —

- 圧 あっ(圧迫·圧巻·圧殺) ↔ あつ(圧延·圧力·圧力団体)
- 迫 ぱく(圧迫·緊迫·切迫) ↔ はく(気迫·脅迫·迫害)

군이 하나면 예전의 평평한 목록으로 돌아간다 — 대조할 게 없는데 새 UI 를 쓰지 않는다.
`CONTRAST_SCAN_LIMIT` 40 — 흔한 음독은 100개가 넘어 숙어마다 `decompose` 를 돌리면 비싸다.
카드 루프가 아니라 "자세히"를 눌렀을 때만 도는 경로라 40이면 충분하다.

### 집중 세션도 번갈아 낸다 (`interleaveBySurface`)

`buildFocus` 에 `surfaceOf` 콜백을 옵션으로 받는다. **core 가 `decompose` 를 직접 안
부르는 이유** — `IdiomEntry` 에는 표기·읽기가 없다. 화면이 계산해 넘기면 core 는
`IdiomEntry` 만 아는 상태로 남는다.

- **자르기 전에 섞는다.** 뒤에 걸면 상위 N 개가 한쪽 표면형에 몰렸을 때 못 갈린다
- 그룹이 하나뿐이면 입력을 그대로 돌려준다 — 대조할 게 없는데 정렬만 흔들지 않는다
- 그룹 안의 순서(틀린 것 먼저)는 유지되므로 "교정 우선"이 안 깨진다

### 검증

- `npm test` 290 → **316** (nextUp 5 / readable 8 / surface 5 / contrastGroups 6 / focus 대조 4)
- `surface.test.ts` 는 고정본 위에서 실제 `decompose` 를 돌린다 — 学校 がっ / 学食 がく 가
  정말 갈리는지 확인하는 것이라 스텁으로는 의미가 없다
- `npm run e2e` 6스펙 통과 (full-flow 에 "3장만" 3장 완주 구간 추가). 카드 전환 p95 10.3ms
- `tsc -b`/`oxlint`/`build` 클린. 요약·대조·짧은 화면 3종 브라우저 캡처

### 남은 것

11-F 실기기 체감(사용자). 예고가 궁금증인지 소음인지, "3장만"을 실제로 누르게 되는지,
읽히는 문장이 보상으로 느껴지는지, 요약 3블록이 대시보드로 읽히지 않는지, 대조가 규칙을
눈에 들어오게 하는지.

---

## 2026-09-07 — 진단 진입점이 동기화를 못 따라갔다

사용자 보고. "동기화를 통해서 진단 결과가 있는 사용자에게는 진단 시작 버튼이 노출되지
않아야 할 것 같다."

### 원인 — 플래그는 기기를 안 넘어간다

`yomenai:diagnosticDone` 은 **localStorage** 다. 동기화는 이벤트 로그만 옮기므로
(기기별 `reviews-{deviceId}.json`, PLAN §5 원칙 3), 새 기기는 기록이 다 있는데 플래그만
비어서 진입 진단을 다시 권받는다.

### 같이 나온 반대 방향 결함 — 초기화 후 진단이 영영 안 뜬다

`resetLearning` 이 이벤트와 Drive 파일만 지우고 플래그는 남긴다. "처음부터 다시"가 목적인
동작인데 진단은 다시 못 받는 상태가 됐다. 같은 플래그의 반대편이라 함께 고쳤다.

### 고침 — 플래그 **또는** 로그에서 파생한 수준

`shouldOfferDiagnostic(done, level)` — 플래그가 없어도 `buildLevel` 이 어느 밴드든
판정을 냈으면(`solidThrough` 나 `edge` 가 섰으면) 안 권한다. 진입 진단의 목적이
"읽기가 흔들리기 시작하는 첫 밴드 찾기"뿐이라(PLAN §6), 그 답이 이미 있으면 진단이
더 할 말이 없다.

- 기각 — **로그만으로 판정(플래그 제거).** 적응형 진단은 3문항에서 끝날 수 있어
  (`bandVerdict` 오답 3 → 종료) 방금 마쳤는데도 `LEVEL_MIN_SEEN`(5)에 못 미쳐 판정이 안
  선다. 그러면 끝내자마자 다시 권하게 된다. 플래그는 그 구멍을 메우는 역할로 남긴다
- 기각 — **플래그를 동기화에 태운다.** 지금 동기화는 이벤트 전용이고 설정 파일을 얹으면
  병합 규칙(어느 기기 값이 이기나)이 새로 필요해진다. 파생으로 풀 수 있는 걸 스키마를
  늘려 풀지 않는다
- 홈은 판정이 섰으면 `markDiagnosticDone()` 을 세워 둔다. 다음 진입부터는 로그를 다 읽기
  전에 정해져 깜빡임이 없다
- 초기화는 `clearDiagnosticDone()` 을 부른다

`shouldOfferDiagnostic` 이 플래그를 직접 안 읽고 인자로 받는 건 테스트 환경이 node 라
localStorage 가 없기 때문이다 (`settings.test.ts` 가 `parseSettings` 만 테스트하는 것과 같은 이유).

### 딸려 온 동작 변화 — 진단을 건너뛰고 세션만 해도 진입점이 사라진다

의도한 결과다. 20장을 풀었으면 리포트가 이미 수준을 말할 수 있어서 진단이 알려줄 게
없다. 다만 9-A "홈 갈림길"이 진단을 권하려던 의도와는 어긋나므로 적어둔다 — 건너뛰기를
고른 사용자가 한 세션 뒤 진단을 못 찾게 된다. 실사용에서 문제가 되면 진단을 설정이나
리포트에서 다시 열 수 있게 한다.

### 검증

- `diagnostic-state.test.ts` 5 — 처음 / 플래그 있음 / 플래그 없고 경계 있음 /
  안정만 있음 / 표본 얇음
- `tests/e2e/diagnostic-flag.spec.ts` — 처음엔 뜨고 → 세션 뒤엔 안 뜨고 →
  **localStorage 를 통째로 비우고 새로고침해도(= 동기화로 기록만 받아온 기기) 안 뜨고** →
  학습 기록 초기화 뒤엔 다시 뜬다
- `npm test` 316 → **321**, `npm run e2e` 6 → **7스펙 통과**(카드 전환 p95 10.7ms),
  `tsc -b`/`oxlint`/`build` 클린

---

## 2026-09-07 — 신뢰도는 층마다 다르다 (Phase 12 기록)

사용자 질문. "전문가 감수가 이 서비스의 신뢰도에 영향을 줄 것 같은데, 이 서비스의
신뢰도는 어때?" 그리고 이어진 되물음이 이 절의 결론을 바꿨다 — **"한자어가 특정인이 새로
만든 것이 아니라 사전에 있는 표제어인데, 사람이 일일이 감수해야 정확한가?"**

### 실측 — `public/dict/base.json` 16,970개 (2026-09-07)

| 항목 | 수치 |
|---|---|
| `classSource: manual` (사람 확정) | **348 (2.1%)** |
| `llm` (Ollama qwen3.5 초벌) | 8,155 (48.1%) |
| `default` (자동 잠정) | 8,463 (49.9%) |
| `koMeaning` 보유 | 13,448 (전부 stdict) |
| 그중 `verified: true` | **0** |
| 동형이의 (category 2) | 7,273 |
| 분해 실패 (`pairIds` 빈 것) | 0 — 실패분은 런타임 사전에 안 실린다 |
| 예문 보유 | 11,225 (66%) |
| 표기가 겹치는 표제어 | 12 (그중 9건에 예문) |

예문 매칭은 `text.startsWith(headword, i)` 라 **읽기를 검증하지 않는다.** 二度目 이 든
문장이 二度 에 붙을 수 있다. 표기 겹침이 12건뿐이라 최악의 경우는 드물다.

### 처음 낸 답 — 감수 3순위 (그리고 그게 과했다)

`MISTAKE_ADVICE` → `decompose` 표본 검수 → 동형이의 뜻. `decompose` 를 1순위로 둔 이유는
음독 맵·취약 음독·처방·대조군이 **전부 그 하나 위에 서 있어서**다. 있는 검증("표면형을
이으면 원래 읽기가 복원된다")은 **무모순성이지 정확성이 아니다** — 여러 해석이 가능한
자리에서 비용 함수가 고른 게 언어학적으로 맞다는 보장이 없다.

### 사용자 지적이 옳았다 — 층을 갈라야 한다

"사전에 있는 걸 왜 사람이 보나"가 맞는 말이다. 감수 대상은 **사전에 있는 것이 아니라
사전에 없어서 우리가 만들어낸 것**뿐이다. 그렇게 갈랐더니 3층이 나왔다.

**A. 사전에 있다 → 사람 불필요.** 표제어·읽기·품사·빈도(JMdict), 음훈독·한국
한자음(KANJIDIC2). 이미 안 보고 있다.

**B. 사전엔 없지만 남이 이미 만들었다 → 대조로 끝난다.** 発達 = はったつ 는 JMdict 에
있지만 **発=はっ / 達=たつ 라는 경계는 어느 사전에도 없다.** JMdict 는 단어 단위,
KANJIDIC2 는 글자 단위이고 그 둘을 잇는 매핑이 없어서 `decompose` 를 만들었다.
그런데 **JmdictFurigana** 가 정확히 그걸 만들어 배포한다 (확인: 2026-09-07 웹 검색).

- JMdict 234,814개 중 **177,770개(75.7%)** 해결분 배포, CC BY-SA (JMdict 승계)
- 방식도 비슷하다 — KANJIDIC 읽기로 재귀 패턴 매칭 + **수작업 예외 목록**
- 스스로 "not 100% accurate", "특수 표현을 잘못 자르는 문제"를 한계로 명시한다
- 출처 — github.com/Doublevil/JmdictFurigana

**C. 아무도 안 만들었다 → 여기만 사람이다.** 한국어 뜻·분류가 여기다. stdict 는 한국어
사전, JMdict 는 일본어 사전인데 **"이 한자어가 한국어와 같은 뜻인가"를 이은 데이터셋은
없다.** 그래서 LLM 초벌을 돌렸고, 그래서 `manual` 이 2.1% 다. 오답 분류기 규칙과
`MISTAKE_ADVICE` 도 여기 속한다.

### 결정 — 표본 검수를 대조로 바꾼다

- **기각 — `decompose` 층화 표본 200~300개 사람 검수.** 외부 데이터셋으로 전수를 훑을 수
  있는데 표본을 사람에게 보내는 건 낭비다. 표본은 오류율만 주고 교정은 표본 크기만큼만 된다
- **채택 — JmdictFurigana 를 두 번째 의견으로 전수 대조.** 두 알고리즘이 각각 KANJIDIC
  에서 출발해 다른 방식으로 풀었으니, **일치하면 신뢰하고 불일치분만 사람에게 보낸다.**
  전수를 훑으면서 사람 노동은 위험 지점에만 쓴다
- **JmdictFurigana 는 정답지가 아니다.** 저쪽도 알고리즘 + 수작업이고 스스로 한계를
  밝혔다. 불일치 = 우리가 틀렸다가 아니라 = 사람이 볼 자리다. 이 구분을 놓치면 저쪽
  오류를 그대로 베끼게 된다
- **대조 범위는 경계까지다.** JmdictFurigana 는 표면형(発 → はっ)만 준다. 원형(はつ)과
  음훈 구분은 우리 몫이고 `(한자, 음독)` 쌍의 정체성이 거기서 나온다. 다만 경계가 틀리면
  원형도 반드시 틀리므로 지배적 위험은 걷힌다
- 커버리지는 실측해야 안다. 미해결 24%가 주로 熟字訓·고유명사·가나 섞임이라 **상용한자
  한자-only 인 우리 코퍼스와는 잘 안 겹칠 것**으로 본다 — 75.7%보다 높게 나올 수 있다

감수가 필요 없어진 게 아니라 **범위가 "분해 전체"에서 "불일치분 + 한국어 뜻"으로 줄었다.**
그리고 한국어 뜻은 "일본어 전문가"가 아니라 한일 이중언어 화자면 된다.

### 별건으로 발견 — 사람의 판정이 커밋 안 돼 있다

`.gitignore` 에 `data/dict/` 가 통째로 들어 있어서 **Phase 3 수동 검수 355건이 추적되지
않는다.** `git ls-files data/` 가 빈 결과다.

재생성 산출물을 안 커밋하는 규칙 자체는 맞다(2026-09-05 "배포 데이터 공백"). 그런데
`korean-review.tsv`·`korean-worklist-t1.tsv` 는 **사람이 만든 원본이라 재생성이 불가능**해
성격이 다르다. 지금은 이 기기가 날아가면 사라진다. Phase 12 의 감수 결과도 같은 자리에
떨어지므로 **12-A 를 다른 것보다 먼저** 둔다.

### 신뢰도 설계에서 잘 된 부분 (감수 결과를 꽂을 자리가 이미 있다)

- 리포트가 미분류 오답을 숨기지 않는다 ("6종 어디에도 안 맞아 분포에서 빠졌어요")
- 표본이 얇으면 처방 대신 "더 봐야 해요"만 말한다
- 밴드에 `thin`(표본 부족) 상태가 있어 2~3문항으로 단정하지 않는다
- 뜻 카드가 이미 `미검수` 뱃지를 단다 (`MeaningCard.tsx`)
- `source`/`verified` 가 필드 단위로 스키마에 있어 **어디를 못 믿는지 기계가 안다**

### 현재 신뢰도 요약

**읽기 교정 도구로는 지금도 신뢰할 만하고, 어휘 확장 도구로는 아직 아니다.** 채점 근거인
JMdict 읽기는 튼튼한데 뜻은 2.1%만 사람이 봤다.

## 2026-09-07 — Phase 12-A·12-B 실행

### 12-A — 사람이 만든 검수 판정을 git 추적

`.gitignore` 가 `data/dict/` 를 통째로 무시해 Phase 3 수동 검수가 커밋 안 돼 있었다.
디렉터리 무시로는 `!` 예외가 안 먹어서 `data/dict/*` 로 바꾸고 아래를 되살렸다.

- `!data/dict/*-review.tsv` `!data/dict/*-review-sample.tsv`
- `!data/dict/*-worklist*.tsv` `!data/dict/*-overrides.json` (12-C 산출물 자리 예약)

추적되는 것 — `korean-review.tsv`(15,357행, tier2 검수 355건) ·
`korean-review-sample.tsv`(200행) · `korean-worklist.tsv`(356행) ·
`korean-worklist-t1.tsv`(440행). 재생성 산출물(bands·onyomi-map·korean-class…)은
계속 무시. `build:bands`·`build:onyomi` 재실행해도 `git status` 에 안 뜸 확인. 커밋 `0fcccb8`.

**LLM 초벌(`korean-llm-draft.tsv`, 8,700건)은 스코프 밖.** 사람이 만든 게 아니라
재생성 가능(Ollama + `.korean-llm-cache.json`)이라 계속 무시한다.

### 12-B — JmdictFurigana 전수 대조 (사람 0명, 경계까지만)

`data/raw/JmdictFurigana.json` (release 2.3.1+2026-08-25, 236,255개, UTF-8 BOM 주의).
`tools/measure-furigana.ts` — 표기+읽기로 조인해 `decompose` 의 **내부 경계 오프셋 집합**을
JF 의 furigana 경계와 대조. 산출물 안 남기는 일회성(measure-tatoeba 급), 불일치 덤프만
`data/raw/furigana-mismatch.tmp.tsv`(gitignore).

**수치 (우리 코퍼스 106,803개 전량):**

| | 수 | 비고 |
|---|---|---|
| 조인 성공 | 104,840 (98.16%) | JF 전체 해결률 75.7%보다 훨씬 높다 — 상용한자 한자-only 라 잘 겹친다 |
| 경계 비교 가능 (조인 + 분해 OK + JF per-kanji) | 102,410 | |
| ├ 완전 일치 | 101,848 (**99.45%**) | |
| ├ 우리가 더 촘촘 (`our_finer`) | 562 (0.55%) | JF 가 안 쪼갠 자리를 우리가 쪼갬. 밴드 4에 546 집중, 밴드 0~3 은 16 |
| └ **충돌** (JF 가 자른 자리를 우리가 안 자름) | **0** | 밴드 무관 0. 위치가 어긋난 경계가 하나도 없다 |
| JF 는 furigana 있는데 우리는 분해 실패 (`our_fail`) | 2,430 | NO_PARSE 2,306 / KATAKANA_READING 124. 밴드 0~3 은 185 |

**육안 확인:**

- `our_fail` 밴드 0~3 **185건 전량** — 全部 熟字訓·当て字·특수독. 為替 かわせ / 果物 くだもの /
  景色 けしき / 時計 とけい / 大人 おとな / 部屋 へや / 相撲 すもう / 今日 きょう / 眼鏡 めがね /
  十八番 おはこ / 香港 ホンコン(가타카나) … **분해기 버그로 보이는 건 0건.** 거부가 정상 동작이다.
  계통적 갭 두 가족만 눈에 띔 — 日本〜 (日=に 가 깔끔한 음독이 아님, ~20건) · 文字〜 (文=も, ~8건).
  둘 다 "쪼갤까 말까"지 "엉뚱한 자리"가 아님
- `our_finer` 밴드 0~3 16건 — 두 종류. ① 連声·유효 읽기라 우리가 맞음 (天皇 てん\|のう 連声,
  上手 じょう\|ず) ② 熟字訓 강제 분할 (一人 ひと\|り, 二人 ふた\|り, 相部屋 あい\|べ\|や) — 과분할이지만
  경계 위치 자체는 JF 가 원하는 자리와 같다

**결론 — `decompose` 경계는 믿을 만하다.** context-notes 「신뢰도는 층마다 다르다」가 걱정한
"여러 해석 가능한 자리에서 비용 함수가 언어학적으로 맞다는 보장이 없다"는 **반증됐다.**
JF 가 의견을 가진 자리에서 우리는 그 의견과 일치하거나(99.45%) 더 잘게 자를 뿐(0.55%),
다른 자리에 경계를 찍은 적이 0회다.

**12-C 는 사실상 할 게 없다.** 검수 워크리스트에 올릴 "충돌"이 0건이다. 남는 판단은
(1) 熟字訓 과분할 562건을 decompose 가 거부/그룹화하도록 바꿀지 (밴드 0~3 은 16건),
(2) 日本〜·文字〜 가족을 분해 대상에 넣을지 — 둘 다 "경계가 틀렸다"가 아니라 "범위" 문제.

## 2026-09-07 — Phase 12-C 범위 변경 (熟字訓 과분할 거부)

12-B 가 경계 충돌 0건을 냈으니 원안(불일치 검수 워크리스트)은 올릴 게 없어 폐기.
대신 12-B 가 남긴 `our_finer` 562건 — "우리가 JmdictFurigana 보다 잘게 자른" 것 —
을 처리한다. 사용자 지시로 12-C 를 이 작업으로 살렸다.

### 갈림 규칙 — "JF 가 묶은 구간에 음운 변형이 있나"

`our_finer` 를 손으로 훑어 두 종류가 섞여 있음을 확인 —

- **음운 파생 분해 (유지해야 함)** — 天皇 てんのう 를 天=てん / 皇=のう 로 자른다.
  皇 은 音 コウ/オウ 뿐이고 のう 는 おう 의 連声(ん+おう→のう)이다. decompose 가 `renjo`
  변형 태그를 달고 있다. 日本→にっぽん (促音+半濁), 島草履→ぞうり(連濁) 도 같은 부류. **459건.**
- **우연한 사전 읽기 연접 (거부해야 함)** — 部屋 べや (部 의 희귀 훈 -べ + 屋 や 가 우연히 이어짐),
  如雨露 じょうろ (当て字), 算盤 そろばん (算 に そろ 없음). 변형 태그가 하나도 없다. **103건.**

두 부류를 가르는 신호는 **JF 가 rt 로 2자 이상 묶은 구간 안에 우리 세그먼트의 `variants`
(連濁·連声·促音·半濁)가 하나라도 있나**다. 있으면 음운 파생이라 유지, 없으면 熟字訓 으로 거부.
cost 는 신호가 안 된다 — our_finer cost 는 0.00~3.53, match 대조군도 최대 2.00 으로 겹친다.

### 구현

- `tools/build-decomp-overrides.ts` → `data/dict/decomp-overrides.json` (커밋. 12-A 의
  `!*-overrides.json` 예외. JF 파생이라 재생성 가능하지만 JF 는 커밋 안 하므로 CI·추적용으로 굳힌다).
  `--validate` 로 규칙/비용함수를 손볼 때 드리프트를 잡는다
- `build-onyomi-map.ts` — `buildMap(idioms, kanji, rejectIds = new Set())`. 파일 없으면 빈 집합이라
  CI(JF 미다운로드)에서도 재현된다. 실패 사유 enum 에 `JUKUJIKUN` 추가 (툴 로컬 타입,
  `src/lib/onyomi.ts` 안 건드림 — 런타임 `src/core/ruby.ts` 가 쓰는 decompose 는 순수하게 둔다)
- `build-runtime-dict.ts` 는 원래부터 `byIdiom[id]` 없으면 스킵한다 ("熟字訓 은 학습 대상 아님").
  103건이 그 경로로 base/band4 에서 빠진다

### 수치

| | 전 | 후 |
|---|---|---|
| `stats.ok` | 102,549 | 102,446 (−103) |
| 고유 (한자,읽기) 쌍 | 4,001 | 3,936 (−65, 그 103건에만 있던 쌍) |
| `public/dict/base.json` | 16,970 | 16,959 (−11, 밴드 0~3 거부분) |
| `public/dict/band4.json` | — | −92 |

밴드 0~3 영향 11건뿐이고 빠지는 쌍(人:り, 部:べ, 乙:おと, 止:と)은 음독 진단 축에서
잡음이라 순개선이다. **JmdictFurigana 는 정답지가 아니다** — 이 규칙은 JF 의 "묶었다"는
사실만 트리거로 쓰고, 유지/거부 판단은 우리 `variants` 태그로 한다. JF 의 분절을 베끼지 않는다.

## 2026-09-07 — Phase 12-E: MISTAKE_ADVICE 6줄 문헌 대조

Phase 11 에서 내가 작성한 규칙 서술이라 검수를 안 거쳤다. `RULE_MISTAKES` 4종
(SOKUON·RENDAKU·CHOON·MIXED_READING)만 대상 — ONYOMI_CHOICE·KO_INTERFERENCE 는
"규칙 없음"으로 이미 명시했고 서술도 "대조를 보라"지 규칙 주장이 아니다. OKURIGANA 는 미사용.

### RENDAKU — 정확. 수정 없음

"뒷 글자 첫소리가 탁음으로 — 三日月 みかづき. 뒷 글자가 이미 탁음을 품으면 잘 안 일어남(라이먼의 법칙)."

- 連濁(sequential voicing)의 표준 정의. 라이먼의 법칙(제2요소에 유성 저해음이 있으면 연탁
  차단) — Lyman(1894)이 정식화, Motoori·Kamo(18C)가 선행 관찰. 근거: Vance,
  *The Sounds of Japanese*(2008) ch.7; Itô & Mester, 연탁·OCP 논의; 窪薗晴夫 *日本語の音声*.
- 예시 三日月: 月 つき→づき 가 연탁. つき 에 유성 저해음 없음 → 차단 안 됨. **coherent.**
  코퍼스에 있고(nf35), `decompose` 도 月 세그먼트에 `rendaku` 태그를 단다. 확인됨.

### SOKUON — C1 은 맞고 C2 목록이 과했다. 수정함

원문 "ツ·チ·ク·キ … 뒤에 カ·サ·タ·ハ행 → 촉음."

- 漢語의 促音化 — 제1형태소가 -ki/-ku/-ti/-tu(중고음 -k/-t 입성운미)로 끝나고 제2형태소가
  무성 저해음(k, s, t, p<h)으로 시작하면 촉음. 근거: Vance(2008) ch.6; Frellesvig,
  *A History of the Japanese Language*(2010); 窪薗晴夫.
- **비대칭이 있다** — ツ·チ(-t 운미)는 카·사·타·하 앞에서 규칙적이지만, ク·キ(-k 운미)는
  カ행 앞에서만 규칙적이고 サ·タ행 앞에서는 대개 안 붙는다: 学校 がっこう(o) ↔
  国際 こくさい / 学生 がくせい / 特定 とくてい(x). 이 -k/-t 비대칭은 문헌이 다루는 알려진 현상.
- 원문 예시 発達 はったつ(つ+た), 学校 がっこう(く+か)는 둘 다 규칙적 구간이라 **예시 자체는 유효**.
  문제는 "ク + サ행"까지 규칙처럼 말한 것. → **カ·タ·ハ행으로 좁히고, ク·キ+サ행 예외(国際)를 명시**로 수정.
  코퍼스 확인: 発達 nf11, 学校 nf01, 国際 nf01 모두 존재.

### CHOON — 규칙 서술은 맞음. "가장 잦아요"는 빈도 주장(미검증)

"장음이 붙는지의 자리 — 特徴 とくちょう. ょ·ゅ 뒤 う 를 빠뜨리는 게 가장 잦아요."

- 漢語 장모음은 어휘별로 고정(音便이 아님)이고 한국어에 음소적 모음 길이 대립이 없어
  한국어 L1 화자가 과소 산출한다 — 대조음운론의 표준 설명. 근거: Shibatani,
  *The Languages of Japan*(1990) 모음 길이 절; 민광준 *일본어 음성학*; 한국어 화자
  일본어 습득 대조 연구. 拗音+장음(きょう·ちょう·しょう)이 漢語에 조밀해 오류가 집중된다는
  것도 이 논리의 연장.
- 다만 "가장 잦아요"는 **우리 오답 로그로 확인한 값이 아니다.** 대조음운론상 개연적이라
  틀렸다고 볼 근거도 없다. 실사용 로그가 쌓이면 재확인 대상. 예시 特徴 코퍼스 존재(nf02).

**2026-09-08 수정 — CHOON 안내를 양방향으로.** 사용자가 健保(けんぽ)에 없는 장음을 붙여
(けんぽう) 오답이 났는데, 분류기는 `stripLongVowels` 동치로 CHOON 을 맞게 냈지만 안내문이
"빠뜨리는 게 가장 잦아요"라 반대 방향만 말하고 있었다. 分類器는 장음 누락·첨가를 다 CHOON
으로 보내므로 안내도 둘 다 포괄해야 한다 → "빠뜨리거나(特徴), 없어야 할 자리에 붙이는
(健保) 실수가 다 나요"로 고치고 미검증 빈도 주장("가장 잦아요")은 뺐다.

### MIXED_READING — 정확. 단 예시 하나가 코퍼스 밖

"음+훈(重箱 じゅうばこ), 훈+음(湯桶 ゆとう)."

- 重箱読み=음+훈, 湯桶読み=훈+음. 두 용어의 이름 자체가 그 패턴의 실례라 표준 교재가
  그대로 쓴다(각 일본어 사전 표제어; 金田一春彦 등). 重 じゅう(음)+箱 はこ→ばこ(훈),
  湯 ゆ(훈)+桶 とう(음) — 분석상 정확.
- **重箱은 코퍼스에 있고(nf34) `decompose` 가 mixed 로 잡지만, 湯桶은 없다** — 桶이 표외자라
  임포트 필터에서 빠졌다. 사용자가 앱에서 湯桶을 눌러볼 수는 없다. 용어(mnemonic)로 쓰는
  거라 그대로 둘지, 코퍼스 내 훈+음 예(場所 ばしょ·株式 かぶしき·黒字 くろじ, 전부 nf01~02)를
  괄호로 덧붙일지는 사용자 판단. 이번엔 텍스트 안 건드림.

### 결론

수정 1건(SOKUON C2 축소 + ク·キ+サ 예외 명시). 나머지 3줄은 문헌상 정확, 예시도 규칙의
실례가 맞음. CHOON 빈도 주장과 MIXED_READING 湯桶 코퍼스 부재는 기록만 하고 남긴다.

## 2026-09-07 — 서비스 워커 + 오프라인

### 증상과 원인

`choan616.github.io/yomenai/` 진입 시 가끔 mmtm 첫 화면이 떴다. HTTP 캐시가 아니라
**mmtm 서비스 워커**다. 두 앱이 같은 오리진 — mmtm 은 사용자 페이지라 루트(`/`), yomenai 는
프로젝트 페이지라 `/yomenai/`. mmtm SW 는 스코프 `/` 라 오리진 전체를 가로채고, 오프라인
우선 전략의 `navigateFallback` 이 `/yomenai/` 내비게이션에 mmtm `index.html` 을 돌려준다.
간헐적인 건 SW 상태·네트워크 경쟁 타이밍 탓.

yomenai 엔 SW 가 아예 없었다 — `vite-plugin-pwa` 가 devDep 에만 있고 `vite.config.ts` 에
연결이 안 돼 있었다. 방어할 SW 가 없으니 mmtm SW 가 그대로 이겼다.

### 대응 — yomenai 가 `/yomenai/` 스코프 SW 를 직접 등록

`/yomenai/sw.js` 는 스코프가 `/yomenai/` 라, 그 경로의 페이지에 대해선 **더 좁은 등록이
mmtm 의 루트 등록을 이긴다**(스코프 매칭은 최장 일치). `registerType: 'autoUpdate'` +
`skipWaiting`/`clientsClaim` 으로 새 SW 가 프롬프트 없이 즉시 제어권을 잡아 mmtm SW 를
빨리 밀어낸다. `injectRegister: 'script'` 라 앱 코드는 안 건드린다 (index.html 에
`registerSW.js` 주입).

**첫 배포 직후 첫 방문 1회**는 여전히 mmtm SW 가 응답을 한 번 줄 수 있다. 그다음 로드부터
yomenai SW 가 제어하고, 이후 자가 치유된다.

### 프리캐시 범위 — 첫 실행부터 오프라인

`generateSW` 모드. 런타임 캐시(SWR)만 쓰면 **첫 방문에서 SW 가 클라이언트를 claim 하기
전에 나간 `base.json` fetch 가 캐시를 안 거쳐** 첫-오프라인이 깨진다(실측함). 그래서
핵심 사전을 프리캐시에 넣는다 —

- 프리캐시(15개, 9.4MB): 앱 셸(js/css/html) + 폰트 3종 + `dict/{base,pairs,kanji,examples}.json`.
  `maximumFileSizeToCacheInBytes: 6MB`(base.json ~5.5MB)
- 런타임 캐시(StaleWhileRevalidate, `yomenai-dict-band4-v1`): `dict/band4.json`(19MB)만.
  밴드 4 는 "선택 · N1 초과"라 켠 적 있을 때만 캐시된다
- `cleanupOutdatedCaches` — 배포 때마다 프리캐시 리비전이 바뀌어 옛 캐시는 정리된다.
  사전 내용이 바뀌어도 프리캐시 revision 으로 새로 받는다

`devOptions.enabled: false` — `npm run dev` 와 e2e(webServer 가 `npm run dev`)에는 SW 가
안 붙어 테스트에 영향 없다. CI(`npm run build` → `dist/` 통째 업로드)는 워크플로 변경 불필요.

### 검증

`vite preview` + Playwright 일회성 — SW 스코프 `http://…/yomenai/` 확인, 온라인에서
"이번 세션 20장", `ctx.setOffline(true)` 후 리로드 → `読めない` 렌더 + "이번 세션 20장"
+ 세션 시작(安価)까지 콘솔 에러 0. `npm test` 325 · `npm run e2e` 7스펙 통과.

영구 오프라인 e2e 스펙은 안 넣었다 — Playwright webServer 가 `npm run dev`(SW off)라
`vite preview` 를 띄우는 별도 프로젝트/설정이 필요하다. 후속 후보.

## 2026-09-07 — 참조 라이선스 기록 (Phase 12 + SW 작업)

이번에 새로 끌어온 외부 리소스·의존성의 라이선스. 서비스화 판단 전 Phase 8
"미검수·승계 범위 문의"와 같은 선상에서 미리 남긴다.

| 리소스 | 위치 | 버전 | 라이선스 | 재배포 여부 | 비고 |
|---|---|---|---|---|---|
| **JmdictFurigana** | `data/raw/JmdictFurigana.json` (커밋 안 함) | release `2.3.1+2026-08-25` | **CC BY-SA 4.0** (JMdict 승계, EDRDG) | 원본은 X. 파생물 `data/dict/decomp-overrides.json`(커밋)에 숙어 id 103개 + 우리가 만든 표면형 문자열만. JmdictFurigana 텍스트 자체는 안 담김 | github.com/Doublevil/JmdictFurigana. 12-B 대조·12-C 거부 근거. 빌드타임 전용 |
| **Noto Sans JP (Bold)** | `data/raw/fonts/NotoSansJP-Bold.otf` (커밋 안 함) | notofonts/noto-cjk, Regular 와 같은 릴리스 (`Sans/SubsetOTF/JP/`) | **SIL OFL 1.1** (`data/raw/fonts/NotoSansJP-LICENSE.txt`) | **O** — `public/fonts/NotoSansJP-Bold-subset.woff2` 커밋 + Pages 배포 | OFL 은 서브셋·변형 허용. Noto 는 Reserved Font Name 을 안 걸어서 내부 이름 `NotoSansJP-Bold` 유지 무방. 배포본에 OFL 사본을 같이 두는 게 안전 (지금은 원본 라이선스 파일만 data/raw 에 있고 커밋 안 됨 — 후속) |
| **vite-plugin-pwa** | devDependency | `1.3.0` (`^1.0.3`) | **MIT** | 빌드 도구, 코드 미포함 | SW·manifest 생성 |
| **Workbox** | `vite-plugin-pwa` 경유 (`workbox-build 7.4.1`) | 7.4.1 | **MIT** (Google) | **O** — 생성된 `dist/sw.js`·`dist/workbox-*.js` 에 런타임 코드 포함, Pages 배포 (커밋 안 함) | |
| **intro_bg.png** | `public/intro_bg.png` (커밋함, 배경 제거본) | — | **미확인 — 사용자 제공** | 아직 코드 참조 없음. 추후 소개 화면에서 쓰면 배포됨 | 손그림/의뢰/AI 생성 여부와 라이선스는 사용자 확인 필요. 소개 화면에 넣기 전에 출처를 이 표에 채운다 |

**후속 (서비스화 시):**
- 배포본(`public/fonts/`)에 SIL OFL 1.1 전문 사본을 동봉. 지금은 `data/raw/fonts/NotoSansJP-LICENSE.txt`
  가 커밋 대상이 아니다
- CC BY-SA 승계 범위 문의(Phase 8)에 JmdictFurigana 도 포함 — 두 알고리즘 파생물의 SA 조항
- `intro_bg.png` 출처 확정

## 2026-09-07 — 한국어 뜻: stdict 대조 → 영어 gloss 번역 (12-D 방향 변경)

### 왜

사용자 지적 — "일한사전에 표제어가 한국어로 풀이돼 있으면 그걸 인용하면 되지, 굳이
국어사전과 비교해서 검수할 필요가 있나?" 전제가 어긋났다. **기계가 읽을 수 있고 라이선스가
열린 일한사전이 없어서** stdict(한국어 사전)를 프록시로 썼던 거다. JMdict 다국어판에
한국어 gloss 가 있지만 커버리지가 한 자릿수 %.

stdict 를 거친 대가 —
- **동형이의에서 틀린 뜻을 보여준다.** 大丈夫→"건장하고 씩씩한 사내"(실제 뜻은 "괜찮다"),
  経済→"세상을 다스리고 백성을 구제함", 架空→"공중에 가설함", 工夫→"학문을 배우고 익힘"(=한국어 공부)
- **백과사전식이라 카드에 안 맞는다.** 亜鉛 → 원자량·녹는점까지 100단어
- **일본고유(category 3) ~3,600건엔 koMeaning 이 아예 null**

→ **JMdict 영어 gloss 를 한국어로 옮겨 `koMeaning` 을 재생성한다.** 영어 gloss 는 *일본어
단어* 를 설명하므로 위 3개가 다 풀린다. 분류(category 1/2/3)는 여전히 stdict 대조가
답하는 질문이라 그 파이프라인(`match-korean`·category·classSource·Phase 3 수동 355건)은
그대로 두고, `koMeaning` 텍스트만 교체한다.

### 모델 선정 — 30개 라벨링 표본 비교 (내가 라벨링)

| 모델 | 깔끔 | 잔노이즈 | 고쳐야 | 건당 | 17k 환산 | 판정 |
|---|---|---|---|---|---|---|
| **gemma4:latest** | ~26 | ~3 | ~1 | 584ms | ~3h | **채택** |
| qwen3.5:latest | ~22 | ~4 | ~3 | 12,244ms | ~57h | 탈락 (느림) |
| qwen3:8b | ~15 | ~6 | ~9 | 446ms | ~2h | 탈락 (출력 붕괴 — цин크·서브스피시즈·"弁当; 랜치"·"캐스케ント 달") |

- gemma4:latest 실수는 잔노이즈 수준 — 工夫→"재주; 꾀; 아이디어"("아이디어" 외래어),
  汽車→"증기기관차; 기차". 放心→"멍함; 정신을 놓음; 안심"은 오히려 JMdict 양쪽 뜻을 다 잡음
- **Phase 3 에서 gemma4:26b 가 *분류* 로 출력 붕괴했던 것과 정반대** — 작은 gemma4:latest 는
  *번역* 이라는 더 단순한 태스크에선 절제돼서 핵심 뜻 한 줄을 잘 뽑는다. 태스크가 다르면 모델 순위도 다르다
- 프롬프트 — few-shot 5개(鉄道·大切·我慢·石鹸·二日, 표본과 안 겹침) + "핵심 뜻 하나,
  갈리면 최대 2~3개" + "물질명·전문용어·고유명사는 한국 표준 표기" + "영어 남기지 말 것"

### 파이프라인 (기존 4~8 뒤에 5.5~7.5 삽입)

1~4 (match:korean → draft → build:review-worklist → apply:korean-review) 그대로.
apply:korean-review 는 `koMeaning` 을 `korean-meaning.json`(있으면) → stdict → null 순으로 채운다.

- **5.5 `build:korean-meaning`** — gemma4 번역 → `korean-meaning.json`. `.korean-meaning-cache.json`
  로 재개(3h 실행 중 크래시 대비). 품질 플래그 부착
- **6.5 `build:korean-meaning-worklist` v2** — glossEn + llm_ko + stdict_def + Phase 3
  manual_verdict/manual_reason 를 한 줄에. 수동 검수분(355+200) 맨 앞, 플래그 우선.
  verdict `o`/`x`/`~`/**`s`**(stdict 정의 채택). 수동 분류 노동이 안 버려진다 (사용자 결정 A)
- **7.5 `apply:korean-meaning`** — verdict → korean-class.json 반영. `--validate` 층별 오류율

### 스키마 — `koMeaning` 에서 word/origin 뺀다

stdict 전용 필드(한국어 표제어·원어 한자)라 LLM 번역엔 의미 없다.
`{word, origin, definition, source, verified}` → `{definition, glossEn, source, verified}`.
`source` 를 `'stdict'` 리터럴에서 `'stdict'|'llm'|'manual'` 로 넓힌다 (CLAUDE.md 스키마 열거와 일치).
영향: `load.ts` 타입 · `MeaningCard.tsx`(`|| .word` 폴백 제거) · `build-runtime-dict.ts` · `apply-korean-review.ts`.

`korean-meaning.json`·`.korean-meaning-cache.json` 은 gitignore 유지 (재생성 가능,
`korean-llm-draft.tsv` 와 같은 취급). 배포 경로는 커밋된 `public/dict`.

## 2026-09-07 — 영어 gloss 번역 실행 결과

`build:korean-meaning` (gemma4:latest) 밴드 0~3 전량 17,217개, 약 110분 (건당 ~340→390ms,
Ollama 웜업 후 빨라짐). `.korean-meaning-cache.json` 50건마다 flush — 30/60/90% 지점 카톡 보고.

**품질 플래그 131개(0.76%)** — 예상보다 훨씬 깨끗하다.

| 플래그 | 수 | 성격 |
|---|---|---|
| kanji | 86 | 대개 良性 — 한글 뒤 괄호 한자 병기("영한(英和)", "관제(官製)"). Sino-Korean 학습엔 오히려 도움. 소수만 진짜 문제(河童 를 "河童"로 남김) |
| latin | 37 | **깨짐** — 희귀 한글 음절이 UTF-8 바이트 이스케이프로 샘("완벽히 `<0xEC><0x95><0x8E>`" = 앎). gemma4 OOV 토큰 처리 문제. 전부 플래그돼 워크리스트 T2 |
| many-senses 6 / kana 2 / long 1 / error 1(浮遊, JSON Unterminated) / untranslated 1 | | |

재적용 — `apply:korean-review --trust-llm` 는 `koMeaning` 을 `korean-meaning.json` 우선으로
바꿨다. base.json koMeaning **~13,448(stdict) → 16,955/16,959(llm)**. **category 3 일본고유
3,510개가 처음으로 뜻을 가진다** — 확장 모드가 이제 제대로 동작한다.

`verified` 는 아직 0. 사람이 워크리스트 verdict 를 채우고 `apply:korean-meaning` 을 돌려야
검수분만 true 가 된다. **T2(깨진 번역 125건)가 최우선** — `<0x..>` 패턴은 기계로도 찾을 수 있어
빠르다. `--validate` 로 tier별 "손댄 비율"(x+s / o+x+s) 을 뽑으면 층별 오류율이 나온다.

플래그 밖 표본은 카드용으로 적절 — 도안/스케치, 작은 칼/단도, 법치국, 부고, 적정량/적당한 양.

## 2026-09-07 — 별도 앱으로 분리 (스코프 결정)

사용자 결정 — "뿌리는 같지만 다른 내용으로 서비스를 분리한다." yomenai 는 음독 교정
(한자 표기 숙어, 읽기 축 = 음독)에만 집중하고, 아래는 각각 별도 앱으로 만든다.

- **앱 B — 오쿠리가나 학습** (동사·형용사 활용형: 認める·忙しい). 학습 축 = 훈독 선택 +
  오쿠리가나 경계. 12-D 대화에서 "확장 가능한가" 물었고, 가능하지만 (1) 앱 핵심(음독 교정)과
  다른 학습 문제고 (2) 한국 한자음 대응이 없어 KO_INTERFERENCE·음독 추적이 안 붙어 사실상
  뜻 중심 별도 트랙이 된다는 점 때문에 in-app 확장 대신 분리로 결정
- **앱 C — 의성어·의태어 + 형용사·부사** (가나 표기 어휘, 뜻·뉘앙스 축)

### 공유 인프라 vs 앱별 차이

| 공유 | 앱별로 다름 |
|---|---|
| 사전 파이프라인(JMdict/KANJIDIC 임포트·밴드 레벨링·LLM 뜻 번역) | 코퍼스 필터 (한자어 vs 활용형 vs 가나 어휘) |
| FSRS 학습 코어·이벤트 로그·재생 | 오답 분류 체계 (음독 6종 vs 오쿠리가나 경계 vs 의태어별) |
| 로컬 우선 PWA + Drive 동기화 + `/앱이름/` 스코프 SW | 핵심 학습 질문 |
| 진단 리포트 디자인 언어·CJK 폰트 처리 | |

### yomenai 에 남기는 것

`OKURIGANA` 오답 유형 enum 슬롯은 **그대로 둔다** — append-only 이벤트 로그라 제거가 더
churn 이고, 슬롯 유지 비용은 0. yomenai 안에서 분류기가 이걸 낼 일은 이제 거의 없다.
checklist "확장 후보" 항목(코퍼스 필터 완화 / 훈독 모드 분리 / 훈독 오답 버킷)은 앱 B 로 이관.

## 2026-09-08 — 1차 검수 반영 + Excel 인코딩 사고 복구

사용자가 `korean-meaning-worklist-flagged.tsv`(191행, 깨진 번역 + 분류 불일치)를 Excel 로
열어 전수 검수했다. `llm_ko` 칸을 직접 고치고 verdict 를 `o` 로 찍는 방식.

### 사고 — Excel 이 UTF-8 TSV 를 EUC-KR 로 저장

`file` 이 "ISO-8859" 로 잡고 UTF-8 로 읽으면 한글이 전부 모지바케. 처음엔 복구 불가로 봤으나
**`TextDecoder('euc-kr')` 로 디코드하면 U+FFFD 0개로 완전 복구.** Excel(한국어 Windows)의
"텍스트(탭으로 분리)" 저장이 CP949/EUC-KR ANSI 라서, UTF-8 로 착각해 읽었던 게 원인.
헤드워드의 비상용 한자 몇 개(`刻々` 등)만 CP949 에 없어 `?` 가 됐지만 `id`·`verdict`·`llm_ko`
는 무손실.

### 후속 조치

- `tools/lib/tsv.ts` — `readTsv`/`readTextAuto`: BOM 으로 UTF-8/UTF-16LE/BE 판별, BOM 없으면
  UTF-8 fatal 디코드 시도 후 실패 시 EUC-KR 폴백. `writeTsvBom`: 출력에 UTF-8 BOM(Excel 더블클릭 대비).
  `build-korean-meaning-worklist.ts`·`apply-korean-meaning.ts` 가 이걸 쓴다
- `apply-korean-meaning.ts` **인라인 수정 지원** — `fix` 칸이 비어도 `llm_ko` 가 현재
  `definition` 과 다르면(사람이 직접 고침) 그 텍스트를 채택(`source: manual`). 사용자 워크플로에 맞춤
- 기각 — 파일 다른 이름으로 만들기 / PDF 변환 / 수동 재작성 (복구돼서 불필요)

### 반영 결과

`apply:korean-meaning` — verdict `o` 191, **인라인 수정 151** (T1 46/60, T2 105/125).
T2(깨진 번역) 84%가 실제로 손봐야 했다 — flagged 집합이라 당연. `build:runtime-dict` →
`base.json` verified **0 → 187** (나머지 4는 밴드 4), `source: manual` 147.

## 2026-09-08 — 뜻 검수를 조금씩: 카테고리별 배치 + 확인용 HTML

표본(tier별 50, 300행) 검수 완료 후 나머지 대량분을 어떻게 나눌지 결정.
`apply:korean-meaning --validate` 층별 손댄 비율 — T1 12% / T2 0% / T3 7% / T4 13% /
T5·T6 20%. tier 4~6(~16,500건)은 전수 검수 대상이나 PLAN대로 보류하고, 대신
**세션마다 조금씩** 검수하는 도구를 얹었다.

### 자르는 축 — category (기각: tier, band)

- **category** — 배치당 검수 질문이 하나로 고정된다. category 3(일본고유)은 "이 한국어
  한 줄이 영어 gloss 를 맞게 옮겼나"만 묻는다. 한자음 대조도 동형동의/이의 판단도 없다.
  가장 단조롭고 빠르다
- **기각 tier** — 내부 우선순위 아티팩트라 관심사가 섞인다 (T3 = 수동검수 + 동형동의 +
  일본고유 한 덩어리, T5 ≈ category 3 이지만 정확히 같지 않음). 깨진 번역을 맨 앞으로
  빼는 이점은 `--batch` 가 슬라이스 안에서도 한다
- **기각 band (1차 축으로)** — 밴드만으로 자르면 한 배치에 세 질문이 섞인다. 대신 category
  배치를 밴드 0→3 순으로 정렬해 자주 나올 카드부터 검수되게 한다 (배치 내 2차 정렬)

### `build:korean-meaning-worklist --batch [--category=1|2|3] [--batch=N]`

- 아직 verdict 없는 다음 N건(기본 40)만 `korean-meaning-worklist-batch-NN.tsv` 로 쓴다
- 마지막 batch 파일에 미기입(`?`) 행이 남았으면 그 파일을 이어 채우고, 다 찼으면 다음 번호
- 모든 `korean-meaning-worklist*.tsv` 를 훑어 이미 실린 id 는 제외 (배치 간 중복 0 — 검증함)
- 배치 내 정렬 = 깨진 번역 먼저 → 밴드 0→3 → 표기
- 진행률 출력 — `일본고유 3643건 중 검수 127 · 남은 3516 · 이번 배치 40`
- `apply:korean-meaning` 은 `korean-meaning-worklist*.tsv` 를 이미 전부 병합하므로 batch
  파일이 쌓여도 자동 반영. batch tsv 는 `!data/dict/*-worklist*.tsv` 로 추적 대상(사람 판정)
- **`apply:korean-meaning --validate` 는 손댄 비율만 출력하고 write 안 함** (`process.exit(0)`).
  반영은 플래그 없이 `apply:korean-meaning` → `build:runtime-dict` 순. 한 번 헷갈렸던 지점
- 검수는 `npm run review -- batch-01` — 인자로 파일명(일부만도 됨) 주면 그 파일로 바로 연다.
  Excel 대신 이걸 쓰면 2026-09-08 EUC-KR 사고를 안 겪는다 (서버가 `tsv.ts` 로 직접 read/write)

### 확인용 HTML — `npm run view:jp-unique` → `data/dict/jp-unique.html`

검수 안 해도 일본고유 3,643건을 브라우저로 훑어볼 수 있게. `korean-class.json` +
`idioms.json` 조인(=`base.json` 재빌드 없이 최신 apply 반영). 밴드 필터·미검수만·텍스트
검색. 일본어 칸은 `lang="ja"` + JP 폰트 스택. `data/dict/*` 라 gitignore(재생성 가능).

---

## 2026-09-09 — 오답 상세: 닫기 바가 내용을 가리던 문제

사용자 보고 — 絶大 오답 상세에서 `닫기` 버튼이 둘째 한자(大) 칸 위에 떠 있고 그 아래로
내용이 잘려 보인다.

### 원인 — .card 3분할을 그대로 물려받았다

`.mistake-detail` 이 `.card`(head 고정 / body `flex:1; overflow-y:auto` / bottom 고정)를
상속받아, 좁은 화면(예: 585×520)에서 body 스크롤 창이 ~310px 밖에 안 됐다. 큰 헤드워드가
그 절반을 먹어 분해 목록 둘째 칸부터 잘렸다. 게다가 `.card-bottom` 이 **투명·경계선 없음**
이라 잘린 자리에 버튼만 떠 보여 "깨진" 인상을 줬다. 추가로 `.card.feedback.is-ng .card-head`
(음수 마진·朱 띠)가 오버레이 헤더로 새어 들어왔다(specificity 0,4,0 이 `.md-head` 를 이김).

### 고침 — 오버레이 전체를 한 덩어리로 스크롤 (PLAN §7 "셸 구간 느린 스크롤")

- `.mistake-detail` `padding:0`, `.md-body` `flex:0 0 auto; overflow:visible` — body 안
  스크롤 창을 없애고 오버레이(`overflow-y:auto`)가 통째로 스크롤한다
- `.md-head` / `.card-bottom` `position:sticky` + 불투명 `var(--bg)` + 경계선. 여백은
  각 구획이 직접(`.card` 기본 20px 대신) 가져 고정 바가 가장자리에 딱 붙는다 —
  안 그러면 sticky 바 밑으로 20px 만큼 내용이 비어져 나온다
- 헤더 누수는 `.card.feedback .mistake-detail .md-head`(0,4,0, 뒤 규칙)로 되받아 차단
- `.md-body` 아래 `padding-bottom:24px` — 최하단까지 스크롤해도 마지막 칸이 닫기 바에 안 가림

CSS 만 수정(`src/study/study.css`). 검증 — `npm test` 325 · `npm run e2e` 8스펙(카드 전환
p95 11.2ms) · tsc/oxlint/build 클린. 폰 뷰포트(390×740)는 스크롤 없이 다 들어오고, 짧은
데스크톱 창은 헤더·푸터 고정한 채 부드럽게 스크롤된다.

### 안 건드린 것

대조군(`contrastGroups`)에 현재 숙어가 예시로 다시 들어가는 건(絶大 "그대로" 줄에 絶大)
사용자가 이번 범위에서 뺐다. `.now` 표시와 별개로 목록 자기 포함은 남아 있다.

---

## 2026-09-09 — 오답 상세의 한국음(kr)이 옛 음·오염 음을 그대로 보여준다

사용자 지적 — 大 가 "대·다·태"로 뜨는데 모던 한국어는 "대"뿐. 다른 한자도 그런지 확인 요청.

### 원인 — KANJIDIC2 `korean_h` 를 무가공 통과

`import-kanjidic.ts` `byType('korean_h')` → `build-runtime-dict.ts` `kr: k.koreanH`.
KANJIDIC 의 `korean_h` 는 옥편식이라 옛 음(大 다·태), 코드포인트 통합 오염(医 = 화살통
'예' + 醫 '의' / 芸 = 향풀 '운' + 藝 '예'), 두음 원음(論 론, 竜 룡)을 다 담는다.
`kr[0]` 만 쓰는 것도 안전하지 않다 — 医→예, 金→김, 芸→운 이 먼저다.

### 규모 — 코퍼스(base) 한자 2,130자 중 kr 2개 이상이 263자

### 방식 — stdict 표제어로 교차검증 (사용자 결정: "stdict 먼저, 그 뒤 사람")

`tools/audit-korean-readings.ts` (`npm run audit:korean-readings`).
Phase 3 `.korean-cache.json`(stdict 응답 캐시)의 표제어를 **위치 정렬**해 근거를 모은다 —
`醫院`↔`의원` 이면 醫→의, 院→원. origin(정자)은 `loadVariantSets()` + 보충 표
(兌계열 説/税/脱/鋭, 内/呉 등 jis208 결손분)로 신자체에 되잇는다. 두음법칙(론→논/룡→용/
녀→여)은 `initialLawForms` 로 원음의 표기형까지 만들어 대조.

산출 `data/dict/korean-reading-audit.tsv` (gitignore) — verdict 열 초벌:

| verdict | 뜻 | 수 |
|---|---|---|
| `keep` | 모든 kr 이 stdict 근거 있음 (不 불·부, 車 차·거, 見 견·현…) | 58 |
| `trim:음·음` | 주음 15+ hits · 나머지 0 hits — 잘라낼 후보 (大 다·태, 医 예, 台 이…) | 122 |
| `?` | 주음 표본 부족(<15) — 코퍼스 갭일 수 있어 사람이 판단 | 83 |

**초벌이라 오탐 있다** — `茶 trim:차` 는 틀림(차 정상, 정렬이 다-어휘만 맞았을 뿐).
則(즉), 単(선우), 邪(막야)처럼 고전 음이 진짜인 것도 `trim` 에 섞인다.

### 방향 수정 (사용자 지적) — "삭제"가 아니라 "옛 음 접기"

사용자 — "현재 사용하지 않는다고 그 음이 아니라고 할 근거는 없다." 옳다.
stdict 교차검증이 재는 건 "현대 어휘에 쓰이는가"지 "맞는 음인가"가 아니다.
KANJIDIC `korean_h` 는 한국 자전 기반이라 大 다·태도 정식 음이다.

→ `trim(삭제)` 프레이밍 폐기. 감사 결과는 **now(지금 쓰는 음) / old(옛·드문 음)** 로
가르는 순위 신호로만 쓴다. 화면은 now 를 크게, old 는 "옛 음 다·태" 로 흐리게/접어서.
정보 손실 0. (사용자 결정: "주 음 + 나머지 접기")

`audit-korean-readings.ts` 재작성 — verdict 열 → now/old 두 열.
now = kanjidic 음 중 stdict 근거 있음(자전 순서) + 교정음(자전에 없는데 5+ hits, 斉→제).
old = 근거 0. now 가 비면 전부 now(판단 보류). 205자에 old 후보 발생.
`korean-reading-review.tsv` = 그 205자만.

### 배선 완료 (2026-09-09, 커밋 `bb62da0`) — override 파일만 생기면 먹힌다

1. `tools/apply-korean-readings.ts` (`npm run apply:korean-readings`) — 검수한
   `korean-reading-review.tsv`(now/old) → `data/dict/korean-reading-overrides.json`.
   `old` 비면 override 안 만듦(passthrough). koreanH 원본 안 건드림. 삭제·중복·1글자
   아님 경고
2. `build-runtime-dict.ts` — `korean-reading-overrides.json` 있으면 `kr: now, krOld: old`,
   없으면 `kr: koreanH, krOld: []`. `public/dict/kanji.json` 에 `krOld` 필드 추가
   (재생성 산출물 → 스키마 불변 대상 아님). 적용 수를 콘솔에 찍는다
3. `src/dict/load.ts` `KanjiInfo.krOld`, `src/study/mistakeDetail.ts` `BreakdownPart.krOld`
   (`breakdown` 이 `k?.krOld ?? []`), `MistakeDetail.tsx` → `한국음 {kr} · 옛 음 {krOld}`
   ( `krOld` 있을 때만, `.md-kr-old` = `--text-faint` )
4. `mistakeDetail.test.ts` — 픽스처/단언에 `krOld` 반영 (識 을 `kr:['식'],krOld:['지']` 로)

**검증** — override 파일 없이 `build:runtime-dict` 재실행 → 전 한자 `kr` 불변, `krOld:[]`,
그 외 0 변화. 임시 override(圧→압/엽 등)로 e2e — 오답 상세에 "한국음 압 · 옛 음 엽"
흐리게 렌더 확인 후 임시본 제거. `npm test` 325 · tsc(app+tools)·oxlint·build·e2e(detail·
full-flow) 클린.

### 미완 — 205자 검수

`data/dict/korean-reading-review.tsv` now/old 열. 감사 초벌은 오탐 있음 — `亀`(균=龜裂 균열,
now 로 올려야), `茶`(차 정상), `斉`(now 제 맞음, old 자·체·채). 검수 후
`apply:korean-readings` → `build:runtime-dict` 하면 "옛 음" 접힘이 화면에 나타난다.

---

## Drive 동기화 파일 정리 (2026-09-11, 사용자 관찰)

**관찰** — `YomenaiSync` 에 `reviews-<uuid>.json` 이 11개. "동기화하면 계속 쌓이는 것 같다".

**진단** — 매 동기화마다 쌓이는 게 아니었다. Drive 수정 시각을 보니 오늘 갱신된 건
`b1dc6cae`(249KB) 하나뿐이고 나머지 9개는 9/6~9/10 에 멈춰 있다. `uploadOrReplace` 의
덮어쓰기는 정상. 9개는 개발 기간에 브라우저 프로파일·시크릿 창·사이트 데이터 삭제·PWA
재설치로 `localStorage` 의 `yomenai:deviceId` 가 날아갈 때마다 새로 생긴 잔해다.
2바이트 파일 3개는 새 프로파일에서 학습 전에 동기화만 눌러 빈 배열이 올라간 흔적.

문제는 파일이 **늘어나는** 게 아니라 **줄지 않는** 것. `syncNow` 는 목록의 모든 파일을
매번 내려받으므로(전량 합집합 병합 구조) 죽은 파일 ~193KB 를 동기화마다 다시 받는다.

**기각한 대안**

- *그냥 Drive 에서 수동 삭제* — 죽은 기기 이벤트의 Drive 사본이 그 파일들뿐이다. 지우면
  지금 기기의 IndexedDB 하나에만 남아 백업이 깨진다
- *각 기기가 로컬 DB 전량을 자기 파일에 올리게 변경* — 어느 파일을 지워도 안전해지지만
  기기 수만큼 내용이 중복돼 파일 크기와 동기화 비용이 배로 는다. 원래 "자기 이벤트만
  쓴다" 설계를 뒤집을 만한 이득이 아니다
- *`device.ts` 수정* — 원인이 localStorage 접근 실패가 아니라 데이터 삭제였으므로 고칠
  게 없다. 모듈 캐시를 넣어도 이 증상은 안 바뀐다

**채택 — 보관 파일로 합치기** (`consolidateSyncFiles`, `src/sync/sync.ts`)

내 파일을 뺀 나머지 `reviews-*.json` 을 전부 읽어 로컬에 흡수하고, 합집합을
`reviews-archive.json` 하나에 쓴 뒤 원본을 지운다. **업로드가 끝난 뒤에 삭제** — 순서를
뒤집으면 업로드 실패 시 기록이 Drive 에서 사라진다.

기기별 파일 분리 원칙(PLAN §5 원칙 3)은 유지된다. 보관 파일에 쓰는 주체는 정리 동작
하나뿐이고 살아 있는 기기는 여전히 자기 파일에만 쓴다. 쓰기 충돌이 생길 자리가 없다.
다른 기기가 정리 뒤에 동기화하면 자기 파일을 다시 만들어 전량을 올린다 — 보관본과
중복되지만 id 가 같으면 내용이 같다는 전제 위의 `bulkPut` 이라 무해하다.
기존 보관 파일도 같이 읽어 합치므로 두 번 정리해도 결과가 같다.

`listSyncFiles` 의 `name contains 'reviews-'` 에 보관 파일이 자연히 걸려서 `syncNow` 는
고칠 게 없다. 정리 뒤 새로 합류한 기기도 보관 파일 하나로 과거 기록을 전부 받는다.

`DriveClient` 에 `deleteFile(fileId)` 추가(`deleteSyncFiles` 가 이걸 재사용).
UI 는 설정 > 백업의 "옛 기기 파일 정리" — Drive 파일을 지우므로 `ResetSetting` 과 같은
2단 확인을 붙였다.

**검증** — `npm test` 35파일 329테스트 통과(`sync.test.ts` +4), `tsc -b`/`oxlint` 클린.

### 동기화 진행 막대 (2026-09-11)

"동기화 중…" 글자만으로는 멈춘 건지 알 수 없다는 요청. `syncNow` 에 `onProgress` 콜백을
4번째 인자로 붙였다(3번째는 기존 `drive` — 테스트 호출부를 안 건드리려고 뒤에 뒀다).

**기각** — 바이트 단위 진행률. `fetch` 는 업로드 진행을 못 알려줘서 `XMLHttpRequest` 로
바꾸고 다운로드는 `ReadableStream` 으로 읽어야 한다. 막대가 매끄러워지는 값보다
`googleDrive.ts` 의 fetch 경로를 갈아엎는 비용이 크다. 무한 슬라이드 막대도 기각 —
정보량이 0 이라 "멈췄나" 질문에 답을 못 한다.

단계는 업로드 1 + 목록 조회 1 + 파일 n. 파일 수는 `listSyncFiles` 뒤에야 알 수 있어
그전까지 `total` 은 잠정 2 로 보고하고, 목록을 받은 뒤 확정값으로 바꾼다. 막대가 한 번
뒤로 당겨지는 것처럼 보이지만(2/2 → 2/4) 글로 "백업 내려받는 중 1/2" 를 같이 보여주므로
무슨 일인지 읽힌다. 정리 후 파일이 2개뿐이라 단계도 3~4개다.

### 재대결이 매번 같은 문제를 냈다 (2026-09-11, 사용자 지적)

`buildRematch` 가 오답 수 내림차순 → `lastAt` 내림차순으로 세우고 앞에서 `limit` 개를
잘랐다. 결정적 정렬이라 후보가 많아도 상위 N 만 계속 나온다. 게다가 `CardState.wrong` 은
누적이라 줄지 않고, 재대결에서 답하면 `lastAt` 이 갱신돼 2차 정렬에서도 앞자리를 지켰다.
상위권이 고착되는 구조였다. (2차 정렬 변수 이름이 `lastWrongAt` 이었는데 `lastAt` 은
마지막 **응답** 시각이라 이름부터 틀렸다 — 지금은 그 정렬이 없어졌다.)

**채택 — 오답 수 가중 무작위 추출** (`pickWeighted`). 자주 틀린 숙어가 더 자주 나오되
매번 조합이 달라진다. 뽑기 전에 후보를 id 로 세워 Map 순회 순서(= 기기 간 이벤트 병합
순서)에 결과가 안 휘둘리게 했다. 난수는 `options.rand` 로 주입 가능 —
`newEventId(at, rand)` 와 같은 관례이고, 테스트는 mulberry32 시드로 300회 뽑아
3:2:1 가중치가 지켜지는지 본다.

**기각한 대안**

- *오래 안 본 날 먼저 (결정적 순환)* — 커버리지는 보장되지만 상습적으로 틀리는 숙어가
  한 바퀴에 한 번씩만 나온다. 가중치가 죽는다
- *오래 안 본 순서만* — 위와 같은 이유에 더해 오답 수를 아예 안 본다

**함께 채택 — 극복한 카드는 후보에서 뺀다.** `CardState.streak`(마지막 오답 이후 연속
정답 수)을 `replay` 가 세고, `REMATCH_CLEARED_STREAK`(2) 이상이면 후보에서 제외한다.
`wrong` 이 안 줄어드는 탓에 이미 극복한 숙어가 영원히 남던 문제를 푼다. 다시 틀리면
연속이 끊겨 후보로 돌아온다. `rematchCount` 도 같은 술어를 써서 홈 버튼 숫자와 세션
내용이 어긋나지 않는다. `streak` 은 이벤트에서 재생되는 파생 상태라 IndexedDB 스키마
불변 조건과 무관하다.

## 지연 검수 응답을 사전으로 되돌리는 도구 (2026-09-12)

Phase 3 이 미뤄둔 마지막 항목. 앱이 "이 숙어, 뜻은 알고 있었어요?" 로 받은 `meaningKnown`
이벤트를 빌드타임 분류(`korean-class.json`)로 되돌리는 쪽이다. 런타임은 이미 이 값을
모드 배정 2단계에서 쓰고 있어서, 여기서 얻는 건 **2 대 3 구분과 영속성**뿐이다.

### 기각 — 응답을 `korean-class.json` 에 바로 반영

「알았다」를 곧 category 1(동형동의)로 굳히는 안. 한 번 굳으면 `classSource: 'manual'` 이
되어 사람 검수와 구분이 안 간다. 그런데 일본어를 따로 공부해서 뜻을 아는 일본고유어도
「알았다」가 나온다 — 응답은 한국어 동형동의의 *신호*지 사전 판정이 아니다. Phase 3 이
"사람 verdict > 초벌 > 잠정값" 으로 신뢰도 층을 나눠둔 걸 무너뜨린다.

**채택 — 이벤트 로그 → 검수 TSV 한 방향만.** 사람이 verdict 칸을 채워야 반영된다.
기존 `apply:korean-review` 가 `korean-worklist*.tsv` 를 전부 읽으므로 도구를 안 건드렸다.

### 담는 행 — 현재 분류와 어긋난 응답만

`알았다` + 분류 2·3, `몰랐다` + 분류 1. 일치하는 응답은 새 정보가 없어 집계로만 보고한다.
사람이 볼 목록을 짧게 유지하는 게 검수를 실제로 굴리는 조건이다 (12-D 배치 모드와 같은 이유).

`몰랐다` 는 2 와 3 을 못 가른다 — 그래서 `proposed` 열은 잠정 분류를 그대로 물려주고
사람이 최종 판단한다. 런타임에 필요한 건 1 대 2·3 갈림뿐이라 (2026-09-04 절) 여기서
급할 게 없다.

### 이미 사람 verdict 가 있는 숙어는 파일에서 뺀다

`apply:korean-review` 는 `readdirSync` 순서로 worklist 를 읽어 **뒤 파일이 이긴다.**
`korean-worklist-events.tsv` 는 이름 순으로 `korean-worklist-t1.tsv`·`korean-worklist.tsv`
보다 앞이라, 같은 id 를 양쪽에 두면 새 판정이 옛 판정에 조용히 덮인다. 그래서 겹치는
숙어는 TSV 에서 빼고 콘솔로만 보고한다 — 고칠 거면 원래 파일에서 고치라는 뜻이다.
(대안 — apply 쪽에 우선순위 규칙을 넣는 것. 기각: Phase 3 도구를 건드리게 되고,
충돌은 드물어 사람이 보는 편이 낫다.)

### 입력은 `data/events/` 의 Drive 백업 파일

앱에 로컬 내보내기가 없어서 Drive 의 `reviews-*.json` 을 직접 내려받아 넣는다.
기기별 파일은 합집합이라 같은 이벤트 id 가 겹쳐도 한 번만 센다. 개인 학습 기록이므로
`data/events/` 는 gitignore, 산출 TSV 는 사람 판정이라 추적한다
(`!data/dict/*-worklist*.tsv` 예외에 자동으로 걸린다).

- 기각 — **설정에 이벤트 내보내기 버튼 추가.** 쓸 일이 드문 빌드타임 작업에 앱 UI 를
  늘릴 이유가 없다. Drive 웹에서 받는 걸로 충분하다

### 검증

`tools/build-event-worklist.test.ts` 9 테스트. 기기 2개 픽스처(중복 이벤트·뒤집힌 응답·
이미 verdict 가 있는 架空 포함)로 실행해 일치 2 / 어긋남 1 / 보류 1 집계 확인.
`npm test` 342 (333 → 342) · `tsc -b` / tools tsc / `oxlint` 클린.

## 검수 목록이 배포 안 되는 숙어를 담고 있었다 (2026-09-12, 사용자 질문에서 나옴)

"apply:korean-meaning 을 빼면 verified 가 날아간다"는 함정이 실제로 문 적이 있나 확인하다
발견했다. `public/dict/base.json` 이력의 verified 는 0 → 187 → 416 → 846 → 966 → 1083 → 1123
으로 **한 번도 안 떨어졌다.** 그 함정에 빠진 적은 없다 (커밋 전에 복구한 경우는 git 에 안 남지만).

대신 다른 게 나왔다. `korean-class.json` 은 verified 1,148 인데 배포되는 `base.json` 은 1,123 이다.

### 원인 — 검수 모수와 배포 코퍼스가 다르다

`build-runtime-dict` 는 `onyomi-map.json` 에 없는 숙어를 안 싣는다. 음독 분해가 안 되면
읽기 카드를 못 만들기 때문이다. `korean-class.json` 17,213건 중 258건이 여기 해당한다
(JUKUJIKUN 103 · KATAKANA_READING · NO_PARSE). 그런데 검수 워크리스트는 `korean-class.json`
전체를 모수로 잡았다. 12-C(2026-09-07)가 熟字訓 103건을 거부한 뒤 09-08 부터 돈 배치에
그것들이 그대로 섞여 들어갔다 — 河童·部屋·二人·風邪·弥生·一昨年 같은 것들이다.

25건은 이미 검수를 마쳤고, 233건이 앞으로의 배치에 나올 예정이었다 (category 3 만 112건).
일본고유 검수가 3,643 중 847 진행된 시점이라 무시할 비율이 아니었다.

### 고침 — 목록 생성에서 거른다

`build-korean-meaning-worklist` 가 `onyomi-map.json` 의 `byIdiom` 키 집합으로 거른다.
배치 진행률의 모수에도 같은 필터를 걸었다 — 모수만 안 고치면 "남은 건수"에 영원히 안 줄어드는
꼬리가 남는다. 정정된 수치는 일본고유 826/3,510.

- **이미 verdict 를 채운 행은 코퍼스 밖이어도 남긴다.** 파일을 다시 쓸 때(`--sample`, 배치
  이어쓰기) 사람 판정이 조용히 사라지는 게 더 나쁘다. `--sample=50` 재생성으로 확인 —
  249건이 빠졌는데도 기존 300행 파일은 바이트 동일이다
- `onyomi-map.json` 이 없으면 안 거른다. build:onyomi 전에 도는 경우가 있고, 없는 채로
  도는 쪽이 덜 놀랍다 (12-C 의 JmdictFurigana 미다운로드 처리와 같은 관례)

### 이미 새어 나간 25건은 그대로 둔다

지운다고 얻는 게 없다. 12-C 의 熟字訓 거부는 `decomp-overrides` 로 뒤집을 수 있는 판정이고,
그때 이 숙어들이 코퍼스로 돌아오면 검수 결과가 그대로 살아난다. `koMeaning.verified` 는
`korean-class.json` 에 남아 있고 런타임에만 안 나갈 뿐이다.

- 기각 — **`build-runtime-dict` 가 이 25건을 실어 주게 한다.** 음독 분해가 없으면 읽기 카드를
  못 만든다. 뜻 카드만 있는 숙어를 코퍼스에 넣는 건 12-C 의 결정을 우회하는 것이고,
  熟字訓 학습은 스코프 밖이다 (2026-09-07 「별도 앱으로 분리」 절)

## 훑어보기 — 채점 없는 화면을 리포트 안에 뒀다 (2026-09-12, 사용자 요청)

"계속 퀴즈처럼 문제만 풀다보니 피로하다"는 지적. 확인해 보니 화면이 전부 출제·채점
루프였다 — 진단·세션·3장만·재대결·집중. 음독 맵과 리포트는 현황 조회지 학습 화면이 아니다.

뜻 카드가 그나마 뜻·읽기·소리를 같이 주지만 (1) 자기 채점이 붙고 (2) `activeCardTypes` 가
교정 모드엔 읽기만 줘서 동형동의로 분류되면 아예 안 나온다. 읽기 카드의 TTS 도 오답
피드백 뒤에만 열린다 — 맞히면 한 번도 못 듣는다.

### 자리는 진단 리포트 안 (사용자 결정)

별도 메뉴를 새로 파지 않았다. 리포트는 이미 "무엇을 틀렸나"를 모아 놓은 화면이라
"그걸 다시 본다"가 같은 자리에서 이어진다. 처방(다음에 볼 것) 바로 뒤에 뒀다 —
할 일 다음에 부담 없는 선택지가 오는 순서다.

### 재대결과 기준이 다르다 — 극복한 카드를 안 뺀다

`buildRematch` 는 `REMATCH_CLEARED_STREAK`(2) 이상이면 후보에서 뺀다. 출제 목록이라
이미 극복한 걸 또 물으면 시간 낭비이기 때문이다. 훑어보기는 **출제가 아니라 노출**이라
그 예외를 안 쓴다. 최근에 맞혔더라도 다시 읽고 듣는 건 그 자체로 복습이다.

- 기각 — *재대결 후보를 그대로 쓴다.* 코드는 줄지만 "재대결할 게 없으면 훑어볼 것도 없다"가
  된다. 피로해서 쉬어 가려는 사람에게 빈 화면을 주는 셈이다
- 동점(오답 1회끼리)은 id 순이다. 기기 간 이벤트 병합 순서에 목록이 안 흔들려야 해서
  `lastAt` 을 안 썼다 — 그 값은 마지막 *응답* 시각이라 "최근에 틀린 순"이 아니다
  (2026-09-11 재대결 절에서 같은 이유로 걷어낸 정렬이다)

### 이벤트를 안 쓴다

펼쳐 보는 행동은 기록하지 않는다. FSRS 도 안 건드린다. append-only 로그에 "봤다" 를 넣으면
재생 결과가 달라지고, 노출과 채점의 경계가 흐려진다. 필요해지면 그때 이벤트 타입을 늘린다
(스키마 불변 조건이라 가볍지 않다 — 2026-09-04 「지연 검수」 절과 같은 판단).

### 예문은 처음 펼칠 때 부른다

`loadExamples` 는 11,217 숙어짜리 번들이다. 리포트 진입에서 받으면 첫 그림이 늦는다.
`SessionSummary` 가 쓰는 캐시된 Promise 라 두 번째부터는 즉시 온다.
`data/dict/examples.json` 을 안 만든 환경에서는 빈 맵이 와서 예문 줄만 빠진다.

### 검증

`report.test.ts` 10 → 16. e2e `report-browse.spec.ts` 신규 — 세션을 전부 틀리며 완주한 뒤
리포트에서 펼침/접힘, `aria-expanded`, 예문 지연 로드, 출제 요소(`.kana-input`·채점 버튼)
부재를 본다. `npm test` 348 · `npm run e2e` · `tsc -b`/`oxlint` 클린. 캡처로 레이아웃 대조
(`.rows > li` 가 baseline flex 라 `.browse-row` 에서 `display: block` 으로 덮는다).

### 목록이 아니라 카드로 (2026-09-12, 사용자 결정)

처음엔 리포트 안 아코디언 목록으로 냈다. 사용자가 "카드처럼 노출되는 거지?" 로 되물어
**카드 한 장씩 넘기는 화면**으로 바꿨다. 목록 섹션은 걷어내고 리포트엔 진입 버튼만 남겼다.

세션과 같은 `.card` 3분할 셸을 쓴다 — 새 레이아웃을 만들 이유가 없고, 같은 모양이라
"이건 카드인데 안 물어보네" 가 바로 읽힌다. `useViewportLock` 도 같이 쓴다.

- `frequentIdioms` 를 `report.ts` 에서 독립 함수로 뺐다. 리포트와 카드 화면이 각자
  목록을 짓는데 기준이 갈리면 "12장" 이라 해 놓고 다른 걸 보여주게 된다
- 카드 화면은 `buildReport` 를 안 부른다 — `pairs.json` 까지 받을 이유가 없다.
  `replay` + `frequentIdioms` 면 충분하다
- **예문을 목록과 같이 받는다.** 아코디언일 땐 펼칠 때 받았는데(첫 그림 우선), 넘기는
  화면에서 매 장 기다리면 넘기는 맛이 죽는다. `loadExamples` 는 캐시된 Promise 라
  리포트에서 이미 받았으면 즉시 온다
- 나가면 홈이 아니라 리포트로 돌아간다. 리포트에서만 들어오는 화면이라 그게 맞다
- 뜻은 뜻 카드의 `.meaning` 을 그대로 쓴다. 처음엔 `.browse-meaning` 을 새로 만들었는데
  `.card-body` 가 가운데 정렬이라 왼쪽으로 붙어 나왔다 — 캡처에서 잡혔다

**리포트 모션의 stagger 를 같이 고쳤다.** `.report > .screen-body > section:nth-of-type(n)`
으로 2~5번에 지연을 주는데, 섹션이 6개가 되면서 마지막이 지연 0 으로 4·5번보다 먼저 떴다.
`nth-of-type(6)` 에 450ms 를 보탰다. 내 변경이 만든 어긋남이라 같이 정리한 것이다.

### 카드를 늘리되 기준은 안 흐린다 (2026-09-12, 사용자 요청 → 정정)

"오답수를 좀 더 완화해서 카드를 좀 늘려줘." 필터가 이미 `wrong > 0` 이라 완화할 여지는
틀린 적 없는 카드를 넣는 것뿐이라 보고 `reps > 0` 으로 넓혔는데, **사용자가 곧바로 기각했다**
— "틀린 적 없는 단어 말고 틀린 단어만 포함해야 해."

맞는 지적이다. "틀린 것을 다시 본다" 가 이 화면의 정체고, 맞힌 것까지 섞으면 훑어보기가
그냥 학습 이력 열람이 된다. 넘길 카드는 **상한(`BROWSE_N`) 으로 늘린다** — 12 → 30.

- 기준을 넓히는 것과 상한을 올리는 것은 효과가 비슷해 보여도 성격이 다르다. 상한은
  "몇 장 보여줄까", 기준은 "무엇을 보여줄까" 다. 늘려 달라는 요청에 기준을 건드린 게 잘못
- `wrong > 0` 유지. 화면의 "N회 틀림" 뱃지도 늘 붙는다 (0 일 수가 없다)
- **2차 정렬 `lastAt` 은 남겼다.** 1회씩 틀린 카드가 대부분이라 id 순으로만 세우면
  사전순 나열처럼 읽힌다. 2026-09-11 재대결 절에서 `lastAt` 정렬을 걷어낸 건 *출제 순서*
  에서 상위권이 고착됐기 때문이고, 여기선 뽑기가 아니라 읽는 순서라 같은 문제가 안 생긴다

**e2e 는 상한을 직접 덮지 못한다.** 스펙의 세션 하나로는 30장이 안 차서 잘리는 경로가
안 돈다. 단위 테스트(`report.test.ts` 18)가 `BROWSE_N` 절단과 정렬을 덮으므로 거기 맡겼다.

### 훑어보기도 섞는다 (2026-09-12, 사용자 요청)

"오답 카드도 섞여서 노출되도록 해줘." 오답 수로 결정적으로 정렬해 앞에서 30장을 자르면
들어갈 때마다 같은 카드가 같은 순서로 나온다. **9/11 재대결에서 고친 것과 같은 결함**이다
(커밋 `cbe933e`).

같은 처방을 쓴다 — 오답 수를 가중치로 둔 무작위 추출. `pickWeighted` 가 `session.ts` 안
비공개 함수였는데 `src/core/pick.ts` 로 빼서 재대결과 훑어보기가 같이 쓴다. 복사하면
한쪽만 고치는 날이 온다.

- `frequentIdioms` 는 이제 **후보 전량**을 낸다. 상한은 `pickBrowse` 가 건다.
  뽑기 전 입력을 id 순으로 세우는 것도 `buildRematch` 와 같은 관례다 — Map 순회 순서
  (= 기기 간 이벤트 병합 순서)에 뽑기 결과가 휘둘리면 안 된다
- **2차 정렬 `lastAt` 은 없앴다.** 무작위로 뽑으니 결정적 2차 기준이 의미가 없어졌다.
  바로 앞 커밋에서 넣은 건데 한 번도 배포되기 전에 걷혔다
- 리포트 버튼은 `Math.min(후보 수, BROWSE_N)` 장으로 표시한다. 후보가 상한을 넘어도
  화면에는 30장만 나오므로 숫자가 어긋나면 안 된다
- 뽑기는 화면에 들어올 때 한 번만. 넘기는 도중에 목록이 바뀌면 안 된다

**기각한 대안 — 매번 다른 30장을 보여주려고 넘길 때마다 다시 뽑기.** 앞으로 갔다 뒤로
오면 다른 카드가 나온다. ‹이전› 이 이전이 아니게 된다.

## 한국어 뜻 구분자를 쉼표로 (2026-09-12, 사용자 요청)

번역 프롬프트가 "뜻이 갈리면 `;` 로 2~3개" 를 지시해서 17,213건 중 9,208건이
`충실함; 성실함` 꼴이었다. 사용자가 쉼표로 바꿔 달라고 했다.

### 고치는 자리는 "반영 시점"

원본 `korean-meaning.json`(gemma4 출력)은 안 건드린다. 재생성 가능한 중간 산출물이고,
17,000건을 다시 번역하면 ~110분이다. `apply-korean-review`·`apply-korean-meaning` 이
`korean-class.json` 에 쓸 때 `normalizeDefinition` 을 통과시킨다.

프롬프트도 쉼표로 바꿨다 — 앞으로 번역하는 것부터는 정규화가 할 일이 없다.

- 기각 — *`build-runtime-dict` 에서만 고치기.* 배포물은 쉼표인데 `korean-class.json` 과
  검수 화면은 `;` 로 남는다. 사람이 보는 값과 앱에 뜨는 값이 갈리면 검수가 어긋난다
- 기각 — *일회성 스크립트로 `korean-class.json` 만 치환.* 다음 번역 배치에서 되살아난다

### `sameText` — 안 넣었으면 검수 1,148건이 뒤집혔다

`apply-korean-meaning` 은 워크리스트의 `llm_ko` 칸이 현재 정의와 다르면 "사람이 인라인으로
고친 것" 으로 보고 `source: manual` 로 바꾼다. 그런데 이미 만들어 둔 워크리스트 20개의
`llm_ko` 는 옛 `;` 표기다. 단순 문자열 비교였으면 정규화 직후 **검수한 1,148건 전부가
가짜 인라인 수정으로 잡혀** manual 로 뒤집히고 `;` 가 되살아났을 것이다.
비교 양쪽에 `normalizeDefinition` 을 걸어 막았다.

부수 효과로 `source: manual` 이 304 → 302 가 됐다. 구분자만 달랐던 2건이 이제 "사람이
고친 것" 이 아니게 됐다는 뜻이다. 뜻 텍스트는 안 바뀌었다.

### 검증 — 전량 대조

파이프라인을 다시 돌린 뒤 이전 `korean-class.json` 과 17,213건을 맞대고
`normalizeDefinition(이전) === 이후` 를 확인했다. **어긋난 뜻 0건.** verified 1,148,
분류 분포(6219/7351/3643) 그대로. `band4`·`kanji`·`pairs`·`examples` 는 `_meta` 의
생성 시각 외에 동일하다 — 재생성이 다른 걸 건드리지 않았다는 뜻이다.

`base.json` 에 남은 `;` 0건, 쉼표 구분 9,110건. 앱 캡처로 "저렴함, 값싼" 렌더 확인.

## 동기화 파일 자동 정리 (2026-09-12, 사용자 요청)

"새 브라우저에서 동기화하면 다른 아이디로 파일이 생기나?" → 그렇다. "어쩔 수 없나?" →
**새 id 가 생기는 건 어쩔 수 없다.** 브라우저는 다른 브라우저의 `localStorage` 를 못 읽으니
새 브라우저가 자신을 "예전의 그 기기" 라고 알아볼 방법이 원리적으로 없다.

문제는 9/11 에 진단한 것과 같다 — 파일이 **생기는** 게 아니라 **줄지 않는** 것.
그때 만든 `consolidateSyncFiles` 를 수동 버튼에서 동기화 흐름으로 옮긴다.

### 기준은 개수가 아니라 시각

"파일이 N개 넘으면 정리" 로 하면 기기를 셋 쓰는 사람이 매 동기화마다 남의 파일을 접고,
그 기기들은 다음 동기화에서 자기 파일을 다시 만든다. 지웠다 올렸다가 끝없이 반복된다.

그래서 **30일 넘게 안 갱신된 파일만** 접는다. 살아 있는 기기는 그보다 자주 동기화하고,
설령 오래 쉬었다 돌아와도 자기 파일을 다시 만들 뿐 기록은 보관본에 남아 있다.
판정 신호로 Drive 목록 쿼리에 `modifiedTime` 을 추가했다 — 기존엔 `id`, `name` 만 받았다.

- `modifiedTime` 을 못 읽으면 **죽지 않은 것으로 본다.** 판단이 안 서면 지우지 않는 쪽이
  안전하다. 파일이 하나 더 남는 건 되돌릴 수 있고, 지운 건 되돌릴 수 없다
- 보관 파일(`reviews-archive.json`)은 아무도 갱신하지 않으니 시각만 보면 늘 죽어 있다.
  이름으로 먼저 걸러 낸다
- 수동 버튼은 `olderThan` 없이 부른다 — "지금 싹 정리" 라는 의도가 분명한 동작이라
  시각을 안 본다. 같은 함수가 두 정책을 태우는 자리는 여기 하나뿐이다

### 자동이라도 조용히 넘기지 않는다

정리가 돌면 설정 화면에 "30일 넘게 안 쓴 기기 파일 N개를 보관 파일로 합쳤어요" 를 띄운다.
Drive 를 직접 열어 보는 사용자가 파일이 줄어든 이유를 모르면 그게 더 불안하다.
진행 막대에도 `consolidate` 단계를 넣어 그 시간 동안 뭘 하는지 보이게 했다.

### 기각한 대안 — 기기 이어받기

새 브라우저가 첫 동기화에서 기존 파일 목록을 보여주고 "이 기기를 이어서 쓸까요?" 를 물어
`deviceId` 를 물려받는 안. 파일이 아예 안 늘어나지만, 두 기기가 같은 id 를 쓰면 서로
덮어쓴다. **쓰기 충돌이 구조적으로 없다** 는 PLAN §5 원칙 3 의 전제를 사용자 판단에
맡기는 셈이라 안 한다. 사용자도 자동 정리를 골랐다.

### 훑어보기 — 손수 짠 스와이프에서 캐러셀로 (2026-09-12, 사용자 요청)

처음엔 `touchend` 에서 이동 거리를 재 한 장씩 갈아끼웠다. 사용자가 "캐러셀을 기대했는데
아무래도 익숙한 UI이고 그게 직관적"이라고 해서 **CSS scroll-snap 캐러셀**로 바꿨다.

**그때 캐러셀을 안 한 근거가 틀렸다.** "끌기 애니메이션은 매 프레임 레이아웃을 건드린다"
고 적었는데, 스크롤과 `transform` 은 합성 단계라 레이아웃을 안 건드린다. 150ms 규칙
(CLAUDE.md 성능 제약)을 지킬 수 없다는 판단 자체가 근거 없는 것이었다.

모든 장을 한 트랙에 깔고 `scroll-snap-type: x mandatory` + `scroll-snap-align: center`.
손가락을 따라 오는 움직임·관성·스냅·축 가르기를 전부 브라우저가 한다.

- **`src/study/swipe.ts` 와 그 테스트를 지웠다.** 브라우저가 이미 하는 일을 손으로 다시
  짜고 있었다. "세로가 가로보다 크면 안 넘긴다" 는 규칙도 직접 쓸 필요가 없어졌다 —
  `.card-body` 의 세로 스크롤과 트랙의 가로 스크롤을 브라우저가 알아서 가른다
- `Browse.tsx` 에 남은 일은 둘뿐이다. `onScroll` 에서 `scrollLeft / clientWidth` 를
  반올림해 머리말에 반영하고, 버튼이 `scrollTo({ behavior: 'smooth' })` 로 트랙을 미는 것.
  값이 바뀔 때만 `setAt` 을 불러 스크롤마다 리렌더하지 않는다
- `scroll-snap-stop: always` — 세게 밀어도 한 장씩 선다. 훑어보는 화면이라 여러 장을
  건너뛰면 뭘 봤는지 놓친다
- `overscroll-behavior-x: contain` — 끝에서 더 밀어도 뒤 히스토리로 안 넘어간다

**e2e 가 보는 것이 바뀌었다.** 합성된 `TouchEvent` 는 브라우저의 패닝을 일으키지 못해서
제스처 자체는 테스트할 수 없다. 대신 JS 가 책임지는 것(스크롤 → 머리말 동기화, 버튼 →
스크롤)과 스냅 CSS 가 실제로 걸려 있는지를 본다. 손가락 동작은 브라우저 몫으로 남긴다.

- 기각 — *마우스 드래그도 같이 지원.* 데스크톱엔 ‹이전/다음› 버튼이 이미 있고,
  트랙이 가로 스크롤이라 휠·트랙패드로도 넘어간다

**넘김 버튼은 트랙 밖에 한 벌만 둔다** (2026-09-12, 사용자 요청). 처음엔 슬라이드마다
`.card-bottom` 을 넣었는데, 트랙 안에 있으니 카드를 따라 흘러간다. 손가락으로 미는 도중에
누르려던 버튼이 같이 움직이는 셈이라 고정으로 뺐다. 덕분에 `disabled` 판정도 슬라이드
인덱스가 아니라 지금 장(`at`) 하나로 정리됐다. e2e 는 버튼이 한 벌인지, 슬라이드 안에
`.card-bottom` 이 안 남았는지, 눌러도 `boundingBox` 가 그대로인지를 본다.

### 예문은 한 줄씩, 넘기는 건 카드 안에서 (2026-09-12, 사용자 요청)

카드에 예문을 2줄씩 깔아 두니 무거웠다. "설정으로 뺄까, 카드에 보기 버튼을 둘까" 를
물어 와서 **둘 다 안 쓰는 쪽**을 권했다.

- *설정 토글* — 지금 설정에 있는 건 세션 길이·모드 비율·테마·관찰 문구로 전부 **학습 루프의
  동작**이다. 예문 노출은 화면 하나의 표시 문제라 성격이 다르고, 껐다 켜려면 화면을 나갔다
  들어와야 한다. 게다가 예문은 세션 확인 단계에도 나와서(`examples.spec.ts`) 설정 하나가
  두 화면을 같이 건드리게 된다
- *「예문 보기」 버튼* — 훑어보기는 "문제는 안 나와요, 눌러서 보기만 하세요" 가 전부인
  화면이다. 가장 읽을 만한 내용을 탭 뒤로 숨기면 그 성격과 어긋난다. 세션 확인 단계는
  정답을 맞힌 *뒤* 주는 보상이라 감추는 게 맞지만 여기선 감출 이유가 없다

**채택 — 한 줄을 바로 보여주고, 여러 개일 때만 「다음 예문」.** 데이터가 이 쪽을 지지한다.
예문 있는 숙어 11,217개 중 3개짜리가 7,224개(64%), 1개짜리가 2,534개(23%). 문장 길이
중앙값 14자. 즉 대부분은 볼 게 더 있고, 1개뿐인 23%에는 버튼이 아예 안 붙는다.

**"다른 카드로 넘어가면 안 된다"** (사용자 지적). 한 표현의 다른 예문이니 표현이 바뀌면
안 된다. 버튼은 그 카드의 `exAt` 만 올린다 — 트랙은 그대로다. e2e 가 클릭 뒤에도
머리말(`n / 총`)이 안 변하는지로 그걸 고정한다.

예문 자리는 `BrowseSlide` 가 들고 있어 카드마다 따로 논다. 화면을 나갔다 들어오면
컴포넌트가 다시 마운트돼 첫 예문부터 — 훑어보는 화면이라 이어서 볼 이유가 없다.
`loadExamples` 결과를 2개로 자르던 것도 걷었다 (원본이 최대 3개다).

**카드를 떠나면 첫 예문으로** (2026-09-12, 사용자 지적 "첫예문으로"). 처음 구현은
"그 카드에 한해 기억한다" 였는데, 트랙이 30장을 다 띄워 두는 구조라 몇 장 전에 넘겨 둔
자리가 그대로 남았다. `at` 이 바뀌면 렌더 중에 `exIndex` 를 0 으로 되돌린다 —
`useEffect` 로 하면 한 번 그린 뒤 다시 그리게 되고 `set-state-in-effect` 린트가 잡는다.
파생만으로는 안 된다: 떠날 때 0 으로 보이게 해도 돌아오면 옛 값이 되살아난다. 잊는 동작이
필요하다.

**e2e 가 실제 순서를 안 따르고 있었다.** 화면 밖 카드의 버튼을 눌러도 통과하게 짜여 있었는데,
실제로는 보이는 카드에서만 누를 수 있다. 예문이 여럿인 카드로 스크롤해 간 뒤 누르도록
고쳤더니 구현의 결함이 드러났다. 또 `scrollLeft` 를 연속으로 두 번 쓰면 스크롤 이벤트가
합쳐져 중간 장을 안 거친다 — 도착을 확인하고 돌아오게 했다.

## 동기화가 자기 파일을 읽지도 않고 덮어썼다 (2026-09-13)

**이 절은 신고를 조사하다 따로 발견한 버그다. 신고된 증상의 원인은 아니었다** —
그쪽은 Drive 수동 삭제였다 (같은 날 「신고된 유실의 진짜 원인」 절). 조사 중 소스에서
찾아 재현한 별개의 결함이고, 고칠 값어치는 그대로다: Drive 에 있던 3건이 동기화 한 번에
0건이 됐다.

### 원인

`syncNow` 가 로컬을 읽어 그대로 올렸다. 비교도 병합도 없었다.

```
const mine = await listDeviceEvents(...)   // 로컬만 본다
await drive.uploadOrReplace(myFileName, JSON.stringify(mine))
```

순서가 `업로드 → 목록 → 남의 파일` 이라 **자기 파일은 한 번도 읽지 않았다.** 자기 기록의
유일한 판단 근거가 로컬 DB뿐이었으니, 로컬이 비면 Drive 사본도 같이 비었다.

**로컬만 비고 `deviceId` 는 남는 경우가 실제로 있다.** `deviceId` 는 localStorage,
학습 기록은 IndexedDB — 서로 다른 저장소다. iOS Safari 는 홈 화면에 설치하지 않은 웹앱의
IndexedDB 를 7일 미사용에 비우고, 저장 공간 압박에서도 IndexedDB 가 먼저 정리된다.
브라우저 데이터를 통째로 지우면 `deviceId` 도 날아가 새 파일이 생기므로 이 경로가 아니다 —
그래서 "가끔" 나는 것처럼 보였다.

**어제 쓴 테스트의 주석이 이 버그를 그대로 기록하고 있었다.** 자동 정리 테스트에
"a1 은 내 파일에 있던 것 — 빈 로컬 DB 를 올리며 덮어썼으므로 안 돌아온다" 라고 적어 두고
그걸 정상으로 받아들였다. 증상을 보고도 결함으로 안 읽은 것이다.

### 고침 — 읽기 전에는 쓰지 않는다

순서를 `목록 → 내 파일 되받기 → 업로드 → 남의 파일 → 정리` 로 바꿨다.

- **내 파일은 `importMissingEvents` 로 없는 것만 받는다.** 남의 파일은 `bulkPut` 이 맞지만
  (id 가 같으면 내용도 같다) 내 파일은 *내가 지난번에 올린 것* 이라, 그 뒤 로컬에서 생긴
  변화를 옛 값으로 되돌리면 안 된다. 지금은 묘비를 만드는 곳이 없지만 스키마에 자리가 있는
  이상 되살아나는 길을 미리 막았다
- **못 읽은 파일은 덮어쓰지 않는다.** 내 파일 다운로드가 실패하면 업로드를 건너뛰고 예외로
  끝낸다. 옛 순서는 업로드가 먼저라 목록 조회가 실패해도 내 기록은 올라갔는데, 그 안전성을
  잃는 대신 "읽지도 않고 덮어쓰기" 를 없앤다. 이번 버그가 그 방향으로 틀린 것이라
  반대로 틀리는 쪽을 택했다

### 값

- 동기화마다 내 파일을 한 번 더 받는다 (9/11 관찰에서 활성 기기 파일 249KB)
- 올라가는 파일이 줄지 않는다 — **로컬 삭제가 더는 Drive 로 전파되지 않는다.**
  다만 애초에 전파된 적이 없다: 「초기화」는 파일을 지우는 방식이고, 다른 기기가 동기화하면
  그 기기 기록이 다시 올라온다. 수정이 그걸 빼앗는 게 아니라 원래 없었다

### 우선순위 (사용자 승인 2026-09-13)

- **P0** 유실 차단 — 확인·재현된 되돌릴 수 없는 손실. 다른 무엇보다 먼저
- **P1** 「초기화」 문구 — 사실이 아닌 문구가 제일 싼 버그. P0 과 같이
- **P2** 가짜 오답 규모 측정 — 9/6 `9b432cb` 이전의 빈 답 오답. **세기 전엔 P3 의 가치를 모른다**
- **P3** 묘비 — 「초기화 전파」와 「구간 삭제」가 걸려 있다. `deletedAt` 은 이벤트마다 붙는
  필드라 초기화를 전파하려면 수만 건을 갱신해야 하는데 "기존 이벤트는 절대 갱신하지 않는다"
  와 부딪힌다. 대안인 「여기까지 지웠다」 표식 이벤트는 2026-09-04 에 한 번 기각한 방향이다.
  **설계 결정을 먼저 받는다**
- **P4** 관찰만 — 보관 파일 동시 정리 경합(가능성만 확인, 겪은 적 없음),
  IndexedDB 가 비었을 때 사용자가 모르는 것

### 실제 동기화를 보는 법 — `npm run audit:sync`

앱은 동기화 로그를 안 남긴다. 남은 증거는 Drive 의 `reviews-*.json` 뿐이다.
`data/events/` 에 내려받아 넣으면 기기별 건수·기간·빈 파일·파일 이름과 다른 기기의 이벤트를
센다. **기록이 있던 기기의 파일이 비어 있으면 이 버그에 당한 것이다.**

- 기각 — *앱에 동기화 로그를 남긴다.* 유실을 이미 막았으니 로그는 사후 분석용인데,
  로그 자체도 IndexedDB 에 있어 같이 날아간다. 증거로서 Drive 파일보다 약하다

## 신고된 유실의 진짜 원인 — Drive 수동 삭제 (2026-09-13)

"웹에서 동기화한 경우와 홈 화면 웹앱의 학습한 한자 읽기 숫자가 다르다. 정답률도 오답률도
다르다." Drive 폴더와 휴지통을 파일 단위로 훑어 원인을 확정했다.

### 증거

| 파일 | 크기 | 생성 | 최종 수정 |
|---|---|---|---|
| `reviews-archive.json` | 193KB | 9/11 17:00 | **9/11 17:00** |
| `reviews-0bc19888…` | 66KB | 9/13 12:00 | 9/13 12:59 |
| `reviews-e2ca9b7d…` | 36KB | 9/13 12:02 | 9/13 13:00 |
| `reviews-794a0ce4…` | 6KB | 9/12 16:51 | 9/12 18:44 |
| 휴지통 7개 (`b1dc6cae` 포함) | — | — | 9/12 16:54 에 버려짐 |

**보관 파일이 9/11 17:00 이후 안 바뀐 것**이 결정적이었다. 앱이 파일을 지우는 경로는
정리(보관 파일을 먼저 쓰고 지운다)와 초기화(보관 파일까지 지운다)뿐인데, 둘 다
수정 시각과 안 맞는다. 그리고 `deleteFile` 은 `DELETE /files/{id}` — **휴지통을 안 거치는
영구 삭제**다. 휴지통에 있다는 것 자체가 앱이 안 지웠다는 증거다.

→ 9/12 16:54 에 사용자가 Drive 앱에서 손으로 지웠다. 보관 파일만 빼고 선택해서.

### 왜 숫자가 갈렸나

지운 `b1dc6cae`(249KB, 1,000건 가까이)를 **삭제 전에 내려받은 기기는 로컬에 들고 있고,
삭제 후 생긴 기기는 못 받는다.** 그런데 그 이벤트들의 `deviceId` 가 `b1dc6cae` 라서
`listDeviceEvents` 가 안 올린다 — 로컬에는 있는데 Drive 에는 없는 상태가 된다.
그래서 기기마다 세는 모수가 달라졌다.

**복구는 휴지통 복원 + 각 기기 동기화로 끝났다.** 코드 수정 없이 숫자가 같아졌다.

### 남은 함정 — 정리는 자기 파일을 보관 파일에 안 넣는다

`consolidateSyncFiles` 는 `f.name !== myFileName` 으로 자기 파일을 아예 안 읽는다.
정리 후 폴더에는 「보관 파일 + 내 파일」이 남는데, 사용자 눈에는 내 파일도 정리 대상처럼
보인다. 이번에 정확히 그걸 지웠다. **9/11 에 "수동 삭제하면 백업이 깨진다" 고 기각 사유로
적어 뒀던 위험이 그대로 실현된 것이다** — 적어만 두고 막지는 않았다.

보관 파일이 그 기기 기록까지 담은 진짜 전체 백업이면 손으로 지워도 안 깨진다.
「이 기기 기록 전부를 보관 파일에 올리기」를 붙일지는 사용자 결정 대기.

### 배운 것

파일 메타데이터(크기·생성·수정 시각)만으로 원인을 갈랐다. 앱에 동기화 로그가 없어도
**Drive 가 이미 로그였다.** `npm run audit:sync` 는 그걸 자동으로 읽는 도구다.

## 백업과 동기화를 가른다 — mmtm 교훈 (2026-09-13, 사용자 결정)

사용자의 실제 요구는 "파일이 계속 늘어나는 게 싫다" 였다. 그리고 **mmtm 에서 교훈을 찾으라는
지적이 방향을 줬다.**

### 무엇을 놓쳤었나

yomenai 는 mmtm 에서 둘을 의도적으로 뺐다 (2026-09-05 절).

- 버전 백업(`MAX_BACKUPS_TO_KEEP`) — "원본이 append-only 라 버전 이력이 필요 없다"
- 충돌 해결 UI(`syncManager.js`) — "기기별 파일이라 쓰기 충돌이 구조적으로 없다"

각각은 맞았다. **그런데 둘을 빼면서 백업과 동기화의 구분 자체가 같이 사라졌다.**
mmtm 에서 백업 zip 은 "쌓이고 오래된 건 지워도 되는 것" 이었고 동기화는 별개 경로였다.
yomenai 의 `reviews-*.json` 은 동기화 수단인 동시에 유일한 백업 원본이다. 한 파일이 두 역할을
겸하니 사용자가 "정리해도 되는 것" 과 "지우면 끝나는 것" 을 구분할 방법이 없었다.

이름까지 반대로 도왔다. `reviews-archive.json` 은 "옛것 보관함" 처럼 읽혀 버려도 될 것 같은데
실은 가장 소중했고, `reviews-b1dc6cae….json` 은 기계가 만든 임시 파일처럼 보이는데 실은
1,000건의 유일한 사본이었다. **지운 쪽이 소중한 쪽이었던 게 우연이 아니다.**

### 위험 평가를 다시 했다 (사용자 지적)

원래 근거는 "학습 데이터 유실은 일기보다 덜 위험하다" 였다. 이건 **유출** 피해를 잰 것이고
암호화를 건너뛴 근거로는 지금도 유효하다. 그런데 **유실** 피해는 다른 축이다 —
학습 기록을 잃으면 쌓아 온 증거가 사라져 계속할 이유가 깎인다. 이 앱이 Phase 11 「지속의
유인」에서 통째로 다루는 문제를, 동기화 위험 평가에서만 빼놓고 있었다.

덧붙여 일기 쪽 평가도 틀렸다 — 일기 유실은 "안 쓰게 되는 것" 이 아니라 **다시 만들 수 없는
개인의 역사가 지워지는 것**이다. 정확한 결론은 "학습 기록이 더 위험하다" 가 아니라
**비교로 느슨함을 정당화한 것 자체가 잘못**이었다.

→ PLAN §5 원칙 3 을 "기기별 파일 분리" 에서 **"성취도는 깎이지 않는다"** 로 올리고,
파일 구조를 그 아래 수단으로 내렸다.

### 채택한 구조

| | `backup.json` | `sync-<기기>.json` |
|---|---|---|
| 역할 | 전체 로그 | 그 기기가 올린 것 |
| 수명 | 길다 | 백업에 접히면 바로 지운다 |
| 사용자가 지우면 | 안 됨 | 아무것도 안 잃음 |

동기화는 목록 → 전부 읽어 로컬에 병합 → 내 전송 파일 쓰기 → 백업 쓰기 → **성공 후** 접은
파일 삭제. 평상시 폴더에는 `backup.json` 하나만 남는다.

- **순수 단일 파일보다 낫다.** 백업 쓰기가 경합에 져도 전송 파일이 그 순간 아직 살아 있어
  다음 동기화가 메운다. 단일 파일은 그 안전망이 없다
- **`STALE_DAYS`(30일)와 수동 정리 버튼을 없앴다.** 매번 접으니 따로 돌릴 일이 없고,
  그 버튼이 이번 사고에서 "정리해도 되는 것" 이라는 오인을 키웠다
- **줄어드는 백업 쓰기 거부**(`assertBackupGrows`). 정상 동작에선 병합이 먼저라 걸릴 일이
  없지만, 병합을 빼먹는 결함(9/13 P0 가 정확히 그랬다)을 쓰기 직전에 잡는다.
  테스트는 인위적인 가짜 대신 **규칙 자체**를 고정한다 — 가짜로 몰아넣으면 전제가 거짓이라
  통과해도 의미가 없다
- **폴더 전체를 본다.** `name contains 'reviews-'` 는 이름 규칙이 바뀌면 깨지는 필터였다.
  `drive.file` 스코프라 이 앱이 만든 파일만 보인다
- 옛 이름은 읽어서 접고 지운다 — **일회성 이관 스크립트가 필요 없다**

### P2 — 가짜 오답을 세는 도구 (2026-09-13)

9/6 `9b432cb` 전까지 `KanaInput` 이 빈 값도 제출했다. 그때 쌓인 `answer:''` 오답이
지금도 오답 유형 분포·취약 음독·밴드 추정·FSRS 를 오염시킨다. **지우기 전에 규모부터 잰다** —
10건대면 P3 을 접고, 수백 건이면 리포트가 실제로 왜곡되고 있다는 뜻이다.

**서명에서 읽기 카드로 좁히는 게 핵심이다.** 뜻 카드는 자기 채점이라 `answer` 가 늘 빈
문자열이다(`recordMeaningAnswer`). 안 좁히면 멀쩡한 뜻 카드 오답이 전부 가짜로 잡힌다 —
픽스처에 뜻 카드 빈 답 오답 5건을 일부러 섞어 이걸 고정했다.

**보고에 "빼면 정답률이 얼마나 오르는지" 를 넣었다.** 건수만으로는 값어치를 모른다.
정답률이 80% → 91% 로 움직인다면 사용자가 보는 숫자가 그만큼 틀렸다는 뜻이고, 그게 곧
지울 이유의 크기다. 0.3%p 면 굳이 건드릴 일이 아니다.

- 기각 — *`audit-sync-files` 에 합치기.* 그쪽은 파일·동기화 건강을 보는 도구다.
  데이터 품질은 다른 관심사라 섞으면 둘 다 읽기 나빠진다

## P2 결과 0건, 그리고 그 과정에서 나온 것 (2026-09-13)

**빈 답 오답 0건.** 그런데 "기록이 없어서 0" 인지 "안 겪어서 0" 인지 갈라야 의미가 있다 —
수정 이전(9/6 12:39) 읽기 채점이 **154건 실제로 있고** 그중 오답이 83건인데 빈 답은 0이다.
1자 이하 답도 0. **그 버그를 안 겪은 것이 맞다.**

→ **P3(묘비)를 접는다.** 「구간 삭제」는 지울 대상이 없어 동기가 사라졌고, 남는
「초기화 전파」 하나를 위해 수만 건 갱신(append-only 원칙과 충돌)이나 새 이벤트 타입
(2026-09-04 에 한 번 기각)을 들일 값어치가 없다. 필요해지면 그때 다시 연다.

### 재는 김에 본 것 — 리포트가 실력을 과소평가한다

사용자가 "진단 리포트에 밴드별로 나눈 거랑 다른가" 를 물어 확인했더니 **같았다.**
`diagnosticSummary` 는 전 기간 읽기 채점 전부를 밴드별로 접고, 리포트의 「지금 수준」
사다리가 그걸 그대로 보여준다. 따로 셀 이유가 없었다.

**대신 리포트가 못 보는 것이 드러났다 — 시간이다.**

| 밴드 | 전 기간 | 최근 3일 |
|---|---|---|
| 0 | 71.4% | 73.9% |
| 1 | 70.3% | **81.3%** |
| 2 | 62.1% | **76.5%** |
| 3 | 59.5% | 68.1% |

전체 정답률이 9/05 40.2% → 9/13 77.5% 로 오르는데, 전 기간을 접으니 **9/05 의 40% 가
아직도 평균을 끌어내린다.** `LEVEL_SOLID_RATE` 0.8 기준으로 **밴드 1 은 이미 넘겼는데
화면은 「흔들림」이라고 말한다.**

이건 오늘 세운 「성취도는 깎이지 않는다」의 다른 얼굴이다. 데이터가 사라지는 게 아니라
**오래된 데이터가 현재를 가린다.** 실력이 늘어도 사다리가 안 올라가면 계속할 이유가 깎인다.

**진단과 묶여 있다.** `bandVerdict` 의 `OK_RATE` 도 0.8 이고 `level.ts` 주석이
"진단이 넘긴 밴드가 리포트에서 흔들림으로 나오면 두 화면이 서로 다른 말을 한다" 고
그 연결을 적어 뒀다. 한쪽만 고치면 정확히 그 상태가 된다.

### 밴드 판정을 최근 30회로 (2026-09-13)

리포트가 실력을 과소평가하던 걸 고쳤다. `buildLevel` 이 밴드별 채점 이력을 시간순으로
모아 **뒤에서 `LEVEL_WINDOW`(30) 개만** 판정에 쓴다.

실제 백업(1,719건, 9일)으로 잰 효과.

| 밴드 | 전 (전 기간) | 후 (최근 30회) |
|---|---|---|
| 0 | 71.4% 흔들림 | **86.7% 안정** |
| 1 | 70.3% 흔들림 | **90.0% 안정** |
| 2 | 62.1% 흔들림 | 76.7% 흔들림 |
| 3 | 59.5% 흔들림 | 66.7% 흔들림 |

전에는 **모든 밴드가 흔들림**이라 사다리에 안정 구간이 아예 없었다. 이제 밴드 1까지 안정,
밴드 2가 경계로 선다. 경계선이 제 역할을 한다 — 2는 문턱 0.8 에 가깝고 3은 확실히 멀어서
"지금 2를 밀면 넘어간다" 가 읽힌다.

- **누적 `totalReadings` 는 안 건드렸다.** 밴드 행의 `seen` 합(≤30×밴드수)과 다른 값이
  됐지만 의도한 것이다. 누적은 쌓아 온 양이라 줄이면 그게 곧 성취도를 깎는 것이고,
  처방의 표본 문턱도 이 값을 본다
- **회수 기준을 골랐다.** 날짜 기준(최근 N일)은 며칠 쉬면 표본이 비어 「표본 부족」으로
  떨어진다 — 쉬었다고 수준이 사라지면 안 된다
- **진단과 어긋나지 않는다.** `level.ts` 주석이 걱정하던 "진단이 넘긴 밴드를 리포트가
  흔들린다고 말하는" 상태는, `bandVerdict` 가 *그 회차 안*의 성적을 보는데 리포트만
  전 기간을 접어서 생긴 것이었다. 창을 씌우니 **오히려 두 화면이 가까워진다** —
  고치려던 문제의 원인이 바로 그 비대칭이었다
- `diagnosticSummary` 는 안 건드렸다. 진단 결과 화면이 *그 회차* 요약에 쓰는 함수다
- 화면에 기준을 밝혔다. 밴드 행의 숫자가 30 에서 멈추는 이유가 안 보이면 오해를 부른다

**30 이라는 값에 강한 근거는 없다.** 실측에서 밴드별 누적이 217~609 라 30 은 최근 2~3일에
해당하고, `LEVEL_MIN_SEEN`(5)의 6배라 「표본 부족」과 겹치지 않는다. 써 보고 너무 요동치면
올리고, 너무 둔하면 내린다.

## 첫 만남을 시험이 아니라 소개로 (2026-09-13, 사용자 결정)

사용자 지적 — "아는 것을 확인하기는 하는데 모르는 걸 배우는 것은 한계가 있다."
그리고 "내가 꽤 안다고 생각해서 앱을 그 방향으로 잡은 건데 더 알고 싶어."

### 전제와 코퍼스가 벌어져 있었다

PLAN 첫 줄이 "뜻은 아는데 못 읽는 사람을 위한 앱" 이다. 읽기 교정은 **이미 아는 뜻**에
읽기만 붙이는 작업이라 시험-피드백이 정확히 맞고, 오답 상세가 렌다쿠·촉음·장음·한국음
간섭을 짚는 것도 **유추할 수 있는 것**을 교정하는 장치다.

그런데 코퍼스에 일본고유(category 3) **3,643개**가 들어왔다. 한국어에 대응어가 없어
유추할 근거가 아예 없는 단어들이다. 「어휘 확장」이 세션의 30%인데 거기서 하는 일은
뜻 카드 자기 채점뿐 — 모르면 「몰랐다」를 누르고 다음에 또 만나고 또 누른다.

**구조적으로 새 단어의 첫 만남이 시험이었다.** `selectSession` 이 신규를 밴드·미숙 음독
가중으로 골라 넣으면 그대로 문제로 나온다. 처음 보는 단어에 "읽어 보세요" 는 배우는 게
아니라 틀리는 것이다. 답을 보여주긴 하지만 그건 **채점 결과**지 **학습 단계**가 아니다.

### "이미 소개했다" 를 어디에 기억하나 (사용자 결정)

- 기각 — *새 이벤트 타입 추가.* 정석이지만 스키마 불변 조건이라 무겁고,
  2026-09-04 에 비슷한 이유로 한 번 기각한 방향이다
- 기각 — *소개하고 같은 카드에서 바로 시험.* 스키마는 안 건드리지만 답을 보고 치는 거라
  첫 채점이 부푼다. 같은 날 밴드 사다리를 최근 30회로 고쳤는데 그게 바로 다시 왜곡된다
- **채택 — `localStorage`.** 이건 학습 기록이 아니라 **화면 진행 상태**다. 잃는 건
  "다른 기기에서 한 번 더 본다" 뿐이고, 새 단어를 두 번 보는 건 손해가 아니다.
  부족해지면 그때 이벤트로 올린다

### 소개한 숙어는 그 세션에서 더 안 낸다

확장 숙어는 읽기·뜻 두 장이 같이 나온다(`activeCardTypes`). 소개에서 읽기와 뜻을 다
보여준 뒤 같은 세션에서 그걸 물으면 첫 채점이 부푼다. `planIntros` 가 세션을 짤 때
그 숙어의 나머지 카드를 걷는다 — **중간에 건너뛰지 않고 짤 때 걷는** 이유는 진행 막대가
처음부터 맞는 수를 써야 해서다. 카드를 중간에 건너뛰면 막대가 튄다.

### 확인 질문이 소개보다 먼저다

새 숙어는 「이 숙어, 뜻은 알고 있었어요?」(지연 검수)가 먼저 나온다. e2e 를 짜다 발견했고,
**그 순서가 맞다** — 보여주기 전에 물어야 답이 의미가 있다. 보여준 뒤 물으면 전부 "알았다"다.

### 남은 것

- **세션 상한 없음.** 지금은 기한 카드가 대부분이라 소개가 몇 장뿐이다. 처음 쓰는 사람은
  첫 세션이 전부 소개가 되는데, 그것도 "여기 20개를 보여줄게요" 라 나쁘지 않다.
  소개로 가득 차 시험이 안 되면 그때 상한을 넣는다
- **다음 후보 — 음독에서 어휘로 잇기.** "이 음독을 익히면 이 12개가 열려요" 의 데이터가
  이미 있다(`unlocksOf`, 처방이 쓴다). 지금은 틀린 뒤에만 나오는데 배우는 입구로 쓰면
  모르는 단어가 무작위 낱개가 아니라 묶음으로 들어온다. 소개가 자리 잡은 뒤에 붙인다

### 소개 카드가 기존 e2e 6개를 깼다 (2026-09-13)

세션을 도는 루프가 8개 스펙에 **거의 같은 모양으로 복사돼** 있었다. 「봤어요」를 모르니
거기서 멈췄고, 같은 수정을 여섯 번 반복했다. 공용 헬퍼로 묶어 두면 다음 제품 변경 때
한 곳만 고치면 된다 — 아직 안 함.

**두 번 헛짚었다.** 처음엔 루프 예산이 모자란 줄 알고 늘렸고(그것도 필요했지만 원인은
아니었다), 그다음엔 세션을 두 번 돌게 했는데도 안 됐다. 디버그 스펙으로 화면을 직접
찍어 보고서야 원인이 나왔다 — **재시작 버튼 이름이 달랐다.**

```
PASS 0 완료화면 버튼 ["홈으로"]
PASS 1 완료화면 버튼 ["진입 진단 시작","세션 시작  · 진단 건너뛰기", ...]
  세션시작 보임? false     ← exact: true 라 안 걸린다
```

**그리고 이게 제품 문제를 드러냈다.** 홈이 "진단 안 함" 상태로 돌아간 이유는
**소개가 이벤트를 하나도 안 남기기 때문**이다. 17장을 보고 완주했는데 앱은 아무것도
안 한 것으로 본다. `buildLevel` 이 볼 게 없으니 `shouldOfferDiagnostic` 이 참이 된다.

오늘 세운 「성취도는 깎이지 않는다」와 정면으로 부딪힌다. 기록이 쌓인 사용자는 기한
카드가 대부분이라 안 보이지만, 구조적으로 **소개는 성취로 안 잡힌다.** 결정 대기.

- 함께 드러난 것 — 새 숙어 한 장에 탭이 두 번이다 (확인 질문 + 봤어요).
  기록이 없는 상태에서는 세션 17장이 전부 그렇게 돌아 연습이 한 번도 안 나온다

### 안다고 답하면 소개를 건너뛴다 (2026-09-13, 사용자 질문에서 나옴)

"안다고 하면 어떻게 되지?" — 답은 **바로 읽기 문제로 간다** 였고, 그게 앱의 원래 흐름이다.
`assignMode` 가 `known: true` 를 교정으로 넘기고 `activeCardTypes('correction')` 이
읽기만 내므로, 뜻 카드는 애초에 안 나온다. 소개까지 붙일 이유가 없다.

덤으로 **「소개만 하면 아무것도 안 쌓인다」가 그 경로에선 아예 안 생긴다** — 안다고 답한
카드는 그 자리에서 채점되고 기록이 남는다.

### "처음 만나요" 를 뺐다 (사용자 지적)

"새로 보는 글자를 알 수도 있잖아." 맞는 말이다. **앱에 기록이 없다는 뜻이지 사용자가 처음
본다는 뜻이 아니다.** 앱이 모르는 걸 사용자에 대해 단정하고 있었다.

합치면 그 문제가 자연히 없어진다 — 카드가 단정 대신 **질문**으로 시작하고, 모른다고 답한
뒤에야 내용이 펼쳐지니 보여주는 게 정당해진다. 태그는 「알아 두기」로 바꿨다.
사용자가 아니라 하는 일을 가리키는 말이다.

남는 경우 하나 — **뜻은 모르는데 읽기는 아는 사람**. 음독 규칙으로 읽히는 한자어면 있을 수
있다. 그 사람은 소개를 보고 읽기 시험이 다음 만남으로 밀린다. 한 슬롯 손해지만 뜻을
새로 아는 값이 있고, 거기까지 나눠 물으면 카드가 다시 복잡해진다.

### 소개만 한 세션이 진단으로 안 잡히는 건 문제가 아니다 (사용자 판단)

e2e 디버그에서 "소개만 한 세션은 이벤트가 0이라 홈이 진단 안 함으로 남는다" 를 문제로
올렸는데, 사용자가 **"문제를 풀지 않은 상태에서 소개만 한 것은 애초에 진단 범주가 아니다"**
라고 정리했다. 맞다 — 아무것도 안 풀었으면 수준을 추정할 근거가 없다. 내가 「성취도는
깎이지 않는다」를 과하게 적용해서 성취가 아닌 것까지 성취로 세려 했다.

### 이 설계가 다른 사람에게도 맞는지 — 모른다 (열어 둠)

사용자가 "이런 경험이 내게만 유효한 건지 판단이 안 된다" 고 했다. 한 사람의 로그
1,719건으로 만든 앱이라 여기서는 알 수 없다. 다만 갈라 볼 수는 있다.

- **언어에 근거한 부분은 일반화된다.** 한자어 겹침 구조와 한국음 간섭·렌다쿠·촉음·장음은
  두 언어의 성질이지 한 사람의 습관이 아니다. 「뜻은 아는데 못 읽는다」는 전제가 거기서 나온다
- **한 로그에 맞춘 상수는 아니다.** `LEVEL_WINDOW` 30, `LEVEL_SOLID_RATE` 0.8, 세션 20장,
  비율 7:3, `BROWSE_N` 30. 전부 상수 하나씩이라 바꾸기 쉽다 — 구조를 뜯을 일이 아니다
- **가장 싼 확인 — 한 명 더.** 비슷한 실력의 한국어 화자가 일주일 쓰고 오답 유형 분포와
  밴드 사다리를 대조하면 된다. 닮으면 전제가 맞고, 다르면 한 사람용 도구인 것이다
- 지금 정할 필요 없다. 일반화는 설정과 온보딩 비용을 부른다

### 처음부터 설명하지 않는다 (2026-09-13, 사용자 결정)

소개 카드를 넣고 나니 **새 사용자의 첫 세션이 전부 소개**가 됐다. 20장이 다 「알아 두기」로
지나가고 연습이 한 번도 안 나온다. 사용자 판단 — **"레벨 테스트처럼 진단을 하고 데이터가
쌓이면 그 때부터 설명을 하는 게 좋겠다."**

맞는 순서다. 기록이 없으면 앱은 이 사람이 뭘 아는지 모른다. 그 상태에서 설명부터 내미는 건
**아는 단어까지 가르치겠다**는 뜻이고, 이 앱의 전제("뜻은 아는데 못 읽는다")와 정면으로
어긋난다. 먼저 풀게 해서 알아본 뒤에 모르는 것만 설명한다.

`INTRO_MIN_READINGS = 30`. 값은 `PRESCRIPTION_MIN_READINGS` 와 같다 — "이만큼은 풀어야
신호로 읽는다" 는 같은 논리다. 다만 독립된 판단이라 상수를 공유하지 않고 따로 두되
이유를 코드에 적었다. 한쪽을 바꿀 때 다른 쪽까지 끌려가면 안 된다.

**문턱 미만에서는 카드를 걷지도 않는다.** 소개를 안 내니 그 숙어의 다른 카드를 뺄 이유가
없다 — 빼면 세션만 짧아진다.

**e2e 는 채점 기록 30건을 IndexedDB 에 직접 심어 그 구간을 건너뛴다.** 세션을 세 번 돌리는
것보다 빠르고 정확하다. 코퍼스에 없는 `idiomId` 라 `selectSession` 이 무시하므로 카드
선택에는 안 끼어들고 건수만 채운다.

### 테스터 사용설명서 (2026-09-13)

다른 사람이 시험 삼아 쓸 수 있도록 아티팩트로 만들었다. 이 앱은 한 사람의 기록으로만
만들어서 **다른 사람이 써 보는 것 자체가 가장 필요한 데이터**라, 문서의 절반이
"테스터에게 묻는 것" 이다. 첫 질문이 오답 유형 분포 캡처인데, 한국음 간섭이 큰 덩어리로
나오는지가 곧 "이 전제가 한국어 화자 일반에 맞는가" 의 답이다.

디자인은 앱 정체성을 그대로 썼다 — 원고지의 옅은 藍 괘선에 교정자의 朱 주석,
표제 `読めない` 밑의 붉은 선은 "못 읽은 자리를 표시한 것" 이다. 일본어는 전부
`lang="ja"` + 일본 폰트 스택으로 조판했다 (CLAUDE.md 의 CJK 자형 규칙).

## 테스터 피드백 폼 — 자체 제작, 자동 수집 0 (2026-09-13)

"구글폼이 아니라 자체제작은 안 될까?" → 된다. 다만 자리를 잘못 잡을 뻔했다.

### 아티팩트 `db` 를 기각한 이유

아티팩트에 폼을 심고 `db` 에 받는 안을 먼저 봤는데 두 가지가 걸렸다.

- `db` 를 선언한 페이지는 **조직 내부 전용**이 되어 공개 공유가 안 된다. 조직 밖 지인은 못 연다
- 규칙상 **쓰기 권한이 읽기보다 낮을 수 없어서** "아무나 쓰고 주인만 읽는" 구성이 안 된다.
  `{self}` 경로가 그 용도지만 페이지가 자기 사용자 id 를 알아야 하고, 그러려면 `user`
  기능이 필요한데 이 계정에 안 열려 있다 → 테스터끼리 서로의 응답을 읽게 된다

→ **앱 안에 두고 Apps Script 로 보낸다.** GitHub Pages 라 CSP 제약이 없고, 응답이 본인
구글 시트로 가고, 아무나 열 수 있다.

### 무엇을 보낼지 — 수준과 모양을 갈랐다 (사용자 판단)

처음엔 리포트 수치를 자동으로 붙여 보내자고 제안했다. 사용자가 **"테스터의 학습 수준을
자세히 알 필요가 없다"** 고 했고, 맞는 말이다. 다만 갈라 볼 게 있었다.

- **수준** (밴드별 정답률, 누적 횟수, 안정/흔들림) — 얼마나 잘하는지. 필요 없다
- **모양** (어떤 종류의 실수를 하는지) — 범용성 판단에 필요한 건 이쪽이다.
  잘하든 못하든 한국음 간섭이 큰 덩어리로 나오는지가 전제의 답이다. 실력 측정이 아니다

그리고 **모양조차 자동으로 보낼 필요가 없었다.** 앱이 리포트에 이미 보여주고 있으니
테스터가 보고 고르면 된다. → **자동 수집 0.** 보내기 전 미리보기도, 동의 문구도,
익명 처리 로직도 필요 없어졌다 — 보낼 게 애초에 없으니까.

**사용량만 남겼다.** "며칠 동안 몇 번" 은 실력이 아니라 얼마나 썼는지인데, 없으면 나머지
답을 읽을 수가 없다. 20문항 뒤의 "도움이 안 됐다" 와 500문항 뒤의 그것은 뜻이 정반대다.
이것도 자동이 아니라 직접 적는다.

### 자체 제작이 프라이버시에 유리했다

Google Form 으로 캡처를 받으면 (1) 화면에 있는 게 통째로 가고 (2) 파일 업로드 문항이
테스터의 **Google 로그인을 강제**해 누가 냈는지 Google 과 폼 주인에게 드러난다.
자체 폼은 익명이 가능하다. 프라이버시 걱정에서 출발한 선택이 더 나은 결과로 갔다.

### 전송 확인 불가를 숨기지 않는다

Apps Script 웹 앱은 리다이렉트를 거쳐 응답하고 그쪽에 CORS 헤더가 없다. `no-cors` 로
보내면 성공 여부를 못 읽는다. "보냈어요" 만 띄우고 넘어가면 실패를 성공으로 보고하는 셈이라,
화면에 그 사실을 적고 「내용 복사」 경로를 함께 남겼다.

- `FEEDBACK_ENDPOINT` 가 비면 전송 버튼 대신 복사만 보인다 — 설정이 덜 됐다고 화면이
  깨지면 안 된다. 주소를 넣기 전에도 앱은 정상 동작한다

### 표식(TOKEN)은 비밀이 아니다

주소가 알려지면 아무나 POST 할 수 있다. 앱이 약속된 문자열을 같이 보내고 `doPost` 가
그게 아니면 버린다. 막으려는 건 아무 데나 찔러 보는 자동 요청이지 작정한 사람이 아니다.

- **`.env` 로 빼지 않는다.** 앱이 정적 번들이라 어디에 두든 파일을 열면 보인다.
  감추면 비밀인 줄 오해하게 만들 뿐이다. 소스에 "비밀이 아니다" 라고 적어 뒀다
- 기각한 대안 — 서명·시간 검증. 넣을 비밀이 없는 클라이언트라 아무 의미가 없다
- 장난이 실제로 들어오면 양쪽 값을 바꾸고 Apps Script 를 다시 배포한다

### 루트에 배포한 PWA 가 다른 프로젝트 페이지를 삼킨다 (2026-09-13)

`choan616.github.io/yomenai` (끝 슬래시 없음) 로 들어가면 mmtm 로그인 화면이 떴다.

- 서버는 정상이다 — `/yomenai` 는 301 로 `/yomenai/` 를 돌려준다
- 그 요청이 네트워크까지 못 간다. mmtm 이 루트(`/`)에 배포돼 있고 그 SW 가
  `NavigationRoute` + `createHandlerBoundToURL("index.html")` 을 denylist 없이 걸고 있다
- SW 스코프는 문자열 접두사다. **`/yomenai` 는 `/yomenai/` 로 시작하지 않아서**
  yomenai SW 관할 밖으로 떨어지고, 루트를 먹은 mmtm SW 가 가로챈다

**yomenai 쪽에서 못 고친다.** SW 스코프를 자기 디렉터리 위로 넓히려면 응답에
`Service-Worker-Allowed` 헤더가 필요한데 GitHub Pages 는 헤더를 못 단다.
고칠 자리는 mmtm 의 `workbox.navigateFallbackDenylist` 다 (다른 저장소).

- 기각한 대안 — yomenai 쪽에 `/yomenai` 를 잡는 리다이렉트 페이지. Pages 에 그 경로의
  파일을 둘 방법이 없고, 있어도 SW 가 먼저 가로채서 소용없다
- 교훈 — **사용자 사이트 루트에 PWA 를 두면 그 계정의 모든 프로젝트 페이지가 그 SW 관할에
  들어온다.** 루트 PWA 는 denylist 를 처음부터 들고 있어야 한다

### guide.html 도 같은 함정을 밟을 뻔했다

`public/guide.html` 은 yomenai 자신의 `navigateFallback: 'index.html'` 에 걸린다.
파일이 멀쩡히 배포돼 있어도 SW 가 설치된 기기에서는 앱 셸이 대신 뜬다.
`navigateFallbackDenylist` 에 넣어서 막았다 — 같은 원리를 두 층에서 두 번 만난 셈이다.

### 자동 갱신을 그만두고 물어보게 바꿨다 (2026-09-13)

`registerType: 'autoUpdate'` + `skipWaiting` 이었다. 새 서비스워커가 프롬프트 없이 바로
활성화되는 설정인데, 두 가지가 문제였다.

- **두 번 열어야 새 내용이 나온다.** 첫 실행 때 새 SW 가 받아져 활성화되지만 화면은 이미
  옛 캐시로 그려진 뒤다. 사용자가 보기엔 "고쳤다는데 그대로" 다
- **끊을 수 없다.** 카드 한 장 푸는 중에 페이지가 갈리면 그 답이 사라진다.
  자동 반영은 그 순간을 고를 수가 없다

`'prompt'` 로 바꾸고 `skipWaiting` 을 뺐다. 새 SW 는 대기 상태로 남고
`UpdateBanner` 가 「지금 적용」을 띄운다. 누르면 `SKIP_WAITING` 메시지가 가서 그때 활성화된다.
빌드된 `sw.js` 에서 `skipWaiting()` 이 메시지 핸들러 안에만 남은 것으로 확인했다.

- `injectRegister: null` — 등록을 `UpdateBanner` 가 한다. `'script'` 주입과 겹치면 이중 등록이다
- 기각한 대안 — 자동 반영 + "새로고침됐어요" 사후 알림. 이미 끊긴 뒤라 알려도 늦다
- 기각한 대안 — 세션 중에만 자동 반영을 미루기. 「세션 중」의 경계가 애매하고,
  리포트를 읽는 중에 갈리는 것도 똑같이 곤란하다
- `autoUpdate` 를 골랐던 원래 이유(mmtm SW 를 빨리 밀어낸다)는 mmtm 쪽 denylist 로 해소됐다

**한 시간마다 `registration.update()` 를 부른다.** 브라우저는 재방문 때만 SW 를 확인하는데,
홈 화면 앱을 며칠 켜 둔 채로 쓰면 그 재방문이 안 일어난다.

### 읽기 입력의 키보드 — email 에서 url 로 (2026-09-13)

iOS 는 일본어 IME 의 한자 변환 후보 바를 숨기는 API 가 없다. 그래서 ASCII 전용 키보드를
부르는 `inputMode` 로 우회해 왔는데, 고른 값이 `"email"` 이라 브라우저가 이메일 필드로
인식하고 **저장된 주소를 자동완성 후보로 띄웠다.**

`"url"` 로 바꿨다. ASCII 전용은 그대로고 이메일 연상만 끊긴다. `name="reading"` 도 줬다 —
이름 없는 필드는 브라우저가 내용을 넘겨짚는다.

- `autoComplete="off"` 는 이미 있었다. **그걸로는 안 막힌다** — 브라우저가 대체로 무시한다.
  자동완성을 끄는 확실한 길은 필드가 무엇처럼 보이는지를 바꾸는 것이다
- 기각한 대안 — `inputMode="text"`. 언어 키보드가 돌아와 IME 후보 바가 다시 뜬다.
  애초에 이 우회를 하게 만든 문제로 되돌아간다
- 대가 — 키보드에 `@` `.` 대신 `/` `.com` 이 보인다. 둘 다 이 필드에서 안 쓰는 키다

### 일본어 키보드로 바꾸면 후보 바가 뜨는 건 못 막는다 (2026-09-13)

`inputMode="url"` 로 ASCII 키보드를 **먼저 띄우는** 데까지는 됐다. 그런데 지구본 키로
일본어 키보드를 고르면 한자 변환 후보 바가 나온다.

**네이티브로 가도 안 풀린다.** iOS 는 키보드 전환을 막거나 언어를 제한하는 공개 API 를
주지 않는다. `keyboardType = .asciiCapable` 도 `inputMode` 와 같은 **요청**일 뿐이다.

진짜 갈림길은 웹/네이티브가 아니라 **시스템 키보드를 쓰느냐 마느냐**다. 직접 만든
입력기(네이티브의 `inputView`, 웹의 `inputMode="none"` + 자체 키패드)면 지구본도 후보 바도
없다 — **웹에서도 되는 일이다.**

**지금은 안 만든다.**

- 사용자가 **일부러 전환해야** 생긴다. 기본값은 ASCII 키보드고, 안 건드리면 볼 일이 없다.
  발생 빈도를 모르는 채로 입력기를 새로 만드는 건 비용이 크다
- 자체 키패드는 시스템 키보드가 공짜로 주던 걸 다 다시 만들어야 한다 — 백스페이스 길게 누르기,
  커서 이동, 데스크톱 물리 키보드, 손에 익은 배열, 접근성. 하루 수백 번 두드리는 칸이라
  어설프면 후보 바보다 훨씬 거슬린다
- 순서 — 테스터 피드백의 「이상했던 곳」에 **후보 바가 실제로 방해가 됐다**가 잡히면 그때 만든다

안내서에는 「영문 키보드 그대로 두세요」 한 줄만 넣었다 (세션 카드 설명의 「가나로 입력」).

### 규칙 해설을 「자세히」 밖으로 꺼냈다 (2026-09-13)

사용자 질문에서 드러난 것 — **만든 사람도 규칙 해설이 있는 줄 몰랐다.**

세션 안에서 해설에 닿으려면 관문이 셋이었다. (1) 틀려야 하고, (2) 「자세히」를 눌러야 하고,
(3) 그 오답이 규칙형이어야 한다. 하루 스무 장을 풀어도 한 번도 안 지나간다 —
「자세히」 옆이 「다음」이고, 세션 중엔 다음을 누르는 게 자연스럽다.

문턱을 높게 잡은 건 9월 7일의 선택이었다("규칙이 없는 자리에까지 글을 늘리면 있는 해설까지
안 읽힌다"). 글을 아끼려던 판단이 **해설 자체를 안 보이게** 만들었다.

규칙형 오답이면 카드에서 바로 한 줄 보이게 했다(`.rule-hint`). 관문 셋 중 둘이 빠진다.

- **어휘형에는 여전히 안 붙인다.** `RULE_MISTAKES` 경계는 그대로다. 9월 7일 판단 중
  "규칙 없는 자리엔 글을 안 늘린다" 는 유효하고, 틀린 건 "규칙 있는 자리마저 숨겼다" 쪽이다
- `MistakeDetail` 의 `.md-rule` 은 남긴다. 오버레이가 카드를 덮으므로 둘이 같이 보이지 않고,
  거기서는 대조군을 가리키는 라벨 역할을 한다
- 기각 — 「자세히」 열림을 이벤트로 남겨 먼저 측정하기. 측정은 맞는 방향이지만
  고치는 비용이 렌더 한 줄이라 재는 비용이 더 크다. **측정 없이 고칠 수 있으면 고친다**
- 남은 관문 — 규칙형 오답이 나야 보인다. 이건 의도한 것이다

**아직 안 한 것** — 한국 한자음 종성 → 일본 음독 말미 대응(종성 ㄱ→く·き, ㄹ→つ·ち,
ㅁ·ㄴ→ん, ㅇ→う·い). `KO_INTERFERENCE` 에 붙일 규칙 후보다. 2026-09-06 에 비슷한 게
기각됐지만 그건 **분류기의 후보 생성**용이었고(정답까지 후보에 들어와 변별 실패),
학습자에게 보여주는 팁은 변별이 아니라 기억의 갈고리라 요구 조건이 다르다.

## 2026-09-14 — 소개 카드 두 건

### 이미 푼 표현에 「알아 두기」가 붙었다

`planIntros` 가 `!c.due` 를 "처음 만나는 숙어" 로 읽고 있었다. **`due` 는 (숙어, 카드종류)
단위다** — `state.cards` 키가 `cardKey(idiomId, cardType)` 라서, 읽기로 풀어 본 숙어의
뜻 카드가 처음 나오면 `due === false` 가 되고 숙어 전체가 신규로 보인다.

`hasHistory(idiomId)` 를 인자로 받게 바꿨다. 호출부가 이벤트 로그에서 `review` 를 숙어
단위로 모아 넘긴다 — 카드 종류를 안 가린다.

- `!c.due` 도 **함께 남겼다.** 판정자와 겹치지만, 기한 카드가 소개로 바뀌는 일은 없어야
  하는 불변조건이라 판정자에 기대지 않고 그 자리에서 막는다
- 테스트 픽스처가 이 모순을 드러냈다 — `due: true` 인데 "기록 없음" 을 답하는 판정자를
  쓰고 있었다. 코드가 아니라 픽스처가 틀린 경우였다
- `ReplayState` 를 넘기지 않고 이벤트에서 직접 센 이유 — `planIntros` 는 순수 함수로 두고
  replay 구조를 모르게 한다. 호출부는 이미 `events` 를 들고 있다

### 집중 세션이 통째로 소개가 됐다

상한이 없었다. 집중 세션(한 쌍만 모아 풀기)은 후보 대부분이 신규라
**"기존 1장 + 나머지 전부 알아 두기"** 가 실제로 나왔다. 소개는 가르치는 자리지 연습하는
자리가 아니라, 세션이 소개로 채워지면 연습이 사라진다.

`INTRO_MAX_SHARE = 1/3`, 최소 1장. 앞에서부터 채워 첫 등장 순서를 지킨다.

- **넘친 신규 숙어는 걷어내지 않고 그냥 출제한다.** 걷으면 세션이 짧아지는데 풀이 좁은
  세션에서는 몇 장 안 남아 세션 자체가 무의미해진다. 모르는 숙어를 그냥 내는 건 소개
  기능이 생기기 전의 원래 동작이기도 하다
- 기각 — **집중 세션에서만 소개를 끈다.** 그 쌍을 쓰는 새 숙어야말로 확장의 자리다.
  문제는 소개가 있다는 게 아니라 비율이었다
- 1/3 은 근거 있는 값이 아니라 **첫 값**이다. 20장 세션이면 소개 6장까지다.
  써 보고 조정한다

### 「세션 20장」은 문제 20개다 — 소개를 따로 센다 (2026-09-14, 사용자 제안)

상한(1/3)을 넣고도 같은 호소가 또 나왔다. 상한은 **소개가 문제 자리를 잠식하는 구조**를
안 건드렸기 때문이다. 소개 숙어 하나가 문제 자리를 최대 두 장(읽기·뜻) 먹으므로,
신규가 많은 세션은 상한 안에서도 문제가 크게 줄었다.

**설정값의 의미를 바꿨다.** 「세션 20장」 = 문제 20개. 소개는 그 위에 얹힌다.

- 호출부가 `limit + introCap * 2` 로 여유분을 얹어 카드를 만든다. 소개 숙어 하나가
  최대 두 장을 먹으므로 상한의 두 배면 충분하다
- `planIntros` 가 `questionLimit` 을 받아 **문제만 세어** 자른다. 소개는 안 센다
- 고르기와 자르기를 **한 번에** 한다. 두 번 훑으면 잘려 나갈 자리의 숙어까지 상한을
  먹어서, 실제로 뜨는 소개가 상한보다 적어진다
- 상한은 이제 `questionLimit` 기준이다 — 카드 수 기준이면 여유분 때문에 상한이 부푼다

화면도 갈랐다. `progress.intros` 를 실어 챕터 타이틀이 "문제 20장 / 처음 보는 3개는 먼저
알려 드릴게요" 로 말한다. **설정이 20인데 화면에 25가 뜨면 설정이 거짓말한 것처럼 보인다.**

- 진행 막대(`index / total`)는 소개를 포함한 전체 장수를 쓴다. 막대는 "얼마나 남았나" 라
  실제로 넘겨야 할 카드 수가 맞다. 약속한 수를 말하는 자리는 챕터 타이틀이다

### 소개를 문제 사이에 흩는다 (2026-09-14)

"섞여 나온다" 고 설명했는데 실제로는 **한 덩어리로 나왔다.** 집중 세션(種 모아 보기)에서
드러났다.

`buildFocus` 가 **틀린 것(rank 0) → 처음 보는 것(rank 1) → 본 적 있고 안 틀린 것(rank 2)**
순으로 세운다. 소개 후보가 곧 rank 1 이라 애초에 연속이다. 고른 순서를 그대로 두면
설명만 연달아 넘기는 벽이 생긴다.

`spread` 를 붙였다. **문제의 순서는 안 건드린다** — 교정 우선도, 표면형 대조
(`interleaveBySurface`)도 그 순서에 실려 있다. 소개만 사이사이로 옮기고, 첫 장은 문제가
되게 첫 소개를 한 칸 뒤부터 둔다.

- "첫 등장 자리를 지킨다" 는 원래 원칙은 **소개가 문제 자리를 먹던 시절의 것**이다.
  이제 소개는 문제 수에 안 드니 위치가 자유롭다
- 기각 — **소개를 앞에 모으기.** 설명만 연달아 보는 벽이 세션 첫머리에 생긴다.
  소개한 숙어는 그 세션에서 다시 안 묻으므로 "미리 보고 바로 풀어 본다" 는 이점도 없다
- 소개가 문제보다 많으면 같은 자리에 몰려 붙어 나온다 — 상한이 1/3 이라 실제로는 안 생긴다

**e2e 가 첫 장을 전제하고 있었다.** `advanceToIntro` 로 바꿨다. 그 과정에서 헬퍼가
뜻 카드에서 멈춰 있었는데, 원인은 **「뜻 보기」 를 눌러야 채점 버튼이 나온다는 것**이었다.
실패 스냅샷(2/26 에 멈춘 화면)이 알려줬다 — 추측으로는 못 찾았을 자리다.

### 「지금 수준」의 퍼센티지가 무엇인지 밝힌다 (2026-09-14)

사용자 질문 — "정확도를 그린 것인지, 흔들리는 정도를 그린 것인지?"

막대는 **그 밴드의 정답률**(`correct / seen`)이고, 흔들림은 거기서 나온 **판정**이다
(80% 이상 안정, 그 아래 흔들림, 5회 미만 표본 부족). 막대가 흔들림을 그리는 게 아니다.

혼동의 출처는 **한 화면에 「정답률」이 두 개**였다는 것이다. 위 줄은 누적 전체,
아래 막대는 밴드별 최근 30회. 같은 말에 다른 뜻인데 구분이 없었다.

- 위 줄을 "전체 정답률" 로 고쳤다
- 힌트를 늘려 막대가 무엇이고 창이 왜 다른지, 임계값이 얼마인지 적었다.
  임계값은 `LEVEL_SOLID_RATE`·`LEVEL_MIN_SEEN` 에서 읽어 온다 — 상수를 바꾸면 문구도 따라간다

### 소개 카드에 요미가나 (2026-09-14)

한자와 읽기를 따로 두면 눈이 두 번 움직이고 **어느 글자가 어떻게 읽히는지를 직접 맞춰야**
한다. 오답 확인 카드가 이미 쓰던 `rubyOf` 를 소개 카드에도 얹었다.

- `rubyOf` 는 분해 실패 시 `[{text: 숙어, rt: 읽기}]` 하나를 돌려준다 — 그래도 읽기는 다 보인다.
  사전이 아직 안 온 경우만 `undefined` 로 두고 예전처럼 두 줄로 그린다
- **ref 를 렌더에서 읽지 않는다.** 사전은 `mistakes` ref 에 있는데 렌더 중 접근은 갱신을
  놓칠 수 있다(oxlint `react(refs)` 경고). 사전이 오면 루비 생성 함수를 상태로도 남기고
  렌더는 그걸 쓴다. 채점 경로는 이벤트 핸들러라 ref 를 그대로 쓴다

### 요미가나는 설명 카드 전부에 (2026-09-14)

처음엔 소개 카드만 고쳤는데, 의도는 **문제가 아닌 설명 카드 전부**였다.
`.reading-shown` 을 쓰던 자리를 다 찾아 셋을 고쳤다.

| 화면 | 성격 | 조치 |
|---|---|---|
| 알아 두기 (`IntroCard`) | 설명 | 루비 |
| 오답 상세 (`MistakeDetail`) | 설명 | 루비 |
| 훑어보기 (`Browse`) | 설명 | 루비 |
| 뜻 카드 (`MeaningCard`) | **문제** | 그대로 |

뜻 카드는 뺐다 — 읽기를 보여주긴 하지만 뜻을 묻는 **문제**다. 문제 카드의 제시 방식을
안 물어보고 바꾸지 않는다.

**CSS 에서 두 가지가 걸렸다.**

- `rt` 색이 `--ng`(朱) 였다. 오답 카드에서 "고쳐졌다" 를 뜻하던 색인데, 설명 카드에 그대로
  실리면 **틀린 것처럼 읽힌다.** 기본을 무채로 돌리고 `.feedback.is-ng` 에서만 朱 로 세웠다.
  전에는 `.feedback.is-ok` 에서 무채로 되돌리는 예외 규칙이었다 — 기본과 예외가 뒤집혀 있었다
- `.headword.sm` 과 `.headword.has-ruby` 는 **특정도가 같다.** 둘 다 font-size 를 잡는데
  `.has-ruby` 가 뒤에 선언돼 있어 오답 상세의 작은 헤드워드가 72px 로 커졌다.
  `.headword.sm.has-ruby` 를 따로 못박았다

### 리포트 설명은 제목 + 끝의 캡션으로 (2026-09-14, 사용자 지적)

앞서 넣은 힌트가 장황했다. 읽기 전에 설명부터 들이밀면 정작 표를 안 본다.

- 표 위에는 제목 한 줄 — 「밴드 정답률」. 무엇을 재는지만 말한다
- 판정 기준은 **표 끝에 작은 캡션**으로 — "최근 30회 기준 · 80% 이상 안정 · 80% 미만 흔들림 ·
  5회 미만 표본 부족". 필요할 때 내려가서 보면 되는 정보다
- 위 줄의 「전체 정답률」 은 남긴다. 제목이 「밴드 정답률」 이라 대비로 뜻이 선다
- 수치는 여전히 `LEVEL_*` 상수에서 읽는다

### 뜻 카드도 요미가나 (2026-09-14)

뜻 카드는 **확장(expansion) 모드 숙어에만** 나온다 — 뜻을 모르는 것들이다
(`assignMode`: 동형이의·일본 고유, 또는 진단에서 "몰랐다", 또는 뜻 카드가 재학습 상태).
뜻이 안정되면 모드가 교정으로 넘어가 자연히 빠진다.

**읽기를 묻는 카드가 아니므로** 루비를 얹어도 답이 새지 않는다. 읽기 문제에만 안 준다.

- `introRuby` → `idiomRuby` 로 넓혔다. `intro` / `meaning` / `meaning-feedback` 에서만 낸다
- **덫 하나** — 뜻 카드는 채점 뒤 `feedback is-ng` 를 단다(오답이 아니라 "뜻을 다시 보여준다"는
  뜻이다). 朱 를 `.feedback.is-ng .has-ruby rt` 로 걸어 뒀더니 뜻 카드 루비까지 붉어질 참이었다.
  읽기 정답에만 붙는 `.answer-ruby` 를 만들어 거기로 좁혔다

## 2026-09-14 — 테스터 피드백 1차

첫 테스터(자칭 N2 미만)의 카톡 피드백. 원문 파일은 UTF-8 인데 첨부로 오며 CP1252 로
읽혀 깨졌다 — 디스크 원본을 바이트로 읽어 복원했다.

**가장 큰 소득은 「자세히」를 최고로 꼽았다는 것이다.** 앱은 "N1 보유자, 한국 한자음 지식이
많은 사람" 을 상정했는데 그보다 한참 아래 학습자가 한자 분해·한국음 대조를 가장 유용하다고
했다. 접어 뒀던 "범용성이 있는가" 에 대한 첫 데이터다 — **전제는 통했고 막힌 건 조작·진입이다.**
테스터 본인이 "내용적 측면보다 외부적 측면을 더 많이 다루게 되었다" 고 요약했다.

### 넘기기는 빈 답으로 남긴다

모를 때 넘길 길이 없어 **아무 글자나 쳐야 했다.** 그 입력이 오답 유형 분류기를 거쳐
밴드 판정·처방까지 오염시켰다. P2(2026-09-13)에서 "가짜 오답" 을 셌을 때 0건이었는데,
그건 만든 사람만 쓰던 시절의 숫자였다.

- **오답으로는 센다** — 못 읽은 건 사실이다. 정답률이 깎이지만 그건 정직한 깎임이다
  (PLAN §5 원칙 3 의 "성취도는 깎이지 않는다" 는 동기화로 기록이 사라지는 것을 막는 원칙이지
  못 읽은 것을 맞혔다고 세라는 뜻이 아니다)
- **오답 유형은 안 붙인다** — 무엇을 잘못 골랐는지가 없다. `classifyMistake` 가 빈 답에
  이미 `null` 을 돌려줘서 코어는 안 고쳤다
- 기각 — **이벤트를 아예 안 남기기.** FSRS 가 그 카드를 영원히 신규로 보게 되고,
  모른다고 인정한 대가로 아무것도 못 얻는다

### 훑어보기 이름 — 「미리보기」도 안 된다

사용자가 "학습하지 않아 뜻을 아는지 모르는 상태니 미리보기로" 라고 제안했으나 전제가 틀렸다.
`frequentIdioms` 가 `c.wrong <= 0` 을 걸러내므로 **읽기로 실제 풀었고 한 번 이상 틀린 숙어만**
들어간다 (2026-09-12 사용자 요청으로 좁힌 조건이다).

- 「복습하기」 — 앱에서 복습은 기한이 찬 카드를 다시 푸는 것(FSRS)이다. 훑어보기는 채점이 없다
- 「미리보기」 — 이미 만났고 틀린 것들이라 미리 보는 게 아니다
- 방향 — 대상이 드러나는 이름 (「틀린 것 다시 보기」 류). P2 로 미뤘다

### 설정에 들어가기만 해도 구글 화면이 떴다 (2026-09-14)

`BackupSetting` 이 마운트될 때 `wasSignedIn()` 이면 `restoreSession()` 을 불렀다.
`prompt: ''` 라 조용히 풀릴 줄 알았는데, **GIS 가 조용히 못 풀면 계정 선택 화면을 띄운다.**
설정을 열었을 뿐인데 구글 화면이 뜨는 셈이었다.

자동 복구를 없애고 **버튼을 눌렀을 때만** 시도한다. 누를 때는 전에 연결한 적이 있으면
`restoreSession` 을 먼저 시도하고, 안 풀릴 때만 `signIn` 으로 넘어간다 —
조용히 풀리는 경우에는 여전히 팝업이 없다.

- 대가 — 전에 로그인했던 사람이 매번 한 번 누른다. 버튼 문구를 「Google 다시 연결」 로
  바꾸고 "전에 연결해 두셨어요" 를 적어 그 한 번이 뜻밖이지 않게 했다
- **누르는 건 의도한 행동이고 뜨는 건 아니다.** 구글 화면이 불쑥 뜨는 쪽이 더 나쁘다

### 홈 마감 — 아이콘만으로는 안 보인다

- 설정 ⚙ 에 「설정」 글자와 테두리(pill)를 붙였다. 아이콘만 두면 눈에 안 띈다는 지적
- 「다시 풀기」 → **「재도전」**. 수는 뱃지로 빼서 이름·수·설명 세 줄로 세웠다
- 안내서의 「재대결」 도 화면과 맞췄다

### `.btn` 은 높이가 고정이다 (2026-09-14)

「틀렸던 것」 버튼의 설명 줄이 **박스 밖으로 흘러나왔다.** 원인은 공용 `.btn` 이
`height: var(--tap)`(52px)으로 **고정**이라는 것 — 여러 줄을 넣으면 넘친 줄이 그냥 삐져나온다.

- `height: auto` + `min-height: var(--tap)` 로 풀었다. 탭 영역은 지키면서 내용이 자란다
- 겸사겸사 세 줄(이름/수/설명)을 두 줄(이름+수 / 설명)로 압축했다
- **공용 클래스에 여러 줄을 넣을 때는 높이부터 본다.** `.btn` 은 한 줄을 전제로 만들어졌다

### 홈은 런처로 남는다 — 회고는 리포트 하나 (2026-09-14, 사용자 결정)

"홈에 마이페이지 같은 걸 같이 띄워도 되나" 라는 질문에서 나온 결정.

**정한 것 — 홈은 런처다. 자기 기록을 돌아보는 자리는 진단 리포트 하나로 둔다.**

- 이건 배치가 아니라 **정보 구조** 문제다. 리포트가 PLAN §7 의 "이 앱의 얼굴" 이고
  지금 수준·처방·오답 유형 분포·취약 음독이 전부 거기 있다. 홈에 얹으면 같은 숫자가
  두 군데 있거나 리포트를 열 이유가 없어지거나 둘 중 하나다
- **홈은 2초 안에 세션을 시작하는 자리, 리포트는 일부러 가는 자리다.** 합치면 앱을 켤
  때마다 자기 평가가 먼저 온다. 정답률이 71% 인 날 켤 때마다 71% 가 먼저 보이는 건
  "성취도가 깎이면 학습 의욕에 영향" (PLAN §5 원칙 3) 과 결이 어긋난다
- 지금 홈의 「이번 세션 20장 · 복습 기한 4」 는 **평가가 아니라 할 일**이다. 그 성격을 지킨다
- 미루는 비용이 0 에 가깝다는 것도 근거다 — 홈은 라우터도 레이아웃 시스템도 없는 평평한
  컴포넌트라 나중에 요약 블록을 얹는 건 `<section>` 하나다
- 수요 근거도 아직 없다. 첫 테스터의 요청은 전부 조작·진입이었다

**이 줄이 있는 이유** — 나중에 누가(에이전트 포함) "홈에 통계를 얹자" 고 할 때 처음부터
다시 논의하지 않으려고 적어 둔다. 다시 열려면 **수요 근거를 먼저 가져온다.**

### 아직 안 정한 것 — 내비게이션 모델

지금은 상태 기반 화면 전환이고 각 화면이 자기 「‹」 를 단다. 하단 탭 같은 셸로 가면
`App` 과 모든 화면의 뒤로가기를 손대야 한다 — **이건 진짜 뼈대라 늦게 바꾸면 비싸다.**
UI 전용 세션에서 먼저 정할 것.

### 하단 탭 — 언제 결정해야 하나 (2026-09-14, 사용자 요청)

하단 탭을 염두에 두고 있다. **작업 중 아래 신호가 보이면 먼저 알린다.**

**지금 비용 (2026-09-14 실측)** — 화면 10개, 그중 `screen-bar`(자기 「‹」)를 직접 그리는 게
5개(Diagnostic·Feedback·OnyomiMap·Report·Settings), `App` 이 넘기는 `onBack`/`onExit` 이 10개.
라우터가 없어 셸을 바꾸면 이 10개의 관계를 다시 짠다. **지금은 반나절 규모다.**

**결정을 앞당겨야 하는 신호 — 하나라도 걸리면 알린다**

1. **화면이 13개를 넘을 때.** 지금 10개다. 화면마다 뒤로가기 관계가 하나씩 늘고,
   비용이 화면 수에 비례한다
2. **「어디서 왔는지」를 기억해야 하는 화면이 셋째로 생길 때.** 이미 둘이다 —
   다시보기(`browseFrom`, 홈·리포트 양쪽에서 진입), 피드백(설정으로 복귀).
   세 번째가 나오면 그건 상태 기반 전환이 한계에 왔다는 뜻이다
3. **뒤로가기가 두 단계 이상 쌓일 때.** 리포트 → 다시보기처럼 지금은 한 단계다.
   두 단계(A → B → C 에서 C 가 B 로, B 가 A 로)가 생기면 스택이 필요해지고,
   그때는 탭이냐 라우터냐를 먼저 정해야 한다
4. **같은 화면을 두 자리에서 띄워야 할 때** — 예: 리포트를 탭으로도, 세션 끝에서도.
   지금 구조로는 복귀 분기가 또 늘어난다
5. **브라우저 뒤로가기 요구가 들어올 때.** 지금은 히스토리를 안 쓴다. 테스터가
   "폰 뒤로가기를 눌렀더니 앱이 닫혔다" 고 하면 그게 신호다

**신호가 없으면 안 건드린다.** 탭은 화면이 적을 때 넣으면 빈 탭이 생기고, 그건 지금
홈을 정리한 방향과 반대다.

### 요미가나는 네이티브 ruby 를 안 쓴다 (2026-09-14, 실기기 확인)

「PC 에서는 적당한데 모바일에서 멀다」 는 보고에서 시작해, 네이티브 `ruby` 배치를
버리고 `<ruby>` 를 `inline-flex` + `column-reverse` 로 직접 쌓는 것으로 바꿨다.

**왜 행간 조정으로는 못 고쳤나.** 한자와 rt 사이의 틈은 행간이 아니라 폰트
메트릭에서 온다. Noto Sans JP 는 ascent 가 1.16em 인데 한자 글리프는 baseline 위
0.88em 까지만 차므로, 한자 윗변 위에 **항상 0.28em 의 빈 공간**이 남는다.
네이티브 ruby 는 그 위에 얹히므로 행간을 0 으로 만들어도 이 여백은 안 없어진다.
게다가 남는 행간을 다루는 방식이 엔진마다 달라 — Blink 는 여백 안으로 rt 를 끼워
넣고 WebKit 은 쌓은 뒤 행간을 더한다 — 같은 CSS 가 PC(Chrome)와 iOS 에서 다르게
보였다. **첫 시도(`rt { line-height: normal }` + 행간 1.9 → 1.6)는 헛다리였다.**

**기각한 대안.**
- `rt { margin-bottom: -Xem }` / `transform: translateY()` — `display: ruby-text`
  상자는 마진·트랜스폼 적용이 엔진마다 다르다. 지금 문제가 바로 엔진 차이인데
  해법이 또 엔진 차이에 기대면 안 된다
- `<ruby>` 를 버리고 div 두 겹으로 — 간격은 잡히지만 의미와 복사 동작을 잃는다.
  flex 로 바꾸면 마크업은 그대로 두고도 같은 제어력을 얻는다

**값은 눈대중이 아니라 실측으로 정했다.** iPhone 폭(375px)에 실제 서브셋 폰트를
올리고 캔버스 `actualBoundingBox` 로 **잉크 기준** 간격을 쟀다. 상자 기준으로
재면 ascent 여백 때문에 실제 보이는 간격과 어긋난다.

| | line-height / gap | 잉크 간격 | 실기기 반응 |
|---|---|---|---|
| 네이티브 ruby | — | 0.124em | 멀다 |
| flex 첫 값 | 0.92 / 0.01em | 0.059em | 조금 붙는다 |
| **확정** | **0.95 / 0.025em** | **0.081em** | 만족 |

간격을 만드는 값은 `.headword.has-ruby ruby` 의 `line-height` 와 `gap` 두 개뿐이다.
다음에 조정할 일이 생기면 `gap` 만 건드린다. 다섯 군데(알아 두기·뜻·다시보기·
오답 상세·정답 카드)가 같은 클래스를 쓰므로 한 곳만 고치면 전부 따라온다.

### 같은 표기를 가진 항목 — 중복과 동형이독은 다르게 다룬다 (2026-09-14)

검수 배치 20 에 裏面 이 두 줄(うらめん/りめん) 보인다는 보고에서 시작했다.
세어 보니 성격이 다른 두 가지가 섞여 있었다.

**1. 표기도 읽기도 같은 쌍 — 69개.** JMdict 에서 한 말이 두 id 로 갈라져 들어온
것이다. 짝은 늘 「검수된 쪽」 + 「한국어 뜻도 분류도 없는 껍데기」 이고, 전부
밴드 4 안에 있었다 (밴드 0~3 은 원래 0). `build-runtime-dict.ts` 가 빌드에서
하나만 남긴다 — **밴드 낮은 쪽 → common → 작은 id** 순. 마지막 id 비교는
동점을 없애려는 것이다. 입력 순서에 결과가 흔들리면 안 된다.

**2. 동형이독 (市場 いちば/しじょう) — base 12그룹, 밴드 4 까지 749그룹.**
지우지 않기로 했다. 한쪽을 지우면 멀쩡한 단어가 학습 대상에서 빠진다.

대신 **채점이 둘 다 받는다** (사용자 결정). 근거는 화면이다 — 읽기 카드는 한자만
보여 주므로 사용자는 둘 중 어느 쪽을 묻는지 알 방법이 없다. 그런데
`isCorrectReading` 은 그 항목의 읽기 하나와만 비교했다. 市場 에 いちば 라고 쓰면
오답이 되고 **오답 유형까지 붙어** 진단 리포트(그 유일한 입력원)가 오염됐다.

사전에 `altReadings` 를 실어 `isCorrectReading(expected, answer, alts)` 이 쓴다.
밴드 4 까지 합쳐 계산한다 — 밴드 4 를 안 켠 사용자가 그 읽기를 써도 일본어로는
맞는 답이라 오답 처리할 이유가 없다. 그래서 base 항목 368개에 붙었다
(base 안에서만 겹치는 12그룹보다 훨씬 많다).

정답으로 치되 **이 카드가 묻는 읽기를 피드백에 한 줄로 알려준다.** 없으면
「정답」 옆 루비가 내가 안 쓴 글자라 헷갈린다.

**기각한 대안.**
- 한쪽만 남긴다 — 가장 단순하지만 멀쩡한 단어가 학습 대상에서 빠진다
- 읽기 두 개를 가진 한 카드로 합친다 — 정확하지만 스키마·채점·리포트가 모두 바뀐다

**남은 구멍 (보류).** `altReadings` 쌍 387개 중 **12개**는 분류기가 규칙형 오답으로
판정하는 차이다 — RENDAKU 7 (安心 あんしん/あんじん, 一見 いっけん/いちげん,
境界 きょうかい/きょうがい …), CHOON 4 (前世 ぜんせい/ぜんせ, 二世 にせい/にせ,
保安 ほあん/ほうあん …), SOKUON 1 (真先 まっさき/まさき). 이 12개는 이제 조용히
정답 처리되어 **진단 기회가 사라진다.** 나머지 375개(MIXED_READING 255,
ONYOMI_CHOICE 120)는 애초에 다른 단어라 문제가 아니다.
막으려면 빌드에서 규칙형으로 판정되는 대체 읽기만 `altReadings` 에서 빼면 된다.
連濁·促音·長音은 이 앱이 가르치는 핵심이라 그쪽이 맞다고 보지만, 사용자가 「둘 다
정답」으로 결정한 범위를 조용히 좁히지 않으려고 그대로 뒀다. **다음에 정한다.**

**안 한 것.** 한 세션에 같은 표기를 두 번 안 내보내는 처리는 넣지 않았다.
base 에 12그룹뿐이라 20문항 한 세션에 둘 다 들어올 확률이 낮고, 채점이 이제
어느 쪽이든 받으므로 남는 피해가 "같은 한자를 두 번 본다" 뿐이다. 실제로 목격되면
그때 넣는다.

### 동형이독 이어 묻기 — 3 + 1 (2026-09-14, 사용자 결정)

「다른 읽기도 아는지 다시 묻고 검증하는 방법」 요청. 앞 절에서 채점이 둘 다 받게
해 놓았더니 **다른 쪽을 아는지는 끝내 모른 채** 지나가는 문제가 남았다.

**먼저 확인한 것 — 뜻도 예문도 구분에 못 쓴다.** base 의 12그룹 전부 예문이 두 id 에
똑같이 붙어 있고(표면형으로 매칭하니 당연하다), 뜻도 절반 이상이 사실상 같다
(市場 시장/시장, 裏面 뒷면·이면/뒷면·이면, 木彫 목각/목각). 그래서 「뜻을 같이
보여줘 어느 쪽인지 알리기」 는 기각했다. 애매함이 구조적이다.

**설계의 축 — 두 읽기는 이미 각각 별도의 숙어다.** 市場 いちば 는 `1308300`,
しじょう 는 `1308305` 이고 `cardKey(idiomId,'reading')` 로 FSRS 상태도 따로 갖는다.
그래서 이건 새 카드 종류를 만드는 일이 아니라 **이미 있는 상대 카드를 지금 꺼낼까**
의 문제다. 스키마에 더한 것이 없다.

**채택 (3 + 1).**
- **3. 문제에 제외 조건을 쓴다** — 「いちば 말고 다른 읽기를 써 보세요」.
  이게 없으면 상대 카드가 나와도 화면이 똑같아 같은 답을 또 쓴다
- **1. 그 자리에서 이어 묻는다** — 넘어가지 않고 입력을 한 번 더 받는다.
  맞히면 이 카드 정답 + **맞힌 쪽 숙어에도 정답 이벤트**를 남겨 그 카드의 FSRS 도
  같이 전진시킨다

**기각한 대안 2 (상대 카드를 세션 뒤에 끼워 넣기).** 3 없이는 무의미하다 —
상대 카드의 문제 화면이 똑같이 생겨서 같은 답을 또 쓰게 된다.

**함정 하나.** 이어 묻기에서 **또 대체 읽기를 쓰면** 분류기가 돌아 엉뚱한 유형이
붙는다. 읽기를 잘못 고른 게 아니라 다른 쪽을 못 꺼낸 것이라 유형을 붙이면 오답
분포가 거짓이 된다 — 「모르겠어요」 와 같은 이유로 막았다(`classify: false`).
**화면만 막으면 안 된다.** `recordReadingAnswer` 가 유형을 다시 계산하므로 화면과
기록이 어긋난다. 그래서 `classify` 를 이벤트까지 넘긴다. 진단 리포트의 입력원은
화면이 아니라 기록이다.

**검증을 결정적으로 만든 방법.** 동형이독 카드를 세션에 띄우는 건 원래 운이지만,
`代替` 는 だいたい 가 `替:on:たい`, だいがわり 가 `替:kun:かわり` 를 **각각 혼자**
쓴다. 오답 하나만 심으면 재도전 세션에 그 카드 한 장이 뜬다. 진단 완료 플래그
(`yomenai:diagnosticDone`)도 같이 세워야 한다 — 안 그러면 홈이 첫 진입 화면이라
「틀렸던 것」 줄이 아예 안 그려진다. `tests/e2e/homograph-followup.spec.ts`.

### 「읽기 둘」 카드로 유형을 분리했다 (2026-09-14, 사용자 결정 — 앞 절을 대체)

앞 절의 **조건부 이어 묻기는 폐기했다.** 사용자가 두 번에 걸쳐 구멍을 짚었다.

**1차 지적 — 「판독은 애매하네」.** 이어 묻기가 *다른 읽기를 썼을 때만* 발동했다.
자기 읽기를 먼저 쓰면 안 묻고 다른 읽기를 쓰면 물어서, **같은 실력이 순서에 따라
다르게 처리됐다.** 게다가 첫 답이 맞는 읽기였는데 두 번째를 못 쓰면 카드가 오답이
됐다 — 「이 카드 오답」 이 「못 읽었다」 를 뜻하지 않게 되어 리포트에서 그 오답이
무엇인지 말할 수 없었다.

**2차 지적 — 「한쪽 표현만 계속 쓸 수도 있는데 오답처리하는 것도 이상하지 않아」.**
맞다. 대칭으로 고쳐도 한쪽만 아는 사람은 **맞는 읽기를 쓰고** 밴드 정답률이 깎인다.
「지금 수준」 은 읽기가 흔들리는 지점을 재는 그래프인데 못 읽은 게 아닌 걸 흔들림으로
세게 된다.

**해법 — 유형을 따로 묶는다 (사용자 제안).** 애매함의 뿌리는 *답이 하나인 카드
모델에 답이 둘인 말을 끼워 넣은 것* 이었다. 동형이독은 세션에서 한 장으로 접어
**처음부터 둘 다 묻는** 별종 카드로 낸다.

기록 규칙이 한 줄로 줄었다.

| 답 | 남는 기록 |
|---|---|
| 쓴 읽기 | 그 숙어 카드에 **정답** |
| 못 쓴 읽기 | **무기록** — 틀린 게 아니라 아직 안 배운 것 |
| 어느 읽기로도 못 읽음 | 서빙된 카드에 **오답 + 유형** |

**오답은 이제 언제나 「이 표기를 어떤 읽기로도 못 읽었다」 만 뜻한다.** 그래서
정확도 그래프를 따로 손볼 필요가 없다 — 특례 집계도, 예외 필드도 없다.

**기각한 대안 (별도 화면으로 빼기).** 화면이 11번째가 되어 하단 탭 결정 신호에
가까워지고, UI 는 별도 세션에서 다루기로 한 방침과 부딪힌다. 더 큰 문제는 市場·
二度·中継·代替 가 밴드 0~1 의 흔한 말이라, 옵트인 묶음으로 빼면 정규 학습에서
통째로 빠진다는 것이다.

**스키마는 안 건드렸다.** 이벤트는 여전히 각 숙어의 `reading` 이벤트이고,
「읽기 둘」 인지는 사전(`altReadings`)과 풀에서 파생한다 — 소개 카드가 `introIds`
로 하는 것과 같은 방식이다. 세션에서 짝을 **접어야** 한다(`foldHomographs`).
안 접으면 방금 다 물어본 표기가 세션 뒤에 또 나온다.

**남긴 특례 하나.** 상대 숙어가 풀에 없으면(밴드 4 만 가진 읽기 — base 368개 중
344개가 이 경우다) 물어볼 카드가 없다. 그때는 보통 카드로 두고 `altReadings` 정답
특례를 그대로 쓴다. 맞는 읽기를 오답으로 만들지 않는 쪽을 택했다. 앞 절에 적은
규칙형 오답 12건이 가려지는 구멍은 여기 남아 있다.

### 세션 인계 (2026-09-14)

이어받는 세션이 볼 곳은 `checklist.md` 맨 앞 **「지금 여기」** 절이다. 거기에 상태와
결정 대기 항목만 짧게 둔다 — 끝난 일은 Phase 절에, 근거는 이 파일에 있다.
그 절은 지우지 말고 **내용을 갈아끼운다.**

**오늘(2026-09-14) 한 일** — 커밋 `ec70a27`..`6151e72`.
테스터 피드백 P0/P1/P2 반영 → 홈 정리 → 요미가나 간격 → 동형이독 → 검수 배치 19·20.

**이어받을 때 특히 조심할 것 두 가지.**

1. **배포 확인.** 같은 함정(새 문구가 옛 문구의 부분 문자열)에 두 번 빠졌다.
   `checklist.md` 의 「배포 확인 방법 (함정)」 을 먼저 읽는다. 사전은 해시 없는
   고정 경로라 확인 방법이 또 다르다
2. **패치 스크립트의 백슬래시.** heredoc 으로 `.mjs` 를 쓸 때 `\` 가 `\` 로 접히고
   백틱이 파싱을 깬다. 앵커에 이스케이프를 쓰지 말고, 줄 배열 + `String.fromCharCode(10)`
   으로 잇는다. 오늘도 두 번 걸렸다

### 「읽기 둘」 카드 — 내부 스크롤 지적 (2026-09-14)

테스터 피드백이 아니라 사용자가 직접 UI 를 보고 지적했다. 「읽기 둘」 문제 화면이
헤드워드 + 안내 문장(`follow-up`, 최대 2줄) + 진행률 줄까지 얹혀 `.card-body` 의
가변 영역을 넘쳤다. 넘치면 `overflow-y: auto` 가 스크롤을 열어주긴 하는데(설계상
의도된 동작, PLAN §7), **그 스크롤 자체가 잘 안 보인다** — 모바일 브라우저는 스크롤
중에만 얇은 바를 보여주므로 사용자 입장에선 "내용이 잘렸다"로 읽힌다.

**대안 두 가지를 사용자에게 물었다.**
1. 내용을 압축해 애초에 안 넘치게 한다
2. 스크롤은 두고 그라데이션·화살표 같은 "더 있다" 표시를 붙인다

**채택 — 1번(압축).** 사용자가 "읽는 방법이 두 개라는 것을 레이블링 해서 간소화"를
명시적으로 골랐다. 2번(스크롤 표시 추가)은 레이아웃을 더 건드리는 쪽이라 기각.

**적용.** 「읽기가 둘이다」 + 진행률(`n/total`)을 본문 문장에서 빼고 카드 헤더의
태그 한 줄로 옮겼다(`읽기 둘 · 1/2`) — 피드백 상태의 `밴드 N` 태그와 같은 자리,
같은 스타일(`band-tag`, 오른쪽 정렬). 본문(`follow-up`)에는 **이미 쓴 답을 빼라는
제외 조건 한 줄만** 남긴다(`given.length > 0` 일 때만 렌더) — 처음 질문(0/2)일 때는
본문에 아무 문장도 없다. `.follow-up-count` CSS 는 쓰는 곳이 없어져 지웠다.
[ReadingCard.tsx](src/study/ReadingCard.tsx#L59-L71,L153-L165)

이어서 문구를 한 번 더 줄였다("…은 맞았어요 — 그것 말고 나머지 하나를 써 보세요."
→ "그것 말고 — <읽기>", 사용자 지적: 그래도 장황함) — `tests/e2e/homograph-dual.spec.ts`
가 옛 `.follow-up` 문구로 검증하던 것도 헤더 태그 기준으로 맞췄다(지난 커밋에서
놓쳤던 반영).

**진짜 원인은 압축이 아니라 `.card-body` 의 `justify-content: center` 였다
(2026-09-14, 이어지는 지적).** 문구를 아무리 줄여도 "iPhone SE 에서는 여전히
공간이 모자라" — 사용자가 소프트 키보드가 차지하는 공간을 UI가 간과했다고 짚었다.
Playwright 로 iPhone SE 폭(375)에 iOS 기본 키보드 높이(216~291pt)만큼 줄인
뷰포트를 흉내 내 실측했다 — 문제 화면(0/2, 2/2)은 괜찮지만 **피드백 화면(정답·
오답)이 `.card-body` 를 넘쳤고, `getBoundingClientRect` 로 보니 루비(rt) 상단이
`.card-body` 상단보다 위(음수 오프셋)에 렌더되고 `scrollTop` 은 이미 0** — 즉
**아래로도 위로도 스크롤해서 닿을 수 없는 채로 잘려 있었다.** `justify-content:
center` 가 넘칠 때 초과분을 앞뒤로 반씩 밀어내는데, `safe` 없이는 브라우저가 그
음수 오프셋 쪽을 스크롤 범위에서 제외한다 — `.summary-screen` 에 이미 있는 처방
(Phase 11 주석)과 같은 문제였는데 `.card-body` 에는 그때 안 옮겨 놨던 것.

**처방 — `justify-content: safe center`.** [study.css:142-155](src/study/study.css#L142-L155)
한 줄. 적용 후 재측정: 루비 상단이 `.card-body` 상단보다 아래로 내려와(130 > 112)
스크롤 범위 안으로 들어왔다. 넘치는 양 자체는 늘었다(64px, 키보드가 두꺼울 때
139px까지) — **당연하다, 전에는 그 초과분이 음수 쪽으로 잘려 계산에서 빠져
있었을 뿐** 이지 내용이 줄어든 게 아니다. 이제는 전부 아래로 스크롤해서 닿는다.
단위 405 · e2e 17(카드 전환 150ms 포함) 전부 통과 확인.

**남은 것.** 키보드가 두꺼울 때(291pt, 예측 바 있는 경우) 오답 카드는 여전히
104px 넘친다 — 스크롤로는 닿지만, 이 세션 앞쪽에서 지적된 "스크롤이 있어도 눈에
잘 안 띈다" 문제는 피드백 화면엔 아직 손 안 댔다. 필요해지면 다음 세션에서.

### follow-up/rule-hint 상자를 캡션으로 (2026-09-14, 이어지는 지적)

`safe center` 로 도달은 가능해졌지만 사용자가 다시 짚었다 — "한자 밑에 나오는
텍스트를 다른 방식으로 노출하는 방법을 찾아야겠어." 확인해 보니 `.rule-hint`/
`.follow-up` 설명 문장을 가리키는 것이었다.

**대안을 물었다.**
1. 상자(배경·패딩) 없이 캡션처럼 — 헤드워드 바로 아래 가벼운 한 줄
2. 스크롤 대상인 `.card-body` 밖, 고정 하단 영역(`.card-bottom` 위)으로 이동

**채택 — 1번.** 사용자가 "가장 단순"한 쪽을 골랐다. 2번(고정 영역 이동)은
레이아웃 구조를 더 건드리는 데다 하단 블록이 늘 커지는 대가가 있어 기각.

**「자세히」 뒤로 다시 숨기는 건 처음부터 후보에서 뺐다** — 2026-09-13 절
"규칙 해설을 「자세히」 밖으로 꺼냈다" 에서 이미 시도하고 되돌린 대안이다
(탭 뒤에 숨겼더니 세 관문 중 하나만 더 있어도 거의 안 열렸다). 상시 노출은
그대로 두고 **상자 크기만** 줄이는 쪽으로 좁힌 이유다.

**적용.** 배경(`--surface-2`)·패딩(10px 12px)·`border-radius` 를 빼고
`margin-top` 도 줄였다(rule-hint 10→8px, follow-up 18→10px).
[study.css:843-869](src/study/study.css#L843-L869) — `.md-rule`(오답 상세
오버레이)은 안 건드렸다, 거기는 스크롤 문제가 없다.

**재측정(iPhone SE 폭 + 키보드 높이 시뮬레이션).** `.card-body` 초과분이
줄었다 — OK 64→42px(얇은 키보드) / 139→117px(두꺼운 키보드),
NG 29→7px / 104→82px. **완전히 없어지진 않았다** — 남은 초과분은 상자
크기가 아니라 tts 버튼·예문이 차지하는 실제 콘텐츠 높이라, 상자를 더
줄여도 안 줄어든다. 단위 405 · e2e 17 통과.

### 「읽기 둘」 안내를 문장에서 **답 칸 두 개**로 (2026-09-15)

이어받은 세션이 지난 압축(2460b5a·be2e619)을 재검토하다 문구 결함을 찾았다.

**결함 1 — 뜻이 뒤집혀 읽힌다.** 남아 있던 한 줄이 `**그것 말고** — <읽기>` 였다.
한국어 「A 말고 B」는 "A가 아니라 B"라, 지시어(`그것`)가 앞·지시 대상이 뒤에 오는
이 어순에서는 **"그거 말고 だいがわり를 써라"** 로 읽힌다 — 의도와 정반대다. 압축 전
문구(`だいがわり 은 맞았어요 — 그것 말고 나머지 하나를`)에는 선행사가 앞에 있어서
성립했는데, 줄이면서 순서가 뒤집혀 지시 관계가 깨졌다.

**결함 2 — 첫 답이 맞았다는 확인이 같이 사라졌다.** 첫 읽기를 맞히면
`answerDual` 이 피드백 없이 return 한다(`useStudySession.ts` 의 `got.length < all.length`).
화면은 같은 한자에 입력창만 비워진 상태라, 「맞았다」를 말해주는 게 태그 숫자
`1/2 → 2/2` 뿐이었다. **정답과 거부가 화면상 구분되지 않는다.** 뒤집힌 대로 읽고
같은 읽기를 다시 치면 `hit` 이 없어 정답 피드백으로 끝나고 둘째 읽기는 안 묻는다.

**후보 넷을 실제로 렌더해서 재 봤다.** 앱 CSS 를 그대로 물린 프로토타입을 375×376
(iPhone SE 폭 + iOS 키보드 291pt)에 띄우고 `.card-body` 초과분과 *마지막 요소 바닥에
남은 여유*를 같이 쟀다 — 초과분만 보면 오해한다. 현행은 초과 11px 이지만 잘리는 건
패딩이라 글자 여유가 1px 있었다.

| 후보 | 초과 | 글자 여유 |
|---|---|---|
| 현행 문장 | 11px | 1px |
| **A. 답 칸 두 개** | 20px | **9px** |
| B. 하단 3슬롯의 빈 왼쪽 칸 | 0 | 23px |
| C. 헤더 태그에 통합 (`읽기 둘 · <읽기> ✓`) | 0 | 23px |
| D. 문구만 교체 | 11px | 1px |

**채택 — A (사용자 선택).** 칸은 지시어가 없어 뒤집힐 여지가 없고, 확인(✓)과 아직
안 쓴 칸을 한 줄이 같이 말한다. **첫 질문(둘 다 빈 칸)에서도 답이 두 개라는 걸
보여준다** — 지금까지 첫 질문은 본문이 비어 있어 구석 태그가 유일한 신호였다.

**기각.** C·B 는 여유가 제일 크지만 정보가 구석·작다(13px, 12px). D 는 최소 변경이나
여유 1px 이 그대로다. **하단 고정 영역으로 빼는 안은 후보에 안 넣었다** — 2026-09-14
절에서 이미 기각(하단 블록이 늘 커진다). B 는 그것과 달리 **이미 비어 있는 슬롯**을
써서 블록이 안 커지는 변형이라 후보에 올렸다.

**적용 후 실측(프로토타입 아닌 실제 앱).** 1/2 초과 3px·여유 9px, 2/2 초과 4px·여유
8px — 현행(여유 1px)보다 나아졌다. `.follow-up` CSS 는 쓰는 곳이 없어져 지웠다.
단위 405 · e2e 17 통과.
[ReadingCard.tsx:161-175](src/study/ReadingCard.tsx#L161-L175) ·
[study.css:857-880](src/study/study.css#L857-L880)

**같이 확인한 것 (고치지 않음).** `justify-content: safe center` 는 `center` 를
**교체**해서 들어갔다. `safe` 를 모르는 브라우저는 선언째로 버리고 `flex-start` 로
떨어진다 — 잘림은 안 생기지만(오히려 안전한 쪽) 안 넘치는 카드의 세로 중앙 정렬이
사라진다. `center` 한 줄을 앞에 두면 막힌다. iOS Safari 지원 버전을 확인 못 해
단정은 피했다. `.summary-screen` 도 같은 모양이다.

**남은 병목은 여전히 피드백 화면**이다(초과 117px, 본문 높이 114px). tts 버튼·예문의
실제 콘텐츠 높이라 이 작업으로는 안 줄어든다.

---

## 읽기로 찾기 — 히라가나 → 한자 표기 검색 (2026-09-16, 사용자 요청)

**동기.** "세션에서 자꾸 비슷한 음에서 틀린다. 모양이 비슷해서 추측했는데 음이 틀려서
궁금증이 생긴다. 그래서 그 음으로 찾을 수 있는 표현을 한 번에 찾아보고 싶다."

오답은 **계기**일 뿐이고 검색은 그와 분리된 별개 행위다. 설계 과정에서 이 둘을 한
덩어리로 묶으려던 시도를 두 번 기각했다 (아래).

### 실측 — 이 화면이 성립하는 근거

| 지표 (밴드 0~3, 16,959 숙어) | 값 |
|---|---|
| 서로 다른 읽기 | 14,067 |
| 그중 숙어가 1개뿐인 읽기 | 12,115 (**86%**) |
| 동음 묶음 (숙어 2개 이상) | 1,952 |
| 묶음에 속한 숙어 | 4,844 (풀의 **29%**) |
| 묶음 크기 2 / 3 / 4 / 5 / 6+ | 1,392 / 356 / 116 / 44 / 44 |
| 한국 한자음까지 겹치는 묶음 | 376 (**19%**) |
| 최대 묶음 | `こうき` 11개 |

**한국 한자음이 결과를 가른다.** 동음 묶음 1,952개 중 1,576개(81%)는 한국 한자음만
붙이면 전부 구분된다 (光輝/광휘 · 好機/호기 · 高貴/고귀). 다른 앱이 11줄 벽을 주는
자리에서 이 앱은 갈라서 준다. `kanji.json` 의 `kr` 이 이미 있어 추가 데이터가 없다.
나머지 376개(後期/후기 · 後記/후기)는 한국어 지식이 안 통하는 구간이라 그 자체가 정보다.

**밴드 4 를 넣으면 그림이 무너진다.** 합치면 102,377 숙어 · 묶음 8,069 · 최대 27
(`こうき` 11 → 27). 검색은 밴드 0~3 고정으로 간다. `loadBand4Idioms` 는 지금 앱
어디서도 안 부른다 — 첫 소비자를 이 화면으로 만들지 않는다.

### 채택 — A. 보기만 하는 검색 (이벤트 0)

결과 줄을 눌러도 학습으로 안 이어진다. 훑어보기(`Browse`)와 같은 무게로 이벤트도
FSRS 도 안 건드린다.

**기각 — B. 「지금 풀어보기」.** 사용자 판단: *"검색해서 내용을 알게 된 사용자가 이걸
바로 문제로 푼다고 해도 그게 제대로 된 학습일까 싶다."* 답을 본 직후의 정답은 FSRS
입장에서 잡음이고, 그게 리포트 정답률·밴드별 통계로 굳으면 **진단 도구라는 포지셔닝이
무너진다.** 지금 이 앱은 무엇을 낼지를 전부 시스템이 고른다 (진단·세션·재대결·집중).
B 는 사용자가 임의로 큐에 집어넣는 첫 경로가 된다. 갈라서 집계하려면 `EventBase` 에
출처 필드가 필요한데 그건 스키마 증가 건이다.

**기각 — C. 「찜해 뒀다가 다음 세션에 섞기」.** 사용자 판단: *"효용이 얼마나 있는지
모르겠다."* append-only 라 담기·빼기가 이벤트 두 종류나 스토어 하나를 새로 부른다.
효용이 불확실한 기능에 스키마를 늘리지 않는다.

A 를 먼저 만들어도 B 를 나중에 붙일 때 버려지는 게 없다 — 결과 줄에 버튼이 하나 느는
것뿐이다.

### 기각 — 설계 과정에서 접은 두 가지 모양

**오답 상세 안에만 붙이는 안.** 「네가 쓴 읽기로 찾기」를 `MistakeDetail` 에 한 줄로
넣는 형태. `ReviewEvent.answer` 에 정규화된 입력이 이미 남아 있어 공짜로 되는데,
**오답을 내야만 열리는 자리**라 사용자가 원한 행위(생각날 때 스스로 찾기)를 못 담는다.
나중에 붙일 수는 있다.

**동음 묶음 브라우즈 화면.** 1,952 묶음을 크기순·이력순으로 훑는 목록. "한 눈에"
라는 말에서 출발했는데 사용자가 원한 건 "한 번에"였다. 1,952줄을 훑는 건 사전을 읽는
동작이라 궁금해서 한 번 찾아보는 동작과 목적이 다르다.

### 인덱스 — `pairIndex` 와 다른 축

`src/dict/readingIndex.ts` 는 `pairIndex.ts` 와 같은 모양이되 키가 다르다.

- `pairIndex` — 키가 `光:on:こう` **쌍**. `光栄`·`光線` 이 묶인다 ("이 한자가 이 음을
  갖는 다른 예")
- `readingIndex` — 키가 **단어 전체 읽기**. `光輝`·`好機` 처럼 한자가 하나도 안 겹치는
  것이 묶인다 ("이 소리로 읽히는 다른 단어")

**`altReadings` 도 키로 넣는다** (368 항목). 御前 을 `おんまえ` 로 찾으면 나와야 한다.
읽기가 여럿인 표기가 별도 엔트리로도 존재하는 경우(高見 たかみ/こうけん, 12건)와
별개 장치다.

**앞부분 일치까지 받는다.** 정확 일치만 받으면 다 치기 전엔 빈 화면이라 찾는 맛이
죽는다. 키 14,067개를 정렬해 두고 이진 탐색으로 구간을 잡는다 — 입력마다 전체 순회를
하지 않는다.

**쿼리 정규화는 `src/lib/readings.ts` 의 `toHiragana` 를 쓴다.** 빌드 스크립트용으로
만든 순수 모듈이고 런타임 의존이 없다. 입력창의 로마자 변환은 `wanakana` bind 라
(`KanaInput` 과 같은 경로) 카타카나·장음부만 여기서 접는다.

### 진입점 — 홈 `home-nav`

「내비게이션 모델(하단 탭)」이 미결이라 새 진입 방식을 만들지 않는다. 지금 셸의
리포트·음독 맵과 같은 줄에 한 칸을 더한다. 셸이 바뀔 때 같이 옮긴다.

**검색 범위를 화면에 적는다.** 검색창은 "아무거나 찾아도 된다"고 약속하는 물건인데
이 코퍼스는 한자 2자 이상 숙어뿐이다. 동사·형용사·가나 어휘·熟字訓이 없어
`よむ`·`ありがとう` 는 계속 빈 결과다. 안 적으면 고장으로 읽힌다.

### 입력 변환 — `bind` 가 아니라 `toKana`

`KanaInput` 은 `wanakana` 의 `bind` 로 요소를 잡고 값을 ref 로 읽는다 (비제어). 검색창은
**입력마다 결과를 다시 그려야** 해서 React state 가 값을 알아야 하는데, `bind` 는 자기
listener 로 `el.value` 를 고쳐 쓴다 — React 의 onChange 와 'input' 이벤트를 두고 순서가
엮여 한 글자씩 늦을 여지가 있다. 제어 입력 + `toKana(v, { IMEMode: 'toHiragana' })` 로
바꿔서 변환을 우리 핸들러 안에 두었다. `toKana` 는 미완성 로마자(`k`)를 그대로 두고
이미 가나인 글자는 통과시켜서 IME 처럼 동작한다 (API 확인: `wanakana/esm/index.d.ts`).

`bind` 를 쓰는 이유였던 것(카드마다 리마운트해도 포커스 유지)은 검색 화면엔 없다.
`inputMode="url"` 처방은 그대로 가져왔다 — iOS 에서 일본어 IME 후보 바를 막는 장치다.

### 진입점을 홈 상단으로 (2026-09-16, 사용자 요청)

처음엔 `home-nav` 의 넷째 칸으로 넣었는데 사용자가 **설정과 같은 층(상단 알약)** 으로
올리라고 했다. `.home-top` 이 `justify-content: flex-end` 로 설정 하나만 두던 줄이라
`space-between` 으로 바꿔 좌 = 읽기로 찾기, 우 = 설정이 됐다.

**맞는 위치다.** `home-nav` 의 리포트·음독 맵은 학습 결과를 보는 자리라 세션을 돌린
뒤에 의미가 생기는데, 읽기로 찾기는 오답과 무관하게 생각날 때 여는 자리다. 같은 무게로
늘어놓으면 성격이 섞인다. 설정·안내서처럼 "언제든 꺼내 보는 것" 쪽에 속한다.

아이콘만 두지 않고 글자를 붙인 것은 2026-09-14 설정 버튼과 같은 이유다 (아이콘만으로는
눈에 안 띈다). 알약 스타일은 `.home-gear, .home-find` 로 둘이 공유한다.

**미결로 남는 것** — checklist 의 「사용 안내서를 설정과 같은 층으로」는 상단에 아이콘을
몇 개까지 둘지의 문제고, 이번에 상단이 두 칸이 되면서 그 결정에 재료가 하나 늘었다.
내비게이션 모델(하단 탭)을 정할 때 셋을 같이 본다.

**아이콘은 인라인 SVG** (사용자 요청 2026-09-16 "단색으로 통일"). 처음 쓴 🔍 는 컬러
이모지라 옆의 ⚙ 와 톤이 안 맞았다. 폰트 글리프(⌕ U+2315)는 기기에 따라 두부가 될 수
있어 안 썼다. `stroke: currentColor` 인 인라인 SVG 로 그려서 알약 글자색·글자 크기를
그대로 따라간다 (`.pill-icon`). 이 앱의 첫 SVG 아이콘이다.

**같이 안 고친 것** — `🔊`(소리 듣기)는 Browse·IntroCard·MeaningCard·ReadingCard 에서
컬러 이모지로 남아 있다. 상단 알약 한 쌍의 톤 문제라 그 범위만 고쳤다.

## 개발 로직 캔버스 갱신 (2026-09-16)

`docs/개발로직.canvas` 를 709a5ff 이후 60여 커밋의 현재 상태로 다시 그렸다. 틀린 채
남아 있던 것은 서비스 워커 설명(`autoUpdate · skipWaiting` → 실제로는
`registerType: 'prompt'` + UpdateBanner 대기)과 동기화 구조(`reviews-{기기}.json` 단일
→ `backup.json` 하나 + `sync-{기기}.json` n개)였다. 둘 다 사고를 겪고 바꾼 부분이라
옛 그림을 보고 판단하면 같은 사고로 되돌아간다.

**추가한 노드** — 화면 쪽 Search·Browse·Feedback·UpdateBanner·IntroCard·「읽기 둘」
카드·SessionSummary, 코어 쪽 level·sessionSummary·diagnostic·onyomiMap 과 「카드
순간들」(echo·observe·ruby·readable·nextUp·surface·pick) 묶음, 빌드 쪽 한국음 감사
(kr/krOld)·뜻 검수 화면·build-event-worklist·build-icon·일회성 조사 도구, DB 쪽
localStorage 경계 노드, 동기화 쪽 assertBackupGrows.

**연결선을 전부 그리지 않기로 했다.** 빌드 파이프라인 왼쪽 줄기는 산출물 의존이 아니라
**실행 순서**로 읽고 (그룹 라벨에 명시), 화면 → 코어 호출도 `useStudySession` 두 개와
리포트 처방 하나만 남겼다. 대안은 실제 의존을 모두 선으로 긋는 것이었는데, 그룹이 다섯
줄로 늘어난 지금은 선이 남의 노드 상자를 관통해서 오히려 못 읽는다. 호출 관계는 각 노드
본문에 적어 두는 쪽이 정확도를 안 잃는다.

**localStorage 노드를 사용자 DB 그룹 안에 둔 이유** — 설정·테마·소개 여부·진단 완료는
스키마 불변 조건(CLAUDE.md)의 대상이 아니다. 밖에 두면 "왜 이건 이벤트가 아닌가"를
매번 다시 따지게 된다. 같은 그룹 안에 「이벤트 로그가 아니다」라고 붙여 두면 경계가
한눈에 보인다.

### 캔버스 정렬 — 참조본 지형 + 선이 지나갈 통로 (2026-09-17, 사용자 요청)

사용자가 옵시디언에서 직접 배치한 판본(`F:\DOCS\Projects\Yomenai 개발 로직.canvas`,
갱신 전 내용)의 **지형**을 저장소 캔버스에 옮겼다. 빌드는 좌상단, 정적 자산은 그 오른쪽,
런타임은 가운데, 코어는 우측, **사용자 DB·동기화는 하단**, 배포는 하단 가로줄이다.
열 x 좌표(-2840 / -2220 / -1240 / -335 / 465 / 1730 / 2120 / 960 / 1920)도 그대로 썼다.

**노드 사이에 통로를 비워 둔다.** 참조본을 재보니 `public/dict → load` 같은 긴 선이
화면 열의 빈틈을 지나가고 있었다. 우연이 아니라 그렇게 둬야 읽힌다. 새 노드를 넣으면서
그 빈틈이 막혀 선 9개가 남의 상자를 관통했다. 두 자리를 통로로 비웠다.

- 화면 열 `-1055..-800` — `public/dict → load`
- 화면 열 `-465..-335` — `public/fonts → ReadingCard`

**같이 옮긴 것** — `replay` 를 코어 둘째 열에서 첫째 열 맨 위로 (사용자 DB 가 아래
왼쪽이라 이벤트 선이 코어 열을 통째로 가로지르고 있었다. 관통 7건이 0이 됐다).
빌드 곁가지는 참조본처럼 셋으로 끊고 `build:event-worklist`·조사 도구·`build:icon` 은
아래 줄로 내렸다 — 곁가지가 길면 `build:runtime-dict → public/dict` 선을 막는다.

검증은 스크립트로 한다 (노드 겹침 · 그룹 밖 노드 · 엣지 관통 0건). 다음에 노드를 더하면
같은 검사를 돌려 통로가 막혔는지 본다. 파일은 옵시디언 저장 포맷(탭, 노드 한 줄, 같은
키 순서)에 맞춰 뒀다 — 앱에서 드래그해 저장해도 diff 가 안 튄다.

## 읽기 규칙을 앱 안에 둔다 (2026-09-17, 사용자 요청)

사용자 지적 — "연탁과 기타 발음 변형 규칙이 헷갈리는데 **원리를 모르니까 계속 틀린다**.
tip 에 정리해 놓은 것보다 내용이 풍부했으면 좋겠다."

지금 규칙 서술은 `MISTAKE_ADVICE` 6줄 + 안내서의 6종 표 한 줄씩이 전부다. 결과만 적혀
있고 왜 그런지가 없다. 연탁 줄이 그 예다 — "뒷 글자가 탁음으로, 이미 탁음이면 잘 안
일어남" 은 맞지만, **연탁이 왜 일어나는지**(두 낱말이 한 낱말이 됐다는 표식)와
**어디서 일어나는지**(훈독 복합어 위주, 음독 한자어는 거의 안 걸림)가 빠져 있다.

### 자리를 먼저 물었고, 기준을 바꿔 다시 답했다

처음 제안은 안내서(`guide.html`) + 오답 상세 링크였다. 사용자가 되물었다 —
"**효율을 따지지 말고 학습 측면에서** 고려하면?" 기준을 바꾸니 답이 뒤집힌다.

- **기각 — 안내서 단독.** 구조적으로 재방문이 안 일어난다. 새 탭으로 열리는 별개
  문서고 성격이 「앱 소개」라 처음 한 번 읽고 끝난다. 음운 규칙은 조금씩 여러 번
  만나야 절차가 되는 자료다. **자료가 앉은 자리가 재방문 빈도를 정한다**
- **기각 — 카드 아래 한 줄을 늘리기.** 2026-09-14 에 키보드가 올라오면 루비가 잘려서
  그 상자를 캡션으로 줄인 자리다. 글을 늘리면 그 문제가 그대로 돌아온다.
  카드 한 줄은 「무슨 유형인지」를 말하는 라벨로 남긴다
- **채택 — 앱 안 화면 + 오답 상세 심화.** 오프라인에서 열리고, 틀린 직후에 그 절로 바로
  갈 수 있고, 홈에서도 열린다. 같은 내용을 여러 경로로 여러 번 만난다

### 조건 — 읽을거리가 아니라 색인이어야 한다

절마다 그 규칙으로 내가 틀린 횟수와 실제 숙어를 얹는다. 그러면 갈 때마다 내용이 달라져
다시 열 이유가 생긴다. 해설만 있는 문서는 한 달 뒤에 아무도 안 연다. 거기서
「다시보기」로 이어지면 채점 없이 그 숙어들만 훑을 수 있다 — 기존 화면 재사용이다.

### 안 건드리는 것

- **규칙형 처방을 세션으로 실행하기** — [prescription.ts](src/core/prescription.ts) 에서
  이미 기각됐다("규칙과 방향은 세션으로 만들 수 없고, 만들면 그냥 평범한 세션이 된다").
  규칙 화면이 생겨도 처방의 그 칸은 「읽을 것」으로 남는다. 다만 **읽을 곳이 생긴다**
- **`RULE_MISTAKES` 경계** — 카드에 규칙 한 줄을 붙이는 기준은 그대로다.
  규칙 화면에는 `KO_INTERFERENCE` 절(종성 대응)이 있지만 카드에는 여전히 안 붙인다.
  2026-09-13 「아직 안 한 것」에서 후보로 적어 둔 규칙을, 카드가 아니라 화면 쪽에 낸다
- **텍스트가 산출을 빠르게 하지 않는다** — 2026-09-07 절의 결론이 유효하다.
  해설은 반복 횟수를 줄여줄 뿐이고 절차화는 대조 반복에서 온다. 규칙 화면은
  "반복을 줄이는 장치" 지 "반복을 대체하는 장치" 가 아니다

### 구현하며 바뀐 것 두 가지 (2026-09-17)

**1. 세션 중에는 규칙 화면으로 안 보낸다.** 계획은 「주 진입로 = 오답 상세」였는데, 오답
상세는 세션 안(`Study` 아래)이라 거기서 화면을 옮기면 `useStudySession` 이 언마운트된다.
답한 카드는 이벤트로 남아 기록은 안 잃지만 **남은 문제가 다시 뽑힌다** — 규칙 하나 읽자고
풀던 세션이 흐트러지는 건 교환이 안 맞는다.

- 세션 중 — 오답 상세가 그 절을 **인라인으로** 펼친다 (`.md-rule-more`). 나갈 일이 없다
- 세션 밖 — 홈 `home-nav`, 리포트 처방의 「이 규칙 읽기」가 화면으로 보낸다.
  리포트는 세션이 아니라 옮겨도 잃을 게 없다
- **기각 — 규칙 화면을 모달로 띄워 세션 위에 얹기.** 카드 위 오버레이는 이미 오답 상세가
  쓰고 있어 오버레이가 두 겹이 된다. 인라인이 같은 내용을 더 싸게 준다

덕분에 리포트 처방의 규칙 칸이 처음으로 **닿을 곳**이 생겼다. 세션으로 만들지 않는다는
[prescription.ts](src/core/prescription.ts) 의 판단은 그대로다 — 읽을 것은 읽을 곳으로 보낸다.

**2. `MISTAKE_ADVICE` 가 한국 자형으로 나가고 있었다.** 규칙 본문을 쓰다 발견했다.
해설 문장은 한국어 안에 일본어가 섞여 있는데(三日月 みかづき, 発達 はったつ … 35조각)
`.rule-hint`·`.md-rule-text` 둘 다 `lang="ja"` 없이 렌더했다. `:lang(ja)` 가 일본어 폰트를
거는 유일한 장치라([fonts.css](src/styles/fonts.css)), **규칙을 가르치는 문장이 정작 틀린
글자 모양을 보여주고 있었다.** CLAUDE.md 가 가장 강조하는 조건이다.

문장 단위로는 `lang` 을 못 붙이니 구간 단위로 가르는 `splitJa`([ja.ts](src/app/ja.ts)) 와
`Mixed` 를 만들어 규칙 본문·예시 설명·대조 설명·기존 해설 네 곳에 다 걸었다.
한글 호환 자모(ㄱ·ㄹ·ㅁ)는 U+3131~ 이라 범위 밖이다 — 받침 이름이 일본어로 안 샌다.
e2e 가 본문 속 `学` 의 computed font 에 `Noto Sans JP` 가 있는지까지 본다.

**남은 자리** — `guide.html` 의 6종 표에도 일본어가 있지만 거기는 `lang="ja"` 가 이미
붙어 있다. 앱 쪽에서 같은 구멍이 더 있는지는 안 훑었다.

## 탁음 세 갈래를 가른다 (2026-09-17, 사용자 지적)

사용자 질문 — "오답일 때 해당 음운 규칙을 연결할 수도 있나? 이건 기각된 의견인가?"
연결은 이미 있었지만 **정확도에 구멍**이 있다고 답했고, 사용자가 "구멍이 생각보다 크다,
수정하자" 로 받았다.

### 무엇이 틀렸나

`classifyMistake` 가 세 현상을 `RENDAKU` 한 유형으로 묶는다.

- 연탁 — 三日月 みかづき (뒷 글자가 탁해짐)
- 반탁 — 出発 しゅっぱつ (っ·ん 뒤 は행 → ぱ행)
- 연성 — 反応 はんのう (ん 뒤 모음 → な행)

묶는 것 자체는 의도였다. 소리가 탁해진다는 점이 같고 진단 축을 잘게 쪼개면 표본이 흩어진다.
문제는 **화면이 그 이름과 해설을 그대로 썼다는 것**이다. 반탁을 틀린 사람이 「연탁」이라는
이름과 연탁 설명을 받았다. 어제 만든 규칙 절 연결도 같은 이유로 연탁 절로 보냈다.

### 고친 방법 — 버리던 정보를 돌려준다

분류기는 이미 `differs('rendaku')` · `differs('renjo')` · `differs('handaku')` 로 **어느
축이 달랐는지 알고** 판정하면서 그 값을 버렸다. `explainMistake` 가 그걸 `voicing` 으로
같이 돌려준다. `classifyMistake` 는 `.type` 만 꺼내는 얇은 래퍼로 남겨 기존 호출부가 안 바뀐다.

- **이벤트 스키마는 안 건드렸다.** `mistakeType` 은 스키마 불변 조건(CLAUDE.md)이라 새 필드를
  넣지 않는다. 대신 이벤트에 `answer`·`expected` 가 남아 있어 **언제든 다시 매길 수 있다** —
  규칙 화면의 기록 색인이 그렇게 지난 기록까지 갈래별로 가른다. 마이그레이션이 없다
- 분해가 안 되는 답(문자열 경로)도 は행↔ぱ행이면 반탁으로 가른다. 연성은 이 경로에 안 온다 —
  の 와 お 는 청탁 짝이 아니라 `unvoiceAll` 이 같아지지 않는다. 테스트로 고정해 뒀다

### 어디까지 정확하게 하고 어디서 멈췄나

- **정확하게** — 카드의 이름(「반탁」), 카드 한 줄 해설, 오답 상세가 펼치는 절, 규칙 화면의
  기록 색인과 그 라벨
- **그대로** — 리포트의 오답 유형 분포와 처방. 이벤트에서 직접 세는 축이라 여기까지 가르려면
  replay 가 사전을 알아야 한다(순수 함수를 깨는 대가). 게다가 진단 축은 묶여 있는 편이
  낫다 — 표본이 셋으로 쪼개지면 「오답의 30%」 문턱을 늦게 넘어 처방이 늦게 뜬다.
  규칙 본문(반탁 절)에 "리포트에서는 한 칸으로 묶인다" 고 적어 사용자가 헷갈리지 않게 했다
- **`MISTAKE_ADVICE` 는 안 고쳤다.** Phase 12-E 문헌 대조를 거친 문장이라 그대로 두고,
  반탁·연성일 때만 해당 절의 요약으로 **바꿔 보여준다**(`mistakeHint`). 문장을 고치면
  검수를 다시 거쳐야 하고, 저 문장은 연탁 설명으로는 여전히 맞다

## 하단 탭으로 셸을 바꾼다 (2026-09-17, 사용자 결정)

사용자 지적 — "메인화면이 점점 길어진다. 정리가 필요하다."

### 먼저 잰 것

390px 기준 홈 실측. **첫 진입 894px / 화면 844px 로 50px 넘쳤다.** `.home` 은 스크롤이
없는 화면(flex 중앙 정렬)이라 넘친 만큼 그냥 잘린다. 돌아온 사용자는 844px 로 여유 0 —
뭐 하나만 더 붙으면 바로 넘어가는 상태였다.

내역에서 `home-nav` 가 232px(27%)인데 정작 본업인 「세션 시작」은 56px 이었다.
**메뉴가 동작보다 네 배 크다**는 게 문제의 모양이었다.

### 신호 셋이 이미 걸려 있었다

2026-09-14 「지켜볼 신호」 다섯 중 셋이 어제 규칙 화면을 만들면서 걸렸는데 못 알렸다.

1. **신호 2 — 「어디서 왔는지」 기억하는 화면이 셋째.** `browseFrom`(홈·리포트),
   피드백(설정 복귀), **`rulesFrom`**(홈·리포트)
2. **신호 3 — 뒤로가기 2단계.** 홈 → 리포트 → 규칙 → 리포트 → 홈
3. **신호 4 — 같은 화면을 두 자리에서.** 다시보기와 규칙 둘 다
4. 신호 1(화면 13개 초과)은 정확히 13개로 경계

사용자가 "효율 말고 학습 기준" 을 물었던 어제와 같은 방식으로, 이번엔 **범위**를 물었다.
선택지는 탭 전환 / 홈만 정리 / 홈 스크롤 허용 / 기록만. **사용자가 탭 전환을 골랐다.**

### 구조

- `tab`(4) + `sub`(탭 안 한 겹) + `flow`(탭바를 덮는 전체화면). 라우터는 여전히 없다
- **탭이 자리를 기억한다** — `browseFrom`·`rulesFrom` 같은 복귀 상태가 통째로 사라졌다.
  신호 2·3·4 가 구조적으로 해소된다
- **흐름에서는 탭바를 안 그린다.** 세션은 키보드가 올라오는 화면이라 하단에 탭이 깔리면
  답 버튼과 겹치고, 카드 전환 150ms 예산에도 불리하다
- 탭 루트 넷은 「‹」 를 뗐다. 돌아갈 자리가 탭바다

### 구현하며 막은 구멍

**읽기 규칙이 처방에서만 닿게 될 뻔했다.** 홈 메뉴에서 빼고 리포트 탭 아래로 옮겼는데,
리포트의 규칙 처방은 읽기 30회를 넘겨야 뜬다(`PRESCRIPTION_MIN_READINGS`). 그러면 **처음
틀린 날 규칙을 못 연다** — 규칙이 가장 필요한 날이 바로 그날인데. 리포트 하단에 「도구」
절(읽기 규칙·음독 맵)을 두고 **기록이 없어도 보이게** 했다.

### 결과

첫 진입 홈이 894px → **791px**, 넘침 0. 탭바 53px 를 쓰고도 100px 이 줄었다.
`home-nav` 232px 와 상단 알약 44px 가 통째로 빠진 자리다.

### 문구 — 전제를 버리고 일본어 한자를 앞에 세운다 (2026-09-17, 사용자 결정)

사용자 지적 — "지금은 뜻을 모르는 숙어도 많다. 일본어 한자를 잘 읽도록 돕는 서비스임을
표현하는 문장으로." **9월 13일에 범위를 「교정 + 습득」으로 넓혔는데 문구만 안 따라왔다.**
전제(「뜻은 아는데」)가 홈·안내서·매니페스트·PLAN·README 다섯 군데에 그대로 남아 있었다.

**홈 태그라인은 사용자가 직접 썼다 — 「일본어 한자, 당황하지 말자!」**
제가 낸 안 여덟 개(설명형 넷, 재치형 넷)를 다 물리고 사용자가 쓴 문장이다.
초안은 「겁내지 말자!」 였고 두 번 다듬어 여기까지 왔다.

- 제가 짚은 것 — 「겁내지 말자」는 사용자가 **겁내고 있다**고 전제한다. 대상은 N2 이상이라
  초급자용으로 읽힐 여지가 있었다
- 제가 낸 대안은 「막히지 말자!」 였는데 **사용자가 물렸다 — "막힌다는 표현은 수동적"**.
  맞는 지적이다. 막힘은 당하는 것이고 당황은 그 순간의 반응이라, 대처를 주는 앱의 자리는
  뒤쪽이다. 「겁내지도 않고 막히지도 않는다, 잠깐 당황할 뿐이다」 가 사용자가 잡은 눈금이다
- 반대쪽 근거 — 홈은 2초 안에 세션을 시작하는 런처다. 진단·리포트 쪽이 담백한 만큼
  현관에 기운이 있어도 결이 깨지지 않는다. 14자라 한 줄에 확실히 들어가는 것도 값이다
  (홈 여유가 0이라 두 줄이 되면 다시 넘친다 — `tab-shell.spec.ts` 가 지킨다)

**나머지 넷은 구호가 아니라 설명문으로 통일했다.** 설치 화면·안내서 제목은 처음 보는
사람이 읽는 자리라 무슨 앱인지 말해야 한다.

| 자리 | 전 | 후 |
|---|---|---|
| 홈 태그라인 | 뜻은 아는데 못 읽는 숙어를 바로잡아요 | 일본어 한자, 당황하지 말자! |
| PWA 설명 | 뜻은 아는데 못 읽는 일본어 숙어를 바로잡는 앱 | 일본어 한자 읽기를 진단하고 교정하는 앱 |
| 안내서 제목 | 뜻은 아는데 못 읽는 한자어를 고치는 앱 | 일본어 한자를 읽게 만드는 앱 |
| PLAN §0 · README | 뜻은 아는데 못 읽는 사람을 위한 일본어 앱 | 일본어 한자를 읽게 만드는 앱 |

안내서 머리글도 고쳤다 — 「뜻은 이미 상당 부분 알고 있습니다」 를 「뜻이 짐작되는 것도,
전혀 낯선 것도 섞여 있습니다」 로. **세그먼트 정의는 안 건드렸다** — 한자 문화권 중상급자
대상이라는 것과 「학습 앱이 아니라 진단 도구」 포지셔닝은 그대로다. 바뀐 건 전제지 대상이 아니다.

### 탭 이름 — 학습 → 홈 (2026-09-17, 사용자 지적)

"이 서비스 전체가 학습이기 때문이다." 맞는 지적이다. 탭 라벨은 **그 안에 뭐가 있는지**를
말해야 하는데 「학습」은 앱 전체를 가리켜 변별이 안 된다. 「메인」도 후보였지만 화면
컴포넌트가 `Home.tsx` 이고 문서가 이미 전부 「홈」이라고 불러서 새 낱말을 안 늘렸다.
내부 id 도 `learn` → `home` 으로 같이 바꿨다.

### 이 앱이 왜 있는지 — 사용자가 밝힌 출발점 (2026-09-17)

"일본어 소설을 읽을 수 있는 수준이다. 다만 한자어가 나오면 어떻게 읽는 건지, 뜻이
무엇인지 정확하게 모르는 표현이 나와서 **진행이 막히는** 경우가 발생한다. 그런 불편을
줄이기 위해 한자어만 따로 학습하기 위해 이런 서비스를 구상한 것이다."

PLAN §0 은 세그먼트(한자 문화권 중상급자)를 적어 뒀지만 **실제 사용 맥락**은 여기 처음
적힌다. 문서에 없으면 다음 세션이 「초급자에게 친절한 앱」 쪽으로 표류할 수 있다.

- **대상은 겁내는 사람도, 막히는 사람도 아니다.** 낯선 한자어에서 잠깐 **당황할** 뿐이다.
  초급자를 달래는 말투도, 수동적인 「막힌다」도 쓰지 않는다 (사용자 지적 2026-09-17)
- **성공은 「소설이 안 막히는 것」이다.** 앱 안의 정답률이 아니라 앱 밖의 읽기가 기준이다.
  세션 요약이 점수 대신 「오늘 맞힌 것으로 읽히는 문장」을 주는 설계(readable.ts)가 이 결과
  지표와 같은 방향이다 — 우연이 아니라 맞는 축이었다는 게 확인됐다
- **읽기로 찾기(2026-09-16)가 이 맥락의 직계다.** 읽다 막힌 한자어를 그 자리에서 찾는 것이
  원래 용도였다. 앞으로 기능을 저울질할 때 "읽다 막힌 자리를 얼마나 빨리 푸는가" 가 기준이 된다

## 2026-09-17 — 다시보기 카드에 규칙 배지 (사용자 요청)

「다시보기에서 분류해놓은 음운 규칙과 연결하는 것이 있으면 좋겠다. 상단의 1회 틀림과 같은
레벨로 배지 형식이 있으면 좋겠다.」

**무엇을 했나** — 카드 머리에 세 번째 배지를 달았다. 「다시보기 · 2회 틀림 · 반탁 규칙」.
누르면 그 절의 요약본이 카드 안에서 펼쳐진다.

**배지에 실을 오답을 어떻게 고르나** — 한 숙어를 여러 유형으로 틀리기도 한다. 배지는 하나뿐이라
**제일 자주 낸 갈래**를 고르고 동점이면 최근 것을 고른다 (`mistakeOfIdiom`). 최근 쪽을 택한 건
지금 상태에 가까운 것이 규칙을 읽을 이유가 되기 때문이다. 연탁·반탁·연성은 저장된 유형이 같아도
**서로 다른 갈래로 센다** — 읽을 절이 다르니 같이 세면 엉뚱한 절로 보낸다.

**규칙 화면으로 보내지 않고 카드 안에서 편 이유** — 다시보기는 캐러셀이라 나가면 넘기던 자리와
섞인 순서를 잃는다. 다시 들어오면 `pickBrowse` 가 다시 뽑아 같은 카드가 같은 자리에 없다.
오답 상세가 세션 큐 때문에 인라인을 택한 것과 같은 처방이다 (2026-09-17 앞 절). 전체 지도로 가는
길은 꼬리말 한 줄로 남긴다.

**기각한 대안**
- *배지에 규칙 절 제목을 싣는다* — 「촉음은 꼬리와 첫소리가 부딪힐 때 생긴다」 는 배지에 안 들어간다.
  짧은 이름(연탁·반탁·촉음)을 싣고 제목은 펼친 뒤 첫 줄에 둔다
- *안 누르고도 늘 펼쳐 둔다* — 다시보기는 읽기·뜻·소리를 보는 자리다. 규칙이 늘 펴져 있으면
  숙어가 아니라 해설을 읽는 화면이 된다
- *배지에 횟수를 같이 적는다 (「반탁 2회」)* — 옆 배지가 이미 「2회 틀림」이다. 같은 숫자가 둘이면
  다른 숫자로 읽힌다

**규칙 절은 카드 맨 아래다** (사용자 지적). 처음엔 예문 바로 아래에 뒀는데 그러면 「다음 예문」
버튼이 제 예문에서 떨어져 규칙 절의 버튼처럼 보인다. 버튼은 자기가 바꾸는 것 옆에 붙어 있어야 한다.
e2e 가 「.card-body 의 마지막 요소가 .browse-rule」 로 차례를 묶는다.

**갈래 매기기를 core 로 올렸다** — `Rules.tsx` 안에 있던 루프를 `voicingByEvent()` 로 빼
규칙 화면과 다시보기가 같은 함수를 쓴다. 두 곳에서 따로 매기면 언젠가 배지와 절이 다른 규칙을
가리킨다. 이벤트 스키마는 여전히 안 건드린다 — 답으로 다시 매기므로 지난 기록에도 소급된다.

**곁가지 하나** — 오답 상세 꼬리말이 「홈의 「읽기 규칙」」이라고 옛 자리를 가리키고 있었다
(하단 탭 전환 때 놓친 줄). 같은 꼬리말을 새로 쓰는 김에 「리포트의」로 고쳤다.

## 2026-09-17 — 홈의 둘째 숫자를 「복습 기한」에서 「새 표현」으로 (사용자 지적)

「복습 기한 20 이건 무슨 의미인가」 에서 시작해 「난이도 상/중/하 배지는 어떤가」 까지 간 대화.

**고친 것** — `due`(기한 지난 카드 수) 대신 `fresh`(`due: false`, 처음 보는 카드 수)를 센다.
「이번 세션 20장 · 새 표현 6」. 전부 복습인 날은 뒤가 아예 안 뜬다.

**옛 문구가 왜 값을 못 했나** — `due` 는 `ready` 의 부분집합인데, 세션은 기한 지난 카드로 먼저
채워지므로 기록이 쌓이면 늘 `due === ready` 가 된다. 같은 숫자를 두 번 말한다. 「복습 기한 20」이
기한이 20일이라는 뜻으로도 읽히는 문제는 그 다음이다.

**기각 — 난이도 상/중/하 배지 (사용자 제안, 사용자가 직접 철회)**
- *「읽기 교정 : 어휘 확장」 비율을 난이도로 읽는다* — **전제가 틀렸다.** `assignMode` 가 가르는
  기준은 뜻을 아느냐이고(동형동의·진단 응답·뜻 카드 안정), 확장은 뜻 카드를 같이 도는 모드다.
  새 표현 비중과 무관하다 — `selectSession` 은 교정·확장 **양쪽에서** 기한 카드를 먼저 채우고
  남는 자리만 신규로 채우므로, 새 표현 장수는 설정이 아니라 밀린 복습의 나머지다
- *세션 길이를 난이도로 읽는다* — 길이는 난이도가 아니라 **분량**이다. 20장이 10장보다 어려운 게
  아니라 길다. 한 눈금에 섞으면 「상」이 어렵다는 말인지 오래 걸린다는 말인지 안 갈린다
- *설정값(길이·비율)에서 계산한 배지* — 둘 다 사용자가 직접 정한 값이라, 배지는 설정 화면을
  되읽어 준다. 매일 봐도 안 변한다. 지금 「복습 기한」이 빠진 함정과 같은 자리다
- *「상/중/하」로 접는 것 자체* — 홈에 난이도 등급이 뜨면 리포트의 수준 판정(밴드)과 섞여
  「세션이 어렵다」인지 「내 실력이 상」인지 갈리지 않는다. 홈은 평가가 아니라 할 일이다
  (2026-09-14 절). 접는 규칙을 감추는 것도 이 앱의 결(리포트의 `unclassified` 노출)과 어긋난다

**남은 축** — 난이도를 정말 재려면 입력은 설정이 아니라 **뽑힌 카드의 밴드 분포 + 새 표현 비중**
이다. 지금은 안 만든다. 수요 근거가 생기면 그때 연다.

## 2026-09-17 — 반탁 절이 「안 걸리는 쪽」을 같이 가르친다 (사용자 지적)

「心不全이 반탁 규칙이 맞는가? 맞다면 しんぷぜん이 되어야 맞지 않나」.

**사용자 말이 맞다.** 心不全 = しんふぜん 이고 반탁 단어가 아니다. 배지가 반탁으로 뜬 건
**오답의 모양**이 반탁이어서다 — 실제 사전으로 확인했다.

```
心不全 しんふぜん ← しんぷぜん : RENDAKU / handaku
心不全 しんふぜん ← しんぶぜん : ONYOMI_CHOICE
```

**드러난 구멍** — 분류기는 규칙을 **덜 쓴** 오답(出発 → しゅっはつ)과 **과하게 쓴** 오답
(心不全 → しんぷぜん)을 같은 「반탁」으로 묶는다. `explainMistake` 는 「は행↔ぱ행 차이」만 보고
방향을 안 본다. 절 본문은 거는 법만 가르쳤으므로, 과잉 적용한 사람에게 **틀린 것을 강화**했다.

**고친 것 (1안)** — 같은 ん + ふ 환경에서 갈리는 최소 대립쌍을 넣었다. 満腹 まんぷく ↔
心不全 しんふぜん, 근거는 「뒤쪽이 이미 한 낱말이면(不全) 제 읽기를 지킨다」. 코퍼스 안에
증거가 하나 더 있다 — 腎不全 じんふぜん. 본문에도 과잉 적용 문단을 넣었다.

**차례를 같이 고쳤다** — `RuleBody short` 는 본문 앞 2문단·대조 1개만 편다. 처음엔 새 문단을
셋째, 새 대조를 둘째에 뒀는데 그러면 **카드에서는 안 보인다.** 사용자가 문제를 본 자리가 바로
그 카드다. 경계 문단을 둘째로, 최소 대립쌍을 첫 대조로 올렸다. 자르는 수를 `SHORT_RULE` 로
빼서 `rules.test.ts` 가 「경계가 요약본까지 닿는지」를 직접 확인한다.
**절의 차례가 곧 우선순위다** — 뒤에 둔 것은 카드에서 잘린다.

**안 한 것 (2안, 보류)** — `explainMistake` 가 과잉/누락 방향을 같이 돌려주고 절이 방향별로
다른 문장을 내는 것. 규칙 데이터가 두 배가 되므로 같은 모양의 오답이 더 쌓인 뒤에 판단한다.

**따로 발견** — 본문 네 군데(`sokuon`·`rendaku`·`onyomi-layers`·`mixed`)에 마크다운 `**` 가
글자 그대로 찍힌다. `RuleBody` 는 텍스트 노드만 그리므로 굵게 안 된다. e2e 로 확인함.
이번 범위가 아니라 안 건드렸다.

## 2026-09-17 — 규칙 본문의 `**` 를 굵게 그린다

규칙 본문은 마크다운 관례로 쓰여 있는데 `RuleBody` 는 텍스트 노드만 그린다. 네 군데
(`sokuon`·`rendaku`·`onyomi-layers`·`mixed`)에서 **별표가 글자 그대로 화면에 찍히고 있었다.**
e2e 로 실제 화면을 떠서 확인한 뒤 고쳤다.

**고른 방법 — `Mixed` 가 `**…**` 를 `<strong>` 으로 바꾼다** (`splitBold`). 강조 구간을 먼저
가르고, 그 안에서 다시 일본어 구간을 가른다. 순서가 중요하다 — 강조가 일본어를 감싸는 문장이
있어서(「**つ·ち 꼬리(한국음 ㄹ)는 か·さ·た·は행 앞에서…**」), 반대로 하면 `lang="ja"` 가
강조 경계에서 끊긴다. **자형 규칙이 강조보다 우선한다** (CLAUDE.md 「일본어 렌더링」).

**기각 — 데이터에서 별표만 지운다.** 지금 화면은 멀쩡해지지만 강조가 사라진다. 저 네 문장의
별표는 절의 **핵심 한 줄**에 붙어 있어(촉음의 비대칭, 연탁이 훈독에서만 일어난다는 것) 정보를
버리는 쪽이다. 글을 쓴 사람이 굵게 하려던 것을 굵게 하는 게 맞다.

**짝이 안 맞는 별표는 그대로 둔다** — 원문을 잃는 것보다 낫다. 대신 `ja.test.ts` 가 규칙 본문
전체(제목·요약·본문·예시 주석·대조 근거)에 별표가 하나도 안 남는지 확인한다. 새 문장을 쓰다
별표를 짝 없이 넣으면 거기서 걸린다.

## 2026-09-17 — 탁음이 어긋났다고 다 연탁이 아니다 (사용자 지적)

「月額의 額는 연탁 규칙이 아니지 않나」. **사용자 말이 맞다.** 사전으로 확인했다 —
額의 음독은 `ガク` 하나다. 연탁이 일어날 청음 원형(かく)이 없으니 연탁이 걸릴 자리가 아니다.
분해 결과가 그대로 말해준다.

```
月額 げつがく   → 月 base げつ variants []          額 base がく variants []      ← 변형 0
三日月 みかづき → … 月 base つき variants [rendaku]                              ← 진짜 연탁
```

**원인** — かく 는 額의 실재 읽기가 아니라서 답이 분해에 실패하고 `fromStrings` 로 떨어진다.
거기 첫 줄이 `unvoiceAll(정답) === unvoiceAll(오답)` 하나만 보고 `RENDAKU` 를 냈다.
**정답 쪽에 변형이 실제로 걸렸는지를 안 봤다.** 바로 옆 케이스가 대조군이다 — がつがく 는
`ONYOMI_CHOICE` 로 제대로 간다. ガツ 가 月의 실재 읽기라 답이 분해되기 때문이다.
**분해되느냐 아니냐가 판정을 가르고 있었다.**

### 어제 고친 心不全 건과 같은 뿌리인가

같은 뿌리인데 한 칸 더 깊다. 心不全 → しんぷぜん 은 적어도 ん+ふ 라는 **진짜 반탁 환경**에서
규칙을 과하게 쓴 오답이었다(그래서 처방이 「과잉 적용 문단 + 최소 대립쌍」이었다).
月額 → げつかく 는 **규칙 환경이 아예 없다.** 한국음 額=액(받침 ㄱ)을 그대로 끌어온 오답에
가깝다. 해설로 덮을 수 있는 종류가 아니라 판정 자체를 막아야 했다.

### 고친 방법 — 판정에 정답 분해를 돌려준다

`explainMistake` 는 `exp`(정답 분해)를 이미 손에 들고 있으면서, 답이 분해 실패하면 그걸 통째로
버리고 `fromStrings` 를 부르고 있었다. 그 값을 넘겨준다.

- `voicedAt(segments, index)` — 어긋난 자리를 덮는 **정답 조각**에 `rendaku`·`renjo`·`handaku`
  가 붙어 있나. 없으면 `stringVoicing` 이 `null` 을 내고 `RENDAKU` 를 안 낸다
- **자리 단위로 본다** (숙어 단위가 아니라). 한 숙어에 진짜 연탁 자리와 원래 탁음인 자리가
  같이 있을 수 있다. 어긋난 자리가 여럿이면 처음 자리로 판단한다 — `fromSegments` 의
  「앞자리가 뒤를 끌고 간다」와 같은 규약이다
- **정답 쪽이 분해 실패면 기존 판정을 남긴다** (`expSegments === null`). 가릴 근거가 없는데
  판정을 없애면 문자열 관계만으로 잡던 것까지 잃는다. 근거 없이 지우는 쪽이 더 손해다
- 분해 경로(`fromSegments`)는 **안 건드렸다.** 거기는 처음부터 `variants` 로 판정한다

### 얼마나 틀리고 있었나 — 전수 대조

분해 성공 숙어(16,959) 전부에 대해 각 조각을 청음으로 바꾼 오답을 만들어 고치기 전/후를 쟀다.

- **거짓 연탁 6,562건 → 0건**
- **진짜 연탁 1,198건 유지, 이번 변경으로 잃은 것 0건**

고치기 전에도 연탁으로 안 가던 1건(`身勝手 みがって ← みかって`)이 있는데, 답이 분해되어
`fromSegments` 로 가 `ONYOMI_CHOICE` 가 되는 별개 건이다. 전후가 같아 이번 범위 밖으로 뒀다.

**어제 만든 배지와 규칙 색인이 이 숫자만큼 엉뚱한 절을 가리키고 있었다.** 忠実 ちゅうじつ,
愛護 あいご, 悪事 あくじ … 전부 청음으로 틀리면 「연탁」이 떴다. 규칙을 가르치는 장치가
틀린 규칙을 가르치던 자리라 어제 건과 같은 무게로 다뤘다.

### 이벤트 스키마는 이번에도 안 건드렸다

저장된 `answer`·`expected` 로 다시 매기므로 **지난 기록의 배지와 색인도 소급해서 고쳐진다.**
마이그레이션이 없다. 2026-09-17 탁음 갈래 절에서 세운 「유형은 저장하고 갈래는 다시 매긴다」가
두 번째로 값을 했다 — 분류기가 틀렸다고 밝혀져도 기록을 안 버린다.

### 기각한 대안

- **`fromStrings` 에서 탁음 분기를 아예 빼기** — 近所 きんじょ ← きんちょ 처럼 **답이 분해되지
  않는 진짜 연탁**이 코퍼스에 108건 있다. 통째로 빼면 그 108건을 잃는다. 문제는 분기가 있는
  것이 아니라 **근거를 안 보는 것**이었다
- **정답 분해에 변형이 하나라도 있으면 통과시키기 (숙어 단위)** — 코드는 더 짧지만, 진짜 연탁
  자리와 원래 탁음인 자리가 같이 있는 숙어에서 다시 엉뚱한 절로 간다. 자리 단위 검사와 코드
  차이가 몇 줄뿐이라 싸게 정확한 쪽을 골랐다
- **오답 유형을 `KO_INTERFERENCE` 로 돌리기** — げつかく 는 한국음 액에서 왔을 가능성이 높지만
  `koInterference` 는 「그 음을 쓰는 **다른 한자**의 음독」을 요구한다. かく 는 額 자신의 것도
  남의 것도 아니다. 억지로 태우면 간섭 판정이 헐거워진다. **분류 안 됨으로 남기는 게 맞다** —
  리포트가 `unclassified` 를 감추지 않는 결과 이 앱의 결에 맞는다

## 2026-09-17 — 앞 수정이 과잉 적용까지 잘라먹었다 (사용자 지적 「愛好도 연탁이 맞나」)

사용자가 愛好를 짚었다. **愛好 あいこう 자체는 연탁이 아니다** — 好의 음독은 コウ 하나고
읽기에 탁음이 없다. 분해 결과도 `愛 あい []` + `好 こう []` 로 변형 0이다. 여기까지는 맞다.

그런데 확인하다 **바로 앞 커밋(3ef2a53)이 낸 구멍**을 찾았다.

### 방향을 안 봤다

`voicedAt` 이 「정답 조각에 변형이 걸렸나」만 보고 두 방향을 같이 잘랐다.

- **놓친 방향** — 정답이 탁음, 답이 청음 (月額 げつがく ← げつかく).
  정답 쪽 근거를 따지는 게 맞다. 額는 원형이 がく 라 연탁이 아니다
- **과잉 방향** — 정답이 청음, 답이 탁음 (愛好 あいこう ← あいごう).
  여기는 **정답에 변형이 없다는 것이 곧 오답의 내용**이다 — 걸면 안 되는 자리에 걸었다.
  같은 잣대를 대면 판정이 무조건 죽는다

전수로 재보니 과잉 방향에서 **706건이 연탁을 못 받고 있었다** (8,381 → 7,675).
전부 촉음 뒤 탁음(悪化 あっか ← あっが) 모양이다 — っ 뒤에 탁음이 못 오니 답이 분해에
실패해 문자열 경로로 오고, 거기서 잘렸다. 愛好 자신은 あいごう 가 연탁 변이형으로 분해돼
**분해 경로**로 가므로 안 잘렸다. 사용자가 짚은 단어는 멀쩡한데 옆 단어가 상한 모양이었다.

**고친 방법** — 게이트를 놓친 방향에만 건다. 어긋난 자리에서 「정답이 탁음이고 답이 청음」일
때만 `voicedAt` 을 따진다. 한 줄이다.

### 전수 재측정 (두 방향)

| 방향 | 결과 |
| --- | --- |
| 놓친 방향 · 진짜 연탁 | 1,198 유지 (잃음 0) |
| 놓친 방향 · 거짓 연탁 | **7,633건 제거, 남음 0** |
| 과잉 방향 | 8,381 유지 — **고치기 전 수치와 정확히 같다** |

### 배운 것

**한 축을 고칠 때 그 축의 양끝을 다 재야 한다.** 3ef2a53 은 놓친 방향만 전수로 쟀고
(청음화 오답만 만들어 돌렸다) 과잉 방향은 아예 생성하지 않았다. 측정이 한쪽만 보면
전수 대조를 해도 반쪽이다. 이번엔 두 방향을 같은 스크립트에서 같이 돌렸다.

**과잉/누락을 이름으로 갈라 보여주는 2안은 여전히 보류다.** 지금은 둘 다 「연탁」으로
같은 절을 연다. 절 본문에 과잉 적용 문단이 이미 있다(2026-09-17 반탁 절). 이름까지 가르는
건 같은 모양의 오답이 더 쌓인 뒤에 판단한다.

## 2026-09-17 — 「소급된다」가 유형에는 해당이 안 됐다 (사용자 지적 「絶好는 촉음 아닌가」)

사용자가 絶好를 짚었다. **絶好 자체는 촉음이 맞다** — ゼツ + コウ → ぜっこう 이고, ぜつこう
오답은 앱이 `SOKUON` 으로 제대로 잡는다. 확인하다 **세 번째 구멍**을 찾았다.

### 내 앞선 보고가 틀렸다

3ef2a53 에서 「이벤트 스키마를 안 건드려도 지난 기록의 배지와 색인이 소급해 고쳐진다」고
보고했다. **유형에는 해당이 안 된다.** `voicingByEvent` 는 갈래(연탁·반탁·연성)만 다시
매기고 유형은 `e.mistakeType` 저장값을 그대로 썼다. 게다가 `voicing ?? 'rendaku'` 라
다시 매긴 결과가 「연탁 아님」이어도 연탁으로 되돌려 놨다. 재현해 확인했다.

```
月額 げつがく ← げつかく  (저장 RENDAKU) → 배지 「연탁」   ← 분류기를 고쳤는데도 그대로
絶好 ぜっこう ← せっこう  (저장 RENDAKU) → 배지 「연탁」   ← 지금 분류기는 KO_INTERFERENCE
```

분류기만 고치고 **읽는 쪽을 안 고쳤다.** 사용자 화면에서는 아무것도 안 고쳐진 상태였다.

### 고친 방법 — 읽을 때 유형까지 다시 매긴다

`voicingByEvent` → `verdictByEvent`. `RENDAKU` 로 저장된 이벤트만 지금 분류기로 다시 돌려
`MistakeVerdict` 를 통째로 돌려준다.

- `effectiveMistake(e, verdictOf)` — 다시 매긴 게 있으면 그것, 없으면 저장값. **규칙 화면의
  절 색인과 다시보기 배지가 둘 다 이걸 통해서만 유형을 본다.** 한 곳만 고치면 갈라진다
- 다시 매겨 `null` 이면 어느 절에도 안 든다 (리포트의 `unclassified` 와 같은 자리)
- **사전을 못 읽으면 저장값을 남긴다** — 항목을 아예 안 넣는다. 근거 없이 지우지 않는
  원칙은 `voicedAt` 때와 같다
- **`RENDAKU` 가 아닌 유형은 다시 안 매긴다.** 이번에 고친 축이 거기라 범위를 거기까지만
  연다. 사전이 바뀌면 다른 유형도 흔들릴 수 있지만 그건 지금 근거가 없는 확장이다

### 기각한 대안

- **저장된 `mistakeType` 을 마이그레이션으로 고치기** — 스키마 불변 조건 위반이다
  (CLAUDE.md). 게다가 분류기는 앞으로도 고쳐질 텐데 그때마다 마이그레이션을 쌓게 된다.
  **읽을 때 매기면 분류기를 고치는 것만으로 지난 기록이 따라온다**
- **`classifiedMistakes` 단계에서 전 유형을 다시 매기기** — 한 줄 더 단순하지만 이번 변경과
  무관한 유형까지 흔든다. 지금 근거가 있는 건 `RENDAKU` 한 축이다

### 배운 것 — 세 번 다 같은 자리에서 놓쳤다

1. 3ef2a53 — 놓친 방향만 재고 과잉 방향을 안 쟀다 (측정이 반쪽)
2. 92e7381 — 고쳤지만 **읽는 쪽이 저장값을 쓰는 것**은 안 봤다
3. 이번 — 「소급된다」를 코드로 확인 안 하고 보고했다

**고친 함수가 아니라 사용자가 보는 화면까지 따라가서 확인해야 한다.** 분류기 단위 테스트가
통과하는 것과 배지가 바뀌는 것은 다른 일이다. `mistakeOfIdiom` 이 `e.mistakeType` 을 읽는
줄을 한 번만 봤으면 3ef2a53 에서 같이 잡혔다.

## 2026-09-17 — 분류 정확도 전수 감사 (사용자 지시 「문법 노출 전체 점검」)

사용자가 전체 점검을 물었다. 축 넷(분류 정확도 / 본문 사실 / 노출 경로 일관성 / 렌더링)을
제시했고 **사용자가 「분류 정확도만 먼저」를 골랐다.**

### 점검 전에 이미 나온 균열 — 리포트가 저장값을 쓴다

ee521b0 이 규칙 화면·배지를 「읽을 때 다시 매김」으로 바꿨는데 **리포트는 여전히
`replay.ts` 가 저장된 `mistakeType` 을 센다.** 지금 상태로는 月額이 규칙 화면에서는 빠졌는데
리포트에서는 연탁으로 세어진다. 「진단 축은 묶어 둔다」는 결정과 별개 문제다 — 그건
연탁·반탁을 안 가른다는 뜻이었지 **틀린 판정을 남긴다**는 뜻이 아니었다.
축 C(일관성)로 넘겼다. 이번 턴 범위 밖.

### 도구 — `npm run audit:mistake-rules`

코퍼스의 각 숙어에서 조각 하나를 축별로 흔들어 오답을 합성하고, **그 축의 변형이 정답에
실제로 걸려 있었는지**와 분류 결과를 대조한다. 月額 건이 드러낸 실패 모양을 다른 축으로
넓힌 것이다. 스크립트로 남겨 분류기를 고칠 때마다 다시 돌린다.

**탐침은 축을 하나만 흔들어야 한다.** 처음엔 촉음을 뺄 때 `base` 로 되돌렸는데, `切 ぎっ` 의
base 는 `きり` 라 **연탁까지 같이 벗겨졌다.** 그렇게 나온 「촉음인데 연탁으로 감」 3건은
분류기 문제가 아니라 탐침 문제였다. 변형이 하나뿐인 조각으로 제한하니 사라졌다.

### 결과 — 月額 모양은 더 없다

| 축 | 결과 |
| --- | --- |
| 촉음 뺌 | 961 / 961 `SOKUON` |
| 촉음 덧댐 | 2,753 `SOKUON` (과잉 방향) |
| 탁음 뺌 | 걸린 자리 1,195 전부 `RENDAKU` · **안 걸린 자리 `RENDAKU` 0** |
| 탁음 덧댐 | 10,534 `RENDAKU` (과잉 방향) |
| 장음 흘림 | 9,839 `CHOON` · 34건만 다른 유형 |

**「규칙이 안 걸린 자리인데 그 규칙으로 가는」 건은 세 축 모두 0건이다.**

### 장음 34건 — 결함이 아니라 분해 모호성이다. 안 고쳤다

둘 다 **답이 다른 실재 읽기로도 분해된다.** 분해만으로는 어느 쪽인지 못 가른다.

- **栄 무리 25건** — 栄華 えいが ← えが. `え` 는 栄의 실재 훈독이다(見栄 みえ). 그래서
  `栄(kun え) + 華(が)` 로 분해돼 `MIXED_READING` 이 우선순위에서 이긴다
- **宮·后 무리 9건** — 神宮 じんぐう ← じんぐ. `ぐ` 는 `宮의 ク + 연탁` 으로 분해된다.
  `CHOON` 가드가 `stripLongVowels(e.base) === stripLongVowels(a.base)` 인데 `ぐう` vs `く` 라
  안 걸린다

**안 고친 이유** — 고치려면 우선순위 규약을 바꿔야 하고, 그게 새 오분류를 낸다.
`unvoiceAll` 로 가드를 느슨하게 하면 **神 シン↔ジン 처럼 진짜로 다른 음독을 고른 것까지
장음으로 본다**(神宮 じんぐう ← しんぐう). 0.34%를 줄이자고 멀쩡한 판정을 깨는 교환이다.
[mistakes.ts](src/core/mistakes.ts) 가 이미 같은 모호성을 문서화해 뒀다 — 「우연히 짧은 쪽도
실재 음독인 경우가 있어(数 の ス) 분해만으로는 구분되지 않는다」. 같은 자리의 다른 모양이다.

**月額 건과 무엇이 다른가** — 月額은 `かく` 가 額의 읽기가 **아니었다.** 근거가 아예 없는데
규칙을 붙였다. 여기는 두 해석이 다 사전에 근거가 있다. 전자는 결함이고 후자는 모호성이다.
이 구분이 이번 감사의 판정 기준이었다.

## 2026-09-17 — 문법 노출 점검 축 B · 본문 사실 검증

축 A 가 「분류가 맞는 절로 보내는가」였다면 B 는 「그 절이 맞는 말을 하는가」다. 心不全 건이
이 축이었다. 세 가지가 나왔고 셋 다 고쳤다.

### ① 연성 절 예시 다섯 중 셋이 연성으로 분해되지 않았다

`rules.test.ts` 는 예시가 **코퍼스에 실재하는지**만 봤다. 그 예시가 **그 절의 규칙을 실제로
보여주는지**는 아무도 안 봤다. 분해로 대조하니 바로 나왔다.

```
天皇 てんのう  皇:のう[renjo]   ← 연성
安穏 あんのん  穏:のん[renjo]   ← 연성
反応 はんのう  応:のう          ← 변형 없음
観音 かんのん  音:のん          ← 변형 없음
因縁 いんねん  縁:ねん          ← 변형 없음
```

KANJIDIC 이 `応: -ノウ`, `音: -ノン`, `縁: -ネン` 을 **읽기로 이미 싣는다.** 어휘화가 깊어
별도 읽기가 된 것이다. 그러면 분해가 のう 를 변형 없는 원형으로 읽고, はんおう 는
「다른 음독을 골랐다」(`ONYOMI_CHOICE`)가 된다 — **음독 층위 절로 간다.**

본문은 「反応 을 はんおう 로 읽으면 틀려요」라고 그 절에서 가르치는데 앱은 다른 절로 보낸다.
心不全 과 같은 모양이다 — 절이 약속한 것을 앱이 안 지킨다.

**고친 방법** — 사실을 본문에 적고(`offRule` 문단), 예시 순서를 바꿔 앱이 실제로 연성으로
잡는 天皇·安穏 을 앞으로 올렸다 (`SHORT_RULE.examples = 3` 이라 카드에는 앞 셋만 보인다).

**기각 — `-ノウ` 류를 분해에서 연성으로 태그하기.** 근본 수정이지만 음독 쌍 식별자
(`応:on:のう`)가 바뀐다. 쌍은 학습 기록이 가리키는 단위라 **지난 기록이 다른 쌍을 가리키게
된다.** 스키마에 준하는 변경이라 이번 범위 밖이다. 사실을 적는 쪽이 싸고 정직하다.

### ② 연성 단위 테스트가 사전을 조작하고 있었다

`mistakes.test.ts` 의 `renjoCtx` 가 応의 음독을 `['オウ','ヨウ']` 로 줘서 `-ノウ` 를 뺐다.
그래서 「연성이 잡힌다」는 테스트가 통과하는데 **실제 사전에서는 그 경로가 안 나온다.**
실재하지 않는 상황을 검증하고 있었다. 실제 읽기를 픽스처에 넣고, 진짜 연성인 天皇 으로
바꿨다. 反応 이 `ONYOMI_CHOICE` 로 간다는 사실도 테스트로 박았다.

**픽스처를 손으로 줄이면 테스트가 현실에서 떨어진다.** 축 A 에서 탐침이 축을 섞었던 것과
같은 실수의 다른 모양이다 — 검증 장치 자체가 틀리면 통과가 아무 뜻이 없다.

### ③ 연탁 절의 「음독 한자어는 거의 안 걸리고」가 과장이었다

코퍼스로 세니 연탁 자리 889곳 중 **훈독 601 / 음독 288** 이다. 음독이 32%다.
「거의 안 걸린다」는 사실과 다르고, 王子 おうじ·因果 いんが 에서 연탁을 만난 학습자에게
「음독은 안 걸려」라고 가르치면 틀린 것을 강화한다.

음독 연탁 288자리의 **앞 글자**를 세니 패턴이 나왔다 — ん 125 · う 64 · い 30.
**넷 중 셋(76%)이 ん 이나 장음 뒤다.** 「거의 안 걸린다」보다 이게 쓸 수 있는 말이다.
본문을 그렇게 고치고 **숫자를 테스트로 고정했다** (훈독 비율 60~75% · 음독 연탁 100건 초과 ·
ん/장음 앞 70% 초과). 코퍼스가 갱신돼 서술이 어긋나면 테스트가 걸린다.

### 확인만 하고 안 고친 것

- **라이먼의 법칙** — 연탁이 걸린 조각에 이미 탁음이 있는 경우 **0건**. 본문 주장 그대로다
- **ㅇ 받침 ↔ 장음** — ㅇ 받침 한자 375 중 371(98.9%)이 장음 음독을 갖는다. 본문이
  「대개」라고만 하고 역방향을 주장하지 않아 그대로 뒀다 (ㅇ 받침이 아닌데 장음인 것도 546개)
- **半分 はんぶん** — 반탁 절에 있지만 반탁이 아니다. 87b993d 가 의도해 넣은 반례라
  `offRule` 로 표시만 하고 뒀다

### 남긴 장치 — `offRule`

「이 예시는 절의 규칙이 분해에 안 잡힌다」를 이유와 함께 적는 필드다. 테스트가 분해 결과와
이 표시를 **양방향으로** 맞춘다 — 변형이 없는데 표시가 없으면 걸리고, 표시가 있는데 변형이
있어도 걸린다. 표시가 낡으면 설명이 거짓이 되는 자리라 한쪽만 봐서는 안 된다.

## 2026-09-17 — 문법 노출 점검 축 C · 노출 경로 일관성

### 고친 것 — 리포트가 저장된 유형을 세고 있었다

ee521b0 이 규칙 화면·다시보기를 「읽을 때 다시 매김」으로 바꿨는데 리포트는 `replay` 를
지나고, `replay` 는 사전을 모르는 순수 접기라 저장값을 그대로 셌다. **月額이 규칙 화면에서는
빠졌는데 리포트에서는 연탁으로 남아 있었다.**

`ReplayOptions` 에 이미 `pairsOf` 라는 관례가 있다 — **사전을 아는 쪽이 함수를 넘겨준다.**
같은 모양으로 `mistakeOf` 를 달았다. replay 는 여전히 순수하다.

**다시 매기는 자리를 하나로 모았다** (`reclassifier`). 규칙 화면·다시보기는 `verdictByEvent`
로, 리포트는 `replay` 의 `mistakeOf` 로 들어오는데 둘 다 이 함수를 지난다. 두 곳에서 따로
매기면 또 갈라진다 — ee521b0 이 규칙 화면만 고쳐서 이미 한 번 갈라졌다.

다시 매겨 `null` 이면 그 오답은 유형을 잃고 `unclassified` 로 간다
(`report.ts` 의 `totalWrong - totalMistakes`). 틀린 횟수 자체는 그대로다 — 유형을 못 붙였을
뿐 오답은 오답이다.

### 남은 균열 — 묶음 버킷 이름이 카드와 어긋난다

카드는 「반탁」이라 말하고 리포트·세션 요약·피드백은 같은 오답을 「연탁」이라 부른다
(`MISTAKE_LABEL.RENDAKU = '연탁'`). 사용자가 결정을 물어 **숫자로 쟀다.**

**코퍼스 전수 — 규칙을 놓친 방향 1,198건**

| 갈래 | 건수 | 비중 |
| --- | --- | --- |
| 연탁 | 888 | 74.1% |
| 반탁 | 310 | **25.9%** |
| 연성 | 0 | 0% |

「연탁」이라는 이름이 **네 건 중 한 건에서 틀린다.** 연성이 0인 건 탐침 문제가 아니라
구조다 — `unvoiceAll` 이 の→お 를 안 바꾸므로 연성은 이 경로에 안 온다(분류기 주석과 일치).
코퍼스에 연성 자리 자체가 6곳뿐이다.

**기각이 숫자로 확인됐다 — 리포트도 갈래로 가르는 안.** 처방은 한 유형이 전체 오답의 30%를
넘어야 뜬다(`DOMINANT_SHARE`). 버킷을 가르면 반탁은 탁음 오답의 25.9%이므로, 반탁 처방이
뜨려면 탁음 오답이 전체의 **116%** 여야 한다 — **넘을 수 없는 문턱이다.** 2026-09-17 에
「표본이 쪼개지면 처방이 늦게 뜬다」로 기각했는데, 늦게 뜨는 게 아니라 안 뜨는 것이었다.

### 각각을 나눌 수 있었다 (2026-09-17, 사용자 지적)

사용자가 「각각을 나눌 수가 없나」로 되물었다. **내가 선택지를 잘못 좁혔다.** 「묶는다 / 가른다」를
전부-아니면-전무로 놓고 쟀는데, **분포 표시와 처방 문턱은 다른 축이다.** 축을 갈라 보니 셋이
동시에 된다.

- **분포는 갈래로 나눈다** — 「연탁 3 · 반탁 2」. 카드가 부르는 이름과 같아진다
- **처방 문턱은 묶은 채로** — `DOMINANT_SHARE` 판정은 탁음 합계로. 쪼개면 반탁 처방이
  문턱을 넘을 수 없다는 계산은 그대로 유효하다
- **처방이 내미는 절은 제일 많이 틀린 갈래의 것** — 반탁만 틀리는 사람에게 연탁 절을
  읽히던 자리다. `ruleForMistake(type, dominantVoicing)`

그래서 **버킷 이름을 새로 지을 필요가 없어졌다.** 「탁음 변화」도 「연탁·반탁」도 안 쓴다 —
`MISTAKE_LABEL.RENDAKU` 는 다시 매기기가 실패한 나머지 몫에만 남는다. 새 이름은 새 부정확을
들인다(반탁은 탁음이 아니고 연성도 탁음이 아니다). **이름을 고르는 문제가 아니라 축을 가르는
문제였다.**

**숫자를 잃지 않는다** — 갈래 합계가 `report.mistakes` 의 RENDAKU 수보다 적으면(다시 매기기가
실패한 이벤트) 남는 만큼을 「연탁」 한 칸으로 남긴다. 화면에서 조용히 사라지는 오답이 없어야 한다.

**처방에 한 줄 단서** — 갈래가 둘 이상 섞였으면 「연탁·반탁·연성을 한 칸으로 묶어 센 값이에요」.
제목은 갈래 이름인데 퍼센트는 묶은 값이라, 안 적으면 숫자가 어긋나 보인다.

**배운 것** — 「A냐 B냐」로 물을 때는 **그 둘이 정말 같은 축인지** 먼저 본다. 이번엔 아니었고,
사용자가 그걸 짚었다. 측정을 아무리 정확히 해도 축이 틀리면 답이 틀린다.

## 2026-09-17 — 문법 노출 점검 축 D · 렌더링

오늘 두 건이 나왔던 축이다(`MISTAKE_ADVICE` 의 `lang="ja"` 누락, 규칙 본문의 마크다운 별표).
「앱 쪽에 같은 구멍이 더 있는지는 안 훑었다」고 적어 둔 자리를 훑었다.

### 소스를 grep 하지 않고 그려진 것을 훑었다

가나가 든 소스 파일이 30개가 넘는데 대부분 주석·테스트 데이터다. 그리고 **오늘 난 두 건은
문자열이 어디에 있느냐가 아니라 어디로 흘러가느냐의 문제였다** — 같은 `MISTAKE_ADVICE` 가
카드에서는 `Mixed` 를 지나고 리포트에서는 안 지났다. 그래서 e2e 로 **실행 중인 화면의 텍스트
노드를 순회**한다 (`tests/e2e/ja-lang.spec.ts`).

판정 범위는 앱 자신의 `JA_RUN`(`src/app/ja.ts`)과 같다 — 가나·한자·々〆ー. 한글 호환
자모는 범위 밖이라 받침 이름(ㄱ·ㄹ)이 일본어로 안 샌다.

### 찾은 것 — 절 제목 세 자리

첫 실행에서 바로 나왔다. **규칙 절 제목이 `Mixed` 를 안 지나고 있었다.**

```
<span.rule-title> っ ん は ぱ  —  っ·ん 뒤에서 は행이 ぱ행으로
<span.rule-title> ん な        —  연성 — ん 뒤의 모음이 な행으로 당겨진다
```

같은 값을 쓰는 자리를 따라가 셋을 고쳤다 — `Rules.tsx`(절 머리), `Browse.tsx`(다시보기
카드가 펼치는 규칙 제목), `MistakeDetail.tsx`(「왜 그런가 — 」). **본문·요약·예시 주석은
처음부터 `Mixed` 를 지나는데 제목만 빠져 있었다.** 데이터 한 덩어리에서 필드 하나가 새면
훑기 전에는 안 보인다.

### 검사가 헛돌지 않게

이동이 실패해 빈 화면을 훑어도 「위반 0」으로 통과한다. 그래서 화면마다 **일본어를 몇 개
봤는지(`seen`)** 를 같이 돌려주고 0이면 실패시킨다. 축 B 에서 픽스처를 조작한 테스트가
실재하지 않는 경로를 통과시키던 것과 같은 자리다.

그 자기검사가 바로 값을 했다 — **홈과 리포트에는 일본어가 아예 안 뜬다** (분포는 유형
이름이 한국어고 처방은 읽기 30회 미만이라 안 뜬다). 두 화면만 기대에서 빼고, 이동 자체는
각 화면의 고유 요소로 지킨다 (`.bars`, `.stat-big` — `.section-title` 은 리포트에도 있어
음독 맵으로 안 가도 통과한다).

### 음성 대조를 둘 다 했다

- **`lang` 누락** — 첫 실행에서 실제로 2건을 잡았다. 대조 완료
- **마크다운 잔여** — 처음엔 `summary` 에 `**` 를 넣어 봤는데 **안 걸렸다.** 그 경로는
  이미 `Mixed` 를 지나 별표가 `<strong>` 이 된다(33b0eb9). 검사가 노리는 건 **`Mixed` 를
  안 지나는 경로**라, `MISTAKE_LABEL` 에 넣으니 「리포트 · 마크다운 잔여 — **촉음**」으로
  걸렸다. 대조를 잘못 세우면 「검사가 작동한다」는 결론도 틀린다

### 훑은 범위와 안 훑은 것

- **훑음** — 홈 · 리포트 · 읽기 규칙(8절 전부 펼침) · 다시보기(규칙 배지 펼침) · 음독 맵 ·
  세션 읽기 카드 · 오답 피드백 · 오답 상세 · 사용 안내서
- **안 훑음** — 진단 흐름, 설정, 피드백 보내기, 세션 요약. 일본어가 뜨는 자리가 아니거나
  (설정·피드백) 앞 화면과 같은 컴포넌트를 쓴다. 필요해지면 `scan()` 한 줄이면 는다

## 2026-09-17 — 축 A′ · 구조 판정 유형 (KO_INTERFERENCE)

### 잣대를 코퍼스에서 귀납했다

분류기가 쓰는 기준(「형제 한자의 음독이면서 자기 읽기가 아님」)으로 채점하면 코드가 자기를
되읽는 것이라, 읽기 표만 보고 **한국음 종성 → 일본 음독 꼬리** 대응을 따로 만들었다
(`npm run audit:ko-interference`).

| 종성 | 꼬리 |
| --- | --- |
| ㄱ | く·き **85%** |
| ㄹ | つ·ち **83%** |
| ㅁ / ㄴ | ん **95% / 96%** |
| ㅂ | う·い **73%** |
| ㅇ | う·い **90%** |
| 받침 없음 | 모음 47% · 장음 **41%** |

**절 서술이 다 맞다.** 축 B 에서 안 박고 넘겼던 것들이 여기서 같이 고정됐다 —
이제 `rules.test.ts` 가 이 여섯 줄을 지킨다.

### 탐침이 또 한쪽만 만들고 있었다

처음엔 청탁 변화만 만들어 놓고 「KO_INTERFERENCE 2,596건이 전부 청탁 오류」라는 결론을
낼 뻔했다. **청탁만 만들었으니 청탁만 나오는 게 당연하다.** 축 A 에서 놓친 방향만 재고
틀렸던 것과 같은 함정이다. 진짜 간섭 오답(형제 한자의 음독을 끌어옴)을 같이 만들어 다시 쟀다.

### 결과 — 분류기는 제 일을 한다

| 오답의 실제 내용 | 판정 |
| --- | --- |
| 형제 한자 음독을 끌어옴 | `KO_INTERFERENCE` **96,433** · `CHOON` 2,058 · `RENDAKU` 1,213 |
| 청탁만 다름 | `RENDAKU` 11,732 · 분류 안 됨 4,139 · `KO_INTERFERENCE` **2,596** · `ONYOMI_CHOICE` 1,269 |

**진짜 간섭은 96.7%가 제 유형으로 간다.** `KO_INTERFERENCE` 자체는 믿을 만하다.

### 드러난 진짜 구멍 — 청탁에는 절이 없다

한국 한자음은 **청탁을 안 가른다.** 같은 「화」인데 化는 カ, 画는 ガ 다. 코퍼스에 이런 짝이
**69쌍** 있다. 그런데 `korean-coda` 절은 처음부터 끝까지 **꼬리(종성)** 얘기뿐이라 청탁이
한 줄도 없다. 청탁 오답 2,596건이 「한국음 간섭」이라는 맞는 이름을 받고 **꼬리를 가르치는
절로** 간다. 게다가 같은 현상이 네 유형으로 흩어진다 (위 표 아랫줄).

**고친 것 — 경계 문단을 절에 넣었다.** 「한국음이 정해주지 않는 것도 있어요 — 첫소리가
탁음인지 아닌지예요」 + 化 カ ↔ 画 ガ (悪化 ↔ 一画), 介 カイ ↔ 慨 ガイ (一介 ↔ 感慨).
心不全 때 반탁 절에 과잉 적용 문단을 넣은 것과 같은 처방이다.

**자리는 뒤다.** 이 절에 오는 오답의 **96.7%가 꼬리 오류**라 꼬리 내용이 앞이어야 한다.
「절의 차례가 곧 우선순위」(87b993d)를 숫자로 적용했다 — 2.6%를 위해 96.7%의 요약본을
밀어내지 않는다.

### 안 한 것 — 새 오답 유형 (사용자 결정 필요)

청탁 오답을 제대로 다루려면 `VOICING_UNMARKED` 같은 **새 유형**이 있어야 한다. 그래야
이름도 절도 맞고 네 유형으로 흩어지지도 않는다. 하지만 `mistakeType` 은 스키마 불변 조건이라
(CLAUDE.md) 먼저 알려야 한다. 그리고 `KO_INTERFERENCE` 는 `fromStrings` 의 마지막 검사라
손대면 `DECOMPOSED_PRIORITY` 까지 번지고, 그건 리포트 진단 축과 처방에 그대로 이어진다.

### 같이 고친 것 — 「받침 없는데 장음인 글자도 있어요」

41%를 「있어요」로 말하고 있었다. 「오히려 흔해요 — 열에 넷」으로 바꾸고 숫자를 박았다.
「음독 한자어는 거의 안 걸리고」(축 B)와 같은 종류의 과소 서술이다.

## 2026-09-18 — 청탁 절 (1단계 / 사용자 결정)

어제 낸 세 안(표시 / 새 유형 / 그냥 둠) 중 **표시**로 정해졌고, 단계로 쪼개 되돌리기 지점을
만들기로 했다. 1단계는 **절만 만든다** — 판정은 아직 이 절로 안 온다 (`mistakes: []`).

### 왜 절이 먼저인가

`ruleForMistake` 를 뜯어보니 **갈래만 만들면 반쪽이 된다.**

```ts
if (type === 'RENDAKU' && voicing !== null) {
  const s = RULE_SECTIONS.find((x) => x.voicing === voicing)
  if (s) return s.id          // ← 없으면 여기서 빠져나가
}
return RULE_OF_MISTAKE[type]  // ← 연탁 절
```

이름은 「청탁 미구분」인데 절은 연탁이면 **지금보다 나쁘다.** 그래서 절이 먼저다.

### 자리 — 연탁 절 바로 뒤

연탁이 「붙어서 탁해지는 변화」를 가르치고 나면 바로 다음이 「변한 게 아니라 원래 그런 것」이다.
둘을 나란히 놔야 연탁 규칙을 아무 데나 대지 않는다. 그러면 마지막 셋(청탁 미구분·음독 층위·
혼독)이 **「규칙으로 유도할 수 없고 낱말로 익힌다」**는 결로 묶인다. 절 순서 주석도 고쳤다.

### 어제 넣은 예가 틀렸다

`korean-coda` 청탁 문단에 「같은 「화」인데 化는 カ, 画는 ガ — 悪化 あっか 와 一画 いっかく」
라고 썼는데, **一画 いっかく 의 画는 カク 다.** ガ 의 예가 아니다. 짝을 보여주려고 든 예가
짝을 안 보여주고 있었다. 사전으로 확인한 「호」 짝(愛護 あいご ↔ 愛好 あいこう)으로 바꿨고,
새 절에는 映画 えいが 를 썼다.

### 그 실수를 잡는 검사를 만들었다

예시의 `note` 는 「護 호 → ご」 꼴이다. 셋이 다 맞아야 한다 — 그 한자의 한국음이 「호」이고,
ご 가 그 한자의 **실재 음독**이고, 그 읽기가 **숙어 안에 실제로 들어 있어야** 한다.
음성 대조로 확인했다 — 一画 을 도로 넣으면 「一画 いっかく 에 が 가 없다」로 걸린다.

**축 B 의 검사는 「예시가 코퍼스에 실재하는가」까지만 봤다.** 一画 いっかく 는 실재하는
숙어라 그 검사를 통과한다. **실재하는 것과 그 예가 논지를 보여주는 것은 다르다.**

### 안 한 것 (2단계)

`VoicingKind` 에 `'unmarked'` 추가, `explainMistake` 가 그걸 내게 하기, 이 절의 `mistakes`
채우기. 조건은 **정답 조각에 탁음 변형이 없고 + 음독**이다. 훈독 자리(453건)는 진짜 연탁
과잉 적용이라 건드리면 92e7381 을 깨뜨린다.

검증 기준 — 움직인 건수가 **7,928과 정확히 같아야 한다.** 하나라도 더 움직이면 경계가 틀린 것이다.

## 2026-09-18 — 청탁 갈래를 낸다 (2단계)

`VoicingKind` 에 `'unmarked'` 를 더하고, `explainMistake` 가 그 자리에서 그걸 내게 했다.
**저장되는 유형(`RENDAKU`)은 안 바뀐다.** `voicing` 은 저장 안 되는 파생값이라 스키마도
백업 형식도 무관하다.

### 경계 — 음독이고 변형이 없을 때만

```ts
function unmarkedVoicing(s: Segment | null): boolean {
  return s !== null && s.kind === 'on' && !hasVoicingVariant(s)
}
```

정답 조각이 음독인데 탁음 변형이 안 걸렸다면 그 글자의 청탁은 **규칙이 아니라 원형**이다
(好 = コウ, 額 = ガク). 연탁을 과하게 쓴 게 아니라 애초에 규칙이 없는 자리다.

**훈독은 뺀다.** 훈독에서 탁음을 덧댄 건 진짜 연탁 과잉 적용이다(春風 はるかぜ ← はるがぜ —
라이먼의 법칙이 막는 자리). 여기까지 끌어오면 92e7381 을 깨뜨린다.

### 검증 — 움직인 건수가 정확히 7,928

착수 전에 정한 기준이었고 그대로 나왔다. 경계 위반 0건.

| 갈래 · 음훈 · 변형 | 건수 |
| --- | --- |
| `unmarked` · 음독 · 변형없음 | **7,928** ← 목표와 일치 |
| `rendaku` · 훈독 · 변형없음 | 453 ← 진짜 과잉 적용, 안 건드림 |
| `rendaku`/`handaku` · 변형있음 | 1,198 ← 진짜 연탁·반탁, 안 건드림 |

합계 9,579 — 어제 잰 탁음 판정 총합과 같다. **다른 유형에서 아무것도 안 끌어왔고
우선순위도 안 건드렸다.**

### 타입이 빠진 자리를 다 잡아줬다

`Record<VoicingKind, …>` 가 exhaustive 라 `VOICING_LABEL`·`voicingCounts`·테스트 기대값까지
컴파일러가 한 번에 짚었다. **갈래를 늘리는 쪽이 유형을 늘리는 쪽보다 안전한 이유가 이거다** —
빠뜨릴 자리를 사람이 찾지 않는다.

### 92e7381 의 테스트를 고쳤다

「과잉 적용 방향은 그대로 연탁으로 잡는다」가 `voicing: 'rendaku'` 를 기대했다. 갈래가 갈렸으니
깨지는 게 맞다. **그 테스트가 지키려던 건 유형이 사라지지 않는 것**이었으므로 `type` 만 보게
고치고 이름도 「판정이 죽지 않는다」로 바꿨다. 갈래 검증은 새 절이 맡는다.

### 화면까지 이어졌다

`voicing-unmarked.spec.ts` — 愛好 오답을 `RENDAKU` 로 심고 리포트 분포에 「청탁 미구분 2」가
뜨는지, 「연탁」 행이 **안 뜨는지**, 규칙 화면의 그 절이 愛好 를 기록으로 들고 있는지,
다시보기 배지가 청탁 절을 펼치는지까지 본다.

## 2026-09-18 — 3단계 · 미분류를 청탁 미구분으로 (사용자 지시)

### 「3단계」라고 묶어 부른 게 판단을 흐렸다

흩어진 7,336을 한 덩어리로 보고 「우선순위를 건드려야 하니 위험하다」고 미뤄 왔다.
갈라보니 **셋의 성격이 다르다.**

| 지금 어디 | 건수 | 이름이 틀렸나 | 끌어오면 |
| --- | --- | --- | --- |
| 미분류 | 3,521 | **이름이 없다** | 아무것도 안 뺏는다 |
| `KO_INTERFERENCE` | 2,596 | **안 틀렸다** — 절만 어긋난다 | 다른 유형에서 뺏는다 |
| `ONYOMI_CHOICE` | 1,219 | **모른다** — 갈라보기 전엔 판단 불가 | — |

**미분류만 떼면 우선순위를 안 건드린다.** 이번에 그것만 했다.

### 방법 — 뒤 검사에 양보한다

`stringVoicing` 이 규칙을 놓친 방향에서 `null` 을 내던 자리를 `'defer'` 로 바꿨다.
`fromStrings` 는 그걸 기억만 해두고 **촉음·장음·한국음 간섭을 다 거친 뒤 마지막 `null`
자리에서만** 쓴다.

```ts
let deferred = false
if (unvoiceAll 동치) { … voicing === 'defer' ? (deferred = true) : return RENDAKU }
if (촉음) return SOKUON
if (장음) return CHOON
if (한국음 간섭) return KO_INTERFERENCE
return deferred ? RENDAKU/unmarked : null      // ← 아무 데도 안 걸린 것만
```

**愛護 あいご ← あいこ 는 뺏지 않는다.** 「한국음 간섭」이 틀린 이름이 아니라서다 — 그 절에는
어제 청탁 문단을 넣어놔서 읽을 내용도 맞다. 이름이 **없는** 것만 받는다.

### 검증 — 착수 전 기준 그대로

직전 커밋의 코드로 같은 스크립트를 돌려 전후를 대조했다.

| | 전 | 후 |
| --- | --- | --- |
| `RENDAKU · unmarked` | 7,928 | **11,449** (+3,521) |
| 분류 안 됨 | 3,966 | **445** (−3,521) |
| `KO_INTERFERENCE` | 2,596 | 2,596 |
| `ONYOMI_CHOICE` | 1,221 | 1,221 |
| `rendaku` / `handaku` / `MIXED` | 1,341 / 310 / 2 | 그대로 |

**정확히 3,521이 미분류에서만 옮겨왔다.** 다른 유형에서 하나도 안 뺏었다.

남은 445는 **훈독 자리의 청탁**이다 (右手 みぎて ← みきて). 한국음은 훈독과 무관하므로
이 이름을 안 붙인다 — 미분류로 남는 게 맞다. 테스트로 박았다.

### 깨진 테스트 다섯 — 전제가 개선된 것이다

전부 「月額은 분류가 안 된다」를 기대하고 있었다. 3ef2a53 이 「연탁이 아니다」를 확정했을 때
쓴 것들이고, **그때는 이름이 없는 게 최선이었다.** 이제 이름이 생겼으니 기대값이 바뀌는 게 맞다.

각 테스트의 **논지는 「연탁이 아니다」**였으므로 그걸 살려 고쳤다 — 「어느 유형도 아니게
된다」를 「연탁이 아니라 청탁 미구분이 된다」로, 「절 색인에서 빠진다」를 「연탁 절에서는
빠지고 청탁 절로 간다」로. 기대값만 갈아끼우지 않고 **이름과 논지를 같이** 고쳤다.

### 남은 둘

- **`ONYOMI_CHOICE` 1,219** — `ONYOMI_CHOICE` 감사가 **선행 조건**이다. 그 글자에 청음 음독이
  따로 실재하면 「다른 음독을 골랐다」가 맞는 판정이라, 갈라보기 전엔 얼마를 끌어올지 모른다
- **`KO_INTERFERENCE` 2,596** — 급하지 않다. 이름이 안 틀렸고 절 내용도 맞다.
  절을 바꾸는 이득이 분포를 흔드는 대가보다 큰지는 실사용 빈도를 봐야 안다

## 2026-09-18 — 설정에 글자 크기 (사용자 요청)

대상은 **탭바·리포트·설정·찾기** 같은 셸 화면 텍스트. 학습 카드(숙어·읽기)는 원래도
`clamp(48px,16vw,88px)` 로 화면 폭에 맞춰 크게 떠 있고, PLAN §7 이 카드 루프를 별도로
다루므로 제외했다 — 사용자 요청도 "일본어 학습 페이지 이외"로 그 경계를 그렸다.

### 방법 — 파일 경계를 그대로 쓴다

이 코드베이스는 이미 스타일시트를 카드 루프(`study.css`)와 느린 모션 셸(`screens.css`)
둘로 갈라 놨다(각 파일 머리 주석 참조). 그 경계가 이번 요청의 경계와 정확히 겹친다.
그래서 `screens.css` 의 `font-size` 선언만 빼서 `calc(원래값 * var(--ui-scale))` 로
감쌌다(sed 일괄 치환, 67곳). `study.css` 는 손대지 않았다 — 학습 카드는 자동으로 빠진다.

**기각한 대안 — 루트 `font-size` 를 배율로 바꾸고 `rem` 통일.** 관례적인 방법이지만 두
문제가 있었다. (1) `screens.css` 안에도 `font-size` 아닌 `rem`(`margin: 0.5rem` 등)이
섞여 있어 루트를 건드리면 무관한 여백까지 같이 늘어난다. (2) 대부분이 이미 `px` 라
`rem` 변환 자체가 전수 계산이 필요해 더 큰 diff가 된다. `calc(px * var())` 방식은
`font-size` 선언 하나만 건드려 "Surgical Changes" 에 더 맞고, 루트 값도 그대로 둬서
`study.css`·기존 `rem` 여백에 부작용이 없다.

`--ui-scale` 은 `:root` 기본 1, `theme.ts` 와 같은 패턴으로 `data-text-scale` 속성이
얹히면 override(`src/index.css`). `src/app/textScale.ts` 가 `localStorage` 저장·적용을
맡고 `main.tsx` 가 첫 페인트 전에 건다(테마와 같은 자리).

### 단계 — 3단(작게 0.9 / 기본 1 / 크게 1.2)

테마·관찰 문구와 같은 3단 세그먼트 패턴을 그대로 따랐다. 값은 iOS 동적 타입의 체감
폭을 참고한 근사치이고, 사용자가 실사용 후 더 큰 단계를 원하면 그때 늘린다.

**검증** — `npm run build`(`tsc -b` + vite) 클린, `npx oxlint` 클린, `npm test` 508
테스트 통과. Playwright 로 설정 화면에서 작게/크게 전환 시 tabbar 글자가
13px→11.7px/15.6px 로 정확히 비례 확인, 학습 카드(`.headword`)는 크게 상태에서도
`clamp` 값(390px 폭에서 62.4px) 그대로임을 확인해 경계가 지켜짐을 실측으로 검증했다.
설정·리포트 화면 스크린샷으로 레이아웃 밀림·잘림 없음 확인(스크린샷은 scratchpad,
커밋 안 함).

## 2026-09-18 — 갈래는 정답 쪽 규칙이 정한다 (사용자 지적 「寸法 는 반탁 아닌가」)

사용자가 寸法 가 연탁으로 뜬다고 했다. 맞다.

```
寸法 すんぽう → 寸:すん[]  法:ぽう[handaku]     ← 분해는 반탁인 걸 안다
寸法 すんぽう ← すんほう  → 반탁 ✓
寸法 すんぽう ← すんぼう  → 연탁 ✗
```

답이 ぼう 면 `ほう + rendaku` 로 분해된다. 그러면 판정 가지가 이 순서로 걸린다.

```ts
} else if (differs('rendaku') || differs('renjo')) {   // ← 여기서 먼저 잡힌다
} else if (differs('handaku')) {                       // ← 여기까지 못 온다
```

**답의 모양이 갈래를 정하고 있었다.** 정답 쪽이 반탁 규칙을 들고 있는데 무시됐다.

### 원칙 — 답이 아니라 정답 쪽 규칙

`voicingOfExpected(e)` 를 만들어 분해 경로 둘과 문자열 경로에 다 걸었다. 정답 조각이
변형을 들고 있으면 그게 갈래고, 없으면 음독은 `unmarked`, 훈독은 부른 쪽 판단에 맡긴다.

**이건 어제부터 해온 것과 같은 원칙이다.** `voicedAt`·`unmarkedVoicing` 은 전부 정답 쪽을
봤는데 **갈래 이름을 고르는 마지막 한 줄만 답을 보고 있었다.** 원칙을 세 군데에 적용하면서
네 번째를 빠뜨린 모양이다.

### 전수 확인

코퍼스 전체에 청음화·탁음화 오답(ぱ행→ば행 포함)을 합성해 「판정된 갈래 = 정답 쪽 변형」을
대조했다. **불일치 0.**

| | 전 | 후 |
| --- | --- | --- |
| `RENDAKU·rendaku` | 2,528 | 2,221 (−307) |
| `RENDAKU·handaku` | 310 | **617** (+307) |
| `unmarked`·`KO_INTERFERENCE`·`ONYOMI_CHOICE`·`null`·`MIXED_READING` | | **전부 동일** |

**307건이 연탁에서 반탁으로만 옮겼다.** 유형은 하나도 안 움직였으므로 리포트의 오답 유형
분포도 그대로고, 탁음 바구니 **안에서만** 갈래가 재배분된다.

### 왜 안 잡혔나

어제 만든 감사 탐침이 **ぱ행을 탁음화하지 않았다.** `VOICE` 표에 か→が·は→ば 는 있었는데
ぱ→ば 가 없었다. 반탁 자리에 탁음을 쓰는 오답을 아예 생성하지 않아 이 구멍이 안 보였다.
탐침 표를 넓혀 다시 쟀다 — **탐침이 못 만드는 오답은 감사에도 안 보인다.**

### 寸法만이 아니었다 — 306개 숙어

넓힌 탐침으로 재니 같은 모양이 **307건 / 306개 숙어**다 — 圧迫 あっぱく ← あっばく,
安否 あんぴ ← あんび, 一杯 いっぱい ← いっばい. 전부 반탁 자리(`っ`·`ん` 뒤 は행)를 탁음으로
쓴 오답이다.

반탁 자리를 틀리는 방식은 둘인데 **한쪽만 제대로 가고 있었다.**

- `ぱ` → `は` (규칙을 안 씀) — 반탁으로 갔다
- `ぱ` → `ば` (탁음으로 씀) — **연탁으로 샜다**

한국음 「압·안」에 받침이 있으니 뒤가 탁해진다고 유추하기 쉬워서, 두 번째가 더 흔한
오답일 수 있다. 그 흔한 쪽이 틀린 절로 가고 있었다.

### 곁가지 — 殺生 는 버그가 아니었다

사용자가 「殺生는 촉음일 것 같은데 음독 선택으로 나온다」고 짚어 확인했다. **실제 기록을
열어 보니** 답이 `さっしょう`·`さつじょう` 였다.

```
さっしょう  殺:さっ[sokuon] 生:しょう          ← 촉음은 제대로 썼다
さつじょう  殺:さつ         生:じょう[rendaku]  ← 원형도 틀리고 탁음도 덧댔다
```

`さっ` 에 촉음이 제대로 걸려 있다. 촉음을 안 틀렸고 `セツ` 자리에 `サツ` 를 넣은 것만
틀렸다. **낱말에 촉음便이 들어 있는 것과 그 사람의 오답이 촉음 오답인 것은 다르다.**
전수 확인 — 촉음만 놓친 오답 **961 / 961 이 `SOKUON`** 이다.

기록이 판단을 끝냈다. 추측으로는 「우선순위가 맞나」를 며칠 붙들 수 있었는데,
`answer` 를 저장해 둔 덕에 한 번에 닫혔다 — **이벤트에 답을 남기는 설계가 값을 했다.**

**남은 질문 하나** — 원형과 촉음이 **둘 다** 틀린 복합 오답 579건에서 촉음은 아무 말도 안
듣는다. 우선순위가 하나만 고르기 때문이고 설계된 동작이다(「`セツ` 를 모르면 촉음을 물어봐야
소용없다」). 「둘 다 틀렸을 때 하나만 말하는 게 맞나」는 검토한 적이 없다.

## 2026-09-18 — 오답 카드에 내가 쓴 답 대조 (사용자 요청)

"틀리면 어디가 틀린 건지 다른 색으로 표시" 요청. 정답은 이미 헤드워드 위 루비로 뜨지만,
**내가 쓴 답 자체**는 잠긴 입력창(`.kana-input`, locked)에만 남고 아무 데서도 안 보였다 —
native `<input>` 안이라 글자 단위로 색을 못 칠한다.

### 방법 — LCS 대조, 자리별 비교 아님

`src/core/answerDiff.ts` `diffAnswer(expected, answer)`. **자리별(index) 비교를 기각했다** —
촉음·장음 하나가 빠지거나 늘면 그 뒤 글자가 전부 밀리는데, 자리별 비교는 밀린 뒤 전부를
오답으로 표시해 실제로 안 틀린 글자까지 빨갛게 만든다. 최장 공통부분수열(LCS)로 대조하면
밀린 뒤에도 같은 글자를 다시 찾아 맞춰, `answer` 문자열에서 **진짜 남는 것만** 틀린 것으로
남는다. `answerDiff.test.ts` 의 촉음 삽입 케이스(`はつひょう`→`はっつひょう`)가 이 차이를
고정한다 — 삽입된 `っ` 하나만 표시되고 뒤 `つひょう`는 안 밀린다.

**빠진 글자(생략)는 answer 쪽에 표시할 자리가 없다.** 장음 누락(とくちょう→とくちょ)처럼
정답보다 짧은 답은 쓴 글자 전부가 "일치"로 나온다 — 없는 글자를 빨갛게 칠할 수는 없어서다.
루비가 이미 정답 전체 길이를 보여주므로 길이 차이 자체는 거기서 드러난다고 보고, 이
비대칭은 기능 한계로 남겨뒀다(추가 표시 없음 — 범위를 넘는 일이라 이번엔 안 함).

### 기각한 대안 — `mistakes.ts` 를 건드려 판정 유형별로 정확한 위치를 낸다

오답 유형 분류기(`explainMistake`)가 이미 정답·오답을 자리별/조각별로 비교하고 있어
그 결과를 재사용하는 안도 있었지만, **지금 다른 세션이 그 파일(`mistakes.ts`)을 몇 분
간격으로 계속 커밋하며 청탁/갈래 분류를 고치는 중**이라 같은 파일을 건드리면 충돌
가능성이 높았다. `answerDiff.ts` 를 완전히 새 파일로 분리해 `mistakes.ts` 는 안 건드렸다 —
화면 표시용 순수 문자열 대조라 분류기의 판정 로직과 독립적으로도 충분했다.

### 정규화 — 채점과 같은 기준을 쓴다

`diffAnswer` 호출 전에 `toHiragana(x.trim())` 을 양쪽에 먼저 건다 — `isCorrectReading`
(`session.ts`)이 정오답을 가르는 기준과 똑같이 맞춰야, 가타카나·trim 차이가 거짓 오답처럼
빨갛게 뜨는 일이 없다.

### 표시 — 색만 안 쓴다

PLAN §7 "정오답은 색만으로 구분하지 않는다"를 따라 틀린 글자에 `--ng` 색 + 굵기 + 밑줄을
같이 준다(`diff-wrong` 클래스, `study.css`).

**검증** — `answerDiff.test.ts` 6 테스트(정답 전부 일치·치환·장음 누락·촉음 삽입·완전
불일치·빈 문자열). `npm test` 517 통과, `npx playwright test` 29 스펙 전부 통과.

### 자리 — 새 줄이 아니라 입력창 "안" (같은 날, 사용자 피드백으로 수정)

첫 구현은 헤드워드 루비 아래 "내가 쓴 답: …" 한 줄을 새로 더했다. **사용자가 "복잡하게
느껴진다"고 지적** — 기대한 건 입력창에 이미 떠 있는 글자 자체가 색만 바뀌는 것이었다.

**기각 — 새 줄 추가.** 화면에 요소가 하나 늘고, 정답(루비)·오답(새 줄)·입력창(원문)
셋이 같은 텍스트를 세 자리에서 반복해 보여주는 꼴이 됐다. 사용자가 원한 그림이 아니었다.

**채택 — `KanaInput` 안에 오버레이.** `<input>` 은 네이티브 요소라 글자별로 색을 못
칠한다. 그래서 `locked` 상태에서 `has-diff` 클래스로 원래 글자를 투명 처리하고, 완전히
똑같은 텍스트를 절대 위치로 겹쳐 그린 뒤 그 오버레이 글자만 대조색을 입힌다 — 입력값
자체(`ref.current.value`)는 그대로 유지되니 제출 로직은 안 건드렸다. `diff` 를 새 prop으로
받아 `ReadingCard` 가 넘기고, `Diagnostic.tsx` 처럼 `diff` 를 안 주는 자리는 기존 그대로.

**곁가지로 잡은 버그 — 이름이 같은 두 `.feedback`.** 오버레이를 넣고도 글자가 안
지워졌다. 범인은 `screens.css` 의 `.feedback input[type='text']` — 설정의 "피드백
보내기" 화면(`onFeedback`)용 규칙인데, `ReadingCard` 의 오답 카드도 `className="card
feedback is-ng"` 를 써서 **같은 이름의 다른 화면**이 우연히 조상 선택자로 걸렸다.
특정도가 `.kana-input.has-diff` 보다 높아 색을 도로 덮어썼다. `.kana-input-shell` 로
한 겹 더 감싸 특정도를 올려 고쳤다 — `.feedback` 이름을 바꾸는 건 범위 밖이라 안 했다.

**검증** — 같은 517·29 스펙 재실행, `getComputedStyle` 로 `color: rgba(0,0,0,0)` 실측
확인(고치기 전엔 `.feedback` 규칙이 이겨 `rgb(26,23,20)` 로 그대로 불투명했다). 설정의
피드백 폼 입력은 안 건드렸는지 스크린샷으로 대조.

## 2026-09-18 — 오답 유형 분포에서 1등만 걸러 다시보기 (사용자 요청)

사용자가 물었다 — "그래프 아래에 나오는 항목(한국음 간섭)이 가장 오답이 많은
유형을 보여주는 것이냐". 확인해 보니 **아니었다.** `Report.tsx` 의 "한국음 간섭"
섹션은 그래프에서 몇 등이든 상관없이 **항상 고정으로 `KO_INTERFERENCE` 유형만**
보여준다(PLAN §6 "한국음 간섭이 이 사용자에게 특히 유효한 축"이라 따로 뽑아둔
것). 사용자는 "그렇다면 실제 1등이 노출되도록 고치고, 거기 다시보기 버튼을 붙여
달라"고 답했다.

### 기각 — 「한국음 간섭」 섹션을 진짜 1등으로 바꿔치기

PLAN이 그 섹션을 **일부러 고정**해 뒀다 — 이 앱만의 진단 축이라 그날그날 등수와
무관하게 항상 보여야 값이 있다는 설계였다. 사용자의 첫 질문은 "그게 1등이 아니라면"
이라는 조건문이었지 "한국음 간섭을 없애라"가 아니었다. 그래서 **그 섹션은 그대로
두고**, 그래프 바로 아래에 진짜 1등을 보여주는 자리를 **새로** 만들었다.

### 구현 — 이미 있는 재료로

- **1등을 아는 자리가 이미 있었다.** `mistakeRows`(Report.tsx)가 분포 막대를 만들며
  이미 count 내림차순으로 정렬해 두므로 `rows[0]` 이 곧 1등 막대다. 이 함수가
  돌려주던 `{key,label,count}` 에 `type`·`voicing` 을 얹기만 하면 다시보기 필터에
  바로 쓸 수 있었다
- **탁음 갈래(연탁/반탁/연성/청탁미구분)까지 정확히 좁힌다.** 그래프가 이미 탁음
  한 바구니를 갈래로 쪼개 보여주고 있어서(2026-09-17 「분포 표시와 처방 문턱은
  다른 축」), 1등이 "반탁"이면 다시보기도 반탁만 나와야 한다 — "연탁"으로 뭉뚱그리면
  그래프와 다시보기가 다른 걸 보여주는 꼴이 된다
- **`ruleRecord.ts` 에 `frequentIdiomsByMistake` 를 새로 추가.** 규칙 절의
  "이 규칙으로 틀린 기록"(`ruleRecord`)과 잣대(오답 유형+갈래 일치)는 같지만, 그건
  절 색인용으로 상위 8개만 정렬해 자르고, 이건 `pickBrowse`(다시보기 셔플)로 넘길
  전량을 `frequentIdioms` 와 같은 규약(id 순 고정, 셔플은 나중)으로 돌려준다.
  이 둘을 하나로 합치지 않았다 — 반환 형태와 정렬 목적이 달라서 억지로 합치면
  한쪽이 다른 쪽 사정에 끌려간다
- **`App.tsx` 의 `Flow['browse']` 가 선택적 `filter` 를 받도록만 확장했다.**
  `focus` flow(집중 세션, 이미 `pairId` 를 받는 패턴)와 같은 관례다. 필터가 없으면
  기존 "다시보기"(전체 자주 틀린 것) 그대로 동작 — 기존 진입점(`onBrowse`)은
  안 건드렸다
- **주차장에 있던 항목이었다.** checklist "다시보기에 남은 것" 절이 이미
  "규칙 절에서 「이 규칙으로 틀린 것만 다시보기」로 잇는 연결 (한 줄 규모)"을
  미뤄 두고 있었다 — 이번 요청이 그 반대 방향(그래프 → 다시보기)이라 규모·설계가
  거의 같았다

### 검증

`ruleRecord.test.ts` +5(`frequentIdiomsByMistake` — 유형만/갈래로 갈라내기/이름
모르는 숙어 제외/불일치 시 빈 배열/id 순 고정). `npm test` 522 통과, `tsc -b`/`oxlint`
클린, `npx playwright test` 29 스펙 전부 통과. IndexedDB 에 SOKUON 5건(明白)·CHOON
2건(御金)을 직접 심어 실측 — 분포가 "촉음 5 / 장음 2" 로 뜨고 버튼이 "촉음 5회
다시보기"로 정확히 1등을 가리켰다. 눌러 들어가면 1/1장(明白만), 태그 "다시보기 ·
촉음" — 御金(CHOON)이 안 섞여 들어옴을 확인했다.

## 2026-09-18 — 고정 「한국음 간섭」 칸을 1등 오답 칸으로 (사용자 지시)

사용자: "진단 리포트가 너무 장황하다. 고정으로 되어 있는 한국음 간섭 항목을 같은 ui로
1등 오답 항목이 노출되도록 하고 모아서 다시보기로 연결되는 버튼을 해당 ui 상단쯤에
노출하라."

### 같은 날 오전에 기각했던 것을 사용자가 뒤집었다

오전 절(「오답 유형 분포에서 1등만 걸러 다시보기」)에서 **"한국음 간섭 섹션을 1등으로
바꿔치기"를 기각**했다 — PLAN이 일부러 고정해 둔 축이라는 이유였다. 그래서 섹션은 두고
그래프 아래에 버튼만 새로 달았는데, 사용자가 그 결과를 보고 "장황하다"고 판정했다.
**칸 하나에 같은 정보가 두 번 나오고 있었다** — 분포 그래프 1등 막대, 그 아래 1등
다시보기 버튼, 그리고 등수와 무관한 고정 칸.

**뒤집은 근거는 표본이 바뀐 것이다** (사용자 설명, 같은 날). 고정 칸은 기록이 적을 때
정해졌고, 그때는 한국음 간섭이 다른 유형을 압도해서 **고정 칸과 1등 칸이 사실상 같은
칸**이었다. 기록이 쌓이자 그에 못지않은 유형들이 올라왔고, 그때부터 고정 칸은 1등을
가리는 칸이 됐다. PLAN의 고정 결정이 틀렸던 게 아니라 그 결정을 떠받치던 분포가 지나갔다.

같은 이유로 **이 칸을 다시 특정 유형에 고정하지 않는다.** 분포는 또 바뀐다. 한국음 간섭이
다시 1등이면 이 칸이 그대로 한국음 간섭을 보여준다 — 잃은 게 없다.

PLAN §7 표의 「진단 리포트 — … 한국음 간섭 패턴」 줄도 같이 고쳤다. 문서가 화면과 다른
말을 하면 다음 세션이 다시 이 칸을 고정으로 되돌린다.

### 구현

- **고정 칸의 골격은 그대로 쓴다** (`.ko-callout` → `.top-mistake`, `.ko-big` →
  `.top-count`). 사용자가 "같은 ui"라고 지정했다. 제목이 유형 이름이 되고, 큰 숫자가
  그 유형의 횟수, 아래 목록이 그 유형으로 틀린 숙어다
- **1등은 `rows[0]`**. `mistakeRows` 가 이미 count 내림차순이라 오전에 만든 버튼과
  **같은 행**을 쓴다 — 그래프 1등 막대 · 칸 제목 · 버튼이 갈라질 수 없다. 탁음이면
  갈래(반탁/연성/청탁 미구분)까지 그대로 따라간다
- **숙어 목록은 `frequentIdiomsByMistake`** — 다시보기 버튼이 여는 목록과 같은 함수·같은
  갈래 판정(`verdictByEvent`)이다. 보이는 숙어와 눌러서 나오는 카드가 어긋나면 안 된다.
  다만 8개만 싣는 표본이라 **자른 뒤에도 값이 있도록 많이 틀린 순**으로 정렬한다
  (`ruleRecord` 와 같은 관례). 예전 고정 칸은 id 순이었다
- **버튼은 칸 맨 위** (제목 바로 아래, 숫자 위). 사용자가 "상단쯤"이라고 지정했고,
  숫자·목록은 근거라 손이 먼저 닿을 자리가 아니다. 오전에 그래프 아래 있던 버튼은
  **옮긴 것이지 새로 만든 게 아니다** — 같은 진입로가 둘이면 그게 장황함이다
- **`Report` 타입에서 `koInterferenceCount`·`koInterferenceIdioms` 를 뺐다.** 이 칸
  하나만 쓰던 필드다. 유형별 집계는 `mistakes` 분포가, 숙어 목록은
  `frequentIdiomsByMistake` 가 이미 유형을 가리지 않고 낸다. 남겨 두면 "한국음 간섭만
  특별히 세는 자리"가 계속 있는 셈이라 다음 사람이 그걸 근거로 칸을 되돌린다
- **분류된 오답이 하나도 없으면 칸이 아예 안 뜬다.** 예전 칸은 0회여도 "해당 숙어가
  아직 없어요"로 자리를 지켰는데, 1등이 없는 상태에서 1등 칸을 띄울 이유가 없다.
  바로 위 「오답 유형 분포」가 같은 빈 상태를 이미 말한다

### 검증

`npm test` 521 통과(`report.test.ts` 의 한국음 간섭 집계 테스트 1건은 필드와 함께 제거 —
같은 잣대는 `ruleRecord.test.ts` 의 `frequentIdiomsByMistake` 5건이 이미 지킨다).
`tsc -p tsconfig.app.json`/`tsconfig.e2e.json` 클린. `report-voicing.spec.ts` 에 실측
추가 — 반탁 2·연탁 1을 심으면 칸 제목이 「반탁」, 큰 숫자 2, 목록에 心配, 맨 위 버튼을
누르면 心配만 나오고 三日月(연탁)은 안 섞인다. `full-flow.spec.ts` 의 `.ko-callout`
단언은 `.top-mistake` 로 바꾸되 **분포 막대가 있을 때만** 본다 — 그 스펙은 'zzz' 로만
답해서 분류기가 유형을 못 붙이는 경우가 있다. 420px 실캡처로 칸 배치도 확인했다.

## 2026-09-18 — 리포트 다시보기 정리 · 미분류를 기타로 (사용자 지시)

사용자: "진단 리포트가 여전히 장황하다. 다시보기 항목을 정리하겠다. 위치를 오답 유형 분포
아래에 두고 현재 한국음 간섭(1등 오답 유형) 목록을 삭제하고 다시보기 영역을 배치한다.
그곳에 한라인에 양쪽으로 무작위 다시보기, 오답유형별 다시보기 버튼을 배치한다. 그리고
오답 유형 분포 맨 아래의 유형을 못 붙인 오답은 기타항목으로 잠정 분류하도록 하라."

### 왜 앞선 두 번으로 안 끝났나

같은 날 두 번 손댔는데도 장황함이 남은 이유는 **진입로와 근거를 섞어 놨기 때문**이다.
오전에는 그래프 아래 버튼을 달았고, 오후에는 고정 칸을 1등 칸으로 바꿨다. 그래서 1등
유형이 그래프 막대·큰 숫자·숙어 목록·버튼으로 네 번 나왔다. 이번에는 **1등 유형에
내주던 칸을 통째로 없애고**, 그 자리에 있던 진입로만 다시보기 영역으로 모았다.
1등 유형의 값은 그래프 맨 윗 막대가 이미 말한다 — 같은 값을 두 번 그리지 않는다.

### 배치

- **분포 → 다시보기 순.** 다시보기는 "무엇을 다시 볼까"의 답이라 분포(무엇을 틀렸나)
  다음이어야 한다. 그 전까지는 분포 위에 있어서 근거보다 행동이 먼저 나왔다
- **버튼 둘을 한 줄에 양쪽으로** (`.browse-pair`, `grid-auto-flow: column`). 무작위와
  1등 유형별은 같은 성격의 선택이라 무게도 같게 둔다. 각 버튼은 이름 + 작은 글씨
  (「1장」 / 「반탁 2회」) 두 줄이다 — 유형 이름을 작은 글씨로 내려야 버튼 이름이
  사용자가 지시한 그대로 남는다. 1등 유형이 없으면(분류된 오답 0) 무작위 버튼 하나가
  줄을 다 쓴다 (`grid-auto-columns: 1fr`)
- **미분류는 분포 안 「기타」 막대로.** 각주(`.unclassified`)로 분포 밖에 빼 두면
  같은 것을 두 군데서 읽어야 한다. **등수와 무관하게 맨 아래**에 고정한다 — 정렬에
  끼워 넣으면 미분류가 제일 많을 때 `rows[0]`(1등 유형)을 밀어내고, 그러면 "오답
  유형별 다시보기"가 가리킬 유형이 사라진다. `maxCount` 에는 포함시킨다(같은 자로
  재야 막대 길이가 비교된다)
- **라벨은 「기타」 한 단어.** 「기타 유형 미분류」로 달았더니 라벨 칸이 5.5em 이라
  두 줄로 접혔다 (420px 실캡처에서 확인). 설명을 붙이려면 줄이 하나 더 드는데, 그게
  방금 걷어낸 각주다

### 검증

`npm test` 521 · `tsc -b` · `oxlint` 클린 · `playwright` 29 스펙 전부 통과.
`report-voicing.spec.ts` 에 **미분류 이벤트 1건**(`mistakeType: null`)을 심어 늘렸다 —
분포가 반탁 2 / 연탁 1 / 기타 1 로 뜨고 기타가 마지막 행이며, 「오답 유형별 다시보기」
버튼이 「반탁 2회」를 달고 눌렀을 때 心配만 나온다(三日月 안 섞임). 420px·360px 실캡처로
버튼 두 개가 한 줄에 접히지 않고 들어가는 것을 확인했다.

## 2026-09-18 — 기타 190건의 정체 · 청탁 관문 버그 · 「잘못 읽기」 (사용자 지시)

사용자: "기타 항목으로 분류한 것이 데이터 상으로 너무 많다. 어떤 유형이 위의 필터를
거치지 못한 것인가."

### 실사용 기록으로 쟀다 (추정이 아니다)

읽기 오답 1,052건 중 **기타 190건(18%)** — CHOON·ONYOMI_CHOICE 보다 많은 2등이었다.
사용자가 브라우저 콘솔에서 뽑아 준 미분류 185건을 분류기에 그대로 넣어 갈랐다.

| 성격 | 건수 | 예 |
|---|---|---|
| 다른 읽기를 통째로 씀 | 129 (70%) | 木炭 もくたん ← きざい(木材) · 削減 ← しょうめつ(消滅) |
| 한 글자만 다름 (음운 축 아님) | 48 (26%) | 隔絶 かくぜつ ← かんぜつ · 悪夢 あくむ ← あくみ |
| 청탁만 다른데 못 걸림 | 8 (4%) | 人心 じんしん ← しんしん · 寸法 すんぽう ← ずんぽう |
| 넘김(빈 답) | 5 (3%) | — |

**내 첫 가설(「모르겠어요」 넘김이 기타를 채운다)은 틀렸다.** 넘김은 5건뿐이었다.
합성 오답으로 먼저 쟀을 때 「빈 답 100% 미분류」가 나와 그쪽을 1순위로 봤는데,
실제 사용자는 넘기지 않고 **답을 쓴다.** 합성 표본이 실사용 분포를 대신할 수 없다는
증거로 남긴다.

**기타의 96%는 분류기 결함이 아니라 6종의 정의 범위 밖이다.** 6종은 「답이 그 한자의
실재 읽기이거나, 정답과 음운 한 축만 다르다」를 전제로 이름을 붙인다 — `ONYOMI_CHOICE`
는 답이 그 한자의 **다른 음독**일 때만, `KO_INTERFERENCE` 는 **한국음 형제 한자의
음독**일 때만 붙는다. 기억이 안 나 근사치를 쓰면 어느 쪽도 아니다.

### 버그 — じ·ず 는 청음이 둘인데 표가 하나만 들고 있었다

`UNVOICE`(readings.ts)는 `RENDAKU` 표를 뒤집어 만드는데 `ち: ['ぢ','じ']` 가
`し: 'じ'` 를 덮어쓴다(`Object.fromEntries` 는 뒤가 이긴다). 그래서
**`unvoiceAll('ず') = 'つ'`, `unvoiceAll('じ') = 'ち'`** 였고, さ행 짝으로는 영영 안 돌아갔다.
탁음 판정의 관문이 `unvoiceAll(expected) === unvoiceAll(answer)` 라 し↔じ·す↔ず 오답은
탁음 경로를 **아예 못 탔다.**

- 고친 자리는 `unvoiceAll` 이 **아니라 관문**이다. `unvoiceAll` 은 `unvoiceHead` 를 거쳐
  음독 쌍 ID 생성(`build-onyomi-map`)에도 쓰여서, 건드리면 사전 산출물이 움직인다
- 새 `sameExceptVoicing(a, b)` 는 되돌리기를 **함수가 아니라 관계**로 본다 — 두 글자의
  청음 후보가 하나라도 겹치면 같은 자리로 본다. 예전 판정을 **전부 포함**한다(증명:
  `unvoiceAll` 이 고른 청음은 언제나 후보 집합의 원소다). づ↔ず 처럼 둘 다 탁음인 짝도
  그대로 통과한다

### 전수 대조 — 합성 오답 43,698건 (자리 하나의 청탁을 양방향으로 뒤집음)

- 미분류 → 청탁 미구분 **1,092** · 미분류 → 연탁 **320**
- KO_INTERFERENCE → 청탁 미구분 **1,147** · → 연탁 **6**
- `ONYOMI_CHOICE`·`SOKUON`·`MIXED_READING`·반탁 **전부 불변**. 바뀐 총계 2,565

**KO 에서 옮긴 1,153건은 정책 변경이 아니다.** 과잉 방향(정답 청음, 답 탁음)은 원래
탁음이 KO보다 앞섰다 — 고치기 전에도 愛好 あいこう ← あいごう, 開国 かいこく ← がいこく
가 청탁 미구분이었다. さ행만 관문이 막혀 KO로 새고 있었고(使者 ししゃ ← じしゃ), 심지어
**같은 さ행인데 자리에 따라 갈렸다**(둘째 자리는 분해 경로로 가서 탁음 판정). 이제 한
잣대다. 누락 방향은 여전히 KO 에 양보한다(学校 がっこう ← かっこう, `'defer'`).

실사용 185건 중 **7건**이 이름을 얻는다. 氏子 うじこ ← うしこ 는 훈독 자리라 그대로
미분류다 — 「청탁 미구분」은 음독 전용이라는 기존 경계(右手 みぎて ← みきて)를 안 건드렸다.

**저장된 이벤트는 안 바뀐다.** `reclassifier` 는 `RENDAKU` 로 저장된 것만 다시 매긴다.
지난 KO_INTERFERENCE 152건은 그대로 KO다 — 새로 쌓이는 오답부터 적용된다.

### 「잘못 읽기」 — 이름은 읽을 때만 붙인다

기타의 96%에 이름을 준다. **`MistakeType` 에 값을 추가하지 않았다** — 스키마 불변 조건
(CLAUDE.md)을 건드리는 결정이라 사용자 승인 없이 못 한다. 대신 이미 있는 관례를 쓴다:
저장은 `null` 그대로, **읽을 때만** 표시 이름(`UNNAMED_LABEL`)을 붙인다. 갈래를 답에서
다시 매기는 길(`reclassifier`)과 같은 성격이다.

- **「잘못 읽기」는 정렬에 든다.** 오늘 오전 「기타는 맨 아래 고정」으로 정했던 걸 뒤집는다.
  그때 이유는 *다시보기 버튼이 가리킬 수 없는 행이라 1등이 되면 안 된다* 였는데, 이제
  가리킬 수 있으니 이유가 사라졌다. 1등이면 그 단어들을 모아 보는 게 맞는 처방이다
- **넘김(빈 답)은 「잘못 읽기」와 분리해 맨 아래 고정.** 이름이 없는 게 아니라 **답이 없는**
  것이라 다시 볼 대상이 아니다. `answer === ''` 로 갈린다
- `frequentIdiomsUnnamed` 는 `frequentIdiomsByMistake` 의 짝이지만 **거르지 않은 이벤트
  전량**을 받는다 — 유형이 붙은 것만 걸러 오면 찾을 대상이 애초에 없다

### 검증

`npm test` **529**(+8: 관문 4 · `frequentIdiomsUnnamed` 5) · `tsc -b`/`oxlint` 클린 ·
`playwright` **30 스펙**(+1: 「잘못 읽기」가 1등이면 그 숙어만 모아 다시본다).
`report-voicing.spec.ts` 에 넘김 이벤트를 심어 분포가 반탁 2 / 다른 읽기 1 / 연탁 1 /
넘김 1 로 뜨고 넘김이 마지막 행인 것을 확인했다. 420px 실캡처로 배치도 봤다.

**e2e 에서 배운 것** — 三日月(1579550)은 **코퍼스에 없는 숙어**다. 집계 경로는 이름이
필요 없어서 분포에는 뜨는데 다시보기 목록에는 안 실린다. 다시보기를 검증하는 스펙은
`base.json` 에 실재하는 숙어로 심어야 한다.

### 이름은 「잘못 읽기」다 (사용자 지적, 같은 날)

처음 붙인 이름은 「다른 읽기」였는데 사용자가 「잘못 읽기가 정확하지 않겠나」라고 물었다.
맞다. **이 앱은 이미 「다른 읽기」를 정답 쪽에 쓴다** — 동형이독의 또 하나의 정답 읽기가
그것이고(`homograph.ts`, `session.ts` 의 `altReadings`, 「읽기 둘」 카드), 그 자리에서
「다른 읽기」는 *맞는* 읽기를 가리킨다. 오답 이름으로 같은 말을 쓰면 분포를 읽는 사람이
「정답이 둘인 단어」와 헷갈린다.

### 기각 — 「잘못 읽기」를 한 글자 차이와 통째로 다름으로 쪼개기

내가 제안했고 사용자가 기각했다. 근거: **"표현의 일부가 틀렸든 전체가 틀렸든 잘못 읽은
것은 맞기 때문이다."** 나는 처방이 다르다고 봤지만(앞쪽은 그 한자의 음을 굳히기, 뒤쪽은
두 단어를 나란히 놓고 구별하기), 학습자가 보는 화면에서 그 둘은 같은 행동으로 이어진다 —
그 숙어를 다시 보는 것이다. 편집 거리로 가르는 경계도 자의적이다(2 이상은 전부 「통째로」).
분포에 행만 하나 늘고 진단은 안 는다.

## 2026-09-18 — 분포 막대를 비중 3단 색으로 (사용자 요청)

"오답률이 미미하면 안심, 높으면 주의"라는 인상을 색으로 달라는 요청. **"같은 계통"**
이라는 조건이 붙었다.

### 퍼센티지의 분모 — 이미 있던 잣대를 그대로 썼다

"몇 %인가"를 새로 정의하지 않았다. `prescription.ts` 의 `RxItem` 이 이미 오답 유형
처방에 "오답의 {Math.round(p.share*100)}%" 를 쓰고 있었고, 그 `share` 는
`count / report.totalWrong` 이다. 분포 막대도 같은 유형·같은 count 를 세므로 같은
분모를 쓰는 게 맞다 — 리포트 안에서 "퍼센티지"가 두 가지 뜻이 되면 안 된다.

높은 쪽 문턱도 새로 안 짓고 처방이 뜨는 문턱(`DOMINANT_SHARE = 0.3`)을 그대로
가져왔다. **의도적이다** — "그래프가 진하게 보여주는 유형" 과 "처방이 짚는 유형"이
다른 기준이면, 진하게 뜬 막대를 눌러도 처방에 안 나오는 모순이 생긴다. 낮은 쪽
문턱은 그 절반(0.15)으로 뒀다 — 대칭이라 설명하기 쉽고, 이 기준을 흔들 실사용
근거가 아직 없다.

### 색 — 새로 안 만들고 짙기만 바꾼다

PLAN §7 "무채색 기반, 색은 오답에만"이 이미 있다. 3단을 초록·노랑·빨강 같은 신호등
배색으로 하면 이 원칙을 정면으로 깨고 "오답"이라는 화면에 새 색 체계를 들이는
꼴이라 기각했다. 대신 `--ng` 하나의 **불투명도**만 0.4 / 0.7 / 1 로 바꿨다
(`.bar-fill.sev-low`·`.sev-mid`, `sev-high` 는 기본값이라 별도 규칙 없음).
막대 트랙 배경(`--surface-2`) 위에 얹히므로 낮을수록 저절로 옅어 보인다 — 색상값을
따로 섞을(`color-mix` 등) 필요가 없었다.

### 검증

`npm test` 529 · `playwright` 30 스펙 · tsc/oxlint 클린. IndexedDB 에 4유형을
50%/25%/15%(경계)/10% 비중으로 심어 실측 — `sev-high`(불투명 1) · `sev-mid`(0.7,
25%·15% 둘 다) · `sev-low`(0.4, 10%) 로 정확히 갈렸다. 스크린샷으로 촉음(진함) →
음훈 혼독(옅음)까지 한눈에 짙기가 빠지는 것 확인.

### 곁가지 버그 — 넘김이 항상 제일 진했다 (같은 날, 사용자 지적)

「넘김」 막대는 `rows` 밖에 있어 `sev-*` 클래스를 안 받고 기본 `.bar-fill` 로
남았는데, 기본값이 곧 `sev-high` 와 같은 불투명 `--ng` 다. 그래서 오답이 하나도
없어도(전부 「모르겠어요」로 넘긴 경우) 넘김 막대가 그래프에서 제일 진하게 떴다 —
"주의해서 봐야 한다"는 인상이 정반대로 가장 안전한 자리에 붙는 꼴이었다.

**색을 오답 계통(`--ng`) 밖으로 뺐다.** `.bar-fill.passed { background:
var(--text-faint) }` — 넘김은 "심각도가 낮은 오답"이 아니라 애초에 오답이 아닌
것(답이 없음)이라, 옅은 빨강이 아니라 아예 다른 색(무채 회색)을 써야 그 구분이
선다. `sev-low` 를 붙이는 것도 고려했지만, 그러면 "이 유형은 15% 미만이라
안심"이라는 잘못된 메시지가 여전히 남는다 — 넘김은 비중이 아무리 낮아도 "오답
패턴"이 아니므로 척도 자체에서 빼는 게 맞다.

검증 — 촉음 3건(사실상 100% 점유) vs 넘김 7건으로 심어, 촉음이 진한 빨강·넘김이
회색으로 뜨는 것을 스크린샷으로 확인. `npm test` 529 · `playwright` 30 스펙 유지.

---

## 2026-09-18 — 예문이 다른 단어를 보여주던 것 (사용자 지적 「日照 예문에 日照り가 나온다」)

`build-examples.ts` 의 매칭이 `text.startsWith(headword, i)` 단순 문자열 포함이었다.
읽기를 검증하지 않아 日照(にっしょう) 카드에 日照り(ひでり) 문장 셋이 전부 실렸다.
`examples.json` 의 `_meta.rule` 에 "읽기 미검증"이라고 적혀 있었고 2026-09-07 절에도
위험을 적어 뒀지만, 그때 센 것은 **표제어끼리 표기가 겹치는 12건**이었다. 실제 문제는
그게 아니라 **표제어가 더 긴 단어의 일부로 걸리는 것**이라 규모를 한 자릿수 잘못 봤다.

### 실측 — JMdict 대조 (고치기 전)

문장 속 표제어 자리를 덮는 더 긴 JMdict 표제어를 찾아, 그 읽기에 표제어 읽기가 안
들어가면 의심으로 셌다.

| | |
|---|---|
| 전체 예문 | 27,517문장 / 숙어 11,377개 |
| 의심 문장 | **416 (1.5%)** |
| 예문이 전부 의심인 숙어 | **95** |

本人 ← 日本人, 家主 ← 国家主義, 旧作 ← 復旧作業, 演者 ← 講演者, 美味 ← 美味しい,
今日(きょう) ← 今日は(こんにちは).

### 문자열 규칙으로는 여기까지다 — 그래서 형태소 분석

같은 JMdict 대조로 활용형(羽織ります ← 羽織)까지 잡아 보려 했더니 426건이 더 나왔는데
**대부분 오탐**이었다(もう一回 를 一回り 로, 過去は 를 過去る 로). 반대로 확정분 416
안에도 오탐이 있다(外出した 를 外出し(そとだし)로). 문장 분절 없이 문자열만으로는
"이 자리에서 무슨 단어인가"를 못 정한다.

**채택 — kuromoji(IPADIC)로 빌드 시점에 읽기를 검증한다.** 사용자 확인 후 devDependency
로 넣었다. 2026-09-01 절에서 kuromoji 를 기각한 이유는 "런타임 의존이 늘고, 분석기는
단어 경계를 주지 한자 단위 읽기 배분을 주지 않는다"였는데 **여기선 둘 다 안 걸린다** —
빌드 전용이고, 필요한 것이 바로 그 단어 경계다. PLAN §3 의 "형태소 분석 (Phase 6) =
kuromoji.js" 가 이 자리다.

### 판정 규칙 — 읽기가 보존되는가 (`readingHolds`)

표제어 구간에 걸치는 형태소들의 읽기를 이어 붙여 표제어 읽기를 품는지로 본다.
弁護士(ベンゴシ) 안의 弁護 는 살고, 日照り(ヒデリ) 안의 日照 와 国家/主義 에 걸친
家主 는 빠진다. **경계 정렬을 요구하지 않은 것은 의도적이다** — 형태소 경계에 딱
맞을 것을 요구하면 弁護士·論文中 처럼 읽기가 그대로 보존되는 7,348문장이 같이
날아간다. 카드가 가르치는 것은 "이 표기가 이 읽기로 읽힌다"라 기준도 읽기여야 한다.

여기에 실측하며 붙인 예외 셋. 전부 "IPADIC 이 틀렸거나 표기법이 다를 뿐"인 경우다.

1. **음운 변형을 지우고 비교한다** (`canonicalReading`). IPADIC 은 一/回 를 갈라
   イチ+カイ 를 주는데 표제어 읽기는 융합형 いっかい 다. 現代仮名遣い(ぢ·づ)·連濁·
   半濁·促音便을 지워 비교한다 — `unvoiceAll` 은 이미 있던 것을 그대로 썼다.
   이걸 안 넣으면 一回·一階·一個·一種·一週間 같은 수사+조수사 246문장이 통째로 날아간다
2. **형태소 하나와 표기가 통째로 맞으면 받는다.** 동형이독에서 IPADIC 이 고른 읽기는
   문맥 판단이 아니라 기본값일 때가 많다 — 日本人 을 ニッポンジン, 右腕 을 ウワン,
   堪能 을 カンノウ, 一文 을 イチブン 으로 준다. 이 274문장을 버리면 「私は日本人です」
   같은 제일 좋은 예문이 사라진다. **대가는 안다** — 一味違う(ひとあじ)나 茶道と花道
   (かどう)처럼 IPADIC 이 문맥을 맞게 읽은 소수가 같이 통과한다. 표기가 통째로 맞는
   자리에서 사전 읽기를 뒤집을 근거로는 IPADIC 이 약하다고 보고 사전 쪽에 섰다
3. **양쪽 끝이 다 형태소 중간이면 뺀다.** 万一|戦争 안의 一戦, 同一|条件 안의 一条 은
   단어 경계를 가로지른 우연한 일치다. 1번 완화가 이것들까지 되살리길래 막았다

읽기를 모르는 형태소(未知語)가 걸치면 뺀다. 단 2번이 먼저라 拒食症처럼 표기가 통째로
맞으면 읽기를 몰라도 받는다.

### 결과

| | 전 | 후 |
|---|---|---|
| 예문 보유 숙어 | 11,377 | 11,130 (−247) |
| 예문 문장 | 27,517 | 26,891 (−972 제거, 331 교체 유입) |
| JMdict 대조 의심 문장 | 416 (1.5%) | **154 (0.6%)** |
| 예문이 전부 의심인 숙어 | 95 | **36** |

日照·演者·苦笑 는 예문이 없어졌고(예문 없는 카드는 줄만 빠진다), 本人 은
「彼本人がやってきた」로, 美味 는 「あの夕食は美味だった」로 바뀌었다.

**남는 것 — 連濁 계열은 일부러 통과시킨다.** 和菓子(わがし) 가 菓子(かし) 예문에,
雪合戦(ゆきがっせん) 이 合戦(かっせん) 예문에 남는다. 1번 규칙이 半濁(一杯 いっぱい)을
받으려면 같은 표를 쓰는 連濁도 같이 받게 된다. 같은 형태소의 유성화라 다른 단어를
보여주는 것과는 성격이 다르다고 봤다. 갈라내려면 방향을 아는 비교가 필요한데, 얻는
것보다 규칙이 복잡해진다.

### 검증

`npm test` 529 → **544** (`build-examples.test.ts` 5 → 20 — 日照り·国家主義·万一戦争·
一回 융합·稲妻 표기·日本人 동형이독·未知語까지 규칙 하나씩 고정), `playwright` 30 스펙,
`tsc -b`/`oxlint`/`build` 클린. 빌드 산출물은 `data/dict/examples.json` 재생성 후
`build:runtime-dict` 로 `public/dict/examples.json` 까지 갱신(런타임 10,984숙어).

---

## 2026-09-19 — 수준 사다리가 「수준」을 재고 있는가 (사용자 문제 제기)

사용자 지적 둘이 출발점이다. **"흔들린다는 것은 순간의 상황이고 수준은 누적된 학습의
순간"** 이고, 실사용에서 **"기존에 숙지했다가 오타가 나거나 틀리는 단어 때문에 수준이
바뀌는 것이 거슬린다"**. 지금은 밴드별 최근 30회 정답률(`LEVEL_WINDOW`) 하나가 두
질문에 동시에 답한다.

### 재는 방법 — 시뮬레이터 안에 정답지를 만들었다

실제 로그로는 "정말 익혔는지"를 알 수 없다. 그래서 `tools/sim-level.ts`
(`npm run sim:level`) 로 학습자를 만들었다 — 숙어마다 익힘 상태를 두고, 만나면 일정
확률로 익히고, 익힌 건 높은 확률로 맞히고, 안 보면 잊고, 그 위에 오타율을 얹는다.
세션 선택·채점·재생은 **앱과 같은 경로**(`buildSession`)를 탄다.

비교 대상 셋 — **A** 지금 것(밴드별 최근 30회 정답률) · **B** 재고(`isStable` 통과 숙어
개수) · **C** 숙어 창(숙어당 최근 1건, 최근 30숙어).

정답지도 둘로 갈랐다. **①수준**(만난 숙어 중 익힌 비율)과 **②기대 정답률**(지금 전부
한 번씩 내면 나올 정답률). A 는 정답률이라 ①과 단위가 다르다 — 모르는 단어도 한국
한자음으로 찍어 맞는 몫이 있다. 이 구분을 안 하면 A 를 부당하게 때린다.

### 결과 (세션 300회 · 풀 밴드당 100 · 오타 3%)

| | 밴드 1 | 밴드 2 | 밴드 3 |
|---|---|---|---|
| A 오차 (vs ②기대 정답률) | +1.4%p | +0.3%p | −12.1%p |
| A 오차 (vs ①수준) | **+17.7%p** | **+10.9%p** | −12.5%p |
| B 오차 (vs ①수준, 문턱 14일) | −3.0%p | −10.0%p | −48.6%p |
| **억울한 뒤집힘** | A 37회 · B 0회 · C 30회 | | |

"억울한 뒤집힘" 은 **익힌 숙어가 줄지 않았는데 판정이 안정 → 흔들림으로 내려간** 횟수다.
사용자가 거슬린다고 한 것이 이것이라 계량 기준을 거기 맞췄다.

- **A 는 정답률로서는 정확하다.** 기대 정답률과 ±1.4%p. 고장 난 게 아니다
- **그런데 수준으로 읽으면 10~20%p 과대평가한다.** 찍어 맞히는 몫이 섞여서다.
  화면은 이 값을 「수준」이라 부른다
- **B 는 억울한 뒤집힘이 0회다.** 학습자 파라미터를 바꿔 가며 다섯 조합을 돌려도 전부 0

### 가설 둘이 기각됐다 — 원인은 편향이 아니라 분산이었다

- **기각 — "창이 어려운 카드로 채워진다"(SRS 잔여물).** 실측하니 창 30회 안에 서로 다른
  숙어가 **27~29개**다. 재노출이 거의 없어서 꼬리로 채워지지 않는다. 내가 이 가설을
  먼저 냈는데 틀렸다
- **기각 — "오타가 뒤집힘의 주범이다".** 오타율 0% 로 같은 시드를 돌려도 뒤집힘이
  **39회**다(3% 일 때 37회). 오타는 원인이 아니다
- **남은 설명 — 표본 분산.** 30회 표본의 정답률 표준오차가 ±7%p 안팎이라 문턱 0.8
  근처에서 계속 넘나든다. 실력이 아니라 표본이 흔드는 것이고, 그래서 **오타를
  되돌려도 안 없어진다**

### 안정 문턱 — 21일보다 14일

`MEANING_STABLE_DAYS`(21) 는 뜻 카드용으로 고른 값이라 읽기에 맞는지 몰랐다. 쓸어 봤다.

| 문턱 | 밴드 1 오차 | 밴드 2 오차 | 뒤집힘 |
|---|---|---|---|
| 7일 | +16.5%p | +8.1%p | 9회 |
| **14일** | **−3.0%p** | **−10.0%p** | **0회** |
| 21일 | −15.9%p | −22.9%p | 0회 |

7일은 아직 안 익힌 것까지 세고, 21일은 익힌 것을 한참 빼먹는다.

### 강건성과 한계

`learn` 0.3/0.5/0.7 · `forget` 0.004/0.012 · `known` 0.9/0.97 · 시드 둘로 돌렸다.
**B 의 뒤집힘은 모든 조합에서 0회**, A 는 18~49회로 흔들린다. 다만 **B 의 절대 오차는
조합마다 −36.9 ~ +5.3%p 로 크게 움직인다** — 망각이 잦은 학습자일수록 stability 가
안 올라 낮게 나온다. **그래서 B 를 비율로 보여주면 안 된다.** 개수로 보여주면 절대값
해석이 필요 없고, 늘어나는 것만 보면 된다.

밴드 3 은 어느 방식이든 얇다 — 300세션에도 만난 숙어가 17개다. `selectSession` 이
신규를 밴드 오름차순으로 넣어서다. 사다리와 별개 문제로 남긴다.

학습자 모델은 가정이다. 이 시뮬레이션이 답한 것은 "어느 지표가 내가 정한 정답지를 잘
맞히나"까지고, 실제 사용자의 학습 곡선이 이 모델과 같다는 보장은 없다.

### 반영 — 사다리 숫자를 개수로 (같은 날)

새 화면을 만들지 않았다 (사용자: "어차피 테스터가 나 혼자니까 별도의 ui는 필요가 없다").
**있던 밴드 행의 숫자만 바꿨다.**

- `buildLevel(events, bandOf, cards?)` 에 카드 상태를 선택 인자로 받아 밴드별 `met`(만난
  숙어)·`stable`(붙은 숙어)을 같이 낸다. 세 번째 인자를 안 주면 0 이라 `Home.tsx` 호출부는
  안 건드렸다 — 거기서는 진단 제안 여부만 보므로 재고가 필요 없다
- `READING_STABLE_DAYS = 14` 를 `level.ts` 에 새로 뒀다. 뜻 카드의
  `MEANING_STABLE_DAYS`(21)와 **따로** 둔다 — 같은 상수를 공유하면 한쪽을 조정할 때
  다른 쪽이 딸려 움직인다. 14 는 위 문턱 쓸기 결과다
- 리포트 사다리 — 제목 「밴드 정답률」 → 「밴드별 붙은 숙어」, 큰 숫자 `82%` → `84개`,
  막대 폭은 `stable/met`. 정답률은 없애지 않고 **상태 줄로 내렸다**
  (「흔들림 · 만난 120개 · 최근 30회 68%」). 경계선은 그대로 정답률이 긋는다 —
  경계는 "지금 어디서 틀리고 있나"라 순간 상태가 맞는 자리다
- 캡션을 두 기준으로 고쳤다 — 「붙음 = 14일 이상 안 잊는 상태 · 안정·흔들림은 최근 30회
  정답률 80% 기준」

**`status`(안정/흔들림)의 정의는 안 건드렸다.** 정답률 기반 그대로다. 재고로 바꾸면
`prescribe` 의 밴드 처방과 `diagnostic-state` 의 진단 제안 조건까지 같이 움직이는데,
그 둘은 "지금 흔들리는 곳"을 찾는 로직이라 흔들림 쪽이 맞다. **수준과 흔들림을 가른다는
결정이 곧 status 를 갈아엎는다는 뜻은 아니다.**

검증 — `npm test` 544 → **557**(`level.test.ts` 에 5건: 인자 없으면 0 / 문턱 경계 /
재학습 중 제외 / 뜻 카드 제외 / 정답률이 흔들려도 개수는 그대로), `playwright` **30 스펙**
(`full-flow.spec.ts` 에 사다리 제목·캡션·개수 표기 고정), `tsc -b`·`oxlint`·`build` 클린.

---

## 2026-09-19 — 키보드가 답과 화면을 망치던 둘 (사용자 보고)

사용자 스크린샷에서 시작했다. 「키보드가 이렇게 나올 경우가 있어」 → 확인해 보니 문제는 둘로
갈렸다. **한글 키보드로 친 답이 그대로 오답이 되는 것**, 그리고 **키보드가 올라오면 한자가
잘리는 것**.

### 먼저 확인한 것 — 키보드 언어는 웹에서 지정할 수 없다

`inputMode` 는 키보드 **종류**(url·email·numeric)를 고를 뿐 언어가 아니고, `lang` 은 콘텐츠
언어 힌트라 키보드 선택에 안 쓰인다. iOS 는 마지막에 쓴 키보드를 기억하고 지구본 키로
사용자가 바꾼다 — 페이지가 되돌릴 수단이 없다.

**네이티브도 언어 지정은 못 한다.** `UIKeyboardInputMode` 계열은 비공개 API다. 다만
`UIKeyboardType.asciiCapable` 로 **ASCII 키보드 제한**은 걸 수 있어서 결과적으로 한글
키보드가 후보에서 빠진다. 웹의 `inputMode="url"` 이 노리는 자리가 거기인데 강제력이 그만큼
아니다 — 지구본으로 넘어가면 그대로 남는다. 이 차이가 이번 증상이다.

**웹에서 확실히 단일화하는 길은 `inputMode="none"` + 앱 내 키패드뿐이고, 그건 주차장에
그대로 둔다** (발동 조건은 이미 찼다 — 한글 혼입·자동완성 바·한자 잘림 셋). 큰 작업이라
값싼 방어부터 냈다.

### 1. 한글 가드 (`src/lib/hangul.ts`)

답에 한글 음절·호환 자모·조합 자모가 있으면 **채점하지 않고** 입력 행 아래에 한 줄을 띄운다
(「한글이 섞였어요. 영문 키보드로 바꿔서 로마자로 입력해 주세요.」). 다시 입력하면 사라진다.

- **오답으로 기록하지 않는 게 요점이다.** 그냥 두면 wanakana 가 변환을 못 해 오답이 되고,
  오답 유형 분포(「잘못 읽기」)와 FSRS 복습 일정이 같이 오염된다. 키보드 사고가 학습 기록에
  남을 이유가 없다
- 판별을 `src/lib/hangul.ts` 로 뺐다 — 컴포넌트 파일이 컴포넌트 외의 것을 내보내면
  fast refresh 가 깨진다(oxlint `only-export-components`)
- 안내 상태를 boolean 이 아니라 **막힌 카드의 `resetKey`** 로 들고 렌더에서 파생시킨다.
  boolean + effect 로 끄면 카드마다 렌더가 한 번 더 돈다(`set-state-in-effect`)
- `Search.tsx` 는 안 건드렸다. 검색은 채점이 없어 기록을 오염시키지 않는다

### 2. 키보드 높이에서 한자가 잘리던 것

375×350(iPhone 8 급 화면에 키보드가 올라온 가시 높이)에서 실측했다.

| | 전 | 후 |
|---|---|---|
| 글리프가 줄 상자를 넘은 양 | 7px | 3px |
| `.card-body` 넘침 | 5px | 3px (패딩 몫) |
| 표제어 잉크 상자 | 본문 밖으로 삐져나감 | **본문 안에 들어옴** |
| 표제어 크기 | 60px | **60px (그대로)** |

원인이 둘이었다. `line-height: 1.2` 가 일본어 글리프에 모자라 줄 상자 밖으로 삐져나오고
(60px 글자에 줄 상자 72px, 글리프가 필요로 한 건 79px), 그 초과분을 `.card-body` 의
`overflow-y: auto` 가 잘랐다.

- `line-height` 를 **1.35** 로. 폰트 고유 높이가 1.4em 쯤이라 0 으로는 안 떨어진다
- `.card-body` 패딩을 `clamp(4px, calc(var(--vvh) * 0.018), 12px)` 로 — 좁아지면 패딩부터 내준다
- **글자 크기는 안 줄였다.** 처음엔 `--vvh` 로 폰트에 상한을 걸어 봤는데(50.75px 로 줄었다),
  `study.css` 의 「글자를 줄이지 않는 게 원칙이다 — 자형 학습에 해롭다」(2026-09-06)와
  정면으로 부딪혀 **되돌렸다.** 패딩만으로 충분했다
- **미디어 쿼리는 못 쓴다** — iOS 는 키보드가 떠도 레이아웃 뷰포트가 그대로라 `max-height`
  가 안 걸린다. `useViewportLock` 이 내려주는 `--vvh` 를 쓰는 이유가 이것이다

### 검증

`npm test` 557 → **561**(`hangul.test.ts` 4건), `playwright` 30 → **32 스펙**.
새 e2e 둘 — `hangul-guard.spec.ts`(한글 답이 채점 안 되고 안내가 뜨고, 다시 입력하면
사라지고, 로마자로 치면 정상 채점), `keyboard-squeeze.spec.ts`(375×350 에서 표제어
**잉크 상자**가 본문 안에 들어오고 스크롤이 0 이며 글자 크기가 48px 이상). 판정을 요소
상자가 아니라 잉크 상자로 잡은 게 핵심이다 — 잘리는 건 잉크지 상자가 아니다.

### 그래서 자판을 직접 그렸다 (같은 날, 사용자 결정)

사용자 질문이 결론을 냈다 — **"파파고는 네이티브라서 로마자 자판을 고정하는 것 같은데
방법이 없는가?"**

**네이티브라도 키보드 언어 강제는 공개 API에 없다.** `UIKeyboardInputMode` 계열은 비공개다.
네이티브가 하는 건 둘 중 하나고, 파파고는 후자로 보인다 — ① `UIKeyboardType.asciiCapable`
로 ASCII 키보드만 남기기 ② **자체 키보드 뷰로 시스템 키보드를 대체하기**. 로마자→가나 변환을
앱이 직접 해야 하는데 그건 시스템 일본어 키보드가 이미 하는 일이라 겹칠 이유가 없다.

**웹에서 ①은 못 하고 ②는 된다.** 그리고 변환기(wanakana)는 이미 있었다 — 없던 건 키 표면뿐.

`inputMode="none"` + `RomajiKeypad` (QWERTY 3행 + ー·⌫·확인). 배열은 사용자가 QWERTY 로
골랐다 — 손가락 위치가 그대로 간다. 기각한 대안 둘: *로마자에 쓰는 자모만 20키*(키는 커지지만
위치가 낯설다), *가나 50음도 직접 입력*(wanakana 가 필요 없어지는 대신 입력이 느려진다).

**핵심 구현 — 값을 대입하면 안 된다.** wanakana 는 `input` 이벤트로 변환하므로 `el.value +=`
는 로마자를 그대로 남긴다. `document.execCommand('insertText')` 로 넣어 네이티브
beforeinput/input 을 내고 캐럿도 맡긴다. 안 되는 환경엔 값 대입 + 합성 `InputEvent` 로 물러선다.
버튼에 `onPointerDown → preventDefault` 를 걸어 **포커스를 안 뺏는다** — blur 되면 캐럿이
사라져 삽입 위치를 잃는다.

`@media (hover: none) and (pointer: coarse)` 에서만 뜬다. PC 는 물리 키보드로 그냥 친다
(`inputMode` 는 가상 키보드에만 걸린다). 같은 조건에서 입력 옆 「확인」 버튼은 숨긴다 —
자판에 확인 키가 있어 자리만 먹는다.

**얻은 것** — 자동 완성 후보, Safari 악세서리 바(44px), 지구본, iOS 가 기억한 한글 자판이
한꺼번에 사라진다. 자판 높이가 iOS 키보드의 2/3 쯤이라 **피드백 화면이 오히려 100px 넘게
넓어졌다** (전에는 피드백 중에도 시스템 키보드를 띄워 둔 채였다 — 포커스 유지 때문).

**남는 위험** — `inputMode="none"` 이라 터치 기기에서 시스템 키보드를 아예 못 부른다.
자판에 문제가 생기면 입력할 길이 없다. 실기기에서 며칠 써 보고, 탈출구가 필요하면 그때 단다.

한글 가드는 그대로 둔다 — 붙여넣기와 PC 입력에 남는 방어다.

검증 — `keypad.spec.ts`: 터치 기기에서만 뜨고, `inputmode="none"` 이고, k+a 가 **か 로
합쳐지고**(값 대입이면 `ka` 로 남는다), ー·⌫ 가 돌고, 촉음(tta → った)이 되고, 확인 키로
채점까지 간다. `npm test` 561 · `playwright` 32 → **33 스펙** · tsc/oxlint/build 클린.
375×667 실캡처로 배치 확인.

#### 자판을 다듬은 것 넷 (같은 날, 사용자 지적 연속)

1. **장음(ー) 키 제거** — "카타카나에서나 필요한 입력 같다". 실측이 맞았다: 밴드 0~3 읽기
   16,959개 중 ー 를 쓰는 것 **0건**, 카타카나 읽기도 **0건**. 뺀 자리는 빈 칸으로 두어
   글자 키가 윗줄과 어긋나지 않게 했다 (시스템 키보드의 shift 자리)
2. **모바일에서만** — `useCoarsePointer` (`matchMedia('(hover: none) and (pointer: coarse)')`)
   하나로 **자판 표시와 `inputMode` 를 같이 정한다.** 둘을 따로 두면 자판은 숨겼는데
   `inputMode="none"` 만 남아 아무것도 못 치는 조합이 생긴다. 이 묶음 덕에 오판해도 안 잠긴다 —
   폰을 PC 로 보면 시스템 키보드가 뜨고, PC 를 폰으로 보면 자판이 떠도 물리 키보드가 먹는다.
   **UA 스니핑은 기각** (사용자 제안) — iPadOS Safari 가 자기를 Macintosh 로 보고해서 정확히
   아이패드에서 틀리고, `navigator.userAgentData.mobile` 은 Chromium 전용이며, 두 값을 따로
   판정하게 되기 쉬워 위 안전성이 깨진다
3. **iOS·안드로이드 양쪽에 맞는 모양** — 어느 한쪽을 흉내 내면 반대쪽에서 어색해진다. 둘이
   공유하는 것만 맞췄다: 키 높이 46px(iOS 44 / 안드로이드 48 사이), 줄마다 키 폭이 같도록
   가운데 줄 양끝에 반 칸, 눌린 키 확대. **다크에서 키가 바닥보다 밝아야 한다** — 라이트에선
   흰 키가 회색 바닥 위에 뜨는데 토큰 명도 관계가 다크에서 뒤집혀 있어 거기만 다시 잡았다
4. **확대 표시를 키에 붙였다** — 처음엔 키 위에 떠 있는 말풍선이었는데 "어느 키인지 모호하다"
   (사용자 지적). 키 바닥에서 시작해 위로 자라는 **하나의 덩어리**로 바꿔 눌린 키를 덮게 했다.
   `:active` 가 아니라 React 상태로 드는 이유는 버튼이 `pointerdown` 의 기본 동작을 막기
   때문이다(포커스 유지) — 막으면 `:active` 시점을 브라우저마다 믿기 어렵다

검증 — `keypad.spec.ts` 3건(자판 입력·변환·채점 / 확대 표시가 누른 동안만 / 장음 키 없음),
`npm test` **561** · `playwright` **35 스펙** · tsc·oxlint·build 클린. 375×667 라이트·다크
실캡처로 배치와 명도 확인.

#### 실기기 1차 피드백 — 느리고 작다 (같은 날)

사용자가 실기기에서 쓰고 바로 왔다. **"반응속도가 느리고 키가 작아서 불편해"**, 그리고
시스템 키보드 캡처를 보내며 **"이 정도는 되야될거같다"**.

- **반응속도 — `click` 에서 `pointerdown` 으로.** click 은 손을 뗄 때 와서 한 박자 늦다.
  시스템 키보드는 눌리는 순간 글자를 낸다. 글자 키·지우기·확인 전부 pointerdown 으로 옮겼다
- **키 크기 — 참고 이미지를 재서 맞췄다.** iPhone 8 기준 시스템 키가 약 31×36 CSS px 인데
  우리는 32×46 이었다. **폭·높이는 이미 크거나 비슷했고 작아 보인 건 다른 이유였다** —
  글자가 작고(19px) 키·바닥 대비가 약했다. 글자 25px, 높이 52px, 모서리 9px 로 올렸다
- **3층 구조를 만들었다.** 참고 이미지는 페이지 → 자판 바닥 → 키 순으로 밝아지는데, 우리
  다크는 자판 바닥이 페이지와 같은 색이라 판 자체가 안 보였다. `color-mix` 로 바닥 7% ·
  키 24% · 기능 키 15% · 확대 32% 로 층을 냈다 (새 색상값은 안 만들고 기존 토큰을 섞는다)
- **확인 키가 안 보이던 버그** (사용자 지적) — 다크 override `.key` 가 `.key-submit` 보다
  특이도가 높아, 확인 키가 글자 키 배경을 뒤집어쓴 채 글자색만 어두워져 읽히지 않았다.
  같은 특이도로 `.key-submit` 을 다시 지정했다
- **피드백 중에는 자판을 접는다.** 칠 수 없는 자판이 250px 을 먹어 오답 설명이 잘려 있었다
  (사용자 캡처에서 「好는 ゴ…」가 중간에 끊겼다). 카드 하단이 바닥에 붙어 있어 「다음」 버튼
  자리는 그대로고 그만큼이 본문으로 간다

#### 「모르겠어요」를 카드 머리의 SKIP 으로 (2026-09-19 사용자 요청)

자판이 아래를 차지하게 되면서 넘김 버튼이 자판 위 한 줄을 더 먹고 있었다. **카드 머리
오른쪽 끝으로 올리고 이름을 `SKIP` 으로 바꿨다.** 매번 쓰는 동작이 아니라 빠져나가는 길이라
아래 황금 자리를 줄 이유가 없다. 모양은 옆의 밴드 태그와 같은 알약에 테두리만 줘서,
눈에 먼저 들어오지는 않되 찾으면 바로 보이게 했다.

**대가** — 아래쪽 3슬롯 행이 사라져서, 피드백이 뜰 때 액션 행이 새로 생기며 입력 행이
한 줄만큼 올라간다. 전에는 넘김 행이 그 자리를 잡고 있어 아무것도 안 움직였다. 모바일에서는
피드백에서 자판이 접히며 250px 이 빠지는 쪽이 훨씬 커서 안 보이고, PC 에서만 54px 흔들린다.
거슬리면 빈 행을 두면 되지만 지금은 안 넣었다 — 안 쓰는 자리를 지키자고 빈 줄을 두는 건
자판을 올린 이유와 어긋난다.

바꾼 자리 — `ReadingCard.tsx`(머리로 이동), `Home.tsx` 첫 안내 문구(「모르겠어요」 →
「카드 오른쪽 위 SKIP」), e2e 셋(`pass-reading`·`homograph-dual`·`welcome`).
리포트의 「넘김」 라벨은 안 건드렸다 — 그건 기록의 이름이지 버튼 이름이 아니다.

#### 확인 키 톤 · 입력 피드백 설정 (2026-09-19 사용자 요청)

**확인 키를 다시 낮췄다.** 안 보이던 걸 고치면서 흰 슬래브로 만들었더니 이번엔 화면에서 제일
먼저 눈에 들어왔다 (사용자 지적). 글자 키와 **같은 층**에 두고 테두리와 글자 굵기로만 구분한다 —
PLAN §7 「색은 오답에만」과도 이쪽이 맞다. 두 번 왕복한 게 기록으로 남을 값이 있다: 대비를
고칠 때 **배경을 바꾸는 것과 강조를 올리는 것은 다른 문제**인데 한 번에 둘 다 했다.

**입력음·진동을 설정에 뒀다** (`keyFeedback`: `off`|`sound`|`haptic`, 기본 `off`).
시스템 키보드를 안 쓰게 되면서 OS 가 주던 키 클릭음·햅틱도 같이 사라진 걸 앱이 대신 낸다.

- **진동은 iOS 에서 안 된다** (실측이 아니라 문헌 확인, 2026-09-19). WebKit 에 공개 진동 API 가
  없다. `<input type="checkbox" switch>` 부작용으로 햅틱을 부르던 우회가 iOS 17.4~26.4 에서
  돌았지만 **26.5 에서 애플이 막았다**. 그래서 `navigator.vibrate` 유무로 물어보고 **없으면
  그 선택지를 비활성**한다 — 눌러도 아무 일 없는 스위치를 두면 설정이 거짓말을 한다
- **소리는 합성한다** — 오디오 파일을 안 쓴다. 자산 하나 더 받게 할 값이 없고 캐시·오프라인도
  신경 쓸 게 없다. WebAudio 로 1180Hz 사각파 12ms. 더 길면 연타에서 겹쳐 지저분하다
- **소리도 iOS 무음 스위치에 묶인다** — TTS 와 같은 제약이다. 설정 힌트에 그대로 적었다
- 설정은 세션 진입 때 한 번 읽는다. 세션 중에 설정 화면으로 갈 수 없어 구독이 필요 없다

검증 — `npm test` 561 → **564**(`parseSettings` 3건: 기본 off / sound·haptic 만 통과 /
모르는 값은 기본으로), `playwright` **35 스펙**, tsc·oxlint·build 클린. 다크·라이트 실캡처로
확인 키가 읽히되 튀지 않는 것 확인.

#### 찾기 화면에도 자판 (2026-09-19 사용자 지적)

세션에만 달고 「읽기로 찾기」를 빼먹었다. 같은 입력창이고 같은 문제(iOS 가 한글 자판을 기억)를
겪는 자리다.

**두 화면의 차이 둘.**

- **변환 경로가 다르다.** 세션은 wanakana 를 요소에 `bind` 해서 input 이벤트로 변환하므로
  `execCommand` 로 넣어야 하지만, 찾기는 값이 React state 라 `toKana(v + ch)` 로 붙이면 끝이다.
  그래서 자판은 글자만 넘기고 넣는 방법은 화면이 정한다 (`onKey` 콜백)
- **뜨는 시점이 다르다.** 세션은 카드가 떠 있으면 늘 입력 중이지만, **찾기는 결과 목록이
  주인공**이라 자판이 늘 떠 있으면 화면을 절반 먹는다. 입력창을 누른 뒤에만 띄우고, 마지막
  큰 키를 「확인」이 아니라 **「닫기」**로 바꿔 접을 길을 뒀다 (`submitLabel` 프롭)

**입력음·진동을 자판 안으로 옮겼다.** 처음엔 `KanaInput` 이 설정을 읽어 콜백마다 울렸는데,
쓰는 화면이 둘이 되면서 같은 코드를 두 번 쓸 자리가 됐다. 자판이 직접 설정을 읽는다.

검증 — `search-keypad.spec.ts`(누르기 전엔 없고 · `inputmode="none"` 이고 · k+a 가 か 로
들어가고 · 지우기가 돌고 · 닫기로 접히되 찾은 값은 남는다). `npm test` **564** ·
`playwright` 35 → **36 스펙** · tsc·oxlint·build 클린.

**찾기 자판을 화면 아래에 고정** (같은 날, 사용자 요청) — 입력창 바로 아래에 끼어 있으면
목록을 스크롤할 때 같이 올라가 버린다. `position: fixed` 로 바닥에 앉히고(`.keypad.docked`),
탭바를 덮는다 — 시스템 키보드와 같은 동작이라 별도 설명이 필요 없다. 탭을 옮기려면 「닫기」를
먼저 눌러야 하는데, 그것도 시스템 키보드와 같다. 목록 끝이 가리지 않게 `.screen-body` 에
자판 높이만큼 아래 여백을 준다(`with-keypad`). e2e 로 **바닥에 붙어 있고 스크롤해도 안 움직이는
것**을 고정했다.

## 2026-09-20 — 자판 배열을 설정으로 (사용자 요청 "안 쓰는 키를 없애면 더 크게 쓸 수 있겠다")

### 먼저 쟀다 — 안 쓰는 글자가 정말 있나

`tools/measure-romaji-letters.ts` (`npm run measure:romaji`). 밴드 0~4 읽기 **102,377개**를
로마자로 펴서 글자를 셌다.

**`l` `q` `v` `x` 는 0회다.** 나머지 22자는 전부 쓰인다.

대체 표기도 확인했다 — し를 `si`, つ를 `tu`, ち를 `ti`, ふ를 `hu`, じ를 `zi` 로 쳐도 전부
남는 글자로 된다. `x`·`l` 은 wanakana 의 작은 가나 접두사(`xa`→ぁ)인데, っ 는 자음 겹침으로,
ゃゅょ 는 `kya` 로 나오니 쓸 일이 없다. **추측으로 뺐으면 ゔ(v)나 작은 가나에서 막혔을 텐데,
세어 보니 코퍼스에 그런 읽기가 아예 없다.**

### 셋 중에 고르게 했다 — 폭과 자리 기억의 맞바꿈이라

| 배열 | 키 | 줄 | 키 폭(375px 화면) |
|---|---|---|---|
| QWERTY (기본) | 26 | 10/9/7 | 32.2px |
| 간결 | 22 | 9/8/5 | **36.3px (+13%)** |
| 넓게 | 22 | 8/7/7 | **41.5px (+29%)** |

「간결」은 넷만 빼고 **자리를 그대로** 둔다 — 손가락이 기억한 위치가 거의 산다.
「넓게」는 22자를 8/7/7 로 다시 나눠 키를 최대로 키우는 대신 자리 기억이 깨진다.
기본은 QWERTY 로 뒀다 — 지금 쓰던 것을 설정 때문에 바꾸지 않는다.

### 검증

`keypad-layout.spec.ts` 둘 — ① 배열마다 화면에 뜨는 글자가 정의와 정확히 같고, 안 쓰는 넷이
간결·넓게에서 빠지고, **키 폭이 실제로 커진다**(위 표는 이 테스트가 찍은 값이다) ②
간결 배열로 `shi`·`tsu`·`cha` 를 쳐서 し·つ·ちゃ 가 나온다 — 뺀 글자가 정말 필요 없는지를
입력으로 확인한다. **배열 데이터를 소스에서 import 하지 않고 스펙에 다시 적었다** — import
하면 소스가 바뀔 때 테스트도 같이 바뀌어 회귀를 못 잡는다.

`npm test` 564 → **566** · `playwright` 37 → **39 스펙** · tsc·oxlint·build 클린.

### 「넓게」는 도로 뺐다 (같은 날, 사용자 지적 "키 배열은 QWERTY 와 유사했으면 좋겠다")

8/7/7 로 재배치한 배열이 41.5px(+29%)로 제일 컸지만 **QWERTY 와 안 닮는다**. 만들어서
실측까지 한 뒤 뺐다 — 재 본 값은 남긴다(위 표). 남은 선택은 **"안 쓰는 넷을 뺄지 말지"**
하나고, 설정도 두 개로 줄었다.

**정확히 적어 둘 것** — 「간결」은 자리를 완전히 보존하지 못한다. q 가 빠지면 w 가 맨 왼쪽으로
한 칸 당겨진다. 순서는 같고 줄 구성도 같지만 위치는 한 칸씩 움직인다. 자리를 그대로 두려면
뺀 키를 빈 칸으로 남겨야 하는데 그러면 **폭이 하나도 안 늘어 이 배열을 만든 이유가 없어진다.**
설정 힌트에도 그대로 적었다.

### 사다리 용어 — 「숙지한 표현」과 「출제된 표현」 (2026-09-20 사용자 질문·지시)

물어본 김에 데이터를 다시 봤더니 **라벨이 과장이었다.** `met` 은 읽기 카드 상태가 있는 숙어를
세는데, 카드 상태는 **채점 이벤트가 있어야 생긴다.** 소개 카드로만 본 숙어는 안 들어간다 —
소개 여부는 이벤트가 아니라 localStorage 에 있어서(2026-09-13 결정) 재생에 안 잡힌다.
그러니 「만난」은 소개만 본 것까지 품는 말이라 실제보다 넓다.

**용어를 두 번 고쳤다.** 「만난」 → 「푼」 → **「출제된 표현」**, 그리고 「붙음」 → **「숙지」**
(사용자 지시). 중간에 사용자가 「맞춘 표현」을 제안했는데 **그건 지금 값과 뜻이 다르다** —
출제된 표현에는 틀린 것도 SKIP 으로 넘긴 것도 들어간다. 이름만 바꾸면 틀린 것까지 맞췄다고
말하게 돼서, 되물어 확인한 뒤 「출제된」으로 정했다. **이름이 포함 범위를 말해야 한다**는 게
이 왕복에서 남은 규칙이다.

캡션도 그에 맞췄다 — 「숙지 = 14일 이상 안 잊는 상태 · 출제된 표현에는 틀린 것·넘긴 것도
들어가요 (소개만 본 건 빼요)」. e2e 가 제목·캡션 문구를 고정한다.

**곁가지** — 찾기 자판 고정 테스트가 전체 실행에서 한 번 흔들렸다(픽셀 정확 비교). 바닥에
붙었는지만 보면 되므로 1px 여유로 바꿨다. 원인은 못 좁혔고, 다시 흔들리면 그때 판다.

**사다리 줄 배치** (같은 날, 사용자 요청) — 밴드 줄 아래 한 줄에 판정과 근거가 뭉쳐 있었다.
**왼쪽에 판정(「흔들림 · 출제된 표현 17개」), 오른쪽에 근거 수치(「최근 17회 0%」)** 로 갈랐다.
어디까지가 판정이고 어디부터가 그 판정을 만든 수인지 눈으로 갈린다.

막대는 **18px → 8px 알약형**으로 얇혔다 (사용자 "날렵하게"). 이 화면에서 큰 숫자는 막대가
아니라 옆의 개수라 막대는 길이만 읽히면 된다. 밴드 간격도 14px → 10px. **오답 분포 막대는
안 건드렸다** — 거기서는 막대가 주인공이다 (`.ladder` 안에서만 좁혔다).

## 2026-09-20 — 「뜻을 몰랐다」고 답했는데 문제가 나오던 것 (사용자 보고)

`useStudySession` 의 상태 판정이 `introIds.has(card.idiomId)` 일 때만 소개로 갔다. `introIds` 는
`planIntros` 가 **빌드 시점에** 고른 것이라 문턱 둘에 걸린다 — 누적 읽기 30회 미만이거나
(`INTRO_MIN_READINGS`), 소개가 문제의 1/3 을 넘거나(`INTRO_MAX_SHARE`), 이미 푼 적 있는
숙어면 안 고른다. 그 경우 사용자가 「몰랐다」를 누른 직후에 그 표현이 그대로 출제됐다.

### 무엇을 뒤집었나

2026-09-13 결정의 절반이다. `INTRO_MIN_READINGS` 주석은 **"처음부터 설명하지 않는다 —
기록이 없으면 앱은 이 사람이 뭘 아는지 모른다"** 이고, e2e 는 그걸 **「확인 질문에 모른다고
답해도 설명이 안 뜬다」** 로 못박고 있었다.

**전제가 「앱이 짐작한다」였다.** 아는 단어까지 가르칠까 봐 둔 문턱이다. 그런데 사용자가
「몰랐다」를 누른 건 짐작이 아니라 **진술**이라 그 전제가 성립하지 않는다. 그리고 같은 날
결정의 나머지 절반 — **「첫 만남을 시험이 아니라 소개로」** — 이 그 자리에서 깨진다.
두 결정이 부딪히는 지점이라 진술이 있는 쪽을 택했다 (사용자 확인: "확인에 모른다고 답한
것에는 질문이 붙는 로직만 변경하면 된다").

### 무엇을 안 건드렸나

**앱이 짐작하는 경로는 그대로다.** 「알고 있었다」로 답하거나 확인 질문이 안 붙은 새 표현은
여전히 30회 문턱과 1/3 상한을 따른다 (사용자: "새로운 표현에 대한 상한은 적당하다").
e2e 이름도 그에 맞게 좁혔다 — 「기록이 없으면 **앱이 짐작해서** 설명하지 않는다」.

### 곁가지 — 소개한 숙어의 남은 카드를 건너뛴다

`planIntros` 는 빌드 때 같은 숙어의 나머지 카드를 걷어낸다(소개에서 읽기·뜻을 다 보여준 뒤
같은 세션에서 물으면 첫 채점이 부푼다). 계획 밖에서 소개로 돌린 카드는 그 처리가 없어서
`skipIds` 로 같은 일을 런타임에 한다.

### 감수한 위험

「몰랐다」 소개는 1/3 상한에 안 들어간다. 이론상 세션이 소개로 찰 수 있지만, 확인 질문은
신규 도입 카드에만 붙어 세션당 몇 장으로 제한된다. 실사용에서 많아지면 상한 안으로
집어넣고 우선순위만 주는 쪽으로 조정한다.

검증 — `unknown-meaning-intro.spec.ts`(기록을 비운 상태에서 「몰랐다」 → 소개가 뜨고 입력창은
없고, 같은 표현이며, 「봤어요」 뒤엔 그 표현을 다시 안 묻는다). `npm test` **566** ·
`playwright` 39 → **40 스펙** · tsc·oxlint·build 클린.

## 채점 화면이 손 떼는 순간 사라진 것 — 자판의 유령 클릭 (2026-09-21 실기기)

사용자 보고: 안드로이드 11, 크롬으로 홈 화면에 설치해 실행하니 **정답 유무 화면이 금방
사라진다.**

원인은 자동 전환 타이머가 아니라 **클릭 하나가 두 화면에 걸친 것**이다. 9월 19일 두 결정이
겹친 자리다.

1. 반응속도 때문에 자판의 확인 키를 `click` → `pointerdown` 으로 옮겼다
2. 피드백 중에는 자판을 접는다 (250px 이 오답 설명을 잘라서)

그래서 확인을 누르면 **손가락이 닿아 있는 동안** 자판이 사라지고 같은 자리에 「다음」이
올라온다. `pointerdown` 을 `preventDefault` 해도 막히는 건 호환 마우스 이벤트(mousedown/
mouseup)뿐이고 **`click` 은 그대로 온다** — 그것도 손을 뗄 때 다시 hit-test 한 자리로.
Playwright 로 이벤트를 찍어 확인했다.

```
pointerdown → BUTTON.key key-submit "확인"
pointerup   → BUTTON.btn-primary "다음"
click       → BUTTON.btn-primary "다음"
```

마우스 `click` 으로는 재현되지 않아 기존 `keypad.spec.ts` 가 못 잡았다. **터치 기기 버그는
`tap()` 으로 써야 한다.**

### 고친 방법 — 뒤따르는 클릭 하나를 먹는다

확인 키 `pointerdown` 에서 `document` 캡처 단계에 일회성 click 리스너를 걸어 첫 클릭을
끊는다. React 는 루트 컨테이너에 듣고 있어서 document 캡처에서 막으면 앱까지 안 간다.
클릭이 아예 안 오는 경우(손가락을 끌고 나가 `pointercancel`)를 위해 500ms 뒤 스스로 걷는다.

`RomajiKeypad` 안에 두어 **찾기 화면의 「닫기」 키도 같이 덮는다** — 거기도 자판이 접히며
그 자리에 결과 목록이 올라와 같은 구조다.

기각한 대안 셋.

- **확인을 `pointerup`/`click` 으로 되돌린다** — 9월 19일 "반응속도가 느리다"를 되돌리는
  것이고, hit-test 는 손 뗄 때 하므로 `pointerup` 으로 옮겨도 유령 클릭은 그대로다
- **피드백 중에도 자판을 띄워 둔다** — 오답 설명이 잘리던 문제가 돌아온다
- **「다음」 행에 잠깐 `pointer-events: none`** — 시간 창을 CSS 로 관리하게 되고, 막아야 할
  것(끝난 제스처의 뒤끝)이 아니라 엉뚱한 것(다음 입력)을 막는다

검증 — `keypad-ghost-click.spec.ts` 2건 (확인을 **탭**해도 채점 화면이 남는다 / 먹는 클릭은
하나뿐이라 바로 이어 「다음」을 탭하면 넘어간다). `npm test` **566** · `playwright` 40 →
**42 스펙** · tsc·oxlint·build 클린.

## 찾기에서 담아 두기 (2026-09-21, 사용자 요청)

**2026-09-16 에 기각했던 C안(찜하기)을 사용자가 뒤집었다.** 같은 날 기각 사유는 둘이었다.

1. *"효용이 얼마나 있는지 모르겠다"* (사용자 판단)
2. append-only 라 담기·빼기가 이벤트 두 종류나 스토어 하나를 새로 부른다

둘 다 지금은 답이 있다.

**효용 — 밴드 0 은 담지 않으면 영영 안 나온다.** `selectSession` 의 `minBand` 기본값이
1 이라 신규 도입에서 밴드 0 을 건너뛴다. 찾기 대상 16,959개 중 **3,976개(23%)가 거기**
있다. 읽다 막혀서 찾아본 표현이 그 구간이면 배울 길이 없었다.

**스키마 — 이벤트 한 종류로 줄었다.** `StarEvent` 에 `on: boolean` 을 둬서 담기·빼기를
접었다. 마지막 이벤트가 이긴다. **스토어도 PK 도 `DB_VERSION` 도 안 건드렸고 마이그레이션도
없다.** 별도 스토어를 안 쓴 건 취향이 아니라 제약이다 — `sync.ts` 가 `events` 만 나르므로
스토어를 새로 두면 **백업에서 조용히 빠진다.**

**B안(지금 풀어보기)은 계속 기각이다.** 담은 것은 다음 세션에 소개로 나온다 — 시험이
아니라 보여주는 자리라 "답을 본 직후의 정답이 FSRS 잡음이 된다"는 문제를 안 만든다.

### 별이 하는 일의 경계

**신규 도입 우선권까지다.** 한 번 나와서 카드가 생기면 별은 아무 일도 안 하고 FSRS 가
이어받는다. 찾기 화면의 목록도 그 기준으로 거른다(`started`). 이벤트는 로그에 남지만
(append-only) 그게 동작에 영향을 주는 구간이 끝난다.

그래서 **정답률·밴드 사다리·리포트 통계는 하나도 안 바뀐다.** 바뀌는 건 "무엇을 먼저
소개할까" 하나뿐이다.

### 담은 것을 due 보다 먼저 낸다 — 상한 1/3

사용자가 직접 지목한 것은 며칠 밀린 복습보다 강한 신호다. 다만 상한이 없으면 열 개를
담은 날 세션이 통째로 신규 도입이 되어 복습이 밀린다. `STAR_MAX_SHARE = 1/3` 로 묶고
남은 것은 다음 세션으로 넘긴다. 짧은 세션에서 상한이 0 이 되지 않게 한 장은 보장한다.

**상한을 넘은 것은 평범한 신규로 되돌린다.** 처음엔 안 그랬는데, 밴드 1 짜리 다섯 개를
담으면 세 개만 나오고 나머지 둘이 **세션에서 통째로 빠졌다** — 담았다고 기회가 줄었다.
단위 테스트(`담은 게 없을 때와 장수가 같다`)가 잡았다.

### 잘려 나가던 버그 — 섞기와 자르기 사이

e2e 가 전체 실행에서만 실패했다. 원인은 상한도 밴드도 아니었다.

호출부(`useStudySession`)는 소개 여유분을 얹어 `limit + introCap*2` 장을 만들고,
`planIntros` 가 **앞에서부터 세어** 실제 문제 수로 자른다. 그 사이에 `shuffleByIdiom` 이
있다. 담은 카드를 맨 앞에 뽑아 놨는데 섞이면서 뒤로 밀리면 그대로 잘려서, **담아 놓고
세션을 시작해도 안 나오는 일이 40% 쯤** 생겼다.

고친 방법 — 섞을 때 **담은 숙어 묶음만 앞자리에 남긴다** (앞자리 안에서는 그대로 섞는다).
섞기의 원래 목적("순서를 예측해 모르는 한자를 찍는 걸 막는다", 2026-09-07)은 밴드 순서로
줄 세운 신규 도입에 대한 것이라, 사용자가 손으로 고른 몇 장에는 해당이 없다.

곁가지로 **상한을 셀 기준도 틀려 있었다.** `limit` 이 여유분만큼 부풀어 있어(20 → 32)
1/3 상한이 10 장으로 커졌다. `questionLimit` 을 따로 받아 실제 문제 수로 센다.

**검증 장치를 먼저 의심한다는 규칙이 여기선 반대로 걸렸다** — 처음엔 테스트 루프가
카드 전환 중에 헤드워드를 놓친 줄 알고 루프부터 고쳤다. 고쳐도 실패해서 세션에 뜬 표기를
전부 찍어 보니 정말 없었다. **탐침을 찍기 전에 테스트를 두 번 고쳤다.**

### 대조군을 돌렸다

`search-star.spec.ts` 가 진짜로 밴드 면제를 잡는지 확인하려고, 담기만 빼고 같은 스펙을
돌렸다 — 明白 은 세션에 안 나왔다. 이 확인이 없으면 "원래 나오는 단어였다"와 구분이 안 된다.

### 화면 — 줄 높이를 안 늘린다

별 버튼에 44px 손가락 자리를 주되 **세로 음수 여백(-9px)으로 줄 높이를 46px 로 유지**한다.
`--tap`(52px)이나 그냥 44px 을 주면 줄이 68px 이 되어 11줄이 화면 하나에 안 들어온다.
한 눈에 훑는 게 이 화면의 목적이다 (2026-09-16 절).

이미 카드가 생긴 줄에는 버튼 대신 「학습 중」을 적는다. 빈칸으로 두면 "왜 이 줄만 없지"가
되고, 담기를 내면 눌러도 아무 일이 안 일어난다.

검증 — 단위 **577** (select 담기 6건 · replay 담기 4건 신규) · `playwright` 42 → **44 스펙**
(`search-star` 2건 신규) · tsc·oxlint·build 클린. 375×667 실캡처로 줄 높이와 담아 둔 목록 확인.

### 곁가지 — ☆ 를 + 로 (같은 날, 사용자 요청)

별은 「즐겨찾기」로 읽힌다. 이 버튼이 하는 일은 **목록에 더하기**라 + 가 맞다.
담긴 상태는 −(U+2212 빼기표) 다 — 색만으로 구분하지 않는다는 원칙(PLAN §7)은 그대로고,
바뀌는 게 별 모양에서 기호로 옮겨졌을 뿐이다. +/− 는 별보다 획이 가늘어 19px 에서
묻혀서 22px 로 올렸다.

**코드 안의 이름(`StarEvent`·`starred`·`.star-btn`)은 안 바꿨다** — 글리프 하나 바뀐
것으로 이벤트 타입 이름까지 갈면 저장된 로그의 `type: 'star'` 와 코드가 어긋난다.

## 예문이 고유명사 이름 안에 파묻힌 것 (2026-09-21, 사용자 지적)

사용자 지적: `協和` 예문이 **１０年前に協和銀行と…** 인데 이건 회사 이름이라 뜻과 무관하다.

읽기 검증(`readingHolds`, 2026-09-18)은 **읽기만** 본다. 和歌山 안의 和歌 는 읽기가
그대로 わか 라 통과한다. 뜻이 다른 건 못 본다.

### 실측 — 좋은 쪽과 나쁜 쪽이 섞여 있다

예문 26,533건 중 표제어가 고유명사 형태소 하나에 통째로 파묻힌 것이 **85건**(숙어 52개).
갈래는 地域 39 · 組織 32 · 一般 14.

나쁜 쪽 — `和歌`/和歌山 · `太平`/太平洋 · `海道`·`北海`/北海道 · `高裁`/**最高裁**(고등법원
↔ 대법원) · `講談`/講談社 · `八重`/八重洲 · `辞林`/大辞林.

좋은 쪽 — `山脈`/アルプス山脈 · `十字`/赤十字 · `外務`/外務省 · `気象`/気象庁. 뜻이 그대로
살아 있다.

**둘을 자동으로 가를 신호가 없다.** IPADIC 은 外務省 도 講談社 도 똑같이 `固有名詞,組織`
이다. 사용자 결정으로 **전부 뺐다** — 좋은 쪽은 다른 예문을 갖고 있어 빈자리가 안 생긴다는
걸 먼저 확인했다.

### 결과

예문 보유 숙어 10,984 → **10,966** (18개가 예문을 잃었다), 문장 26,533 → 26,478.
후보에서 걸러진 문장은 364개인데 실제 감소가 55건인 건, 빠진 자리를 다음 후보가 채워서다.

**감수한 것** — 잃은 18개 중 `議院`/衆議院 · `自衛`/自衛隊 · `商務`/商務省 · `文部`/文部省 ·
`造幣`/造幣局 · `社会保険`/社会保険庁 여섯은 뜻이 통하는 쪽이었다. 가를 선이 없어 같이 나갔다.

### 정작 지적받은 協和 는 못 잡는다

IPADIC 이 `協和銀行` 을 `協和`(名詞,サ変接続) + `銀行`(名詞,一般) 으로 가른다. **고유명사
표시가 아예 안 붙어서** 이 규칙의 사정권 밖이다.

**접미사 목록(銀行·会社·新聞·大学…)으로 잡는 안을 기각했다.** 실측하니 32건이 걸리는데
그중 31건이 정상이다 — 海運会社 · 合弁会社 · 公共放送 · 校内放送 · 英字新聞 · 私立大学 ·
市立病院 · 大手銀行 · 国有鉄道. **좋은 예문 31건을 죽이고 나쁜 것 1건을 잡는 거래**라
성립하지 않는다.

처음엔 **그대로 뒀다** — `協和` 는 타토에바에 단독 출현이 0건이라 빼면 예문이 없어지고,
그 문장이 읽기 きょうわ 자체는 맞게 보여준다는 이유였다. **사용자가 그 근거를 기각했다**
(같은 날): *"예문이 하나뿐이어서 빼지 않았다는 것은 뜻을 이해할 수 있도록 예문을 둔 의미와
이미 뜻을 알고 있는 사람들을 대상으로 만든 고유 명사와 맞지 않는다."*

맞는 지적이다. **예문은 뜻을 잡으라고 두는 자리**고, 고유명사는 이미 뜻을 아는 사람을 상대로
지은 이름이라 애초에 그 자리에 못 선다. 예문이 하나뿐이라는 건 남길 이유가 아니라 그냥
예문이 없다는 뜻이다. 읽기는 카드가 직접 묻고 TTS 도 있어서 예문이 떠받칠 몫이 아니다.

그래서 `data/dict/propn-overrides.json` 에 **형태소 분석이 못 잡는 이름을 손으로 적는다.**
지금은 `協和銀行` 한 건이다. 예문을 읽다 이런 걸 만나면 이름을 더하고
`build:examples` → `build:runtime-dict` 를 다시 돌린다. 문장이 아니라 **이름**을 적는 이유는
타토에바가 갱신돼도 규칙이 살아남고, 같은 이름에 걸린 다른 표제어까지 같이 잡히기 때문이다.

### 규칙이 대칭이었던 버그 — 이름 뒤쪽까지 막고 있었다

사용자 질문: *"銀行의 예문으로 協和銀行의 경우는 나와도 된다. 반대로 協和의 예문으로
協和銀行는 빼야 한다. 이런 규칙이 적용되어 있는가?"* — **안 돼 있었다.** 이름 안이면
표기가 무엇이든 막았고, 그 동작을 테스트로 못 박아 두기까지 했다.

**이름은 「고유한 앞부분 + 종류를 가리키는 뒷부분」으로 짜인다.** 協和銀行 의 銀行 은 은행이
맞고 アルプス山脈 의 山脈 도 산맥이 맞다. 막아야 하는 건 앞부분(協和·和歌·太平)과
가운데(東京都庁 의 京都)다. 판정은 한 줄이다 — **이름이 표기 뒤로 더 이어지면 막는다.**

이걸 넣으니 85건 중 36건이 풀렸고, 그중 14종이 살아남았다 — 山脈/アルプス山脈 ·
十字/赤十字 · 半島/朝鮮半島 · 半球/北半球 · 列島/火山列島 · 議院/参議院 · 高原/志賀高原 ·
維新/明治維新 · 朝鮮/北朝鮮 · 共産党/中国共産党 · 近東/中近東 · 南米/中南米 · 内海/瀬戸内海.
**앞서 "가를 신호가 없어 같이 나갔다"고 적은 것들이 대부분 여기서 돌아왔다.**

뒤쪽인데 뜻이 다른 예외는 자동으로 못 가른다. 最高裁 의 高裁 는 고등법원이고 大西洋 의
西洋 은 서양이 아니다. 그건 손으로 적는다 — `propn-overrides.json` 10개 이름.

**풀어 준 자리는 전부 눈으로 훑었다.** 그 감사에서 `東海道線` 이 `海道` 예문으로 새로
들어온 걸 잡았다 — 뒤쪽을 허용하면 다른 이름으로 같은 문제가 생긴다. 규칙을 넓힐 때는
넓어진 쪽을 전수로 보는 게 맞다.

최종 — 예문 보유 숙어 10,984 → **10,968** (16개가 예문을 잃었다), 문장 26,533 → **26,486**.

검증 — `build-examples.test.ts` **12건 신규** (자동 규칙 7건 + 수동 목록 5건).
`npm test` 577 → **590** · `playwright examples` 통과 · tsc·oxlint·build 클린.
대조 — `協和`·`海道`·`高裁` 는 예문이 없어지고 `銀行`·`山脈`·`半島` 는 그대로다.

## 홈·리포트가 덜컥거린 것 (2026-09-21, 사용자 실기기 지적)

사용자가 화면 녹화를 보내며 *"페이지 렌더링이 되는 모습이 약간 덜컥거린다"*, 이어서
*"홈화면과 진단리포트 페이지가 덜컥거린다"*. 프레임 둘을 잘라 보내 줘서 크기까지 확인했다 —
**제목이 190 기기픽셀(≈95 CSS px) 위로 튄다.**

### React 가 아니다

처음 물음이 *"react라서 그런지"* 였다. 실측은 아니라고 했다. 프로덕션 빌드 + CPU 4배 +
1.6Mbps 에서 CLS 0, 롱태스크 105ms 한 건. 렌더링 자체는 조용하다.

기전은 둘이었다.

1. **2단계 렌더 × 세로 중앙 정렬.** `.home` 이 `justify-content: center` 인데 데이터가 늦게
   와서 `3장만`·`틀렸던 것` 묶음이 그때 생긴다. 아래에 190px 이 붙으면 중앙 정렬이
   그 절반만큼 **위에 있는 제목까지** 끌어올린다. 사용자 프레임 둘이 정확히 이 모습이다
2. **탭을 옮길 때마다 처음부터 다시 계산.** 화면이 언마운트되므로 다시 들어오면
   `listEvents` → `replay` → `buildSession`(후보 16,959개)을, 리포트는 거기에 재분류 ·
   `buildReport` · `buildLevel` · `prescribe` 까지 다시 돈다

### 스켈레톤을 안 쓴 이유

사용자 질문: *"스켈레톤 UI 같은 걸 써야 하는 지."* 안 썼다. 스켈레톤은 「기다리는 중」을
알리는 장치인데 **지금 문제는 다 그려진 뒤에 내용이 움직이는 것**이다. 높이가 안 맞는
스켈레톤은 교체될 때 똑같이 덜컥하고, 맞추려면 결국 자리를 예약하는 것과 같은 일을 한다.

이 앱엔 이미 그 관례가 있다 — `.answer-row` 3슬롯 고정, `.slot`(`visibility: hidden`)으로
빈자리를 지켜 「주 동작의 위치가 안 움직인다. 엄지가 한 자리에 머문다」. **홈이 자기 규칙을
안 지키고 있었다.**

### 고친 방법 둘

**자리 예약 — 높이를 숫자로 안 적는다.** 로딩 중에는 `3장만` 버튼과 「틀렸던 것」 묶음의
**진짜 마크업을 `.slot` 으로 숨겨서** 렌더한다. 매직 넘버를 쓰면 버튼 높이가 바뀔 때 어긋난다.

**모듈 캐시 + `dataVersion`.** 홈·리포트가 마지막 결과를 모듈 변수에 들고, `useState`
초기화 함수에서 꺼낸다 — effect 로 넣으면 로딩 화면이 한 번 그려진 뒤에 바뀌어서 의미가 없다.
무효화 키는 `src/core/dataVersion.ts` 의 카운터고, **이벤트 쓰기 전부**(`appendEvent`·
`importEvents`·`importMissingEvents`·동기화 초기화)와 **설정 저장**에서 올린다. 설정도 세는
이유는 `sessionLimit`·`ratio` 가 미리보기를 바꾸기 때문이다.

시각(now)은 안 센다. 앱을 켜 둔 동안 카드가 기한을 넘겨 숫자가 한 장 어긋날 수 있지만,
세션을 마치면 이벤트가 쌓여 어차피 무효화된다. 디스크에 안 남기는 것도 같은 이유다 —
오래된 값을 들고 있느니 다시 계산하는 게 낫다.

### 테스트가 헛돌고 있었다

처음 쓴 e2e 는 **로딩 상태를 한 번도 못 잡았다.** 빈 DB + 개발 서버라 CPU 8배로 조여도
계산이 첫 스냅샷 전에 끝났고, 그래서 예약이 있든 없든 통과했다. 두 군데를 고쳤다.

- `page.route` 로 `dict/base.json` 을 **붙잡아 로딩을 고정**하고, 로딩 상태를 잡았는지
  자체를 단언한다 (`expect(loading.loading).toBe(true)`)
- **진짜 숙어 id 로 오답을 심는다.** 기록이 없으면 「틀렸던 것」이 로딩 뒤에 접혀서
  예약이 제대로 돼 있어도 움직인다 — 실사용과 조건이 다르다. 앞서 심은 가짜 id 는
  풀에 없어 버튼이 아예 안 생겼다

그 과정에서 나온 두 수치가 예약의 증거다. 빈 DB 에서는 예약이 접히며 **57px 움직이고**,
오답이 쌓인 실사용 조건에서는 **0px** 다.

### 남겨 둔 것 — 폰트

첫 실행에는 폰트가 늦다. 1.6Mbps 기준 `Pretendard-Regular.woff2`(766KB)가 1,256ms 에
시작해 11,456ms 에 끝나고, `font-display: swap` 이라 그때 글자가 다시 그려진다. 프리로드가
없어 시작도 1.25초 늦다. **Pretendard 는 서브셋을 안 한다** — 사전 정의문에 실제로 쓰인
한글은 11,172자 중 **1,032자**뿐인데 완성형을 통째로 싣는다.

서비스워커가 프리캐시하므로 두 번째 실행부터는 안 보인다. 홈·리포트 건과 원인이 달라
같이 안 고쳤다. 손댈 때는 프리로드 → 서브셋 순이고, **일본어 쪽에 `font-display: optional`
은 쓰지 않는다** — 폰트가 늦으면 한자가 시스템 자형으로 나가 잘못된 글자 모양을 학습한다
(CLAUDE.md 「일본어 렌더링」).

검증 — `screen-cache.spec.ts` 2건 신규(탭을 오가도 로딩 문구가 안 뜬다 / 계산 중에도 제목과
주 버튼이 안 움직인다), `dataVersion.test.ts` 2건. `npm test` 590 → **592** ·
`playwright` 44 → **46 스펙** · tsc·oxlint·build 클린.

### 리포트는 캐시만으로 안 됐다 (같은 날, 사용자 재확인)

배포 뒤 사용자: *"리포트는 아직 로딩될 때 덜컥 거리는 게 보이고, 메인은 이제 괜찮은데."*

홈에는 자리 예약을 넣고 리포트에는 **캐시만** 붙였다. 캐시는 탭을 다시 열 때만 듣고,
앱을 켠 뒤 **처음 여는 한 번**은 그대로 계산한다. 그 한 번이 보인 것이다.

원인은 홈과 달랐다. `.screen-body` 는 위에서부터 쌓이는 flex(`gap: 24px`)인데
**「불러오고 있어요」가 도구 묶음 위에 있었다.** 계산이 끝나 그 문구가 사라지면 도구가
통째로 **24px 올라간다** — 이미 보이던 것이 움직이는 게 덜컥거림의 정체다.

고친 건 순서 하나다. **알림을 도구 아래, 곧 본문이 들어설 자리로 옮겼다.** 그러면 문구
자리를 본문이 그대로 이어받아 보이던 것은 하나도 안 움직이고 아래로만 자란다. 마크업을
더하지 않았고 빈자리도 안 생긴다 — 홈의 슬롯 예약보다 이쪽이 싸다. 화면 구조가 달라서다
(홈은 세로 중앙 정렬이라 아래가 자라면 위가 밀리고, 리포트는 위에서부터 쌓인다).

**측정에 2~3px 잡음이 있었다.** 도구 위에 남은 건 제목줄뿐인데, 두 측정 사이에 폰트가
도착하면 행간이 그만큼 달라진다. 실행마다 흔들려서 단언에 4px 허용치를 뒀다 — 잡으려는
것은 24px 급이고, 대조군(순서를 되돌린 것)은 그대로 24px 로 실패한다.

검증 — `screen-cache.spec.ts` 3건째 신규. `npm test` **592** · `playwright` 46 → **47 스펙** ·
tsc·oxlint·build 클린.

### 찾기도 같은 캐시로 (같은 날, 사용자 재확인)

배포 뒤 사용자: *"찾기에서 검색 창 하단의 글자가 로딩되는 순간이 목격된다."* 홈·리포트와
같은 기전이라 같은 처방을 했다 — `dataVersion` 키 모듈 캐시. 담기/빼기도 이벤트라
버전이 올라 자동으로 버려진다.

**자리 예약은 안 넣었다.** 찾기는 알림과 본문이 같은 자리를 쓰고 그 위의 검색창은 안
움직인다 — 이미 보이던 게 움직이는 게 아니라 늦게 차는 것뿐이라 캐시로 충분하다.
셋이 원인은 같은데 처방이 다 달랐던 건 화면 구조가 달라서다.

| 화면 | 구조 | 처방 |
|---|---|---|
| 홈 | 세로 중앙 정렬 | 슬롯 예약 (아래가 자라면 위가 밀린다) |
| 리포트 | 위에서부터 쌓임, 알림이 도구 **위** | 순서 교체 |
| 찾기 | 위에서부터 쌓임, 알림이 본문 자리 | 캐시만 |

### 테스트가 또 헛돌았다 — 이번엔 기록 규모

찾기 캐시 테스트가 **캐시를 빼도 통과했다.** 심은 이벤트가 몇 건이라 재생이 순식간이었다.
**2,000건으로 늘리니 대조군이 실패한다.** 사용자 실기기는 다시보기 대상만 388개다.

같은 날 세 번째다 — 로딩 상태를 못 잡아서(홈), 조건이 실사용과 달라서(홈의 오답 없음),
규모가 작아서(찾기). **화면 성능 테스트는 「무엇을 잡는지」를 대조군으로 확인하기 전까지
초록색이 아무 뜻이 없다.**

검증 — `screen-cache.spec.ts` 4건째 신규. `npm test` **592** · `playwright` 47 → **48 스펙** ·
tsc·oxlint·build 클린.

### 곁가지 — 폰트 (같은 날, 이어서)

홈·리포트 건과 원인이 달라 따로 잡았다. 1.6Mbps · CPU 4배 실측 기준이다.

**전** — `Pretendard-Regular.woff2` 가 **1,256ms 에 시작해 11,456ms 에 끝났다**(766KB).
`font-display: swap` 이라 그 10초 동안 시스템 글꼴로 그려지다가 한 번에 다시 그려진다.
프리로드가 없어서 CSS 를 파싱하고 글자를 그린 뒤에야 `@font-face` 를 발견하느라 시작도 늦었다.

**서브셋 — 748KB → 96KB.** 통째로 복사하고 있었다(일본어만 서브셋했다). 실제로 쓰는 한글은
완성형 11,172자 중 **1,163자**뿐이다. 대상은 사전의 뜻·한국 한자음 + `src/**/*.{ts,tsx,css}`·
`guide.html`·`index.html` 에서 긁은 UI 문구 + 라틴/기호 전부다. 라틴·기호를 통째로 넣는 건
숫자·영문이 한국어 문장에 섞여 나오는데 그쪽이 폴백을 타면 한 줄 안에서 글꼴이 갈려
더 눈에 띄기 때문이다.

**「완성형 전체」 안(598KB)은 기각했다.** 내용과 안 엮여 안전하지만 20% 밖에 안 줄어든다.
빠진 글자의 대가가 일본어(**틀린 자형을 학습한다**)와 달리 **시스템 한글 폰트로 떨어지는
것뿐**이라, 87KB 를 택하고 대신 다시 돌릴 자리를 문서에 박았다 (`docs/tools.md` 「폰트」).

**프리로드는 한국어만.** 일본어 Bold(509KB)까지 얹었더니 JS 번들과 대역폭을 나눠 가져
**FCP 가 1,500ms → 2,584ms 로 되레 나빠졌다.** 한국어만 남기니 1,892ms 고, 한글 폰트가
**1,321ms** 에 끝난다. 일본어 Bold 는 여전히 7초쯤 걸려 홈 제목(`読めない`)이 한 번 바뀌지만,
`font-display: optional` 로 막지 않는다 — 폴백으로 굳으면 그게 곧 한국 자형이다.

**되레 나빠진 걸 실측으로 잡았다는 게 기록할 값이다.** 「프리로드는 좋은 것」이라는 통념대로
셋 다 얹었으면 첫 화면이 1초 늦어지는 걸 모르고 넘어갔다.

검증 — `npm test` **592** · `playwright` **46 스펙** · tsc·oxlint·build 클린. 375×667 실캡처로
한글 렌더링 확인(흥기·광휘·공공기관 등 폴백 없음).

## 한 한자의 음독을 갈라 내는 대조 세션 (2026-09-21, 사용자 요청)

사용자 질문 — 「같은 한자라도 오음인지 한음인지에 따라 발음이 다른데, 이런 읽기 규칙별로
세션을 만들 수 있나」.

### 기각 — 오음·한음 층위를 라벨로 붙이는 안

**데이터가 없다.** `pairs.json` 의 `kind` 는 `on | kun` 둘뿐이고, 출처인 KANJIDIC2 도
`ja_on` 을 한 덩어리로 준다. 층위를 실으려면 음독 쌍마다 오음·한음·당음·관용음을 새로
붙여야 하는데 출처가 없다. LLM 으로 만들면 `verified=false` 필드가 하나 더 느는데 검수할
근거 자료가 또 없다.

**라벨이 있어도 축으로 약하다.** `rules.ts` 의 음독 층위 절이 이미 「경향일 뿐 규칙은
아니에요」라고 쓰고 있고, PLAN §6 이 그 절을 「규칙으로 유도할 수 없고 낱말로 익힌다」는
셋(청탁 미구분·음독 층위·혼독)으로 묶어 뒀다. 오음만 모은 40문항을 풀어도 **다음 숙어에서
오음을 고를 근거가 안 생긴다.** 층위는 내력이지 예측 규칙이 아니다.

### 채택 — 층위 이름 없이 대조만 한다

층위를 몰라도 **한 한자에 음독이 둘이라는 사실**은 지금 데이터로 안다. 코퍼스 실측이다.

| 사전 최소 숙어 수 | 음독이 2개 이상 실현되는 한자 | 3개 이상 |
|---|---|---|
| 2 | 159 | 6 |
| 3 | **114** | **0** |
| 5 | 63 | 0 |

`人 じん(158)/にん(75)` · `行 こう(113)/ぎょう(26)` · `力 りょく(91)/りき(18)` 꼴이다.
임계를 3으로 잡으면 **한자당 음독이 최대 둘**이라 형제 수 상한이라는 값을 안 만들어도 된다.

### 집중 세션을 넓힌다 — 새 함수를 안 만든다

`buildFocus` 가 하는 일(기한 무시 · 틀린 것 → 안 본 것 → 맞힌 것 · 표면형으로 번갈아)이
대조 세션에 그대로 맞는다. 다른 건 **표적이 쌍 하나가 아니라 쌍 집합**이라는 것뿐이라
`pairId: string` 을 `pairIds: string[]` 로 넓혔다. 쌍 하나를 주면 예전 그대로다.

번갈아 내는 키는 **쌍이 아니라 표면형 그대로 둔다.** 표면형이 쌍보다 잘게 갈리므로
人間 にん ↔ 人口 じん(쌍 사이)과 発達 はっ ↔ 発言 はつ(쌍 안)가 **한 장치로** 다 걸린다.
쌍을 키로 삼으면 후자가 죽는다.

### 처방이 대조로 바뀐다 — 버튼을 늘리지 않는다

형제 음독이 있으면 그 자리의 집중 세션을 **대조로 갈아끼운다.** 버튼을 하나 더 붙이지
않는 이유는 `人:じん` 만 반복해 푸는 것이 이 경우엔 **틀린 처방**이어서다 — 「人 은 じん」
이라는 과잉일반화를 그 세션이 직접 가르친다. 이미 `FocusOptions` 주석이 같은 경계를
표면형 층위에서 말하고 있었고, 그게 쌍 층위에도 그대로 적용된다.

대가는 「뚫으면 N개가 열린다」의 N 이 흐려지는 것이다. 문구를 음독별 숙어 수를 나란히
적는 쪽으로 바꿔서 맞췄다.

### e2e 는 기록을 심는다 — 무작위로 틀리면 운에 달린다

`focus-session.spec.ts` 는 세션을 무작위로 틀려 취약 음독을 만든다. 대조는 그 방식으로는
못 잡는다 — **틀린 한자에 형제 음독이 있는지가 운**이라, 없는 날엔 검사가 초록색인 채로
아무것도 안 본다. 그래서 `人:on:じん` 숙어 36개에 오답을 심어 표적을 고정했다.

**대조군을 돌렸다** — 처방이 형제 쌍을 안 넘기게 되돌리면 실패한다. 처음 판은 요미가나가
붙은 소개 카드에서 `innerText` 가 `悪\nあく\n人\nにん` 로 나와 표기 조회가 전부 빗나갔고,
그 바람에 「음독 하나만 쓴다」로 실패했다 — 세션은 이미 맞게 섞여 있었는데 **검사가 못 읽고
있었다.** `rt` 를 걷어내고 고쳤다.

검증 — `npm test` 592 → **608** · `playwright` 48 → **49 스펙** · tsc·oxlint·build 클린.

### 기각 — 음독 맵에서 대조 세션을 바로 여는 통로 (2026-09-21, 사용자 판단)

처방은 조건이 겹겹이라(읽기 30회 · 쌍 노출 3회 · 오답 1회 · 파급력 상위 2) 대조 세션을
아무 때나 못 연다. 음독 맵은 쌍을 전부 나열하고 있으니 거기에 버튼을 붙이면 조건 없이
아무 한자나 고를 수 있다 — 그 안을 냈고 **사용자가 기각했다.**

> 배우다가 헷갈리는 것이 낫다. 처음부터 음독이 여러 개라는 걸 알면 더 헷갈릴 수 있다.

**대조는 틀린 다음에 와야 값이 있다.** 「人 은 음독이 둘」을 미리 알려 주면 아직 한쪽도
안 굳은 상태에서 선택지를 둘로 벌려 놓는 셈이다. 오답이 먼저 나야 그 대조가 **자기 오답의
설명**이 되고, 그 전에는 그냥 외울 것이 두 배가 된다.

그래서 진입로는 처방 하나로 둔다. 조건이 까다로운 것은 결함이 아니라 **이 순서를 강제하는
장치**다. 「어느 화면에서든 열 수 있어야 한다」는 이유로 이 안을 다시 꺼내지 않는다.


## 다시보기에서 한 벌 더 뽑기 (2026-09-21, 사용자 요청)

「맨 마지막에 돌아가기로 빠져 나가거나 다른 표현 30개를 보던가」.

### 안 본 것부터 채운다

`pickBrowse` 를 그냥 다시 부르면 오답 수 가중 무작위라 **자주 틀린 숙어가 또 앞자리를
차지한다.** 후보가 70개인데 같은 얼굴이 도는 셈이라, 이번 방문에서 안 낸 것부터 채우는
`pickBrowseMore` 를 따로 냈다. 안 낸 것이 모자라면 이미 낸 것에서 메워 **수는 늘 채운다** —
「다른 30개」라고 해 놓고 7장만 주면 그게 더 이상하다.

문구는 후보 수를 보고 갈린다. 후보가 화면 한 벌보다 많으면 「다른 N개」, 아니면
「한 바퀴 더」다. 같은 30개를 도로 내면서 「다른」이라고 하면 거짓말이다.

### 버튼 자리는 3슬롯으로 고정한다

마지막 장에만 「돌아가기」가 늘어나는데, `.answer-row.choice`(2열) 그대로 두면 그 장에서만
이전·가운데 버튼 폭이 달라진다. 기본 `.answer-row`(1fr 1.4fr 1fr)로 바꾸고 그 전 장에서는
오른쪽을 빈 `.slot` 으로 뒀다 — 카드 화면이 이미 쓰는 관례다.

### 스냅 컨테이너가 직전에 스냅된 **요소**를 쫓아간다

첫 판은 통과했는데 **후보가 적을 때 깨졌다.** 마지막 장(30/30)에서 한 벌 더 부르면
12번째 장으로 끌려간다. `scrollTo(0)` 를 즉시 걸어 뒀는데도 그랬다.

원인은 `scroll-snap-type: x mandatory` 다. 스냅 컨테이너는 내용이 바뀌면 **직전에 스냅돼
있던 요소**를 다시 찾아간다. 두 벌째가 이미 본 것으로 메워지면 그 요소가 `key={item.id}` 로
살아남아 새 목록 어딘가에 있고, 브라우저가 거기로 감는다. 후보가 70개인 첫 판은 두 벌이
완전히 갈라져(겹치는 요소가 없어) 우연히 통과했다.

**트랙에 `key={round}` 를 걸어 통째로 새로 단다.** 직전 스냅 요소가 DOM 에 없으면 쫓아갈
것도 없다. `scrollTo` 로 밀어내는 쪽은 같은 기전과 경쟁하는 것이라 안 걸었다.

**대조군** — 후보 35개 판(겹치는 목록)을 테스트로 박았다. 되돌리면 「12 / 30」로 실패한다.
70개 판만 있었으면 이 버그를 못 잡고 넘어갔다.

검증 — `npm test` 608 → **612** · `playwright` 49 → **51 스펙** · tsc·oxlint·build 클린.

### 빈 슬롯이 쏠림을 만들었다 (같은 날, 사용자 지적)

3슬롯 고정(`1fr 1.4fr 1fr`)으로 자리를 잡아 뒀는데, 마지막 장이 아닌 곳에서는 오른쪽
한 칸이 **빈 채로 남는다.** 375px 에서 103px 이 비어 버튼 둘이 왼쪽으로 몰려 보였다.

「버튼이 안 움직인다」와 「줄이 폭을 다 쓴다」가 fr 로는 같이 안 된다 — 칸을 몇 개로
나눌지가 장마다 다른데 fr 은 그 수로 폭을 나누기 때문이다. **양 끝을 글자 폭(`auto`),
가운데를 `1fr`** 로 바꿔 풀었다. 「‹ 이전」은 글자가 같으니 어느 장에서도 같은 폭·같은
자리이고, 늘고 주는 것은 가운데 버튼의 오른쪽 끝뿐이다.

검사도 fr 로는 못 잡던 것이다. 줄의 오른쪽 끝과 마지막 버튼의 오른쪽 끝 차이를 재는
`gap()` 을 넣고 **375px 에서** 본다 — 넓은 화면에서는 버튼 셋이 다 들어가 안 드러난다.
되돌리면 103 으로 실패한다.

## 다시보기 요미가나 가리기 (2026-09-21, 사용자 요청)

「스크래치 카드처럼 가렸다가 버튼을 클릭하면 보였으면 한다.」

### 가림막은 글자를 **치우지 않는다**

`rt` 를 `display:none` 이나 `visibility:hidden` 으로 두면 벗기는 순간 한자가 내려앉는다 —
루비는 한자 위 줄을 차지하는 요소라 빼면 줄 상자가 줄어든다. 글자를 투명하게 두고 그
자리에 색을 깔았다. 벗겨도 상자가 1px 도 안 움직인다.

~~읽기의 **길이**는 막대 폭으로 드러난다. 그래도 두는 쪽을 골랐다.~~
**같은 날 사용자 지적으로 고쳤다.** rt 에 배경을 깔면 막대 길이가 곧 읽기 길이라
두 자인지 네 자인지가 덮은 채로 드러난다 — 그게 이 화면이 감추려는 것의 절반이다.

막대를 `rt::after` 로 옮겨 **고정 폭(2.2em, 한자 폭의 0.66배)** 으로 그린다. rt 자체의
상자는 원래 폭 그대로 남으므로 **루비 열이 안 넓어지고 벗겨도 한자가 옆으로 안 밀린다** —
rt 에 직접 `width` 를 주면 그 반대가 된다. 절대 배치라 읽기가 한 자여도 막대가 rt 상자
밖으로 나가 가운데 정렬로 보인다.

검사는 **30장 전부의 막대 폭**을 재서 값이 하나인지 본다. 한 장만 보면 그 장의 읽기들이
우연히 같은 길이일 수 있다. 막대가 0px 인 경우를 따로 막아 뒀다 — 안 그리면 「폭이 다
같다」가 저절로 통과한다.

### 버튼을 치우지 않고 반대말로 바꾼다

처음엔 벗기면 「읽기 보기」를 없앴다. **카드 본문이 세로 가운데 정렬이라 한 줄이 빠지자
한자가 19px 내려앉았다** (e2e 가 잡았다). 빈 자리를 예약하는 대신 「다시 가리기」로
뒤집었다 — 자리도 지키고 쓸모도 있다. 다시보기 하단 버튼의 빈 슬롯 건(같은 날)과 결론이
반대인데, 거기는 **빈 자리가 보이는** 게 문제였고 여기는 **자리가 사라지는** 게 문제였다.

### 벗김은 카드를 떠나면 풀린다

예문 자리·규칙 펼침과 같은 관례다. 다시 만났을 때 또 스스로 떠올려 보는 게 이 화면의
값이라, 벗겨 둔 채로 남기면 두 번째 만남이 그냥 읽기가 된다.

### 설정 기본값은 가림

`browseMask` 는 저장된 적 없으면 켬이고 `false` 만 명시적인 끔으로 받는다. 읽기가 보이는
채로 넘기면 「아는 것 같은 느낌」만 남는다.

검증 — `npm test` 612 → **613** · `playwright` 51 → **53 스펙** · tsc·oxlint·build 클린.
375px 실캡처로 가림·벗김 두 장 확인.

### 실기기에서 깨졌다 — 가림막이 카드 높이만 한 기둥이 됐다 (같은 날, 사용자 제보)

iPhone 스크린샷: 막대 넷이 카드 한가운데에 **겹쳐 쌓여** 카드를 세로로 가로지르는 기둥이
됐다. 폭(2.2em)은 맞는데 높이와 자리가 전부 틀렸다.

**`rt` 의 `position: relative` 를 iOS Safari 가 무시한다.** 그래서 `::after` 의 기준 상자가
rt 가 아니라 위쪽의 위치 지정된 조상(카드)이 됐다. `top:0;bottom:0` 이 카드 높이가 되고
`left:50%` 가 카드 중앙이 되니, 넷이 같은 자리에 겹친다. `display: block` 으로 덮어써도
루비 레이아웃 경로를 벗어나지 못한 것이다.

**위치 지정을 아예 안 쓰는 쪽으로 바꿨다.** 배경 한 겹(`linear-gradient`)을
`background-size: 2.2em 100%` 로 잘라 가운데에 깐다. 기준 상자가 제 요소라 그 경로를 안 탄다.

그런데 배경은 **제 상자 밖으로 못 나간다.** 한 자짜리 읽기(御金 의 お)에서 막대가 상자
폭으로 잘려 도로 짧아졌다 — 크롬 실캡처에서 잡았다. `min-width: 2.2em` 을 같이 줘서
상자가 막대보다 좁아지지 않게 했다. 2.2em 은 한자 폭의 0.66배라 **루비 열 폭(= 한자 폭)을
안 넘고**, 그래서 벗겨도 한자가 안 밀린다.

검사도 고쳤다. 설정값(`background-size`)이 아니라 **그려진 폭(`min(설정값, 상자 폭)`)** 을
잰다. 설정값만 보면 위 잘림을 영영 못 본다.

### e2e 에 WebKit 을 하나 더 둔다 (같은 날, 사용자 지시)

이 건은 **크롬 e2e 를 다 통과하고 실기기에서 깨졌다.** 브라우저가 크롬 하나뿐이면 WebKit
전용 동작을 검사가 볼 방법이 없다.

**진단을 엔진에서 직접 확인했다.** 같은 CSS 에서 `rt` 의 computed `position` 이
크롬은 `relative`, WebKit 은 **`static`** 이다. 엔진이 루비 텍스트의 위치 지정을 거부하는
것이고, 그래서 `::after` 가 카드를 기준으로 잡혔다.

**전부는 안 돌린다.** 시간이 두 배가 되고, CDP 를 쓰는 `screen-cache` 는 크로미움 전용이라
WebKit 에서 돌지도 않는다. **그려진 결과를 재는 스펙만** 올렸다 — `browse-mask`·
`browse-reroll`·`ja-lang`. 흐름·로직은 크롬 한 번으로 족하다.

**올리자마자 WebKit 전용 문제를 하나 잡았다.** `ja-lang` 이 DB 심기에서 2분을 멈췄다.
원인은 테스트 쪽이다 — 「세션 시작」이 *보이는* 시점에는 앱이 아직 DB 를 안 만들었는데
거기서 `indexedDB.open` 을 하면 **스토어 없는 빈 DB 가 생기고**, 이어지는 `transaction` 이
콜백 안에서 던져 아무도 못 받는다. 크롬은 그 전에 앱이 DB 를 만들어 우연히 통과했다.
버튼이 *눌리게 될* 때까지 기다리게 고치고, 스토어가 없으면 멈추는 대신 실패하게 했다.

데스크톱 WebKit 이 iOS Safari 와 같지는 않다. 같은 엔진 계열이라 이런 종류를 잡을 뿐,
통과가 실기기 확인을 대신하지 않는다.

## 밴드 사다리를 배지 + 한 줄로 (2026-09-22, 사용자 요청)

「흔들림·안정은 알약 배지로, 막대 우측 숫자는 빼고, 수치는 아래 한 줄로.」

### 기각 — 「맞춘 표현 N개」

사용자 문구는 「맞춘 표현」이었는데 **그 수는 데이터에 없다.** 막대가 그리는 값은
`stable`(FSRS 안정도 14일 이상)이지 정답 낸 표현 수가 아니다. `correct` 는 최근 30회
창 안의 **채점 횟수**라 표현 수도 아니다. 「맞춘」으로 쓰면 사다리 제목(「밴드별 숙지한
표현」)·캡션과 그 자리 숫자가 서로 다른 것을 가리킨다. 「숙지한 표현」으로 냈다.

### 재 보고 정했다 — 줄이지 않았다

문구가 길어 폰에서 접힐 것 같아 「출제 40개 · 숙지 12개」로 줄이려 했는데, **재 보니
들어간다.** 375px 기본 글자에서 수치 261px, 가용 286px 이다. 줄여 놓았으면 「출제」·「숙지」
라는 모호한 명사만 남을 뻔했다.

넘치는 경우는 「크게」(`--ui-scale: 1.1` → 287px)와 세 자리 수다. 눌러 찌그러뜨리는 대신
`flex-wrap` 으로 배지 아래로 접는다.

### 배지 색은 흔들릴 때만

안정에도 색을 주면 상태 둘 다 이벤트가 되어 경계선이 안 보인다. `--ok` 를 무채로 둔 것과
같은 이유다. 안정은 무채 알약, 흔들림만 `--ng`.

### 덤 — 죽어 있던 규칙 하나

`.band-shaky .band-note` 는 안 먹고 있었다. `band-{status}` 가 `.bar-row` 에 붙는데
`.band-note` 는 그 형제라 선택자가 애초에 안 맞았다. 배지 색을 주려면 필요한 자리라
`.band-note` 에도 상태 클래스를 붙여 살렸다.

검증 — `npm test` **613** · `playwright` **60** (크롬 53 + WebKit 7) · tsc·oxlint·build 클린.
375px 다크 실캡처로 한 줄 유지 확인. `full-flow` 가 막대 우측 텍스트 0개·배지 모양
(`border-radius: 999px`)·수치 한 줄(`getClientRects().length === 1`)을 본다.

### 배지는 밴드 이름 옆이 아니라 막대 오른쪽 끝 (2026-09-22, 사용자 요청 + 고민 위임)

사용자 지시는 「밴드 구분 텍스트 우측」이었고, 「그래프 우측 끝이 정렬이 맞을까」를 같이
물었다. **두 안을 다 그려서 골랐다.**

밴드 이름 옆은 자리가 **글자 길이를 탄다.** 「N3 이하」와 「N2~N1 경계」의 폭이 달라
줄마다 배지가 어긋난다. 고정 칸을 끼워 넣어도 막대가 그만큼 짧아지고(150px → 123px),
배지가 행 가운데에 떠서 어디에도 안 붙는다.

막대 오른쪽 끝이면 **배지와 아래 수치가 같은 오른쪽 선**에 선다. 오른쪽 한 칸이
「상태·수치」로 읽히고 막대도 길게 남는다. 사용자가 물은 쪽이 맞았다.

- 칸은 **고정 폭**이라야 한다. 행마다 그리드가 따로라 `auto` 면 배지 폭을 타고 어긋난다.
  5em(≈70px)은 가장 긴 「표본 부족」(63px)이 들어가는 폭이다 (실측)
- 칸 안에서 배지를 오른쪽에 붙인다(`justify-self: end`). 왼쪽에 붙이면 폭이 제각각이라
  오른쪽 선이 깨진다

**배지가 막대 값의 라벨로 오독될 여지**는 남는다. 막대는 숙지/출제 비율이고 배지는
최근 30회 정답률 판정이라 축이 다르다. 배지 글자가 수치가 아니라 판정어(안정·흔들림)라
실제 오독 위험은 낮다고 보고 감수했다.

검사는 **배지 오른쪽 끝과 수치 오른쪽 끝의 차이**를 잰다(≤1px). 배지를 밴드 이름 옆으로
되돌리면 315px 로 실패한다 — 대조군으로 확인했다.

덤 — 수치 줄 끝에 `/` 가 홀로 남던 것을 고쳤다. 아직 안 푼 밴드(`seen === 0`)에서
「출제 3개 / 숙지 0개 / 」로 끝났다.

## 2026-09-22 — Jev 를 붙여 뜻 검수 관문을 다시 쟀다 (API 키 발급됨)

### 계약은 문서가 아니라 프로브로 확정했다

공개 문서에 raw 요청·응답 예시가 없어서 `tools/jev-probe.ts` 로 한 번 쏴 보고 고정했다.

- 인증은 **`Authorization: Bearer`** — `x-api-key` 는 403 이다
- 질문 객체에 **판별자 `type`** 이 필요하다 (`{ type: 'noul', instructions }`). 없으면 422
- 응답은 `{"answers":{"<이름>":{"type":"noul","noul":0.08}},"usage":{...}}`

키는 `.env` 의 `TYPESAFE_API_KEY` 에만 둔다 (공식 SDK 가 읽는 이름과 같게 맞췄다).
**빌드 시점 배치 판정 전용이다 — 런타임 앱은 이 키를 모른다.**

### 결과 (사람이 `o` 로 통과시킨 1,411건, 같은 질문·같은 정보)

| | Ollama `qwen3.5` | Jev `jev-1.13.0` |
|---|---|---|
| 걸린 시간 | 약 26분 (0.9건/초) | **45초 (32.9건/초)** |
| 「틀렸다」 지목 | 43 | 52 |
| 확률 | 없음 (이진) | **있음** |
| 비용 | 0 (로컬) | 약 $0.02 |

확률 분포 — `0.0~0.2: 8 · 0.2~0.5: 44 · 0.5~0.8: 318 · 0.8~1.0: 1,041`.

**확률이 값을 한다.** 낮은 구간이 진짜 오류에 몰린다 — `島内` 「어떤 **도(道)**의 구역 안」
(← on an island. 島/道 혼동이다), `引導` 「이끌어 지도함」(← requiem), `公庫` 「공공의 창고」
(← finance corporation), `取組` (← effort; initiative). 그리고 **0.5~0.8 의 318건**이
"아마 맞지만 확신은 없다" 구간인데, 이 축은 Ollama 의 이진 판정으로는 못 얻는다.

### 둘이 서로 다른 걸 잡는다

숙어 단위로 합치면 **59건** — jev 만 24 · ollama 만 17 · 둘 다 18. 예를 들어 `島内` 는
Jev 만 잡았다. 한쪽만 쓰면 절반을 놓친다.

### 「오탐률」이라는 이름을 더는 못 쓴다

3.4% / 3.7% 를 오탐률이라 불렀는데, 지목분을 보면 **상당수가 사람 검수가 놓친 진짜 오류**다.
지금 상태에서 정확히 말할 수 있는 건 **"사람 판정과 모델 판정이 갈린 것이 1,411건 중 59건
(4.2%)"** 까지다. 어느 쪽이 틀렸는지는 사용자 눈으로만 갈린다 —
`data/dict/meaning-review-disputed.tsv` 에 확률 낮은 순으로 모았다 (`verdict` 칸 비워 둠).

### 59건 사람 재판정 — 앞 절의 추정이 틀렸다

사용자가 59건을 전부 봤다. **모델이 틀림 53 · 사람이 놓침 1 · 애매 5.**

| | 총 | 모델 오탐 | 진짜 오류 | 애매 |
|---|---|---|---|---|
| jev 만 | 24 | 20 | 0 | 4 |
| 둘 다 | 18 | 16 | **1** | 1 |
| ollama 만 | 17 | 17 | 0 | 0 |

앞 절에 「지목분 상당수가 사람 검수가 놓친 진짜 오류」라고 적었는데 **틀렸다.** 근거로 든
`島内`·`引導`·`取組` 셋 다 오탐이었다. 영어 gloss 만 보고 한국어 뜻의 타당성을 넘겨짚은 것이다.
진짜 오류는 `公庫` 「공공의 창고」 하나다 (→ 「정부가 전액 출자해 세운 정책 금융 기관」으로
고쳤다. `verdict=x` + `fix` 칸, `apply:korean-meaning` 반영).

**확률로도 문턱을 못 정한다.** `0.0~0.2` 6건 중에도 진짜는 1건이고(`公庫` p=0.14),
`0.2~0.3` 3건·`0.3~0.4` 12건·`0.4~0.5` 21건은 전부 오탐이거나 애매다. 유일한 오류가
최저 구간에 있는 건 방향은 맞지만 n=1 로는 기준선을 못 긋는다.

애매 5건 — `見返`·`旧交`·`同人`·`国対`·`目玉商品`. 손대지 않았다.

### 그래서 남은 15,806건 전수 뜻 검수는 **기각한다**

같은 비율(4.2%)이면 지목이 600건쯤 나오고 그중 진짜는 한 자릿수다. 사람이 600건을 보게
만드는 셈이라 **일을 줄이는 게 아니라 늘린다.** 비용($0.25)이 아니라 사람 시간이 이유다.

대신 얻은 것은 **뜻 데이터가 생각보다 정상**이라는 측정값이다 — 검수 1,411건 + 재검 59건에
확정 오류 1건. 체크리스트의 「`verified=false` 전수 검수」 위험도를 낮춰 잡을 근거다.

### 큐레이션(고유명사·훈독어)으로 넘어가되, 200건 표본부터

여긴 결함이 눈으로 확인된 자리다 (밴드 검토에서 `荒木`·`滝川`·`稲田`, `浜辺`·`生糸`·
`山奥`·`北風`). 질문 성격도 다르다 — 「이 한국어 뜻이 적절한가」는 **품질 판정**이라 기준이
흐릿하지만 「`荒木` 가 고유명사인가」는 **사실 판정**이다.

그래도 이번 교훈은 **전수로 돌리기 전에 표본으로 오탐률부터 재라**는 것이다. 뜻 관문에서
90% 오탐이 나온 모델을 같은 종류의 「이상한 것 찾기」 질문에 그대로 믿고 쓰지 않는다.

## 2026-09-22 — 훈독 숙어를 별도 트랙으로 갈랐다

### 큐레이션에 모델을 안 썼다 — 데이터가 이미 답을 갖고 있었다

「고유명사·훈독어 큐레이션」을 Jev 로 돌릴 준비를 하다가 두 가지를 확인했다.

1. **고유명사 갈래는 없다.** `base.json` 의 `pos` 에 `n-pr` 이 0건이다. JMdict 본체에
   인명이 안 들어 있다 (인명은 JMnedict 로 따로다). `荒木`·`滝川`·`稲田` 는 성씨가 아니라
   **성씨로 읽히는 훈독 숙어**였다.
2. **훈독 갈래는 `pairIds` 가 이미 판정한다.** 사용자가 지적한 일곱 개가 전부 `:kun:` 만
   갖고 있다. 모델에 물을 게 없다.

앞 절의 교훈(「전수로 돌리기 전에 표본으로 오탐률부터」)을 적용할 자리조차 아니었다.
**모델을 쓰기 전에 가진 데이터로 답이 나오는지부터 본다** — 이게 이번의 진짜 교훈이다.
Jev 로 남아 있던 작업 셋 중 마지막 하나도 이렇게 없어졌다.

### 왜 빼지 않고 나눴나

처음에 사용자에게 「빼자/두자」로 물었더니 **「훈독만 카테고리로 따로 빼서 학습하는 건
안되나」** 라고 답했다. 빼는 쪽 근거는 이렇다 — 훈독은 한국 한자음으로 유추할 근거가 없고,
음독 맵·형제 대조·`ONYOMI_CHOICE` 분류가 전부 안 붙는다. 그런데 **연탁·촉음은 그대로
걸린다**: `羽子板` 는 は**ご**いた, `稲妻` 는 いな**ず**ま. 빼면 이 연습거리 1,502개를 같이
버린다. 나누면 둘 다 얻는다. 사용자 안이 맞았다.

### 갈래는 셋, 트랙은 둘

`readingKind: 'on' | 'mix' | 'kun'` 을 빌드가 매긴다 (`build-runtime-dict.ts`).

| | on | mix | kun |
|---|---|---|---|
| base | 14,721 | 736 | 1,502 |
| band4 | 70,549 | 8,807 | 6,062 |

**혼독(`mix`, 重箱·湯桶読み)은 음독 트랙에 남긴다.** 기각한 대안이 「혼독까지 같이 뺀다」인데,
혼독은 음독 쌍이 있어 대조·처방이 그대로 걸리고 **읽기가 갈리는 자리라 오히려 핵심**이다.

### 자른 자리는 풀 하나뿐이다

`inTrack(all, track)` 을 세션 훅의 **풀에만** 건다. 세션을 짜는 쪽(정규·재대결·집중)을
하나도 안 건드리고 갈리는 자리가 여기뿐이다. 그래서 `track` 은 `kind` 와 직교한다 —
훈독 트랙 안에서 재대결이 그대로 된다.

수준 판정도 `level.ts` 를 안 건드렸다. `buildLevel` 이 `bandOf(id) === undefined` 면
이벤트도 카드도 건너뛰므로, 호출부(`Home`·`Report`)의 `bandOf` 가 훈독에 `undefined` 를
주면 끝이다. 훈독 성적이 음독 사다리를 흔들지 않는다.

진입 진단도 음독 풀만 본다 — 섞이면 시작 지점이 실제보다 낮게 잡힌다.

### 검사가 잡은 내 회귀

훈독 버튼을 자리잡기(slot) 없이 붙이면서 주석에 「아래라 접혀도 위가 안 밀린다」고 적었다.
틀렸다. `.home` 은 **세로 중앙 정렬**이라 맨 아래에 붙는 것이라도 뒤늦게 생기면 위가 그
절반만큼 밀린다. `screen-cache` 스펙이 제목 42px 이동으로 잡았다. 같은 함정을 이 파일이
2026-09-21 절에 이미 적어 뒀는데(가림막 버튼 19px) 또 밟았다.

## 2026-09-22 — 국어사전 정의 인용을 폐기했다 (사용자 판단)

### 지적

> 국어사전의 같은 모양이 뜻이 비슷해보여도 그 표현이 정의가 되는 배경은 조금씩 달라서
> 다른 뜻까지는 담지 못한다. 국어사전의 뜻을 인용하는 것은 올바른 방법이 아닌 것 같다.

`s`(stdict_def 채택)로 들어온 9건을 꺼내 보니 그대로였다.

| 표제어 | LLM 초벌 | 국어사전 채택분 | 무엇이 틀어졌나 |
|---|---|---|---|
| `発達` | 발달, 성장 | 신체, 정서, 지능 따위가 성장하거나 성숙함 | 経済の発達·台風が発達する 를 **정의가 배제한다** |
| `図形` | 도형, 모양 | 그림의 모양이나 형태 | 일본어는 수학의 도형이 주다 |
| `対照` | 대조, 비교 | 둘 이상인 대상을 맞대어 같고 다름을 검토함 | 일본어 対照(的) 은 「대비되는 성질」 쪽 |
| `死語` | 사어 | …고대 그리스어, 고대 라틴어 따위가 있다 | 死語になった流行語 용법이 빠진다 |
| `水源地` | 물줄기 근원지 | …선상지 말단의 용수대 따위이다 | 한국 지리 용어 꼬리 |

형식도 혼자 어긋나 있었다 — 적용된 뜻의 **평균 길이가 6.0자**(「요점, 핵심」·「성향, 경향」)
인데 stdict 9건만 **29.4자**다.

### 갈라 읽은 것 — 인용은 폐기, 대조는 유지

두 용도를 구분했다. 기각한 대안은 「stdict 를 파이프라인에서 통째로 뺀다」이다.

- **정의를 가져오는 것 → 폐기.** `s` 판정, `apply-korean-review` 의 stdict 폴백, `source: 'stdict'`
- **분류에 대조하는 것 → 유지.** `category` 1/2/3 은 「한국어에 같은 모양이 있고 뜻이 대략
  맞나」를 **탐지**할 뿐 정의 텍스트를 인용하지 않는다. 지적한 「배경이 달라 다른 뜻까지
  못 담는다」가 바로 **동형이의(category 2)의 정의**라, 이건 남겨야 그 함정을 짚어 준다.
- `audit-korean-readings` 의 stdict 표제어 대조도 유지 — 한국 한자음 근거지 뜻이 아니다.

### 조용한 폴백이 하나 더 있었다

`apply-korean-review.ts:127` 이 LLM 번역이 없으면 stdict 정의를 넣고 있었다. 실제 사용자는
`浮遊` 1건뿐이었다. 지금은 **null 로 둔다** — 국어사전 정의를 채워 넣느니 뜻이 없는 채로
검수 목록에 남는 쪽이 낫다. (`浮遊` 는 사람이 이미 `o` 로 확인한 행이라 워크리스트의
`llm_ko` 를 타고 `manual` 로 살아남았다. stdict 에서 온 문장이 남은 유일한 자리다.)

### 이게 앞 절의 측정을 뒤집는다

「남은 미검수분의 기대 불량 958건」은 **`s` 를 불량으로 센 결과**였다. `s` 는 오역이 아니라
폐기한 규칙 아래 내린 판정이므로 모수에서도 불량에서도 빠진다. `--validate` 집계식도 고쳤다.

```
T4  2/53 → 0/51   남은 6,890건 기대 불량   72~879 → 0~483건
T6  6/51 → 0/45   남은 5,929건 기대 불량  326~1386 → 0~466건
확정 오류(x)  1,348건 중 1건 (公庫) — 0.07%
```

**두 큰 층의 표본에서 확정 오답이 한 건도 안 나왔다.** 상한이 아직 7% 대인 건 표본이
51·45 라서지 불량이 관측돼서가 아니다. 「미검수 15,632건이 썩어 있다」는 그림은 근거가 없다.

### 남은 8건

폐기한 `s` 가 붙어 있던 8건은 LLM 초벌로 되돌리고 **미검수(verified false)로 돌렸다**.
폐기한 규칙 아래 내린 판정이라 그대로 쓸 수 없다. `apply:korean-meaning` 이 남은 수를
경고로 찍는다 — 0 이 되면 워크리스트에서 그 판정이 사라진 것이다.

## 2026-09-22 — 훈독을 트랙이 아니라 **비율**로 바꿨다 (사용자 요청)

앞 절에서 훈독을 별도 트랙(홈 진입로)으로 냈는데 사용자가 둘을 지시했다.
**홈에는 두지 말 것**, 그리고 **설정에서 레인지로 섞는 비율을 정할 것.**

### 홈에서 뺀 이유

훈독은 「갈래」가 아니라 「범위」다. 매일 고르는 것이 아니라 한 번 정해 두는 값이라
홈의 주 동작 옆에 설 것이 아니다. 그래서 `Flow` 의 `kind: 'kun'`, 홈 버튼·자리잡기·
`preview.kun`·`.kun-track` CSS 를 전부 걷어냈다. `StudyTrack`/`inTrack` 도 없애고
`studyPool(idioms, includeKun)` 하나로 합쳤다.

### 풀에 섞는 것으로는 비율이 안 나온다 (이게 핵심)

사용자가 「확률을 높이면 되지 않나」라고 물었는데, **후보 풀의 비율과 출제 비율이 다르다.**

`select.ts` 의 `byIntroOrder` 가 밴드 다음으로 **미숙 음독 가중(`weak`)** 으로 정렬한다.
훈독 숙어는 음독 쌍이 없어 `weak` 이 늘 바닥이라 **같은 밴드 안에서 줄 맨 뒤로 밀린다.**
실측으로 200장을 뽑으면 훈독은 2장(1%)이었다 — 풀 비율 8.9% 의 1/9 다. 게다가 그 2장은
같은 숙어(一口)의 읽기·뜻 카드였다. 즉 앞 절의 「섞기」 토글은 **사실상 섞지 않고 있었다.**

그래서 풀이 아니라 **선택 단계에 정원을 뗀다.** `selectOptions.kunShare` (0~1) 가
남은 자리에서 훈독 몫을 먼저 가져가고, 나머지를 음독이 채운다. 한쪽이 정원을 못 채우면
반대쪽이 마저 가져간다 — 세션 길이는 안 줄어든다.

구현은 대기열을 갈래별로 둘로 쪼갠 것이다(`Group`). 기존 「모드가 정원을 못 채우면 다른
모드가 가져간다」 규칙을 `fill()` 로 뽑아 두 갈래에 똑같이 적용했다.

기각한 대안 — **`weak` 계산을 훈독에도 맞게 고치기.** 그러면 비율이 「우연히」 맞을 뿐
사용자가 지정할 수가 없고, 음독 처방의 근거인 가중치를 훈독 때문에 흔들게 된다.

### 검증

`src/dict/track.test.ts` 가 배포 번들 + 고정 시드로 200장을 뽑아 잰다.

| kunShare | 출제 훈독 |
|---|---|
| 0 | 0장 |
| 0.2 / 0.5 / 0.8 | 지정 비율 ±3%p 안 |
| 1 | 200장 전부 |

어느 값에서도 세션 길이는 200장 그대로다. e2e 는 **양끝만** 본다(0%면 화면에 훈독 0장,
100%면 전부 훈독) — 가운데 값은 세션 하나로는 표본이 얇아 화면으로 증명할 성질이 아니다.

### 설정 값

`includeKun: boolean` → **`kunPercent: number` (0~100, 10 단위)**. 0 보다 크면
`studyPool` 이 후보에 넣고, 실제 몫은 `kunShare = kunPercent / 100` 이 정한다.
0 보다 크면 진입 진단과 밴드 사다리도 같은 범위를 본다.

## 2026-09-22 — 재생에 「모르는 이벤트 타입」 가드 (새 타입을 붙이기 전의 선행 조건)

뜻 신고 기능을 위해 `LearningEvent` 유니온에 타입을 더하려다 먼저 확인한 것이다.

`replay` 는 아는 타입만 `continue` 로 걸러내고 **나머지를 전부 채점 경로로 떨어뜨리고**
있었다. 모르는 타입이 들어오면 `applyGrade(card, at, undefined)` 가 되고, 실측하니
조용히 망가지는 게 아니라 **`FSRSValidationError: Invalid rating:[undefined]` 로 던진다.**
홈·세션·리포트가 모두 재생을 부르므로 **화면 전체가 멈춘다.**

터지는 조건이 실제로 있다. 로그는 append-only 이고 기기 간 동기화가 합집합이라,
기기 하나가 새 타입을 쓰기 시작하면 **아직 옛 빌드를 캐시한 다른 기기**가 그걸 받는다.
이벤트를 지울 수도 없다.

그래서 **가드를 먼저 배포하고, 기기들이 업데이트된 뒤에 새 타입을 쓴다.** 오늘 넣은
가드가 어제 캐시된 빌드를 구해 주지는 못하기 때문에 순서가 유일한 방어다.
앞으로 붙일 모든 이벤트 타입에도 남는 보험이다.

검사는 `replay.test.ts` 의 「모르는 이벤트 타입」 절이다. 가드를 빼면 3건 다 실패하는 것을
확인하고 붙였다 — 안 그러면 「통과하지만 아무것도 안 지키는 검사」가 된다.

## 2026-09-22 — 뜻 신고를 엄지 둘로 바꿨다

배포하고 써 보니 **「이 뜻 이상해요」 글자 버튼이 눈에 안 들어온다**는 지적이 왔다.
같은 자리에 엄지 위/아래를 놓는 것으로 바꿨다.

### 「맞다」를 다시 넣은 이유

오전에 사용자가 「맞다는 필요 없다, 불편하다」로 뺐었다. 되돌린 근거는 **형태가 바뀌어서**다 —
글자 버튼 두 개는 매번 문장을 읽게 만들지만, 엄지 둘은 눌러도 안 눌러도 부담이 같다.
그리고 「맞다」가 있어야 **미검수 모수가 실제로 준다** (오전 절에서 짚었던 그 문제다).

### `verified` 는 앱이 못 쓴다

사용자가 「엄지 위 = verified:true 로 가늠」이라고 했는데, `verified` 는 사전 빌드
(`data/dict/korean-class.json`)에 있고 앱은 IndexedDB 만 쓴다. 그래서 이렇게 갈랐다.

- **앱** — 엄지를 이벤트로 남기고, 엄지 위를 누르면 **화면의 「미검수」 꼬리표를 걷는다**
  (내가 봤으니까). 다음 빌드 전까지의 임시 표시다
- **빌드** — 피드백으로 넘어온 것을 워크리스트에 찍으면(`o` / `x`) 다음 빌드에서
  진짜 `verified` 가 된다

### 옛 이벤트 호환

오전 배포분(`79c0b60`)이 이미 로그에 `{ type:'flag', on: boolean }` 을 남겼을 수 있다.
로그는 append-only 라 지울 수 없으므로 `voteOf()` 가 `verdict ?? (on ? 'bad' : null)` 로
읽는다. 새로 쓸 때는 `verdict` 만 쓴다. 타입 이름만 `FlagEvent` → `MeaningVoteEvent` 로
바꾸고 **`type: 'flag'` 문자열은 그대로 뒀다** — 그걸 바꾸면 옛 이벤트가 미아가 된다.

### 피드백은 판정별로 나눠 보낸다

`[이상하다고 본 뜻 N건]` 과 `[맞다고 본 뜻 N건]` 을 따로 낸다. 받는 쪽에서 하는 일이
다르기 때문이다 — 전자는 고칠 목록, 후자는 워크리스트에 `o` 로 찍을 목록이다.
빈 절은 안 붙인다: 「0건」과 「기능을 안 씀」이 구분되지 않는다.

### 안 누른 엄지는 색을 뺀다

`filter: grayscale(1); opacity: .45`. 카드마다 총천연색 엄지 둘이 뜨면 **뜻보다 먼저
눈에 든다.** 누른 쪽만 색이 붙고 테두리가 생긴다.

### 엄지 모양·문구·노출 창 (같은 날, 실물 스크린샷을 보고 세 번 고쳤다)

**A — 흑백 45%, 테두리 없음.** 「뜻보다 먼저 눈에 들면 안 된다」를 과하게 적용했더니
「선뜻 보이지 않는다」는 처음 지적을 그대로 반복했다. 실제 카드를 찍어 보고서야 보였다.

**B — 테두리를 세우고 52×40, 75%.** 누를 것이라는 게 보이면서 뜻은 안 가린다.
사용자가 여기에 **원형**과 **설명 문구**를 얹자고 했다. 원형은 46×46 (가로세로가 같아야
\`radius: 999px\` 이 알약이 안 된다).

**문구 — 「뜻을 잘 옮겼나요?」.** 기각한 후보와 이유가 이 결정의 내용이다.

| 후보 | 왜 뺐나 |
|---|---|
| `해석은 어때요?` | 「어때요」가 **좋다/나쁘다**로 읽힌다 (사용자 지적) |
| `이 뜻 어때요?` | 산뜻하지만 **뜻 자체를 평가**하는 걸로 읽힌다 |
| `이 뜻 맞나요?` | 바로 아래 「알았어요」와 겹쳐 **내가 맞혔냐**로 읽힌다 |

「옮기다」가 주어를 **앱이 쓴 한 줄**로 못 박아 셋을 한 번에 푼다. 검수 도구가 사람에게도
모델에게도 써 온 동사와 같다 (`audit-meaning-judge.ts` — "맞게, 어색하지 않게 옮겼는가").

**노출 창 — 뜻을 연 뒤 ~ 채점 전.** 사용자가 「뜻을 모르는데 해석을 평가하는 건 이상하다,
알았어요를 눌렀을 때만 나와야 하지 않나」라고 했는데, **「알았어요」는 누르는 즉시 다음
카드로 간다** (`submitMeaning` 의 `if (known) advance(true)`, PLAN §7). 거기 세우면 아는
단어마다 한 번씩 더 누르게 되어 루프가 느려진다 — 기각.

대신 **「몰랐어요」 뒤에서 없앴다.** 방금 그 뜻을 배운 사람이 옳고 그름을 가리는 건 막히고,
아는 사람이 「어? 이거 아닌데」 하는 순간은 남는다. 남는 틈은 *채점 전에 모르는 사람이
누르는 것*인데, 👎 는 지식 없이도 값이 있고(「한국어가 말이 안 된다」) 👍 는 누를 이유가
없어 실질적으로 아는 사람만 누른다고 봤다.

## 2026-09-22 — 동기화 전송 파일을 백업 차분만 담게 했다

사용자가 「백업 파일 쓰는 데 시간이 많이 걸린다, 정상인가」라고 물어 재 봤다.
이벤트 1건이 JSON 으로 **279바이트**, 로그가 **5,054건**이니 약 1.35MB 다.
직렬화는 안 느리다(10,000건에 13ms) — 전부 네트워크다.

### 같은 데이터를 한 동기화에 두 번 올리고 있었다

```
받기   backup.json                1.35MB
올리기 sync-<기기>.json  (mine)    ~1.35MB   ← 거의 같은 내용
올리기 backup.json      (all)      1.35MB
                          합계   약 4MB
```

전송 파일은 `listDeviceEvents(…, deviceId)` 로 **이 기기의 이벤트 전부**였는데,
기기가 사실상 하나면 `mine ≈ all` 이다.

### 고친 것

전송 파일이 있는 이유는 하나다 — **백업 쓰기가 실패해도 이번 기록이 안 사라지게** 하는 것.
그러니 보험이 필요한 건 **백업에 아직 없는 이벤트뿐**이고, 방금 그 백업을 받아 파싱했으니
무엇이 들어 있는지 이미 안다. `mine.filter(e => !inBackup.has(e.id))` 한 줄이다.
올릴 게 없으면 파일을 아예 안 쓴다.

**파일 구조도 형식도 안 바뀐다.** 이름은 여전히 기기당 하나(`sync-<기기>.json`)고,
백업 쓰기가 성공하면 지워지는 것도 그대로다. (사용자가 「증분이면 파일이 쌓이는가」를
물었는데, 파일이 쌓이는 쪽은 **백업 청크 분할** 안이고 그건 기각했다 — 지금 규모에
합치기·「백업은 줄어들 수 없다」 검사 재설계를 들일 값이 없다.)

### 검사가 지키는 것

`sync.test.ts` 의 「전송 파일은 백업에 없는 것만 담는다」 절 3건. 되돌리면 3건 다
실패하는 것을 확인하고 붙였다. 셋째가 특히 중요하다 — **백업 쓰기가 실패해도 새 이벤트가
전송 파일에 남아 다른 기기가 이어받아 복구**되는지를 본다. 줄여 놓고 이게 깨지면 기록이
사라지는 자리다.

### 아직 안 한 것

- **gzip 압축** — 남은 2.7MB(백업 받기 + 백업 올리기)를 약 400KB 로. 읽기 호환 분기가 붙어
  따로 본다
- `uploadOrReplace` 가 올릴 때마다 `listSyncFiles()` 를 다시 부른다 — 왕복 두 번이 공짜로
  더 나간다. `DriveClient` 서명이 바뀌어 이번 범위에서 뺐다

## 2026-09-22 — 업데이트 배너가 반만 작동하고 있었다

사용자가 「버전 체크하는 기능이 제대로 작동하는지 확인하라」고 해서 실제로 돌려 봤다.
**검사가 하나도 없던 자리다** — `vite.config.ts` 의 `devOptions: { enabled: false }` 라
dev 서버에는 서비스워커가 안 붙고, e2e 는 `npm run dev` 를 쓴다. 볼 방법이 없었다.

### 증상

프로덕션 빌드를 preview 로 띄우고 v1 → v2 로 갈아 보니.

| | 배너 | 「지금 적용」 후 새 버전 |
|---|---|---|
| 처음 깐 탭 그대로 | O | **X** |
| 한 번 새로고침한 뒤 | O | O |

배너는 뜨고 새 서비스워커도 **활성화까지 된다**(`controllerchange` 1회, `waiting` 비워짐).
그런데 **페이지를 새로고침하지 않아 화면은 옛 버전 그대로**다 — 눌러도 아무 일도 안
일어난 것처럼 보이고 배너도 안 사라진다.

### 원인

`vite-plugin-pwa` 의 프롬프트 모드는 새로고침을 workbox `controlling` 이벤트에 건다.

```js
wb?.addEventListener('controlling', (event) => {
  if (event.isUpdate) { ... window.location.reload() }
})
```

workbox 는 `isUpdate` 를 **등록 시점에 컨트롤러가 있었는가**로 정한다. 앱을 처음 깐 탭은
등록 시점에 컨트롤러가 없어서 그 뒤 무슨 일이 있어도 `isUpdate` 가 거짓이다.

재방문(평소)은 멀쩡하다. 깨지는 건 **처음 깐 탭을 켜 둔 채 업데이트를 받는 경우**다.
이 테스터에게 드문 일이 아니다 — 하루에 다섯 번 배포한 날이 있었다.

### 고친 것

「지금 적용」이 **직접** `controllerchange` 에 새로고침을 건다 (`UpdateBanner.tsx`).
평소 경로에서는 workbox 도 새로고침하지만 둘 다 같은 `controllerchange` 한 번에서 도는
것이라 이동은 한 번이다. 기각한 대안 — `onNeedReload` 콜백. 그것도 같은 `isUpdate` 가드
**안쪽**에서 불리므로 아무 도움이 안 된다.

### 검사를 도구로 남겼다

`npm run check:update` (`tools/check-update-banner.mjs`). build → preview → v2 빌드 →
`registration.update()` → 배너 → 「지금 적용」까지 실제로 돌리고, **두 경우를 다 본다.**
`npm run e2e` 에는 못 넣는다 — dev 에 서비스워커가 없다.

고친 것을 되돌려 돌리면 「첫 설치한 탭 그대로 · X」로 **실패하는 것을 확인**하고 넣었다.
