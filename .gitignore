node_modules/
dist/
dist-ssr/
*.log

# Playwright
test-results/
playwright-report/
playwright/.cache/

.env
.env.local
*.local

# 사전 원본 (재다운로드 가능, 커밋 금지)
data/raw/
# 빌드타임 사전 DB 산출물 — Phase 3 한국어 대조 검수 결과가 여기 들어 있어
# 원본 재다운로드만으로는 재생성이 안 된다 (context-notes 2026-09-05 "배포 데이터 공백").
# 그래서 커밋 안 하지만 CI 로도 못 만든다 — public/dict 를 대신 커밋해 배포 경로로 쓴다
data/dict/

.DS_Store
.vscode/*
!.vscode/extensions.json
