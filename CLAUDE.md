# CLAUDE.md — yomenai 프로젝트 규칙

전역 CLAUDE.md 규칙을 그대로 따르고, 아래를 추가로 적용한다.

## 작업 시작 전

`PLAN.md`, `checklist.md`, `context-notes.md`를 먼저 읽는다.
결정 근거는 `context-notes.md`에 있다. **이미 기각된 대안을 다시 제안하지 않는다.**

작업 중 새로운 결정을 내리면 `context-notes.md`에 근거와 함께 append한다.
결론만 적지 말고 기각한 대안과 이유를 같이 적는다.

## 스키마 불변 조건

IndexedDB는 primary key 변경이 불가능하다. 아래는 v1부터 반드시 포함한다.

- 모든 사용자 데이터 테이블의 복합 PK에 `userId` 포함 (값은 당분간 고정)
- `deletedAt` 소프트 삭제 필드
- 학습 기록은 **append-only 이벤트 로그**. 상태를 덮어쓰지 않는다
- 이벤트에 `cardType` (`reading` | `meaning`)
- 이벤트에 오답 유형 필드 (`ONYOMI_CHOICE` | `RENDAKU` | `SOKUON` | `CHOON` |
  `MIXED_READING` | `KO_INTERFERENCE` | `null`)
- 뜻 필드에 `source` (`stdict` | `jmdict` | `llm` | `manual`) + `verified` (boolean)

스키마 변경이 필요하다고 판단되면 **먼저 사용자에게 알린다.** 조용히 마이그레이션을
추가하지 않는다.

## 사전 DB와 사용자 DB 분리

- **사전 DB** — 읽기 전용, 재생성 가능한 빌드 산출물. 정적 자산으로 배포. IndexedDB에
  넣지 않는다
- **사용자 DB** — IndexedDB. 유일한 원본. 백업 대상

이 경계를 넘는 코드를 쓰지 않는다. 사전 데이터를 사용자 DB에 복사하면 백업 용량이
수십 MB가 되고 사전 갱신마다 마이그레이션이 발생한다.

## 일본어 렌더링

- 일본어 텍스트를 출력하는 모든 요소에 `lang="ja"`를 명시한다
- 일본어 폰트 스택에 한국어 폰트나 일반 `serif`/`sans-serif` 폴백만 남기지 않는다.
  한중일 한자는 코드포인트가 통합돼 있어 **한국 자형으로 렌더링되면 사용자가 잘못된
  글자 모양을 학습한다**
- 통합 CJK OTC 폰트를 도입하지 않는다 (`context-notes.md` 참조)

## 데이터 출처 기록

외부 사전 데이터를 가공해 저장할 때 출처를 필드 단위로 남긴다.
CC BY-SA 라이선스 대응과 나중의 출처 교체를 위한 장치다.

## 명령 실행

- `npm install`은 사용자 확인 후 실행한다
- `data/raw/`의 사전 원본 파일은 커밋하지 않는다
- stdict API 키는 `.env`에만 둔다. 코드·로그·커밋 메시지에 값을 출력하지 않는다

## 완료 기준

`checklist.md`의 각 항목에 붙은 **검증 기준을 실제로 실행해서 통과한 뒤** 체크한다.
"구현했다"는 완료가 아니다. 검증 실행 결과를 보고에 포함한다.

## 성능 제약

카드 전환은 150ms 이하로 유지한다. 하루 수백 회 반복되는 루프라 애니메이션 지연이
누적되면 학습을 방해한다. 느린 모션은 셸과 리포트 화면에만 쓴다.
