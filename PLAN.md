# 계획서

## 0. 포지셔닝

**뜻은 아는데 못 읽는 사람을 위한 일본어 앱.**

한자 문화권 중상급 학습자를 대상으로 한다. 초급자는 자연히 배제된다. 기존 일본어 앱이
뜻 학습 중심이라 이 세그먼트가 비어 있고, 영어권·일본 개발사가 한자어 지식을 전제한
설계를 할 이유가 없었기 때문이다.

차별점은 진단 리포트다. 다른 앱이 학습량을 보여줄 때 이 앱은 오답 패턴을 보여준다.
**학습 앱이 아니라 진단 도구로 포지셔닝한다.**

## 1. 목적

**1차 — 읽기 교정.** 한국 한자음 지식으로 뜻은 짐작되지만 일본어 읽기가 불안한 숙어를
찾아내 교정한다. 성공 지표는 오답 유형별 발생률 감소다.

**2차 — 어휘 확장.** 뜻 자체를 모르는 숙어를 늘린다. 성공 지표는 신규 숙어 누적 수다.

두 목적은 학습 대상이 다르므로 모드를 분리한다. 한 카드에 섞으면 둘 다 흐려진다.

## 2. 배포 형태

로컬 우선 PWA + Google 로그인 + 클라우드 백업. 런타임 백엔드 없음.
GitHub Pages 정적 배포.

서비스화 여부는 **본인이 3개월 이상 실제로 쓴 뒤에** 판단한다. 그 전에 인증·서버·약관을
만드는 것은 과설계다. 다만 전환을 막지 않는 설계 원칙만 지킨다 (5절 참조).

## 3. 데이터 소스

### 빌드타임 임포트

| 리소스 | 용도 | 라이선스 |
|---|---|---|
| KANJIDIC2 (XML) | 한자 학년·획수·음훈독·한국 한자음(`korean_h`)·빈도(`freq`) | CC BY-SA 4.0 |
| JMdict | 숙어 표제어·읽기·품사 태그·우선순위(`ke_pri`/`re_pri`) | CC BY-SA 4.0 |
| jmdict-simplified | 위 둘의 JSON 변환본 + npm 타입 패키지 | 원본 승계 |
| Tatoeba | 예문 (Phase 6, 채택 여부 미정) | CC BY (음성은 화자별 상이) |

### 배치 API

- 표준국어대사전 오픈 API — `https://stdict.korean.go.kr/api/search.do`
  - 인증 키 필요, XML/JSON, 일 호출 한도 있음
  - 1회 배치 실행 후 결과를 DB에 굳힌다. 런타임 의존 없음

### 런타임 라이브러리

| 용도 | 선택 |
|---|---|
| SRS 스케줄러 | `ts-fsrs` |
| 로마자→가나 입력 변환 | `wanakana` |
| IndexedDB | `dexie` + `dexie-react-hooks` |
| 형태소 분석 (Phase 6) | `kuromoji.js` |

## 4. 난이도 밴드

JMdict 우선순위 태그가 빈도 지표다. `nfxx`는 500단어 단위 순위,
`news1`은 상위 12,000단어, `news2`는 그다음 12,000단어를 뜻한다.

| 밴드 | 태그 범위 | 대략 대응 | 취급 |
|---|---|---|---|
| 0 | `nf01`~`nf10` | N3 이하 | 건너뛰기 기본 |
| 1 | `nf11`~`nf20` | N2 대 | 시작점 |
| 2 | `nf21`~`news1` 끝 | N2~N1 경계 | |
| 3 | `news2` 구간 | N1 대 | 주력 |
| 4 | 태그 없음 | N1 초과 | 선택 |

JLPT는 2010년 이후 공식 어휘 목록을 내지 않는다. 위 대응은 근사이므로 **UI 라벨은
자체 밴드명을 쓰고, JLPT 등급은 설명 문구에만 병기**한다.

밴드의 역할은 모드별로 다르다. 확장 모드에서는 학습 순서, 교정 모드에서는 투자
우선순위다.

**KANJIDIC2 `grade`는 난이도 축에서 제외한다.** grade 8이 중학 상용한자 전체를 한 덩어리로
묶고 있어 N1/N2 구간에서 변별력이 없다. 표외자 필터 용도로만 쓴다.

## 5. 아키텍처

```
[빌드타임 파이프라인]  Node.js 스크립트, tools/
  KANJIDIC2.xml ─┐
  JMdict.json   ─┼→ 정규화 → 레벨링 → 음독 매핑 → 사전 DB (정적 자산)
  stdict API    ─┘   (배치 1회, 결과 캐싱)
                     → 폰트 서브셋 (등장 문자 집합 기반)

[런타임]  브라우저만
  React 19 + Vite → 사전 DB(읽기 전용 정적 자산)
                  → IndexedDB(학습 이벤트 로그)
                  → Google Drive(백업)
```

### 설계 원칙

1. **사전 DB와 사용자 DB를 분리한다.** 사전은 재생성 가능한 산출물, 사용자 DB는 유일한
   원본. 섞으면 사전 갱신 때마다 마이그레이션이 발생한다.
2. **학습 기록은 append-only 이벤트 로그.** 상태를 덮어쓰지 않는다. 기기 간 병합이 합집합
   연산이 되고, FSRS 파라미터 재최적화 시 과거 기록으로 재계산이 가능해진다.
3. **동기화는 기기별 파일 분리.** 각 기기가 `reviews-{deviceId}.json`에 자기 이벤트만
   append하고 자기 파일만 쓴다. 쓰기 충돌이 구조적으로 발생하지 않으므로 충돌 해결
   UI가 필요 없다.
4. **콘텐츠 필드마다 출처를 기록한다.** `source` + `verified`. 특정 출처를 못 쓰게 되면
   그 필드만 교체할 수 있다.

## 6. 학습 설계

### 모드

| | 읽기 교정 | 어휘 확장 |
|---|---|---|
| 대상 | 뜻은 아는데 읽기가 불안 | 뜻 자체를 모름 |
| 출제 | 읽기 입력형 | 뜻 + 읽기 |
| 세션 비율 기본값 | 7 | 3 |

모드 배정은 자동으로 한다.
1. 한국어 대조 결과로 사전 분류 (동형동의 → 교정, 동형이의·일본 고유 → 확장)
2. 진입 진단에서 보정 — 읽기를 틀린 문항에만 "뜻은 알고 계셨나요" 질문
3. 학습 중 재배치 — 뜻이 안정되면 교정으로 이관, 뜻을 틀리면 확장으로 되돌림

### 출제 유형

- **주** — 숙어 제시 → 히라가나 입력. 객관식이 아니어야 한다. 보기에서 역추론이 되면
  산출 능력을 측정하지 못한다
- **보조** — 히라가나 제시 → 한자 표기 선택. 동음이의(こうせい → 構成/公正/厚生/更生) 대응
- **뜻 확인** — 동형이의·일본 고유 그룹에서만

### 카드와 FSRS

숙어 하나에 카드가 최대 2장이다. `reading`과 `meaning`은 망각 곡선이 달라서
한 카드로 묶으면 FSRS가 둘의 평균을 학습한다.

- 교정 모드 숙어 — `reading`만 활성
- 확장 모드 숙어 — 둘 다 활성, `meaning` 안정 시 저빈도로 내림

### 음독 추적

숙어를 (한자, 음독) 쌍으로 분해해 숙달도를 파생시킨다. 진도 지표가 아니라 **진단 도구**다.

- 출제 순서 최적화 — 미숙한 음독을 포함한 숙어 우선
- 진도 표시 — "숙어 340개" 대신 "음독 210개 중 156개 숙달"
- 약점 세션 제안 — 특정 음독 반복 오답 시

별도 FSRS 스케줄을 걸지 않는다. 숙어 카드 하나에만 걸고 음독은 집계로 파생시킨다.

### 오답 분류

| 유형 | 예 |
|---|---|
| `ONYOMI_CHOICE` | 発端을 ハツタン으로 |
| `RENDAKU` | 三日月을 みかつき로 |
| `SOKUON` | 発達을 ハツタツ로 |
| `CHOON` | 特徴을 とくちょ로 |
| `MIXED_READING` | 湯桶·重箱 읽기 미인식 |
| `KO_INTERFERENCE` | 한국 한자음에 이끌린 오답 |

대부분 정답·오답 문자열 비교로 자동 판정된다. 마지막 항목이 이 사용자에게 특히 유효한
축이며, 일반 학습 앱에는 없는 진단이다.

### 진입 진단

각 밴드에서 30~50개를 무작위 출제해 밴드별 숙달 수준을 추정하고 시작점을 정한다.
없으면 밴드 1 구간에서 이미 아는 숙어를 대량 통과해야 해서 첫 주에 이탈한다.

## 7. UI

### 폰트 (가장 중요)

한중일 한자는 코드포인트가 통합돼 있어 폰트에 따라 글리프가 다르다(直, 骨, 次, 令 등).
한국어 폰트로 폴백하면 **잘못된 자형을 학습하게 된다.**

- 숙어·가나 — `Noto Serif JP`, `lang="ja"` 명시
- 한국어 UI — `Pretendard` 또는 `Noto Sans KR`, `lang="ko"`
- **통합 CJK OTC는 쓰지 않는다.** `locl` 기능과 언어 태깅 지원에 의존하는 구조라 태그가
  누락되면 일본어 글리프로 폴백하고, 반대로 한국어 UI에 일본 자형이 샐 수 있다.
  파일 크기도 과하다
- 폰트 서브셋은 학습 대상 문자 집합을 DB에서 뽑아 빌드타임에 생성한다
- 폴백 체인의 마지막 `serif`가 한국 자형으로 떨어지므로, 서브셋 누락 문자가 없는지
  검증이 필요하다

### 모션

- **카드 루프** — 전환 150ms 이하. 하루 수백 회 반복되므로 누적 지연이 방해가 된다
- **셸·리포트** — 느리고 신중한 모션. 빈도가 낮고 결과를 음미하는 구간

정답 피드백은 애니메이션이 아니라 색과 위치로 즉시 처리한다. 오답일 때만 멈춘다.

### 레이아웃

한 손 조작 전제. 숙어는 화면 중앙 상단, 입력 필드와 버튼은 하단 1/3.

채점은 자동 판정이므로 FSRS 4단계를 다 노출하지 않는다.
오답이면 `Again` 자동, 정답이면 `Good` 자동에 "쉬웠다"·"헷갈렸다" 두 버튼만 선택 노출.
카드당 탭 수가 0~1회가 된다.

### 화면

| 화면 | 역할 |
|---|---|
| 홈 | 오늘 복습 수, 세션 시작 버튼. 그 외 없음 |
| 학습 카드 | 위 구조 |
| 오답 상세 | 음독 분해, 한국 한자음 대조, 같은 음독을 쓰는 다른 숙어 |
| 진단 리포트 | 오답 유형 분포, 취약 음독, 한국음 간섭 패턴 |
| 음독 맵 | (한자, 음독) 쌍 숙달 현황 |
| 설정 | 세션 길이, 모드 비율, 백업 |

**진단 리포트가 이 앱의 얼굴이다.** 다른 학습 앱과 다른 지점이 여기이므로 디자인 공을
홈이 아니라 여기에 들인다.

### 비주얼

무채색 기반, 다크 모드 우선. 색은 정오답 피드백과 리포트 차트에만.
정오답은 색만으로 구분하지 않고 아이콘·위치를 병행한다.

## 8. mmtm 재사용

`E:\GIT_choan\Mmtm` (공개 저장소 `choan616/choan616.github.io`)에서 가져올 것.

| 대상 | 비고 |
|---|---|
| `src/services/cloudStorage/` 전체 | `CloudStorageInterface` + Google/Dropbox 구현 + Factory. 도메인 의존 없음 |
| `src/utils/crypto.js` | AES-GCM 256 + PBKDF2. 기본 off, 설정에서 on |
| `src/db/syncMetadata.js` | deviceId 패턴 |
| 앱 인프라 | `ErrorBoundary`, `Toast`, `OfflineBanner`, `Modal`, `Switch`, `Icon`, `ThemeContext` |
| 배포 설정 | `manifest.webmanifest`, `registerSW.js`, `predeploy`/`deploy` 스크립트 |

**가져오지 않을 것 — `syncManager.js`의 충돌 처리.** mmtm은 파일 단위 타임스탬프 비교로
충돌을 감지해 사용자에게 선택을 요구한다. 다이어리에는 맞지만 학습 앱에서는 충돌이
일상이고 한쪽을 버리면 기록이 소실된다. 기기별 파일 분리로 대체한다.
상태 머신(`setStatus`, 진행률 콜백)은 재사용 가능하다.

**mmtm에서 배울 실패 사례.** `src/db/db.js`가 v1에서 `entries: 'date'`로 시작했다가 v2에서
`[userId+date]`로 PK를 바꾸느라 임시 테이블 3단계 우회를 했다. IndexedDB는 PK 변경이
불가능하다. 이 프로젝트는 처음부터 복합 PK에 `userId`를 넣고 `deletedAt`도 함께 넣는다.

## 9. Phase 로드맵

| Phase | 내용 | 완료 기준 |
|---|---|---|
| 0 | 문서 3종 + 스코프 확정 | 완료 |
| 1 | 스캐폴딩 + 데이터 파이프라인 | 임의 숙어 20개의 읽기·품사·구성 한자가 정확히 조회됨 |
| 2 | 레벨링 + 음독 매핑 | 밴드 분포 스냅샷 테스트 통과, 음독 그래프에 순환 없음 |
| 3 | 한국어 대조 배치 | 알려진 동형이의어 10개가 전부 2번으로 분류됨 |
| 4 | 학습 코어 (FSRS + 출제 + 이벤트 로그) | UI 없이 테스트로 세션 시뮬레이션 통과 |
| 5 | UI | 진입 진단 → 세션 → 리포트 전체 흐름 동작 |
| 6 | 예문·음성 (선택) | — |
| 7 | 서비스화 (선택) | — |

Phase 4까지가 MVP다. Phase 5를 먼저 하고 싶은 유혹이 있지만 레벨링과 출제 로직이
확정되기 전에 UI를 만들면 두 번 만들게 된다.

## 10. 리스크

| 리스크 | 대응 |
|---|---|
| 자동 레벨링이 체감 난이도와 어긋남 | 밴드별 표본 30개 검수 + 밴드 경계 집중 검수 + 학습 중 플래그 버튼 |
| 동형이의어에 자동 대조 뜻을 그대로 쓰면 틀린 뜻을 학습 | 해당 그룹은 `verified=false` 기본, 미검수 표시 |
| 빈도 데이터의 신문 편향 | EDRDG 자신이 명시한 한계. 저빈도 구간 변별력이 약하다는 전제로 사용 |
| 일본 고유 숙어의 한국어 뜻 부재 | Phase 1에서 규모 실측 후 방식 결정 (영어 병기 / 직접 작성 / LLM 초벌+검수) |
| 폰트 서브셋 누락 문자가 한국 자형으로 렌더링 | 문자 집합을 DB에서 자동 추출, 빌드 시 커버리지 검증 |
| CC BY-SA의 SA 조항 승계 범위 | 개인 사용 단계에서는 출처 기록만. 공개 결심 시 EDRDG 직접 문의 |
